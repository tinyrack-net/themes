import { Meta } from '@storybook/addon-docs/blocks';
import { Card } from '../../src/components/card/index.js';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import * as Stories from './card.stories';

<Meta of={Stories} />

# Card

Structured content surfaces with semantic sections, elevation, and padding density.

## Contract

| Axis | Values | Default |
| --- | --- | --- |
| `variant` | `default`, `outlined`, `elevated` | `default` |
| `padding` | `none`, `sm`, `md`, `lg` | `md` |
| parts | `Root`, `Header`, `Title`, `Description`, `Content`, `Footer` | compound API |

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/card.css';", "import { Card } from '@tinyrack/ui/components/card';"] }]} />

## Usage

<ComponentExampleTabs
  id="card-basic"
  title="Structured surface"
  preview={<Card.Root className="w-full max-w-md" variant="elevated"><Card.Header><Card.Title>Rack A</Card.Title><Card.Description>12 nodes</Card.Description></Card.Header><Card.Content>All systems operational.</Card.Content><Card.Footer>Updated now</Card.Footer></Card.Root>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<Card.Root variant="elevated">
  <Card.Header>
    <Card.Title>Rack A</Card.Title>
    <Card.Description>12 nodes</Card.Description>
  </Card.Header>
  <Card.Content>All systems operational.</Card.Content>
  <Card.Footer>Updated now</Card.Footer>
</Card.Root>` }]}
/>

## Examples

<ComponentExampleTabs
  id="card-variants"
  title="Surface variants"
  previewLayout="stretch"
  preview={(
    <div className="grid gap-4 md:grid-cols-3">
      <Card.Root variant="default"><Card.Title>Default</Card.Title><Card.Content>Standard contained surface.</Card.Content></Card.Root>
      <Card.Root variant="outlined"><Card.Title>Outlined</Card.Title><Card.Content>Transparent outlined surface.</Card.Content></Card.Root>
      <Card.Root variant="elevated"><Card.Title>Elevated</Card.Title><Card.Content>Raised above its context.</Card.Content></Card.Root>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<Card.Root variant="default">...</Card.Root>
<Card.Root variant="outlined">...</Card.Root>
<Card.Root variant="elevated">...</Card.Root>` }]}
/>

<ComponentExampleTabs
  id="card-padding"
  title="Padding"
  description="Compare all four interior spacing values without changing content structure."
  previewLayout="stretch"
  preview={(
    <div className="grid gap-4 sm:grid-cols-2">
      <Card.Root padding="none"><Card.Content>None</Card.Content></Card.Root>
      <Card.Root padding="sm"><Card.Content>Small</Card.Content></Card.Root>
      <Card.Root padding="md"><Card.Content>Medium</Card.Content></Card.Root>
      <Card.Root padding="lg"><Card.Content>Large</Card.Content></Card.Root>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<Card.Root padding="none">...</Card.Root>
<Card.Root padding="sm">...</Card.Root>
<Card.Root padding="md">...</Card.Root>
<Card.Root padding="lg">...</Card.Root>` }]}
/>

## API

Every part forwards native props and `ref`; keep headings in document order when composing cards.
