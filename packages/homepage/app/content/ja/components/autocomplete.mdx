---
title: "Autocomplete"
description: "自由入力テキストを受け取り、既知のコレクションから候補を絞り込んで通知します。"
section: components
order: 27
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/autocomplete.demo.js';

## コントラクト

| 軸 | 動作 |
| --- | --- |
| 値 | 項目と一致しない場合でも、テキスト値を自由に編集できます。 |
| 入力の構造 | Label → InputGroup → 任意の InputAdornment + Input + Clear + Trigger の順で、Icon は Trigger 内に配置します。 |
| ポップアップの構造 | Status が結果を通知し、List にグループ、項目、区切り線、Empty フィードバックを含めます。 |

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/autocomplete.css';", "import { TRAutocomplete } from '@tinyrack/ui/components/autocomplete';", "import { TRForm } from '@tinyrack/ui/components/form';", "import { TRField } from '@tinyrack/ui/components/field';", "import { TRButton } from '@tinyrack/ui/components/button';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="autocomplete-basic"
  title="Rack 検索"
  preview={<Stories.AutocompletePreview autoHighlight={false} defaultValue="" disabled={false} disabledItem={false} mode="list" openOnInputClick={false} placeholder="Search racks" readOnly={false} required={false} submitOnItemClick={false} />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteBasicSource }]}
/>

## 例

<ComponentExampleTabs
  id="autocomplete-states"
  title="利用可能状態"
  preview={<Stories.AutocompleteStateComparison />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteStatesSource }]}
/>

<ComponentExampleTabs
  id="autocomplete-validation"
  title="必須テキストと復旧"
  preview={<Stories.AutocompleteValidationPreview />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteValidationSource }]}
/>

<ComponentExampleTabs
  id="autocomplete-behaviors"
  title="自由入力、空、無効な項目の動作"
  description="最初の入力はコレクション外の値を保持します。開いた 2 番目の入力は Empty を表示し、値を消去または編集して常に無効な Rack Gamma 項目を確認できます。"
  preview={<Stories.AutocompleteBehaviorComparison />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteBehaviorsSource }]}
/>

<ComponentExampleTabs
  id="autocomplete-reset"
  title="ネイティブフォームのリセット"
  description="リセットすると非制御の初期値が復元され、送信結果が消去されます。"
  preview={<Stories.AutocompleteResetPreview />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteResetSource }]}
/>

## API

`@tinyrack/ui/components/autocomplete` は、`Root`、`Value`、`Trigger`、`Input`、`InputGroup`、`Icon`、`Clear`、`List`、`Status`、`Portal`、`Backdrop`、`Positioner`、`Popup`、`Arrow`、`Group`、`GroupLabel`、`Item`、`Row`、`Collection`、`Empty`、`Separator` という Base UI の完全な構造を export します。`InputAdornment` は Tinyrack が提供する入力装飾パートです。各パートの named export と prop type に加え、Base UI の state、root action/event-detail、filter、filter-option type を `TRAutocomplete*` 名で利用できます。ロケールを考慮した照合には `useAutocompleteFilter`/`TRAutocomplete.useFilter` を使い、`Root` 内でフィルタ済みコレクションを読むには `useAutocompleteFilteredItems`/`TRAutocomplete.useFilteredItems` を使います。
