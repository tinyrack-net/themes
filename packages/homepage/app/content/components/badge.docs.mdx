import { ComponentPlayground } from '../../playground/playground.js';

import { CircleCheck, Clock } from 'lucide-react';
import { Badge } from '@tinyrack/ui/components/badge';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import * as Stories from './badge.demo.js';


# Badge

Compact status labels with semantic color and density axes.

## Contract

| Axis | Values | Default |
| --- | --- | --- |
| `variant` | `neutral`, `info`, `success`, `warning`, `danger` | `neutral` |
| `size` | `sm`, `md`, `lg` | `md` |

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/badge.css';", "import { Badge } from '@tinyrack/ui/components/badge';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs id="badge-basic" title="Status label" preview={<Badge variant="success">Healthy</Badge>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<Badge variant="success">Healthy</Badge>` }]} />

## Examples

<ComponentExampleTabs
  id="badge-icon-and-long-label"
  title="Icon and constrained long label"
  description="Icons are decorative when the adjacent text communicates the same status; long labels wrap inside their parent."
  preview={(
    <div className="grid max-w-64 gap-3">
      <Badge variant="success"><CircleCheck aria-hidden size="1em" />Healthy</Badge>
      <Badge variant="warning"><Clock aria-hidden size="1em" />Waiting for maintenance approval from operator</Badge>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { CircleCheck, Clock } from 'lucide-react';

<div className="grid max-w-64 gap-3">
  <Badge variant="success">
    <CircleCheck aria-hidden size="1em" />
    Healthy
  </Badge>
  <Badge variant="warning">
    <Clock aria-hidden size="1em" />
    Waiting for maintenance approval from operator
  </Badge>
</div>` }]}
/>

<ComponentExampleTabs
  id="badge-size-variant-matrix"
  title="Size × variant"
  description="All fifteen supported combinations are grouped by size."
  previewLayout="stretch"
  preview={(
    <div className="grid gap-5">
      <div className="grid gap-2"><strong>Small</strong><div className="flex flex-wrap gap-2"><Badge size="sm" variant="neutral">Neutral</Badge><Badge size="sm" variant="info">Info</Badge><Badge size="sm" variant="success">Success</Badge><Badge size="sm" variant="warning">Warning</Badge><Badge size="sm" variant="danger">Danger</Badge></div></div>
      <div className="grid gap-2"><strong>Medium</strong><div className="flex flex-wrap gap-2"><Badge size="md" variant="neutral">Neutral</Badge><Badge size="md" variant="info">Info</Badge><Badge size="md" variant="success">Success</Badge><Badge size="md" variant="warning">Warning</Badge><Badge size="md" variant="danger">Danger</Badge></div></div>
      <div className="grid gap-2"><strong>Large</strong><div className="flex flex-wrap gap-2"><Badge size="lg" variant="neutral">Neutral</Badge><Badge size="lg" variant="info">Info</Badge><Badge size="lg" variant="success">Success</Badge><Badge size="lg" variant="warning">Warning</Badge><Badge size="lg" variant="danger">Danger</Badge></div></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import type { BadgeSize } from '@tinyrack/ui/components/badge';

const sizes = [['sm', 'Small'], ['md', 'Medium'], ['lg', 'Large']] as const satisfies readonly (readonly [BadgeSize, string])[];

<div className="grid gap-5">
  {sizes.map(([size, label]) => (
    <div className="grid gap-2" key={size}>
      <strong>{label}</strong>
      <div className="flex flex-wrap gap-2">
        <Badge size={size} variant="neutral">Neutral</Badge><Badge size={size} variant="info">Info</Badge><Badge size={size} variant="success">Success</Badge><Badge size={size} variant="warning">Warning</Badge><Badge size={size} variant="danger">Danger</Badge>
      </div>
    </div>
  ))}
</div>` }]}
/>

## API

`Badge` forwards native `span` props, events, styles, and React 19 `ref`. Keep status text visible; an icon alone needs an application-provided accessible label.
