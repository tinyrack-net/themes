---
title: "Input"
description: "ラベル、ref、妥当性、フォーム送信を保持するトークン化されたネイティブ input です。"
section: components
order: 34
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/input.demo.js';

## コントラクト

`TRInput` はネイティブの `<input>` をレンダーし、props、ref、イベント、妥当性、フォーム動作を保持します。ラベルは別のネイティブ要素です。表示される `<label>` を `htmlFor` と `id` で関連付け、説明やエラーには `aria-describedby` を使います。

密度の高いコントロールには `uiSize="sm"`、通常のフォームにはデフォルトの `md`、大きな操作領域が必要な場合は `lg` を使います。`disabled`、`readOnly`、`required`、`aria-invalid` はネイティブの意味と Tinyrack の状態スタイルを保持します。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/input.css';", "import { TRInput } from '@tinyrack/ui/components/input';", "import { TRButton } from '@tinyrack/ui/components/button';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="input-basic"
  title="label 付き Input"
  preview={<div data-docs-example-item-count="1"><Stories.InputPreview defaultValue="" disabled={false} label="Rack 名" placeholder="rack-alpha" readOnly={false} required={false} /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/input.css';
import { TRInput } from '@tinyrack/ui/components/input';

export function RackNameInput() {
  return (
    <label htmlFor="rack-name">
      Rack name
      <TRInput id="rack-name" name="rack" placeholder="rack-alpha" />
    </label>
  );
}` }]}
/>

## 例

<ComponentExampleTabs
  id="input-sizes"
  title="サイズ"
  preview={<div data-docs-example-item-count="3"><Stories.InputSizeComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/input.css';
import { TRInput } from '@tinyrack/ui/components/input';

export function InputSizes() {
  return (
    <div className="grid gap-4">
      <label>小 <TRInput placeholder="rack-alpha" uiSize="sm" /></label>
      <label>中 <TRInput placeholder="rack-alpha" /></label>
      <label>大 <TRInput placeholder="rack-alpha" uiSize="lg" /></label>
    </div>
  );
}` }]}
/>

<ComponentExampleTabs
  id="input-states"
  title="Input の状態"
  preview={<div data-docs-example-item-count="4"><Stories.InputStateComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/input.css';
import { TRInput } from '@tinyrack/ui/components/input';

export function InputStates() {
  return (
    <div className="grid gap-4 sm:grid-cols-2">
      <label>デフォルト <TRInput defaultValue="rack-alpha" /></label>
      <label>無効 <TRInput defaultValue="rack-disabled" disabled /></label>
      <label>読み取り専用 <TRInput defaultValue="rack-locked" readOnly /></label>
      <label>
        Invalid
        <TRInput aria-invalid="true" defaultValue="rack duplicate" required />
      </label>
    </div>
  );
}` }]}
/>

<ComponentExampleTabs
  id="input-validation"
  title="必須送信と復旧"
  preview={<div data-docs-example-item-count="1"><Stories.InputValidationPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/button.css';
import '@tinyrack/ui/components/form.css';
import '@tinyrack/ui/components/input.css';
import { TRButton } from '@tinyrack/ui/components/button';
import { TRForm } from '@tinyrack/ui/components/form';
import { TRInput } from '@tinyrack/ui/components/input';
import { useId, useRef, useState } from 'react';

export function RequiredInput() {
  const inputId = useId();
  const errorId = useId();
  const inputRef = useRef<HTMLInputElement>(null);
  const [attempted, setAttempted] = useState(false);
  const [submitted, setSubmitted] = useState(false);
  const [value, setValue] = useState('');
  const invalid = attempted && value.trim().length === 0;

  return (
    <TRForm
      noValidate
      onSubmit={(event) => {
        event.preventDefault();
        const valid = event.currentTarget.checkValidity();
        setAttempted(true);
        setSubmitted(valid);
        if (!valid) inputRef.current?.focus();
      }}
    >
      <label htmlFor={inputId}>
        Rack name
        <TRInput
          aria-describedby={invalid ? errorId : undefined}
          aria-invalid={invalid || undefined}
          id={inputId}
          name="rack"
          onValueChange={(nextValue) => {
            setValue(nextValue);
            setSubmitted(false);
          }}
          onInvalid={(event) => event.preventDefault()}
          ref={inputRef}
          required
          value={value}
        />
      </label>
      {invalid ? <p id={errorId} role="alert">ラック名を入力してください。</p> : null}
      <TRButton type="submit">続行</TRButton>
      <output aria-live="polite">
        {submitted ? 'Ready to create ' + value + '.' : ''}
      </output>
    </TRForm>
  );
}` }]}
/>

## API

`TRInputProps` は Base UI Input props を拡張し、デフォルトが `md` の `uiSize?: 'sm' | 'md' | 'lg'` を追加します。`name`、`type`、`autoComplete`、`disabled`、`readOnly`、`required` などのネイティブ input 属性に加え、`ref`、`className`、`style`、イベントハンドラーを転送します。

制御されたテキストには `value` と `onValueChange(value, details)` を使い、ネイティブの非制御およびフォームリセット動作には `defaultValue` を使います。イベントオブジェクトが必要な場合はネイティブの `onChange` を使います。`aria-invalid="true"`、Base UI の `data-invalid`、ネイティブの `:invalid` はすべて invalid border を有効にします。`render` prop はレンダーされる要素を置き換え、Base UI が動作、状態、ref、`.tr-input` スタイルをマージします。
