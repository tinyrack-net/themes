---
title: "Radius"
description: "Shape 토큰은 컨트롤을 선명하게 만들고 계층형 표면을 구분하며, 형태가 의미를 전달하는 역할에만 완전한 원형을 사용하게 합니다."
section: foundations
order: 6
---
import { Badge } from '@tinyrack/ui/components/badge';
import { Button } from '@tinyrack/ui/components/button';
import { Code } from '@tinyrack/ui/components/code';
import { Table } from '@tinyrack/ui/components/table';
import { tinyrackRadii } from '@tinyrack/ui/core';

## 원칙

<div className="grid gap-3 md:grid-cols-3" role="list">
  <section className="grid content-start gap-2 border border-tinyrack-border p-4" role="listitem">
    <h3 className="m-0 text-tinyrack-lg font-semibold leading-tinyrack-sm">Control · sm–md</h3>
    <div className="text-tinyrack-sm leading-tinyrack-md text-tinyrack-text-muted">Buttons, inputs, and segmented controls stay compact and predictable.</div>
  </section>
  <section className="grid content-start gap-2 border border-tinyrack-border p-4" role="listitem">
    <h3 className="m-0 text-tinyrack-lg font-semibold leading-tinyrack-sm">Surface · lg–xl</h3>
    <div className="text-tinyrack-sm leading-tinyrack-md text-tinyrack-text-muted">Containers soften only when they hold a distinct panel, popover, or overlay layer.</div>
  </section>
  <section className="grid content-start gap-2 border border-tinyrack-border p-4" role="listitem">
    <h3 className="m-0 text-tinyrack-lg font-semibold leading-tinyrack-sm">Pill · full</h3>
    <div className="text-tinyrack-sm leading-tinyrack-md text-tinyrack-text-muted">Use full radius only when capsule, status, avatar, or circle shape communicates the role.</div>
  </section>
</div>

## 시각적 스케일

<div className="grid gap-4 sm:grid-cols-2 md:grid-cols-3 xl:grid-cols-6" role="list">
  {[
    { token: 'xs', value: tinyrackRadii.xs, use: 'Micro detail' },
    { token: 'sm', value: tinyrackRadii.sm, use: 'Small control' },
    { token: 'md', value: tinyrackRadii.md, use: 'Default control' },
    { token: 'lg', value: tinyrackRadii.lg, use: 'Surface' },
    { token: 'xl', value: tinyrackRadii.xl, use: 'Overlay' },
    { token: 'full', value: tinyrackRadii.full, use: 'Pill or circle' },
  ].map(({ token, value, use }) => (
    <section className="grid content-start gap-2" key={token} role="listitem">
      <div className="grid min-h-24 place-items-center border border-tinyrack-border bg-tinyrack-surface-muted" style={{ borderRadius: value }}>{token}</div>
      <strong>{use}</strong>
      <Code>{value}</Code>
    </section>
  ))}
</div>

## 적용 패턴

<div className="grid gap-4 md:grid-cols-3">
  <section className="grid content-start gap-3 border border-tinyrack-border p-4">
    <h3 className="m-0 text-tinyrack-lg font-semibold leading-tinyrack-sm">Control and status</h3>
    <div className="flex flex-wrap items-center gap-2"><Button variant="primary">Deploy</Button><Badge variant="success">ready</Badge></div>
    <div className="text-tinyrack-sm text-tinyrack-text-muted">The Button uses control radius; the status badge uses full radius because its capsule shape carries meaning.</div>
  </section>
  <section className="grid content-start gap-3 border border-tinyrack-border p-4">
    <h3 className="m-0 text-tinyrack-lg font-semibold leading-tinyrack-sm">Nested surface</h3>
    <div className="grid min-h-24 place-items-center border border-tinyrack-border bg-tinyrack-surface-muted p-3" style={{ borderRadius: tinyrackRadii.lg }}>
      <div className="grid min-h-14 w-full place-items-center border border-tinyrack-border bg-tinyrack-surface" style={{ borderRadius: tinyrackRadii.md }}>inner radius is tighter</div>
    </div>
    <div className="text-tinyrack-sm text-tinyrack-text-muted">Nested elements stay equal to or tighter than their containing surface.</div>
  </section>
  <section className="grid content-start gap-3 border border-tinyrack-border p-4">
    <h3 className="m-0 text-tinyrack-lg font-semibold leading-tinyrack-sm">Focus follows shape</h3>
    <Button appearance="outline" style={{ outline: '2px solid var(--tinyrack-focus)', outlineOffset: '2px' }}>Focused control</Button>
    <div className="text-tinyrack-sm text-tinyrack-text-muted">Match the focus ring to the control shape, then use offset to preserve clearance.</div>
  </section>
</div>

## 구현

Radius 값은 public TypeScript metadata와 runtime CSS token입니다. 컴포넌트 CSS는 semantic radius role을 사용하고 애플리케이션 layout은 접두사 Tailwind utility를 사용할 수 있습니다.

```tsx
<Card.Root>
  <Button>Deploy</Button>
  <Badge>ready</Badge>
</Card.Root>
```

```ts
import { tinyrackRadii } from '@tinyrack/ui/core';

const panelRadius = tinyrackRadii.lg;
const statusRadius = tinyrackRadii.full;
```

CSS에서는 `--tinyrack-radius-*`를, Tailwind markup에서는 `rounded-tinyrack-*` utility를 사용하세요.

## 참조

<Table.Root className="mt-4" containerProps={{ 'aria-label': 'Radius token reference', tabIndex: 0 }} data-foundation-reference="radius">
    <thead><tr><th scope="col">Token</th><th scope="col">Value</th><th scope="col">Role</th><th scope="col">표준 Tailwind</th><th scope="col">TypeScript</th></tr></thead>
    <tbody>
      {[
        { token: 'xs', value: tinyrackRadii.xs, role: 'Micro detail', utility: 'rounded-xs' },
        { token: 'sm', value: tinyrackRadii.sm, role: 'Small control', utility: 'rounded-sm' },
        { token: 'md', value: tinyrackRadii.md, role: 'Default control', utility: 'rounded-md' },
        { token: 'lg', value: tinyrackRadii.lg, role: 'Surface', utility: 'rounded-lg' },
        { token: 'xl', value: tinyrackRadii.xl, role: 'Overlay', utility: 'rounded-xl' },
        { token: 'full', value: tinyrackRadii.full, role: 'Pill or circle', utility: 'rounded-full' },
      ].map(({ token, value, role, utility }) => (
        <tr key={token}><th scope="row"><Code>{token}</Code></th><td><Code>{value}</Code></td><td>{role}</td><td><Code>{utility}</Code></td><td><Code>tinyrackRadii.{token}</Code></td></tr>
      ))}
    </tbody>
  </Table.Root>