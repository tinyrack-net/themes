---
title: "색상과 테마"
description: "팔레트 값보다 먼저 테마에 대응하는 표면, 콘텐츠, 동작, 상태 역할을 선택해요."
section: foundations
order: 1
---
import { TRButton } from '@tinyrack/ui/components/button';
import { TRCode } from '@tinyrack/ui/components/code';
import { TRTable } from '@tinyrack/ui/components/table';
import { tinyrackPalettes, tinyrackSemanticColors } from '@tinyrack/ui/core';

## 시맨틱 역할 선택

색상 값보다 의미부터 정하세요.

- **표면:** 캔버스와 표면, 낮은 강조 표면, 포인터 올림, 선택, 누름 역할은 레이어와 상호작용을 나타내요. 각각 `canvas`, `surface`, `surfaceMuted`, `surfaceHover`, `surfaceSelected`, `surfacePressed` 토큰을 사용해요.
- **콘텐츠:** 기본·보조 텍스트, 자리표시자, 테두리, 반전 역할은 전경색을 맥락에 연결해요. `text`, `textMuted`, `textPlaceholder`, `border`, `surfaceInverse`처럼 의미에 맞는 토큰을 선택하세요.
- **동작:** 기본 동작의 평상시, 포인터 올림, 누름, 전경색은 `primary`, `primaryHover`, `primaryPressed`, `onPrimary`로 한 세트를 이뤄요.
- **상태:** 정보, 성공, 경고, 위험에는 각각 콘텐츠, 표면, 상호작용, 테두리, 전경색 역할이 있어요.

## 테마 구조

상위 요소에 `data-theme="tinyrack-light"` 또는 `data-theme="tinyrack-dark"`를 설정하세요. 두 선택자는 같은 시맨틱 변수를 정의하므로 테마가 바뀌어도 하위 요소의 의도가 유지돼요.

```html
<html data-theme="tinyrack-light">
```

평상시, 포인터 올림, 누름 역할을 한 세트로 사용하세요. 밝은 테마 값으로 어두운 테마 값을 계산하거나 관련 없는 채움에 전경색을 사용하지 마세요.

<div className="flex flex-wrap gap-2"><TRButton intent="primary">배포</TRButton><TRButton intent="info">세부 정보</TRButton><TRButton intent="success">승인</TRButton><TRButton intent="warning">위험 검토</TRButton><TRButton intent="danger">삭제</TRButton></div>

## 기본 동작 색상 안전하게 확장

Tinyrack 테마 선택자는 그대로 두고 별도의 제품 강조색 선택자를 추가하세요. 평상시, 포인터 올림, 누름, 전경 대비가 하나의 규약으로 맞물리도록 네 역할을 모두 재정의하세요.

```css
[data-accent="rack-blue"] {
  --tinyrack-primary: #1d4ed8;
  --tinyrack-primary-hover: #1e40af;
  --tinyrack-primary-pressed: #1e3a8a;
  --tinyrack-on-primary: #ffffff;
}
```

```html
<main data-theme="tinyrack-dark" data-accent="rack-blue">
```

## 피해야 할 사용

- 제품 UI에서 팔레트 값을 직접 사용하지 마세요. 테마에 대응하지 못해요.
- 상태 색상을 장식에만 쓰거나 경고와 위험 역할을 바꾸어 쓰지 마세요.
- `primary` 하나만 재정의하지 마세요. 상태 세트가 불완전하면 상호작용 피드백이 일관되지 않아요.
- `on-*` 색상을 다른 배경 역할 위에 놓지 마세요.

## 기능 색상 참고

<TRTable.Root containerProps={{ 'aria-label': '기능 색상 참고', tabIndex: 0 }} data-foundation-reference="colors"><thead><tr><th scope="col">역할</th><th scope="col">밝은 테마</th><th scope="col">어두운 테마</th></tr></thead><tbody>{Object.entries(tinyrackSemanticColors.light).map(([token, lightValue]) => (<tr key={token}><th scope="row"><TRCode>{token}</TRCode></th><td><TRCode>{lightValue}</TRCode></td><td><TRCode>{tinyrackSemanticColors.dark[token]}</TRCode></td></tr>))}</tbody></TRTable.Root>

## 구현

```css
@import "@tinyrack/ui/core.css";
.rack-status { color: var(--tinyrack-info); background: var(--tinyrack-info-surface-subtle); border-color: var(--tinyrack-info-border); }
```

접두사가 붙은 유틸리티는 [Tailwind 색상 매핑](/ko/foundations/tailwind/#colors)에서 확인하세요.

<details><summary>토큰 작성자용 기본 팔레트 참고</summary><div className="grid gap-4 pt-4"><p>기본 색상 단계는 토큰 작성 도구에서 사용하도록 내보내요. 실행 시점 CSS와 Tailwind 연결 계층에는 포함되지 않아요.</p>{Object.entries(tinyrackPalettes).map(([name, palette]) => (<section className="grid gap-2" key={name}><h3><TRCode>{name}</TRCode></h3><TRTable.Root containerProps={{ 'aria-label': `${name} 기본 팔레트`, tabIndex: 0 }}><thead><tr><th scope="col">단계</th><th scope="col">값</th></tr></thead><tbody>{Object.entries(palette).map(([step, value]) => (<tr key={step}><th scope="row">{step}</th><td><TRCode>{value}</TRCode></td></tr>))}</tbody></TRTable.Root></section>))}</div></details>
