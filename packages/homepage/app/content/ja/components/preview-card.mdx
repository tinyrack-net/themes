---
title: "PreviewCard"
description: "リンク先コンテンツを hover または focus したときに表示する非モーダルプレビューです。"
section: components
order: 40
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRPreviewCard } from '@tinyrack/ui/components/preview-card';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/preview-card.demo.js';

## コントラクト

この Tinyrack wrapper は Base UI の動作、React 19 refs、ネイティブ props、events、`render` contract を保ちます。Playground control は `align`、表示される `description`、`label`、`side`、`title` に対応します。open 状態は内部で管理されるため、hover と focus は通常の component 動作を使用します。Reset は preview を閉じた状態で再 mount し、content と positioning の既定値を復元します。

`Trigger` は既定で開く前に `600ms`、閉じる前に `300ms` 待ちます。
trigger から card へ移動する pointer path に十分な余裕がある場合だけ、`delay` と `closeDelay` で値を override します。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/preview-card.css';", "import { TRPreviewCard } from '@tinyrack/ui/components/preview-card';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="preview-card-basic"
  title="既定"
  preview={<TRPreviewCard.Root><TRPreviewCard.Trigger href="#rack-alpha">Rack Alpha</TRPreviewCard.Trigger><TRPreviewCard.Portal><TRPreviewCard.Positioner><TRPreviewCard.Popup><strong>Rack Alpha</strong><p>正常 · 12 services</p></TRPreviewCard.Popup></TRPreviewCard.Positioner></TRPreviewCard.Portal></TRPreviewCard.Root>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.previewCardBasicSource }]}
/>

## 例

<ComponentExampleTabs
  id="preview-card-states"
  title="分離した trigger handle"
  description="TRPreviewCard.createHandle は同じ場所に配置できない trigger と root を関連付けます。preview は non-interactive のまま link destination を反映します。"
  preview={<Stories.PreviewCardHandlePreview />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.previewCardHandleSource }]}
/>

<ComponentExampleTabs
  id="preview-card-positioning"
  title="timing と collision-safe な配置"
  description="card が top-side 配置、end alignment、arrow、viewport-aware containment を示します。"
  preview={<Stories.PreviewCardPreview align="end" defaultOpen description="Collision-aware preview content remains inside the viewport." label="Edge rack" side="top" title="Edge rack" />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.previewCardPositioningSource }]}
/>

## API

すべての compound part と prop/state type は `@tinyrack/ui/components/preview-card` から export されます。style は対応する CSS subpath による opt-in のままです。`Root` は制御付き・非制御の open 状態を持ちます。`Trigger` は destination link で、`delay`、`closeDelay`、`handle`、optional payload を持ちます。`Portal` は DOM container を選択し、`Backdrop` は任意です。`Positioner` は collision-aware な配置、`Popup` は preview、`Arrow` は active trigger の方向を示します。複数 trigger 間で animated content を切り替える場合だけ `Viewport` を使用します。`TRPreviewCard.createHandle<Payload>()` は分離した trigger と root を関連付けます。

Popup content は non-interactive に保ってください。preview content は touch および screen-reader ユーザーには提供されないため、trigger destination にも存在する情報だけを要約します。Escape は現在の focus context を移動せずに開いた preview を閉じます。

TRPreviewCard is a progressive enhancement for mouse hover and keyboard focus, not
a touch-only disclosure. The trigger remains a real link: tapping it follows its
`href`, so do not put required information or the only path to an action inside
the preview. Provide a separate button/dialog pattern when touch users must open
the supplemental surface without navigating.
