import { Meta } from '@storybook/addon-docs/blocks';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import { baseUiExampleSources } from '../shared/base-ui-example-sources.js';
import * as Stories from './toolbar.stories';

<Meta of={Stories} />

# Toolbar

Groups related controls with roving keyboard focus.

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Storybook controls cover `label`, `disabled`, `orientation`.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/toolbar.css';", "import { Toolbar } from '@tinyrack/ui/components/toolbar';"] }]} />

## Usage

<ComponentExampleTabs
  id="toolbar-basic"
  title="Default"
  preview={<Stories.ToolbarPreview label={'Editor controls'} disabled={false} orientation={'horizontal'} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['toolbar'] }]}
/>

## Examples

<ComponentExampleTabs
  id="toolbar-states"
  title="State comparison"
  description="Use Controls above to inspect every supported public state."
  preview={<Stories.ToolbarPreview label={'Editor controls'} disabled={true} orientation={'vertical'} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['toolbar'] }]}
/>

## API

Every compound part and prop/state type is exported from `@tinyrack/ui/components/toolbar`; styles remain opt-in through the matching CSS subpath.
