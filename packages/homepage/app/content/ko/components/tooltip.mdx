---
title: "Tooltip"
description: "hover 및 focus 가능한 trigger를 위한 짧고 비상호작용적인 설명이에요."
section: components
order: 25
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRTooltip } from '@tinyrack/ui/components/tooltip';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/tooltip.demo.js';

## 계약

`Provider`는 관련 trigger group 전체의 열기·닫기 지연을 조정해요.
`Root`는 `defaultOpen` 또는 제어형 `open`/`onOpenChange` 상태를 지원하고
`Positioner`는 side, alignment, offset, collision 처리를 담당해요. popup content는
보조적이고 비상호작용적이어야 하며 action은 TRPopover나 TRDialog에 배치하세요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/tooltip.css';", "import { TRTooltip } from '@tinyrack/ui/components/tooltip';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs
  id="tooltip-basic"
  data-docs-example-item-count="1"
  title="접근 가능한 아이콘 설명"
  description="icon button은 tooltip content에 의존하지 않고 accessible name을 유지해요. 이 문서 preview는 inverse surface와 arrow가 보이도록 열린 상태로 시작해요."
  preview={<Stories.TooltipExample closeDelay={100} content="Rack 온도: 24°C" delay={400} open side="top" trigger="Rack 온도 세부 정보" triggerMode="icon" />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.tooltipBasicSource }]}
/>

## 예시

<ComponentExampleTabs
  id="tooltip-sides"
  title="배치 방향"
  description="각 trigger를 hover하거나 focus해 요청한 방향을 비교하세요. 선호 방향의 공간이 부족하면 collision 회피가 tooltip을 반대쪽으로 바꿀 수 있어요."
  preview={<div data-docs-example-item-count="4"><Stories.TooltipSideComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.tooltipSidesSource }]}
/>

<ComponentExampleTabs id="tooltip-alignments" title="면을 따른 정렬" preview={<div data-docs-example-item-count="3"><Stories.TooltipAlignComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: Stories.tooltipSidesSource }]} />

<ComponentExampleTabs
  id="tooltip-long-content"
  data-docs-example-item-count="1"
  title="collision에 안전한 긴 콘텐츠"
  description="default-open surface가 긴 운영 문구를 줄바꿈하고 right-side 선호 위치가 맞지 않을 때 사용 가능한 viewport 너비를 사용해요."
  preview={<Stories.TooltipExample align="end" content="서비스재시작전에운영자확인이필요한유지보수기간" delay={0} open side="right" trigger="유지보수 기간 세부 정보" triggerMode="icon" />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.tooltipLongContentSource }]}
/>

<ComponentExampleTabs
  id="tooltip-delay-group"
  title="Provider 지연 group"
  description="status control을 순서대로 hover하거나 focus하세요. Provider가 group에 500ms initial delay와 여유 있는 100ms close delay를 적용해요."
  preview={<div data-docs-example-item-count="3"><Stories.TooltipDelayGroupExample /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.tooltipDelayGroupSource }]}
/>

<ComponentExampleTabs
  id="tooltip-handle-viewport"
  title="공유 handle과 viewport"
  description="분리된 trigger가 payload-aware Root 하나를 공유해요. active trigger의 content가 바뀔 때 Viewport가 transition container를 제공해요."
  preview={<div data-docs-example-item-count="1"><Stories.TooltipHandleViewportExample /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.tooltipHandleViewportSource }]}
/>

## API

Base UI가 hover/focus timing, accessible description 연결, portal positioning,
collision 처리와 Escape 닫기를 담당해요. 모든 compound part와 prop/state type은
`@tinyrack/ui/components/tooltip`에서 export되며 대응하는 CSS는 opt-in이에요.
icon-only trigger에는 자체 `aria-label`을 제공하고, 터치 사용자는 content를
hover할 수 없으므로 tooltip만을 필수 정보의 유일한 출처로 삼지 마세요.

`Provider`는 delay group을 조정하고 `Root`는 state를 소유해요. `Trigger`는
mouse hover나 keyboard focus로 열려요. `Portal`은 portal container를 정하고,
`Positioner`는 배치와 collision을 처리해요. `Popup`은 `role="tooltip"`인
accessible description이며 `Arrow`는 trigger를 가리켜요. `Viewport`는 여러
trigger가 공유하는 content를 전환해요. `createHandle`은 분리된 trigger를
payload-aware root에 연결해요. Escape는 focus를 옮기지 않고 닫으며 touch
press는 tooltip을 열지 않아요.
