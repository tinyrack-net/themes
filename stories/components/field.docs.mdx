import { Meta } from '@storybook/addon-docs/blocks';
import { Field } from '../../src/components/field/index.js';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import * as Stories from './field.stories';

<Meta of={Stories} />

# Field

Accessible field composition for labels, controls, descriptions, and validation errors.

## Contract

| Part | Responsibility |
| --- | --- |
| `Root` | disabled, invalid, and validation state |
| `Label` | accessible control name |
| `Control` | native input behavior through Base UI render |
| `Description` | persistent supporting text |
| `Error` | validation-specific feedback |
| `Validity` | render-prop access to native validity state |
| `Item` | repeatable collection field item |

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/field.css';", "import { Field } from '@tinyrack/ui/components/field';"] }]} />

## Usage

<ComponentExampleTabs
  id="field-basic"
  title="Labeled field"
  previewLayout="stretch"
  preview={<Field.Root><Field.Label>Email</Field.Label><Field.Control required type="email" placeholder="ops@example.com" /><Field.Description>Operational alerts are sent here.</Field.Description><Field.Error match="valueMissing">Email is required.</Field.Error></Field.Root>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<Field.Root>
  <Field.Label>Email</Field.Label>
  <Field.Control required type="email" placeholder="ops@example.com" />
  <Field.Description>Operational alerts are sent here.</Field.Description>
  <Field.Error match="valueMissing">Email is required.</Field.Error>
</Field.Root>` }]}
/>

## Examples

<ComponentExampleTabs
  id="field-field-states"
  title="Field states"
  description="Default, invalid, disabled, and read-only fields are compared together."
  previewLayout="stretch"
  preview={(
    <div className="grid gap-5 sm:grid-cols-2">
      <Field.Root><Field.Label>Default</Field.Label><Field.Control placeholder="rack-a" /><Field.Description>Editable value.</Field.Description></Field.Root>
      <Field.Root invalid><Field.Label>Invalid</Field.Label><Field.Control aria-invalid="true" defaultValue="bad value" /><Field.Error match>Use a valid rack name.</Field.Error></Field.Root>
      <Field.Root disabled><Field.Label>Disabled</Field.Label><Field.Control defaultValue="rack-b" /><Field.Description>Not available.</Field.Description></Field.Root>
      <Field.Root><Field.Label>Read only</Field.Label><Field.Control readOnly value="rack-c" /><Field.Description>Managed by policy.</Field.Description></Field.Root>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<Field.Root>...</Field.Root>
<Field.Root invalid>...</Field.Root>
<Field.Root disabled>...</Field.Root>
<Field.Root><Field.Control readOnly value="rack-c" /></Field.Root>` }]}
/>

## API

Parts are individually exported and forward Base UI or native props, events, styles, and React 19 refs.
