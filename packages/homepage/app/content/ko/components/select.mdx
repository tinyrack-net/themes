---
title: "Select"
description: "알려진 컬렉션에서 값을 선택하고 버튼으로 열 수 있는 키보드 탐색 popup을 제공해요."
section: components
order: 44
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/select.demo.js';

## 계약

| 축 | 동작 |
| --- | --- |
| value | `defaultValue`로 비제어하거나 `value`와 `onValueChange`로 제어해요. |
| open | `defaultOpen`으로 비제어하거나 `open`과 `onOpenChange`로 제어해요. |
| disclosure | `Trigger`가 focus를 소유하고 `Portal`, `Positioner`, `Popup`이 floating layer를 구성해요. |
| collection | group, label, separator, disabled item, text, indicator가 menu anatomy를 설명해요. |
| form | `name`, `form`, `required`, `inputRef`가 native 제출, validation, reset과 통합돼요. |

trigger가 밀도 높은 컨트롤 표면에 속하면 `TRSelect.Trigger uiSize="sm"`을 사용하세요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/select.css';", "import { TRSelect } from '@tinyrack/ui/components/select';", "import { TRForm } from '@tinyrack/ui/components/form';", "import { TRField } from '@tinyrack/ui/components/field';", "import { TRButton } from '@tinyrack/ui/components/button';", "import { ChevronDown } from 'lucide-react';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs
  id="select-basic"
  title="그룹으로 묶은 배포 랙"
  preview={<Stories.SelectPreview data-docs-example-item="" defaultValue="alpha" disabled={false} disabledItem readOnly={false} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/select.css';
import { TRSelect } from '@tinyrack/ui/components/select';
import { ChevronDown } from 'lucide-react';

const racks = { alpha: '랙 알파예요', beta: '랙 베타예요', gamma: '랙 감마예요', staging: '스테이징 랙이에요' };

<TRSelect.Root defaultValue="alpha" items={racks} name="rack">
  <TRSelect.Label>배포 랙</TRSelect.Label>
  <TRSelect.Trigger aria-label="배포 랙이에요">
    <TRSelect.Value placeholder="랙을 선택하세요" />
    <TRSelect.Icon aria-hidden="true"><ChevronDown /></TRSelect.Icon>
  </TRSelect.Trigger>
  <TRSelect.Portal>
    <TRSelect.Positioner sideOffset={8}>
      <TRSelect.Popup>
        <TRSelect.Arrow />
        <TRSelect.ScrollUpArrow aria-label="위로 스크롤하세요">↑</TRSelect.ScrollUpArrow>
        <TRSelect.List>
          <TRSelect.Group>
            <TRSelect.GroupLabel>프로덕션이에요</TRSelect.GroupLabel>
            <TRSelect.Item value="alpha"><TRSelect.ItemText>랙 알파예요</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
            <TRSelect.Item value="beta"><TRSelect.ItemText>랙 베타예요</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
            <TRSelect.Item disabled value="gamma"><TRSelect.ItemText>랙 감마 · 오프라인이에요</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
          </TRSelect.Group>
          <TRSelect.Separator />
          <TRSelect.Group>
            <TRSelect.GroupLabel>비프로덕션이에요</TRSelect.GroupLabel>
            <TRSelect.Item value="staging"><TRSelect.ItemText>스테이징 랙이에요</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
          </TRSelect.Group>
        </TRSelect.List>
        <TRSelect.ScrollDownArrow aria-label="아래로 스크롤하세요">↓</TRSelect.ScrollDownArrow>
      </TRSelect.Popup>
    </TRSelect.Positioner>
  </TRSelect.Portal>
</TRSelect.Root>` }]}
/>

## 예시

<ComponentExampleTabs
  id="select-sizes"
  title="트리거 크기"
  description="팝업 목록과 별개로 트리거 밀도를 선택하세요."
  preview={<div data-docs-example-item-count="3"><Stories.SelectSizeComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/select.css';
import { TRSelect } from '@tinyrack/ui/components/select';

{(['sm', 'md', 'lg'] as const).map((uiSize) => (
  <TRSelect.Root defaultValue="alpha" items={{ alpha: '랙 알파' }} key={uiSize}>
    <TRSelect.Label>{uiSize.toUpperCase() + ' 배포 랙'}</TRSelect.Label>
    <TRSelect.Trigger uiSize={uiSize}><TRSelect.Value /></TRSelect.Trigger>
    <TRSelect.Portal><TRSelect.Positioner><TRSelect.Popup><TRSelect.List>
      <TRSelect.Item value="alpha"><TRSelect.ItemText>랙 알파예요</TRSelect.ItemText></TRSelect.Item>
    </TRSelect.List></TRSelect.Popup></TRSelect.Positioner></TRSelect.Portal>
  </TRSelect.Root>
))}` }]}
/>

<ComponentExampleTabs
  id="select-states"
  title="사용 가능 상태"
  preview={<div data-docs-example-item-count="3"><Stories.SelectStateComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.selectStatesSourceKo }]}
/>

<ComponentExampleTabs
  id="select-validation"
  title="필수 선택과 복구"
  preview={<div data-docs-example-item-count="1"><Stories.SelectValidationPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/button.css';
import '@tinyrack/ui/components/field.css';
import '@tinyrack/ui/components/form.css';
import '@tinyrack/ui/components/select.css';
import { TRButton } from '@tinyrack/ui/components/button';
import { TRField } from '@tinyrack/ui/components/field';
import { TRForm } from '@tinyrack/ui/components/form';
import { TRSelect } from '@tinyrack/ui/components/select';
import { ChevronDown } from 'lucide-react';
import { useState } from 'react';

const racks = { alpha: '랙 알파예요', beta: '랙 베타예요', gamma: '랙 감마예요', staging: '스테이징 랙이에요' } as const;

function RackSelectForm() {
  const [attempted, setAttempted] = useState(false);
  const [value, setValue] = useState<string | null>(null);
  const invalid = attempted && value === null;

  return (
    <TRForm className="grid w-full max-w-80 min-w-0 gap-3" noValidate onSubmit={(event) => { event.preventDefault(); setAttempted(true); event.currentTarget.checkValidity(); }}>
      <TRField.Root invalid={invalid}>
        <TRSelect.Root items={racks} name="rack" onValueChange={setValue} required value={value}>
          <TRSelect.Label>배포 랙</TRSelect.Label>
          <TRSelect.Trigger aria-label="배포 랙이에요"><TRSelect.Value placeholder="랙을 선택하세요" /><TRSelect.Icon aria-hidden="true"><ChevronDown /></TRSelect.Icon></TRSelect.Trigger>
          <TRSelect.Portal><TRSelect.Positioner sideOffset={8}><TRSelect.Popup><TRSelect.Arrow /><TRSelect.ScrollUpArrow aria-label="위로 스크롤하세요">↑</TRSelect.ScrollUpArrow><TRSelect.List>
            <TRSelect.Group><TRSelect.GroupLabel>프로덕션이에요</TRSelect.GroupLabel>
              <TRSelect.Item value="alpha"><TRSelect.ItemText>랙 알파예요</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
              <TRSelect.Item value="beta"><TRSelect.ItemText>랙 베타예요</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
              <TRSelect.Item value="gamma"><TRSelect.ItemText>랙 감마예요</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
            </TRSelect.Group><TRSelect.Separator /><TRSelect.Group><TRSelect.GroupLabel>비프로덕션이에요</TRSelect.GroupLabel>
              <TRSelect.Item value="staging"><TRSelect.ItemText>스테이징 랙이에요</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
            </TRSelect.Group>
          </TRSelect.List><TRSelect.ScrollDownArrow aria-label="아래로 스크롤하세요">↓</TRSelect.ScrollDownArrow></TRSelect.Popup></TRSelect.Positioner></TRSelect.Portal>
        </TRSelect.Root>
        {invalid ? <TRField.Error match>배포할 랙을 선택하세요.</TRField.Error> : null}
      </TRField.Root>
      <TRButton type="submit">배포하세요</TRButton>
      <output aria-live="polite">{attempted && value ? racks[value as keyof typeof racks] + '에 배포할 준비가 됐어요.' : ''}</output>
    </TRForm>
  );
}` }]}
/>

<ComponentExampleTabs
  id="select-long-collection"
  title="길게 스크롤되는 collection"
  description="팝업을 열어 제한된 높이, 스크롤 화살표, 긴 레이블과 강조된 포커스 처리를 확인하세요."
  preview={<div data-docs-example-item-count="1"><Stories.SelectLongCollection /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/select.css';
import { TRSelect } from '@tinyrack/ui/components/select';
import { ChevronDown } from 'lucide-react';

const racks = Object.fromEntries(
  Array.from({ length: 18 }, (_, index) => [
    'rack-' + (index + 1),
    '랙 ' + String(index + 1).padStart(2, '0') + ' · 서울 가용 영역이에요',
  ]),
);

<TRSelect.Root defaultValue="rack-1" items={racks}>
  <TRSelect.Label>긴 랙 목록</TRSelect.Label>
  <TRSelect.Trigger aria-label="긴 랙 목록이에요">
    <TRSelect.Value /><TRSelect.Icon aria-hidden="true"><ChevronDown /></TRSelect.Icon>
  </TRSelect.Trigger>
  <TRSelect.Portal>
    <TRSelect.Positioner sideOffset={8}>
      <TRSelect.Popup>
        <TRSelect.Arrow />
        <TRSelect.ScrollUpArrow aria-label="위로 스크롤하세요">↑</TRSelect.ScrollUpArrow>
        <TRSelect.List>
          {Object.entries(racks).map(([value, label]) => (
            <TRSelect.Item key={value} value={value}><TRSelect.ItemText>{label}</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
          ))}
        </TRSelect.List>
        <TRSelect.ScrollDownArrow aria-label="아래로 스크롤하세요">↓</TRSelect.ScrollDownArrow>
      </TRSelect.Popup>
    </TRSelect.Positioner>
  </TRSelect.Portal>
</TRSelect.Root>` }]}
/>

## API

전체 anatomy는 `Root`, `Label`, `Trigger`, `Value`, `Icon`, `Portal`, `Backdrop`, `Positioner`, `Popup`, `List`, `Item`, `ItemIndicator`, `ItemText`, `Arrow`, `ScrollDownArrow`, `ScrollUpArrow`, `Group`, `GroupLabel`, `Separator`예요. 모든 part는 `TRSelect.Part`와 named `TRSelectPart` export로 제공되며 대응하는 `TRSelectPartProps`, `TRSelectPartState` type을 export해요.

| 목적 | 공개 prop과 기본값 |
| --- | --- |
| 값 | `defaultValue` 또는 제어형 `value`를 사용해요. `multiple` 기본값은 `false`이고 `items`는 값을 표시 label에 연결해요. |
| 열림 | `defaultOpen` 기본값은 `false`예요. 제어형 `open`도 사용할 수 있고 `modal`과 `highlightItemOnHover` 기본값은 `true`예요. |
| 사용 가능 상태 | `disabled`, `readOnly`, `required` 기본값은 모두 `false`예요. |
| 트리거 | `uiSize="sm" | "md" | "lg"`를 받고 기본값은 `"md"`예요. |
| 배치 | `Positioner`의 `alignItemWithTrigger` 기본값은 `false`, `sideOffset` 기본값은 `4`예요. 나머지 충돌·배치 prop은 Base UI에서 합성돼요. |
| form | `name`, `form`, `autoComplete`, `inputRef`를 받아요. 비제어 select는 native reset에서 `defaultValue`로 돌아가요. |

`Trigger`에 접근 가능한 이름을 제공하세요. Arrow Down으로 열고 사용 가능한 option 사이를 이동하거나 typeahead를 사용할 수 있어요. Enter로 확정하고 Escape로 닫으면 trigger로 focus가 돌아가요. 필요하면 `value`와 `open`을 독립적으로 제어하세요. 사용할 수 없는 control에는 `disabled`, 변경할 수 없는 focusable value에는 `readOnly`를 사용하세요.
