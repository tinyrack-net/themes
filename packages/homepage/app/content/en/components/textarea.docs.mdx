---
title: "Textarea"
description: "A tokenized multiline native control with vertical resizing and full form behavior."
section: components
order: 50
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/textarea.demo.js';

## Contract

Textarea preserves native refs, names, FormData, reset, disabled, read-only, required, and invalid states.

Use `uiSize="sm"` for shorter multiline controls in dense surfaces.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/textarea.css';", "import { Textarea } from '@tinyrack/ui/components/textarea';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs id="textarea-basic" title="Operational notes" preview={<Stories.TextareaPreview disabled={false} label="Rack notes" placeholder="Operational notes" readOnly={false} required={false} value="" />} sources={[{ label: 'React', language: 'tsx', code: String.raw`<label htmlFor="rack-notes">Rack notes</label>
<Textarea id="rack-notes" name="notes" placeholder="Operational notes" />` }]} />

## Examples

<ComponentExampleTabs id="textarea-states" title="Default, disabled, read-only, and invalid" preview={<Stories.TextareaStateComparison />} sources={[{ label: 'React', language: 'tsx', code: String.raw`<Textarea aria-label="Default" placeholder="Operational notes" />
<Textarea aria-label="Disabled" disabled />
<Textarea aria-label="Read only" defaultValue="Locked note" readOnly />
<Textarea aria-invalid="true" aria-label="Invalid" defaultValue="Incomplete note" />` }]} />

<ComponentExampleTabs
  id="textarea-validation"
  title="Required submit and recovery"
  description="Submit empty to move focus to the invalid textarea, then enter a reason and submit again."
  preview={<Stories.TextareaValidationPreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<Form noValidate onSubmit={validateAndSubmit}>
  <Field.Root invalid={invalid}>
    <Field.Label htmlFor="reason">Change reason</Field.Label>
    <Textarea aria-invalid={invalid} id="reason" name="reason" required />
    <Field.Error match>Add a reason before submitting.</Field.Error>
  </Field.Root>
  <Button type="submit">Submit change</Button>
</Form>` }]}
/>

## API

`Textarea` forwards every native textarea prop, ref, event, class, and style. Its default resize direction is vertical and native validity remains authoritative.
