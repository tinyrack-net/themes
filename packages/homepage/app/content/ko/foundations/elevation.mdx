---
title: "레이어와 고도"
description: "평면, 띄움, 오버레이를 고른 뒤 밝고 어두운 테마에서 레이어 순서, 스크림, 모달성, 그림자를 함께 조정해요."
section: foundations
order: 7
---
import { TRCode } from '@tinyrack/ui/components/code';
import { TRTable } from '@tinyrack/ui/components/table';
import { tinyrackLayers, tinyrackShadows } from '@tinyrack/ui/core';

## 평면, 띄움, 오버레이 선택

- **평면:** 일반 카드, 패널, 페이지 섹션이에요. 표면 색상과 테두리로 구분해요.
- **띄움:** 페이지를 계속 사용할 수 있지만 분리가 필요한 비차단형 플로팅 콘텐츠예요.
- **오버레이:** 두드러진 포털 콘텐츠예요. 그림자는 분리를 강화하지만 모달 여부는 동작이 결정해요.

## 밝고 어두운 테마 비교

<div className="grid gap-4 sm:grid-cols-2">{['light', 'dark'].map((mode) => <section className="grid gap-3 border p-4" data-theme={`tinyrack-${mode}`} key={mode} style={{ background: 'var(--tinyrack-canvas)', borderColor: 'var(--tinyrack-border)', color: 'var(--tinyrack-text)' }}><strong>{mode}</strong><div className="rounded-tinyrack-lg border p-4" style={{ background: 'var(--tinyrack-surface)', borderColor: 'var(--tinyrack-border)', boxShadow: tinyrackShadows.raised }}>띄운 표면</div></section>)}</div>

어두운 테마에서는 표면 대비와 테두리가 경계를 더 많이 담당해요. 그림자는 유일한 경계가 아니라 보조 단서예요.

## 공통 레이어 순서

<TRTable.Root containerProps={{ 'aria-label': '레이어 순서 참조', tabIndex: 0 }} density="compact"><thead><tr><th scope="col">레이어</th><th scope="col">값</th><th scope="col">CSS 변수</th></tr></thead><tbody>{Object.entries(tinyrackLayers).map(([name, value]) => <tr key={name}><th scope="row"><TRCode>{name}</TRCode></th><td>{value}</td><td><TRCode>{`--tinyrack-layer-${name}`}</TRCode></td></tr>)}</tbody></TRTable.Root>

`backdrop`은 `dialog`보다 아래에 두세요. 드롭다운, 팝오버, 대화상자, 토스트, 툴팁과 충돌하는 임의의 z-index를 만들지 마세요.

## 스크림, 배경막, 모달성

두 테마에서 차단된 페이지 콘텐츠를 분리하는 배경막에는 `--tinyrack-scrim`을 사용해요. 모달에는 포커스 제한, 닫기 동작, 배경 상호작용 차단도 필요해요. 스크림이나 그림자만으로 모달이 되지는 않아요.

## 그림자 선택

<TRTable.Root containerProps={{ 'aria-label': '그림자 참조', tabIndex: 0 }} density="compact"><thead><tr><th scope="col">그림자</th><th scope="col">목적</th><th scope="col">값</th></tr></thead><tbody><tr><th scope="row"><TRCode>raised</TRCode></th><td>은은하게 띄운 분리</td><td><TRCode>{tinyrackShadows.raised}</TRCode></td></tr><tr><th scope="row"><TRCode>overlay</TRCode></th><td>두드러진 포털 분리</td><td><TRCode>{tinyrackShadows.overlay}</TRCode></td></tr></tbody></TRTable.Root>

## 적용 예시와 구현

<div className="grid gap-4 md:grid-cols-3"><section className="border border-tinyrack-border bg-tinyrack-surface p-4"><strong>평면 패널</strong></section><section className="rounded-tinyrack-lg border border-tinyrack-border bg-tinyrack-surface p-4 shadow-tinyrack-raised"><strong>띄운 액션</strong></section><section className="relative overflow-hidden bg-tinyrack-surface-muted p-6"><div className="absolute inset-0 bg-tinyrack-scrim" /><div className="relative rounded-tinyrack-xl bg-tinyrack-surface p-4 shadow-tinyrack-overlay"><strong>오버레이 작업</strong></div></section></div>

TypeScript에서는 `tinyrackLayers`와 `tinyrackShadows`, CSS에서는 `--tinyrack-layer-*`, `--tinyrack-scrim`, `--tinyrack-shadow-*`, Tailwind에서는 대응하는 접두사 유틸리티를 사용해요.
