---
title: "Dialog"
description: "다섯 가지 배치를 지원하는 focus-trap 대화상자 표면입니다."
section: components
order: 16
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { Dialog } from '@tinyrack/ui/components/dialog';
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/dialog.demo.js';

## 계약

| 축 | 값 | 기본값 |
| --- | --- | --- |
| `placement` on `Popup` | `middle`, `top`, `bottom`, `start`, `end` | `middle` |
| `modal` on `Root` | focus trap and scroll lock when true | `true` |
| state | controlled `open` or uncontrolled `defaultOpen` | closed |

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/dialog.css';", "import { Dialog } from '@tinyrack/ui/components/dialog';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="dialog-basic" title="확인 dialog" preview={<Dialog.Root><Dialog.Trigger>dialog 열기</Dialog.Trigger><Dialog.Portal><Dialog.Backdrop /><Dialog.Popup><Dialog.Title>Deploy changes</Dialog.Title><Dialog.Description>This restarts the service.</Dialog.Description><Dialog.Close>Cancel</Dialog.Close></Dialog.Popup></Dialog.Portal></Dialog.Root>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<Dialog.Root>
  <Dialog.Trigger>Open dialog</Dialog.Trigger>
  <Dialog.Portal>
    <Dialog.Backdrop />
    <Dialog.Popup placement="middle">
      <Dialog.Title>Deploy changes</Dialog.Title>
      <Dialog.Description>This restarts the service.</Dialog.Description>
      <Dialog.Close>Cancel</Dialog.Close>
    </Dialog.Popup>
  </Dialog.Portal>
</Dialog.Root>` }]} />

## 예시

<ComponentExampleTabs
  id="dialog-placements"
  title="배치"
  description="같은 compound 구조를 사용하고 popup 배치만 변경하세요."
  preview={<div className="flex flex-wrap gap-2">{['middle', 'top', 'bottom', 'start', 'end'].map((placement) => <Dialog.Root key={placement}><Dialog.Trigger>{placement}</Dialog.Trigger><Dialog.Portal><Dialog.Backdrop /><Dialog.Popup placement={placement}><Dialog.Title>{placement} dialog</Dialog.Title><Dialog.Description>Placement preview.</Dialog.Description><Dialog.Close>닫기</Dialog.Close></Dialog.Popup></Dialog.Portal></Dialog.Root>)}</div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`{['middle', 'top', 'bottom', 'start', 'end'].map((placement) => (
  <Dialog.Root key={placement}>
    <Dialog.Trigger>{placement}</Dialog.Trigger>
    <Dialog.Portal>
      <Dialog.Backdrop />
      <Dialog.Popup placement={placement}>
        <Dialog.Title>{placement} dialog</Dialog.Title>
        <Dialog.Description>Placement preview.</Dialog.Description>
        <Dialog.Close>Close</Dialog.Close>
      </Dialog.Popup>
    </Dialog.Portal>
  </Dialog.Root>
))}` }]}
/>

<ComponentExampleTabs
  id="dialog-handle"
  title="분리된 trigger handle"
  description="안정적인 handle이 공통 ancestor를 공유하지 않는 trigger와 root를 연결하고, Viewport가 mobile containment를 담당합니다."
  preview={<Stories.DialogHandleExample />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`const handle = Dialog.createHandle<void>();

export function DetachedDialog() {
  return <>
    <Dialog.Trigger handle={handle}>Open detached dialog</Dialog.Trigger>
    <Dialog.Root handle={handle}>
      <Dialog.Portal>
        <Dialog.Backdrop />
        <Dialog.Viewport>
          <Dialog.Popup placement="middle">
            <Dialog.Title>Detached trigger</Dialog.Title>
            <Dialog.Description>A shared handle connects this surface.</Dialog.Description>
            <Dialog.Close>Close</Dialog.Close>
          </Dialog.Popup>
        </Dialog.Viewport>
      </Dialog.Portal>
    </Dialog.Root>
  </>;
}` }]}
/>

<ComponentExampleTabs
  id="dialog-task-form"
  title="form이 있는 긴 modal 작업"
  description="modal은 열린 동안 focus를 가두고 긴 content를 viewport 안에 유지하며 실제 form을 submit하고 닫힌 뒤 dialog 열기로 focus를 복원합니다."
  preview={<Stories.DialogExample description={'저장하기 전에 deployment name, environment, rollback note를 검토하세요. The result is announced below the form without closing the dialog.'} longContent modal={true} open={false} placement={'middle'} title={'Edit deployment'} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`function DeploymentDialog() {
  const inputId = useId();
  const notesId = useId();
  const [result, setResult] = useState('No changes saved');

  return <Dialog.Root modal>
    <Dialog.Trigger>Open dialog</Dialog.Trigger>
    <Dialog.Portal>
      <Dialog.Backdrop />
      <Dialog.Popup placement="middle">
        <Dialog.Title>Edit deployment</Dialog.Title>
        <Dialog.Description>
          Review the deployment name, environment, and rollback notes before saving.
        </Dialog.Description>
        <div className="tr-dialog-body grid gap-4">
          <p>Confirm the environment, maintenance window, and rollback owner.</p>
          <Field.Root>
            <Field.Label htmlFor={notesId}>Rollback notes</Field.Label>
            <Textarea defaultValue="Restore the previous deployment and verify rack health." id={notesId} name="notes" rows={8} />
          </Field.Root>
          <Form onSubmit={(event) => {
            event.preventDefault();
            setResult('Saved ' + new FormData(event.currentTarget).get('rack'));
          }}>
            <Field.Root>
              <Field.Label htmlFor={inputId}>Rack name</Field.Label>
              <Input defaultValue="rack-alpha" id={inputId} name="rack" required />
            </Field.Root>
            <Button type="submit">Save changes</Button>
          </Form>
          <output aria-live="polite">{result}</output>
        </div>
        <Dialog.Close>Cancel</Dialog.Close>
      </Dialog.Popup>
    </Dialog.Portal>
  </Dialog.Root>;
}` }]}
/>

## API

Base UI owns focus trapping, scroll lock, portal, backdrop interaction, restoration, and Escape dismissal. `Popup` uses one responsive width recipe across all placements. Put long task content in a `tr-dialog-body` container between Description and the action row so the popup's `minmax(0, 1fr)` body scrolls while its title and actions remain visible.