---
title: "Callout"
description: "Semantic documentation notices built on the shared TRAlert surface."
section: components
order: 51
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/callout.demo.js';

## Contract

`note`, `tip`, `caution`, and `danger` map to the corresponding TRAlert semantics without coupling authored MDX to an application framework. The description accepts MDX block content such as paragraphs, lists, and code blocks without invalid HTML nesting.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/callout.css';", "import { TRCallout } from '@tinyrack/ui/components/callout';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs id="callout-basic" title="Authored notice" preview={<Stories.CalloutPreview variant="note" />} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRCallout variant="note">Keep credentials out of source control.</TRCallout>` }]} />

## Examples

Use a localized `title` when the default English heading is not appropriate.

## API

`TRCallout` forwards native `div` props and React 19 `ref`. Use `title` to localize or replace the default heading; authored children may be inline React content or block-level MDX.
