---
title: "Popover"
description: "ダイアログの意味、タイトル、説明、閉じるアクション、配置、解除を備えた非モーダルのアンカーコンテンツです。"
section: components
order: 18
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRPopover } from '@tinyrack/ui/components/popover';
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/popover.demo.js';

## コントラクト

制御付き `open` または非制御の `defaultOpen` を使います。`Positioner` は Base UI の side、alignment、offset、collision prop を受け取ります。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui tailwindcss react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/popover.css';", "import { TRPopover } from '@tinyrack/ui/components/popover';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs id="popover-basic" title="Rack の詳細" preview={<TRPopover.Root><TRPopover.Trigger>Rack の詳細</TRPopover.Trigger><TRPopover.Portal><TRPopover.Positioner sideOffset={8}><TRPopover.Popup><TRPopover.Arrow /><TRPopover.Title>Rack A</TRPopover.Title><TRPopover.Description>すべての node が online です。</TRPopover.Description><TRPopover.Close>閉じる</TRPopover.Close></TRPopover.Popup></TRPopover.Positioner></TRPopover.Portal></TRPopover.Root>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRPopover.Root>
  <TRPopover.Trigger>Rack details</TRPopover.Trigger>
  <TRPopover.Portal><TRPopover.Positioner sideOffset={8}><TRPopover.Popup>
    <TRPopover.Arrow />
    <TRPopover.Title>Rack A</TRPopover.Title>
    <TRPopover.Description>All nodes online.</TRPopover.Description>
    <TRPopover.Close>Close</TRPopover.Close>
  </TRPopover.Popup></TRPopover.Positioner></TRPopover.Portal>
</TRPopover.Root>` }]} />

## 例

<ComponentExampleTabs
  id="popover-sides"
  title="配置する側"
  description="各 trigger が要求した側に実際の anchored popup を開き、collision を処理します。"
  preview={<div className="flex flex-wrap gap-3">{['top', 'right', 'bottom', 'left'].map((side) => <TRPopover.Root key={side}><TRPopover.Trigger>{side}</TRPopover.Trigger><TRPopover.Portal><TRPopover.Positioner side={side} sideOffset={8}><TRPopover.Popup><TRPopover.Arrow /><TRPopover.Title>{side}</TRPopover.Title><TRPopover.Description>Anchored content.</TRPopover.Description><TRPopover.Close>閉じる</TRPopover.Close></TRPopover.Popup></TRPopover.Positioner></TRPopover.Portal></TRPopover.Root>)}</div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`{['top', 'right', 'bottom', 'left'].map((side) => (
  <TRPopover.Root key={side}>
    <TRPopover.Trigger>{side}</TRPopover.Trigger>
    <TRPopover.Portal>
      <TRPopover.Positioner side={side} sideOffset={8}>
        <TRPopover.Popup>
          <TRPopover.Arrow />
          <TRPopover.Title>{side}</TRPopover.Title>
          <TRPopover.Description>Anchored content.</TRPopover.Description>
          <TRPopover.Close>Close</TRPopover.Close>
        </TRPopover.Popup>
      </TRPopover.Positioner>
    </TRPopover.Portal>
  </TRPopover.Root>
))}` }]}
/>

<ComponentExampleTabs
  id="popover-collision-offset"
  title="制御付き lifecycle、collision、offset"
  description="live state は制御された open の変更を反映し、side と alignment の offset は inline edge 付近で collision-aware のままです。"
  previewLayout="stretch"
  preview={<Stories.PopoverControlledLifecycle />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`function ControlledPopover() {
  const [open, setOpen] = useState(false);
  return <>
    <TRPopover.Root onOpenChange={setOpen} open={open}>
      <TRPopover.Trigger>Rack details</TRPopover.Trigger>
      <TRPopover.Portal>
        <TRPopover.Positioner
          align="end"
          alignOffset={12}
          collisionAvoidance={{ align: 'flip', side: 'flip' }}
          side="right"
          sideOffset={16}
        >
          <TRPopover.Popup>
            <TRPopover.Arrow />
            <TRPopover.Title>Controlled edge popover</TRPopover.Title>
            <TRPopover.Description>Collision-aware content.</TRPopover.Description>
            <TRPopover.Close>Close</TRPopover.Close>
          </TRPopover.Popup>
        </TRPopover.Positioner>
      </TRPopover.Portal>
    </TRPopover.Root>
    <output aria-live="polite">TRPopover is {open ? 'open' : 'closed'}.</output>
  </>;
}` }]}
/>

## API

Base UI が anchor の配置、collision 回避、focus 動作、portal、dialog 関係、outside/Escape による解除を担当します。補助的な action では surface を non-modal に保ち、背後の page をブロックする必要がある task には TRDialog を使います。