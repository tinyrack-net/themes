---
title: "Spinner"
description: "サイズと意味のある色の軸を持ち、通知される不確定な進行状況です。"
section: components
order: 21
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRButton } from '@tinyrack/ui/components/button';
import { TRSpinner } from '@tinyrack/ui/components/spinner';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/spinner.demo.js';

## コントラクト

| 軸 | 値 | 既定値 |
| --- | --- | --- |
| `uiSize` | `sm`, `md`, `lg` | `md` |
| `variant` | `current`, `muted`, `primary`, `danger` | `current` |
| `label` | アクセス可能なステータステキスト | `Loading` |
| `decorative` | `true`, `false` | `false` |

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/spinner.css';", "import { TRSpinner } from '@tinyrack/ui/components/spinner';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs id="spinner-basic" title="loading 状態" preview={<TRSpinner data-docs-example-item="" label="サーバーを読み込んでいます" variant="primary" />} sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/spinner.css';
import { TRSpinner } from '@tinyrack/ui/components/spinner';

<TRSpinner label="サーバーを読み込み中" variant="primary" />` }]} />

## 例

<ComponentExampleTabs
  id="spinner-size-variant-matrix"
  title="サイズ"
  description="周囲のテキストやコントロールにスピナーのサイズを合わせてください。"
  preview={<div className="flex flex-wrap items-end gap-5"><span className="grid justify-items-center gap-1"><TRSpinner data-docs-example-item="" decorative uiSize="sm" /><span>小</span></span><span className="grid justify-items-center gap-1"><TRSpinner data-docs-example-item="" decorative uiSize="md" /><span>中</span></span><span className="grid justify-items-center gap-1"><TRSpinner data-docs-example-item="" decorative uiSize="lg" /><span>大</span></span></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/spinner.css';
import { TRSpinner } from '@tinyrack/ui/components/spinner';

export function SpinnerSizes() {
  return <div className="flex items-end gap-5">{(['sm', 'md', 'lg']).map((uiSize) => <TRSpinner decorative key={uiSize} uiSize={uiSize} />)}</div>;
}` }]}
/>

<ComponentExampleTabs
  id="spinner-variants"
  title="カラーバリアント"
  description="周囲の文字色を継承する場合は `current`、強調を抑える場合は `muted`、進行中の作業には `primary`、失敗した作業には `danger` を使用してください。"
  preview={<div className="flex flex-wrap items-center gap-5" data-docs-example-item-count="4">{(['current', 'muted', 'primary', 'danger']).map((variant) => <span className="grid justify-items-center gap-1" key={variant}><TRSpinner data-docs-example-item="" decorative variant={variant} /><span>{variant}</span></span>)}</div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/spinner.css';
import { TRSpinner } from '@tinyrack/ui/components/spinner';

export function SpinnerVariants() {
  return <div className="flex items-center gap-5">{(['current', 'muted', 'primary', 'danger']).map((variant) => <span className="grid justify-items-center gap-1" key={variant}><TRSpinner decorative variant={variant} /><span>{variant}</span></span>)}</div>;
}` }]}
/>

<ComponentExampleTabs
  id="spinner-status-ownership"
  title="単独 status または control の装飾"
  description="単独の TRSpinner が status を担当します。名前付き TRButton は loading 状態を持ち、内部の spinner を隠します。reduced motion では回転を止めます。"
  preview={(
    <div className="flex flex-wrap items-center gap-4">
      <TRSpinner data-docs-example-item="" label="サーバー一覧を読み込み中" variant="primary" />
      <TRButton data-docs-example-item="" aria-busy="true" disabled><TRSpinner decorative uiSize="sm" />インベントリを更新しています</TRButton>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRButton } from '@tinyrack/ui/components/button';
import { TRSpinner } from '@tinyrack/ui/components/spinner';

<div className="flex flex-wrap items-center gap-4">
  <TRSpinner label="サーバー一覧を読み込み中" variant="primary" />
  <TRButton aria-busy="true" disabled><TRSpinner decorative uiSize="sm" />インベントリを更新しています</TRButton>
</div>` }]}
/>

<ComponentExampleTabs
  id="spinner-task-lifecycle"
  title="タスク lifecycle"
  description="task を start、finish、reset し、作業中だけ通知された status が存在することを確認します。"
  preview={<div data-docs-example-item-count="1"><Stories.SpinnerLifecyclePreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRButton } from '@tinyrack/ui/components/button';
import { TRSpinner } from '@tinyrack/ui/components/spinner';
import { useState } from 'react';

function DeploymentStatus() {
  const [state, setState] = useState('idle');
  return (
    <div className="grid justify-items-start gap-3">
      <div className="flex flex-wrap gap-2">
        <TRButton disabled={state === 'running'} onClick={() => setState('running')}>
          Start task
        </TRButton>
        <TRButton appearance="outline" disabled={state !== 'running'} onClick={() => setState('complete')}>
          Finish task
        </TRButton>
        <TRButton appearance="ghost" disabled={state === 'idle'} onClick={() => setState('idle')}>
          Reset
        </TRButton>
      </div>
      {state === 'running' ? <TRSpinner label="ラックをデプロイしています" variant="primary" /> : null}
      {state === 'idle' ? <output aria-live="polite">タスクはまだ開始していません。</output> : null}
      {state === 'complete' ? <output aria-live="polite">タスクが完了しました。</output> : null}
    </div>
  );
}` }]}
/>

## API

TRSpinner は既定で `role="status"` を公開するため、空でないタスク固有の `label` を使います。空の label は `Loading` にフォールバックします。すでに名前のある loading control や status region 内では `decorative` を設定し、1 つの element だけを通知します。作業中だけ spinner を mount し、必要に応じて更新対象の region に `aria-busy="true"` を設定して、その region から完了を通知します。ユーザーが reduced motion を要求すると回転は停止します。

| CSS token | 既定値 | 用途 |
| --- | --- | --- |
| `--tr-spinner-size` | size recipe | Indicator diameter |
| `--tr-spinner-stroke-width` | `--tinyrack-spinner-stroke-width` | Ring thickness |
| `--tr-spinner-color` | semantic variant color | Active and track color |
| `--tinyrack-spinner-size-sm` | foundation metric | Small diameter |
| `--tinyrack-spinner-size-md` | foundation metric | Medium diameter |
| `--tinyrack-spinner-size-lg` | foundation metric | Large diameter |
| `--tinyrack-spinner-track-opacity` | foundation metric | Inactive ring opacity |
