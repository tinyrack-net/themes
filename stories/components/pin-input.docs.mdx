import { Meta } from '@storybook/addon-docs/blocks';
import { PinInput } from '../../src/components/pin-input/index.js';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import * as Stories from './pin-input.stories';

<Meta of={Stories} />

# Pin Input

One-time-code entry with grouped keyboard input, validation, and form integration.

## Contract

`length` must match the number of `Input` parts. Base UI provides paste, arrow-key, disabled, read-only, and controlled value behavior.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/pin-input.css';", "import { PinInput } from '@tinyrack/ui/components/pin-input';"] }]} />

## Usage

<ComponentExampleTabs id="pin-input-basic" title="Four-digit code" preview={<PinInput.Root aria-label="Verification code" length={4}><PinInput.Input /><PinInput.Input /><PinInput.Input /><PinInput.Input /></PinInput.Root>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<PinInput.Root aria-label="Verification code" length={4} name="code">
  <PinInput.Input /><PinInput.Input /><PinInput.Input /><PinInput.Input />
</PinInput.Root>` }]} />

## Examples

<ComponentExampleTabs
  id="pin-input-length-states"
  title="Length and validation states"
  previewLayout="start"
  preview={(
    <div className="grid gap-5">
      <div className="grid gap-2"><strong>Four digits</strong><PinInput.Root aria-label="Four digit code" length={4}><PinInput.Input /><PinInput.Input /><PinInput.Input /><PinInput.Input /></PinInput.Root></div>
      <div className="grid gap-2"><strong>Six digits</strong><PinInput.Root aria-label="Six digit code" length={6}><PinInput.Input /><PinInput.Input /><PinInput.Input /><PinInput.Input /><PinInput.Input /><PinInput.Input /></PinInput.Root></div>
      <div className="grid gap-2"><strong>Required</strong><PinInput.Root aria-label="Required code" length={4} required><PinInput.Input /><PinInput.Input /><PinInput.Input /><PinInput.Input /></PinInput.Root></div>
      <div className="grid gap-2"><strong>Disabled</strong><PinInput.Root aria-label="Disabled code" disabled length={4}><PinInput.Input /><PinInput.Input /><PinInput.Input /><PinInput.Input /></PinInput.Root></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<PinInput.Root length={4}>...</PinInput.Root>
<PinInput.Root length={6}>...</PinInput.Root>
<PinInput.Root required length={4}>...</PinInput.Root>
<PinInput.Root disabled length={4}>...</PinInput.Root>` }]}
/>

## API

Give the root an accessible label and `name` when its hidden input participates in form submission.
