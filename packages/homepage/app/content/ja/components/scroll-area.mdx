---
title: "ScrollArea"
description: "ネイティブスクロールを保つカスタムスクロールバーサーフェスです。"
section: components
order: 43
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRScrollArea } from '@tinyrack/ui/components/scroll-area';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/scroll-area.demo.js';

## コントラクト

この Tinyrack wrapper は Base UI の動作、React 19 refs、ネイティブ scrolling、`render` contract を保ちます。Playground control は vertical、horizontal、two-axis content に対応します。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/scroll-area.css';", "import { TRScrollArea } from '@tinyrack/ui/components/scroll-area';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="scroll-area-basic"
  title="縦方向の event log"
  preview={<Stories.ScrollAreaPreview autoHide={false} content={'Rack event log'} orientation={'vertical'} variant={'surface'} />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.scrollAreaVerticalSource }]}
/>

## 例

<ComponentExampleTabs
  id="scroll-area-states"
  title="2 軸 overflow"
  description="overflow する各 axis に scrollbar を 1 つ描画し、track が交わる場所に Corner を含めます。"
  preview={<Stories.ScrollAreaPreview autoHide={false} content={'Rack event log'} orientation={'both'} variant={'plain'} />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.scrollAreaBothSource }]}
/>

<ComponentExampleTabs
  id="scroll-area-horizontal"
  title="横方向 overflow"
  description="content が inline axis で overflow する場合は horizontal track だけを描画します。Viewport の native touch、trackpad、wheel、keyboard scrolling は維持されます。"
  preview={<Stories.ScrollAreaPreview autoHide={false} content={'Deployment stage'} orientation={'horizontal'} variant={'plain'} />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.scrollAreaHorizontalSource }]}
/>

<ComponentExampleTabs
  id="scroll-area-auto-hide"
  title="自動非表示 scrollbar"
  description="autoHide を設定して idle track を隠します。viewport を hover、scroll、focus すると再表示されます。reduced-motion 設定は opacity transition を取り除きます。"
  preview={<Stories.ScrollAreaPreview autoHide content={'Hover or focus this event log'} orientation={'vertical'} variant={'surface'} />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.scrollAreaAutoHideSource }]}
/>

<ComponentExampleTabs
  id="scroll-area-rtl"
  title="右から左への scrolling"
  description="DOM direction と DirectionProvider を同期し、horizontal wheel、track、thumb、overflow-edge の動作で RTL 座標を使用します。"
  preview={<Stories.ScrollAreaRTLPreview />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.scrollAreaRTLSource }]}
/>

## Anatomy

- `Root` は measurement、overflow-edge state、`surface` または `plain` の `variant`、任意の `autoHide` styling を所有します。
- `Viewport` は native scroll container で、accessible name、keyboard focus、wheel、touch、trackpad input を受け取ります。
- `Content` は vertical、horizontal、または 2 軸 overflow を作る layout を包みます。
- `Scrollbar` は `vertical` または `horizontal` track を 1 本描画し、`keepMounted` で維持できます。
- `Thumb` は `Scrollbar` 内の draggable position indicator です。
- `Corner` は両方の scrollbar が存在し overflow するときに交点を埋めます。

## API

| Part | Props と state | Notes |
| --- | --- | --- |
| `Root` | `TRScrollAreaRootProps`, `variant`, `autoHide`, `overflowEdgeThreshold`, `render`, `ref` | 既定は `surface` variant です。`plain` は surface border と content padding を取り除きます。 |
| `Viewport` | `TRScrollAreaViewportProps`, `TRScrollAreaViewportState`, `render`, `ref` | Native scroll container。Base UI は axis が overflow するときだけ focusable にします。 |
| `Content` | `TRScrollAreaContentProps`, `TRScrollAreaContentState`, `render`, `ref` | intrinsic width を保ち horizontal overflow を有効にします。 |
| `Scrollbar` | `TRScrollAreaScrollbarProps`, `TRScrollAreaScrollbarState`, `orientation`, `keepMounted`, `render`, `ref` | `orientation` の既定は `vertical`。track wheel input は対応する viewport axis を scroll します。 |
| `Thumb` | `TRScrollAreaThumbProps`, `TRScrollAreaThumbState`, `render`, `ref` | drag で native viewport scroll position を更新します。 |
| `Corner` | `TRScrollAreaCornerProps`, `TRScrollAreaCornerState`, `render`, `ref` | 両 axis が overflow している間だけ mount します。 |

Overflow は `data-overflow-x-start`、`data-overflow-x-end`、`data-overflow-y-start`、`data-overflow-y-end` に反映され、scrolling と track hover は `data-scrolling` と `data-hovering` を使用します。surface は `--tr-scroll-area-background` と `--tr-scroll-area-color`、track は `--tr-scroll-area-track-background`、thumb は `--tr-scroll-area-indicator-background` で customize できます。すべての part が consumer class、style、native prop、React 19 ref を保ちます。
