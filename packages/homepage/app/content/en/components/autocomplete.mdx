---
title: "Autocomplete"
description: "Accepts free-form text while filtering and announcing suggestions from a known collection."
section: components
group: selection-controls
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/autocomplete.demo.js';

## Contract

| Axis | Behavior |
| --- | --- |
| value | the text value remains editable even when it does not match an item |
| input anatomy | Label → InputGroup → optional InputAdornment + Input + Clear + Trigger; Icon belongs inside Trigger |
| popup anatomy | Status announces results; List contains groups, items, separators, and Empty feedback |

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/autocomplete.css';"],
      codeImports: ["import { TRAutocomplete } from '@tinyrack/ui/components/autocomplete';","import { TRForm } from '@tinyrack/ui/components/form';","import { TRField } from '@tinyrack/ui/components/field';","import { TRButton } from '@tinyrack/ui/components/button';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs
  id="autocomplete-basic"
  title="Rack search"
  preview={<div data-docs-example-item-count="1"><Stories.AutocompletePreview autoHighlight={false} defaultValue="" disabled={false} disabledItem={false} mode="list" openOnInputClick={false} placeholder="Search racks" readOnly={false} required={false} submitOnItemClick={false} /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteBasicSource }]}
/>

## Examples

<ComponentExampleTabs
  id="autocomplete-sizes"
  title="Sizes"
  description="Pass uiSize to InputGroup to align the Autocomplete control with neighboring Input, NumberField, or Button heights."
  preview={<div data-docs-example-item-count="3"><Stories.AutocompleteSizeComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteSizesSource }]}
/>

<ComponentExampleTabs
  id="autocomplete-states"
  title="Availability states"
  preview={<div data-docs-example-item-count="3"><Stories.AutocompleteStateComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteStatesSource }]}
/>

<ComponentExampleTabs
  id="autocomplete-modes"
  title="Input completion modes"
  description="Choose list suggestions, combined list and inline completion, inline completion only, or no completion behavior."
  preview={<div data-docs-example-item-count="4"><Stories.AutocompleteModeComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteBasicSource }]}
/>

<ComponentExampleTabs
  id="autocomplete-option-states"
  title="Option and result states"
  description="Compare an available collection, a disabled option, and empty feedback without mixing root availability states."
  preview={<div data-docs-example-item-count="3"><Stories.AutocompleteOptionStates /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteBehaviorsSource }]}
/>

<ComponentExampleTabs
  id="autocomplete-validation"
  title="Required text and recovery"
  preview={<div data-docs-example-item-count="1"><Stories.AutocompleteValidationPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteValidationSource }]}
/>

<ComponentExampleTabs
  id="autocomplete-behaviors"
  title="Free-form input semantics"
  description="A value may remain outside the collection; selecting a suggestion still updates the same editable text value."
  preview={<div data-docs-example-item-count="2"><Stories.AutocompleteBehaviorComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteBehaviorsSource }]}
/>

<ComponentExampleTabs
  id="autocomplete-overlay"
  title="Overlay composition"
  description="Portal mounts the popup outside the control; Positioner anchors it, and Escape dismisses it and restores focus."
  preview={<div data-docs-example-item-count="1"><Stories.AutocompleteOverlayPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteBasicSource }]}
/>

<ComponentExampleTabs
  id="autocomplete-keyboard"
  title="Keyboard selection"
  description="Type to filter, use Arrow keys to move the highlight, press Enter to accept it, and press Escape to close the popup."
  preview={<div data-docs-example-item-count="1"><Stories.AutocompleteKeyboardPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteBasicSource }]}
/>

<ComponentExampleTabs
  id="autocomplete-reset"
  title="Native form reset"
  description="Reset restores the uncontrolled default value and clears submitted output."
  preview={<div data-docs-example-item-count="1"><Stories.AutocompleteResetPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.autocompleteResetSource }]}
/>

## API

The complete Base UI anatomy is exported from `@tinyrack/ui/components/autocomplete`: `Root`, `Value`, `Trigger`, `Input`, `InputGroup`, `Icon`, `Clear`, `List`, `Status`, `Portal`, `Backdrop`, `Positioner`, `Popup`, `Arrow`, `Group`, `GroupLabel`, `Item`, `Row`, `Collection`, `Empty`, and `Separator`. `InputAdornment` is Tinyrack's decorative input companion. `InputGroup` accepts `uiSize?: 'sm' | 'md' | 'lg'` (default `md`) to align the input, adornment, clear, and trigger heights with neighboring controls. Every part has a named export and prop type; Base UI state, root action/event-detail, filter, and filter-option types are also available with `TRAutocomplete*` names. Use `useAutocompleteFilter`/`TRAutocomplete.useFilter` for locale-aware matching and `useAutocompleteFilteredItems`/`TRAutocomplete.useFilteredItems` inside `Root` to read its filtered collection.
