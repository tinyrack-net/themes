import { Meta } from '@storybook/addon-docs/blocks';
import { Code } from '../../src/components/code/index.js';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import * as Stories from './code.stories';

<Meta of={Stories} />

# Code

Inline code tokens that stay readable inside prose, lists, and dense metadata.

## Contract

`Code` is a thin semantic `code` element. It has no visual variant axis and inherits surrounding text size.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/code.css';", "import { Code } from '@tinyrack/ui/components/code';"] }]} />

## Usage

<ComponentExampleTabs id="code-basic" title="Inline command" preview={<p className="m-0">Run <Code>pnpm verify</Code> before publishing.</p>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<p>Run <Code>pnpm verify</Code> before publishing.</p>` }]} />

## Examples

<ComponentExampleTabs
  id="code-contexts"
  title="Content contexts"
  description="Inline code keeps its semantic surface in prose, configuration, and long tokens."
  previewLayout="start"
  preview={(
    <div className="grid max-w-full gap-3">
      <p className="m-0">Import <Code>@tinyrack/ui/components/button</Code>.</p>
      <p className="m-0">Set <Code>data-theme=&quot;tinyrack-dark&quot;</Code> on the root.</p>
      <p className="m-0 max-w-full overflow-x-auto"><Code>very-long-rack-identifier-with-overflow-safe-scrolling-01</Code></p>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<p>Import <Code>@tinyrack/ui/components/button</Code>.</p>
<p>Set <Code>data-theme="tinyrack-dark"</Code> on the root.</p>` }]}
/>

## API

All native `code` props, `className`, `style`, and React 19 `ref` are forwarded.
