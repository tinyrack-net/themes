---
title: "Controls"
description: "하나의 크기 레시피로 컴포넌트 시스템 전체의 액션, 필드, 탐색을 정렬합니다."
section: foundations
order: 7
---
import { TRButton } from '@tinyrack/ui/components/button';
import { TRField } from '@tinyrack/ui/components/field';
import { TRCode } from '@tinyrack/ui/components/code';
import { TRTable } from '@tinyrack/ui/components/table';
import { tinyrackControlMetrics } from '@tinyrack/ui/core';

## 원칙

밀도 높은 보조 액션에는 `sm`, 기본 운영 인터페이스에는 `md`, 터치 편의나 강조가 필요할 때만 `lg`를 사용하세요.

## 시각적 스케일

<div className="grid gap-tinyrack-md" role="list">
  {['sm', 'md', 'lg'].map((size) => (
    <section className="grid items-center gap-tinyrack-md border border-tinyrack-border p-tinyrack-lg md:grid-cols-[7rem_minmax(0,1fr)]" key={size} role="listitem">
      <div><strong>{size}</strong><div className="text-tinyrack-sm text-tinyrack-text-muted"><TRCode>{tinyrackControlMetrics[size].height}</TRCode></div></div>
      <div className="flex flex-wrap items-center gap-tinyrack-sm"><TRButton uiSize={size}>Deploy</TRButton><TRField.Root><TRField.Control aria-label={`${size} rack name`} data-uiSize={size} placeholder="rack-a" /></TRField.Root></div>
    </section>
  ))}
</div>

## 적용 패턴

<section className="grid gap-tinyrack-lg border border-tinyrack-border p-tinyrack-lg">
  <div className="grid gap-tinyrack-sm md:grid-cols-[minmax(0,1fr)_auto]"><TRField.Root><TRField.Control aria-label="Rack 이름" placeholder="rack-a" /></TRField.Root><TRButton variant="primary">Save rack</TRButton></div>
  <div className="text-tinyrack-sm text-tinyrack-text-muted">두 컨트롤은 동일한 40px 중간 높이와 타이포그래피 레시피를 사용합니다.</div>
</section>

## 구현

제품 전체에 다른 밀도가 필요하면 전역 metric을 재정의하고, 하나의 컴포넌트에 의도적인 예외가 필요하면 `--tr-*` 레시피 토큰을 사용하세요.

```css
:root {
  --tinyrack-control-height-md: 2.5rem;
}

.rack-toolbar {
  --tr-btn-height: 2.25rem;
}
```

## 참조

<TRTable.Root containerProps={{ 'aria-label': 'Control metric reference', tabIndex: 0 }} data-foundation-reference="controls">
    <thead><tr><th scope="col">Size</th><th scope="col">Height</th><th scope="col">Padding</th><th scope="col">Gap</th><th scope="col">Type</th></tr></thead>
    <tbody>{['sm', 'md', 'lg'].map((size) => <tr key={size}><th scope="row">{size}</th><td><TRCode>{tinyrackControlMetrics[size].height}</TRCode></td><td><TRCode>{tinyrackControlMetrics[size].paddingInline}</TRCode></td><td><TRCode>{tinyrackControlMetrics[size].gap}</TRCode></td><td><TRCode>{tinyrackControlMetrics[size].fontSize}</TRCode></td></tr>)}</tbody>
  </TRTable.Root>

## Tailwind 레퍼런스

컨트롤 치수와 타이포그래피는 [간격과 컨트롤 utility 매핑](/ko/foundations/tailwind/#spacing-controls)에서 확인하세요.
