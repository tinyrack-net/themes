---
title: "Field"
description: "ラベル、コントロール、説明、ネイティブ妥当性、エラーを組み合わせるアクセシブルな構成です。"
section: components
order: 13
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/field.demo.js';

## 主なプロパティ

`TRField` は Base UI Field の全構造をラップします。`Label`、`Description`、表示中の `Error` はコントロールに自動的に関連付けられます。`Root` の `name` と `disabled` は `Control` の同名プロパティより優先されます。

| パーツ | 責務 |
| --- | --- |
| `Root` | 名前、利用可否、検証タイミング、dirty・touched・filled・focused の状態 |
| `Label` | アクセシブルな名前と、クリックでフォーカスする対象 |
| `Control` | ネイティブ入力の値、イベント、妥当性、フォームデータ、リセット |
| `Description` | `aria-describedby` に含まれる常時表示の説明 |
| `Error` | ネイティブの妥当性または外部の状態に応じた復旧メッセージ |
| `Validity` | 値、エラー、ネイティブの `ValidityState` を読み取るレンダープロップ |
| `Item` | コレクション内のチェックボックス・ラジオとラベル・説明の配置 |

値をフォーカス可能なまま送信する場合は `Control` の `readOnly`、操作不可かつフォームデータから除外する場合は `Root` の `disabled` を使います。標準的なパーツの組み合わせでは、`aria-labelledby` と `aria-describedby` を手動で設定する必要はありません。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/field.css';", "import { TRField } from '@tinyrack/ui/components/field';"] }]} />

## プレイグラウンド

プレビュー内で入力すると、操作対象の値が変わります。プレイグラウンドのコントロールでは required、invalid、read-only、disabled、size を切り替えられ、リセットするとコントロールとプレビューの値が初期値に戻ります。

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="field-basic"
  title="ラベル付きメール Field"
  previewLayout="stretch"
  preview={<Stories.FieldPreview data-docs-example-item="" defaultValue="ops@example.com" disabled={false} invalid={false} label="メール" required size="md" />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/field.css';
import { TRField } from '@tinyrack/ui/components/field';

export function AlertEmailField() {
  return <TRField.Root name="email">
    <TRField.Label>メール</TRField.Label>
    <TRField.Control defaultValue="ops@example.com" required type="email" />
    <TRField.Description>運用アラートの送信先です。</TRField.Description>
    <TRField.Error match="valueMissing">メールアドレスは必須です。</TRField.Error>
    <TRField.Error match="typeMismatch">有効なメールアドレスを入力してください。</TRField.Error>
  </TRField.Root>;
}` }]}
/>

## 例

<ComponentExampleTabs
  id="field-sizes"
  title="サイズ"
  description="周囲のコントロールとコンテンツの密度に合うサイズを選びます。"
  previewLayout="stretch"
  preview={<div className="grid gap-5 sm:grid-cols-3"><Stories.FieldPreview data-docs-example-item="" defaultValue="rack-small" disabled={false} invalid={false} label="小さいフィールド" size="sm" /><Stories.FieldPreview data-docs-example-item="" defaultValue="rack-medium" disabled={false} invalid={false} label="中サイズのフィールド" size="md" /><Stories.FieldPreview data-docs-example-item="" defaultValue="rack-large" disabled={false} invalid={false} label="大きいフィールド" size="lg" /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/field.css';
import { TRField } from '@tinyrack/ui/components/field';

export function FieldSizes() {
  return <div className="grid gap-5 sm:grid-cols-3">
    <TRField.Root uiSize="sm"><TRField.Label>小さいフィールド</TRField.Label><TRField.Control defaultValue="rack-small" /></TRField.Root>
    <TRField.Root uiSize="md"><TRField.Label>中サイズのフィールド</TRField.Label><TRField.Control defaultValue="rack-medium" /></TRField.Root>
    <TRField.Root uiSize="lg"><TRField.Label>大きいフィールド</TRField.Label><TRField.Control defaultValue="rack-large" /></TRField.Root>
  </div>;
}` }]}
/>

<ComponentExampleTabs
  id="field-field-states"
  title="利用可否と有効性の状態"
  description="回復案内には invalid、フォームデータから値を除外する場合は disabled、フォーカスと送信値を保つ場合は readOnly を使います。"
  previewLayout="stretch"
  preview={<div className="grid gap-5 sm:grid-cols-3"><Stories.FieldPreview data-docs-example-item="" defaultValue="bad value" disabled={false} invalid label="無効なフィールド" size="md" /><Stories.FieldPreview data-docs-example-item="" defaultValue="rack-disabled" disabled invalid={false} label="無効化したフィールド" size="md" /><Stories.FieldPreview data-docs-example-item="" defaultValue="rack-managed" disabled={false} invalid={false} label="読み取り専用フィールド" readOnly size="md" /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/field.css';
import { TRField } from '@tinyrack/ui/components/field';

export function FieldStates() {
  return <div className="grid gap-5 sm:grid-cols-3">
    <TRField.Root invalid><TRField.Label>無効なフィールド</TRField.Label><TRField.Control defaultValue="bad value" /><TRField.Error match>有効なラック名を入力してください。</TRField.Error></TRField.Root>
    <TRField.Root disabled name="disabled-rack"><TRField.Label>無効化したフィールド</TRField.Label><TRField.Control defaultValue="rack-disabled" /></TRField.Root>
    <TRField.Root name="managed-rack"><TRField.Label>読み取り専用フィールド</TRField.Label><TRField.Control defaultValue="rack-managed" readOnly /></TRField.Root>
  </div>;
}` }]}
/>

<ComponentExampleTabs
  id="field-validation"
  title="送信、復旧、リセット"
  description="メールのネイティブな妥当性に対応するエラーを選び、リセットすると入力とフィードバックが復元されます。"
  preview={<div data-docs-example-item=""><Stories.FieldValidationPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { useState } from 'react';
import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/button.css';
import { TRButton } from '@tinyrack/ui/components/button';
import '@tinyrack/ui/components/field.css';
import { TRField } from '@tinyrack/ui/components/field';
import '@tinyrack/ui/components/form.css';
import { TRForm } from '@tinyrack/ui/components/form';

export function EmailValidation() {
  const [attempted, setAttempted] = useState(false);
  const [valid, setValid] = useState(false);
  return <TRForm noValidate onReset={() => { setAttempted(false); setValid(false); }} onSubmit={(event) => { event.preventDefault(); event.currentTarget.checkValidity(); setAttempted(true); }}>
    <TRField.Root invalid={attempted && !valid} name="email">
      <TRField.Label>アラートメール</TRField.Label>
      <TRField.Control onChange={(event) => setValid(event.currentTarget.validity.valid)} onInvalid={(event) => { event.preventDefault(); setValid(false); }} required type="email" />
      <TRField.Error match="valueMissing">メールアドレスは必須です。</TRField.Error>
      <TRField.Error match="typeMismatch">有効なメールアドレスを入力してください。</TRField.Error>
    </TRField.Root>
    <TRButton type="submit">保存</TRButton><TRButton type="reset" variant="secondary">リセット</TRButton>
  </TRForm>;
}` }]}
/>

<ComponentExampleTabs
  id="field-item-validity"
  title="コレクション項目とネイティブの妥当性"
  description="Item は選択肢と説明を配置し、Validity は検証ルールを重複させずに現在の状態を公開します。"
  preview={<div data-docs-example-item=""><Stories.FieldItemValidityPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/checkbox.css';
import { TRCheckbox } from '@tinyrack/ui/components/checkbox';
import '@tinyrack/ui/components/field.css';
import { TRField } from '@tinyrack/ui/components/field';

export function NotificationChannels() {
  return <TRField.Root><TRField.Label>通知チャネル</TRField.Label>
    <TRField.Item><TRCheckbox.Root defaultChecked name="channel" value="email"><TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator></TRCheckbox.Root><div><TRField.Label>メール</TRField.Label><TRField.Description>デプロイ結果をメールで送信します。</TRField.Description></div></TRField.Item>
    <TRField.Validity>{(state) => <output>{state.validity.valid === null ? '未検証' : state.validity.valid ? '有効' : '無効'}</output>}</TRField.Validity>
  </TRField.Root>;
}` }]}
/>

## API

### `TRField.Root`

| プロパティ | 型 / 既定値 | 用途 |
| --- | --- | --- |
| `name` | `string` | フィールドを命名し、`Control.name` より優先します |
| `disabled` | `boolean` / `false` | コントロールを無効化し、すべてのパーツに `data-disabled` を付与します |
| `invalid`, `dirty`, `touched` | `boolean` | 外部で管理するフィールドの状態 |
| `validate` | 同期・非同期バリデーター | エラー、エラー配列、または `null` を返します |
| `validationMode` | `'onSubmit' \| 'onBlur' \| 'onChange'` / `'onSubmit'` | 検証タイミング |
| `validationDebounceTime` | `number` / `0` | onChange 検証のデバウンス時間（ミリ秒） |
| `actionsRef` | `RefObject<TRFieldRootActions>` | `validate()` を公開します |
| `uiSize` | `'sm' \| 'md' \| 'lg'` / `'md'` | コントロールの表示サイズ |

`Control` はネイティブ入力のプロパティ、`value` / `defaultValue`、`onValueChange(value, details)` を受け取ります。`Label.nativeLabel={false}` は、`render` が label 以外のホスト要素を使う場合に指定します。`Error.match` は `true` または `valueMissing` などの `ValidityState` のキー、`Item.disabled` は単一の選択肢の無効化、`Validity` は `validity`、`value`、`initialValue`、`error`、`errors` を提供します。

すべてのホストパーツは `ref`、ネイティブ属性・イベント、関数形式の `className` / `style`、Base UI の `render` を保持します。名前空間、7 つの名前付きパーツ、各プロパティ・状態型、`TRFieldUiSize`、`TRFieldRootActions`、`TRFieldValidityData`、Control の変更イベント型はすべて個別にエクスポートされます。CSS は明示的に読み込みます。
