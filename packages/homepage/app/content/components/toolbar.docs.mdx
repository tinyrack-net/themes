import { ComponentPlayground } from '../../playground/playground.js';

import { Toolbar } from '@tinyrack/ui/components/toolbar';
import { Toggle } from '@tinyrack/ui/components/toggle';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import * as Stories from './toolbar.demo.js';


# Toolbar

Groups related controls with roving keyboard focus.

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Playground controls cover `label`, `disabled`, `orientation`, and `loopFocus`.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/toolbar.css';", "import { Toolbar } from '@tinyrack/ui/components/toolbar';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs
  id="toolbar-basic"
  title="Formatting controls"
  description="Compose Toolbar.Button with stateful controls through Base UI's render contract. Every Toolbar and Group has its own accessible name."
  preview={<Toolbar.Root aria-label="Editor controls"><Toolbar.Group aria-label="Text formatting"><Toolbar.Button render={<Toggle defaultPressed />}>Bold</Toolbar.Button><Toolbar.Button render={<Toggle />}>Italic</Toolbar.Button></Toolbar.Group><Toolbar.Separator /><Toolbar.Link href="#help">Help</Toolbar.Link></Toolbar.Root>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/toggle.css';
import '@tinyrack/ui/components/toolbar.css';
import { Toggle } from '@tinyrack/ui/components/toggle';
import { Toolbar } from '@tinyrack/ui/components/toolbar';

export function FormattingToolbar() {
  return (
    <Toolbar.Root aria-label="Editor controls">
      <Toolbar.Group aria-label="Text formatting">
        <Toolbar.Button render={<Toggle defaultPressed />}>Bold</Toolbar.Button>
        <Toolbar.Button render={<Toggle />}>Italic</Toolbar.Button>
      </Toolbar.Group>
      <Toolbar.Separator />
      <Toolbar.Link href="#help">Help</Toolbar.Link>
    </Toolbar.Root>
  );
}` }]}
/>

## Examples

<ComponentExampleTabs
  id="toolbar-states"
  title="Orientation and disabled states"
  description="Left/Right moves through horizontal controls; Up/Down follows the visual column in vertical toolbars. Disabled groups remain visible and named while suppressing activation."
  preview={<Stories.ToolbarStateComparisonPreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid gap-6 sm:grid-cols-2">
  <section className="sm:col-span-2">
    <strong>Horizontal</strong>
    <Toolbar.Root aria-label="Horizontal editor controls">
      <Toolbar.Group aria-label="Text formatting">
        <Toolbar.Button>Bold</Toolbar.Button>
        <Toolbar.Button>Italic</Toolbar.Button>
      </Toolbar.Group>
      <Toolbar.Separator />
      <Toolbar.Link href="#help">Help</Toolbar.Link>
    </Toolbar.Root>
  </section>

  <section>
    <strong>Vertical</strong>
    <Toolbar.Root aria-label="Vertical editor controls" orientation="vertical">
      <Toolbar.Group aria-label="Text formatting">
        <Toolbar.Button>Bold</Toolbar.Button>
        <Toolbar.Button>Italic</Toolbar.Button>
      </Toolbar.Group>
      <Toolbar.Separator />
      <Toolbar.Link href="#help">Help</Toolbar.Link>
      <Toolbar.Input aria-label="Document title" placeholder="Document title" />
    </Toolbar.Root>
  </section>

  <section>
    <strong>Disabled group</strong>
    <Toolbar.Root aria-label="Save history controls" orientation="vertical">
      <Toolbar.Group aria-label="Available commands">
        <Toolbar.Button>Save</Toolbar.Button>
      </Toolbar.Group>
      <Toolbar.Separator />
      <Toolbar.Group aria-label="Unavailable history commands" disabled>
        <Toolbar.Button>Undo</Toolbar.Button>
        <Toolbar.Button>Redo</Toolbar.Button>
      </Toolbar.Group>
    </Toolbar.Root>
  </section>
</div>` }]}
/>

<span id="help"></span>

## API

Every compound part and prop/state type is exported from `@tinyrack/ui/components/toolbar`; styles remain opt-in through the matching CSS subpath. `Root` defaults to horizontal arrow-key focus with looping enabled; vertical toolbars use Up/Down. `Group` names related controls, `Input` keeps normal text-editing keys, and disabled groups/items suppress activation. In a horizontal toolbar, use at most one Input and place it last so text-editing keys do not block later toolbar items. Keep a focusable disabled Button visibly unavailable, and give every Toolbar and nested Group an accessible name.
