---
title: "Separator"
description: "Semantic separators for horizontal sections and vertical control groups."
section: components
order: 12
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRSeparator } from '@tinyrack/ui/components/separator';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/separator.demo.js';

## Contract

`TRSeparator` marks a meaningful boundary with `role="separator"`. Its `orientation` accepts `horizontal` or `vertical` and defaults to `horizontal`. Use `role="presentation"` only when the line is purely visual and does not separate content.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/separator.css';", "import { TRSeparator } from '@tinyrack/ui/components/separator';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs id="separator-basic" title="Section separator" previewLayout="stretch" preview={<section className="grid w-full gap-3"><h3 className="m-0">Overview</h3><TRSeparator /><p className="m-0">Network activity is healthy.</p></section>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/separator.css';
import { TRSeparator } from '@tinyrack/ui/components/separator';

export function NetworkOverview() {
  return (
    <section className="grid gap-3">
      <h3>Overview</h3>
      <TRSeparator />
      <p>Network activity is healthy.</p>
    </section>
  );
}` }]} />

## Examples

<ComponentExampleTabs
  id="separator-orientations"
  title="Orientations"
  previewLayout="stretch"
  preview={(
    <div className="grid gap-6">
      <div className="grid gap-2"><strong>Horizontal</strong><TRSeparator /></div>
      <div className="grid gap-2"><strong>Vertical</strong><div className="flex h-12 items-center gap-4"><span>CPU</span><TRSeparator orientation="vertical" /><span>Memory</span><TRSeparator orientation="vertical" /><span>Storage</span></div></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/separator.css';
import { TRSeparator } from '@tinyrack/ui/components/separator';

export function SeparatorOrientations() {
  return (
    <div className="grid gap-6">
      <div className="grid gap-2">
        <strong>Horizontal</strong>
        <TRSeparator />
      </div>
      <div className="grid gap-2">
        <strong>Vertical</strong>
        <div className="flex h-12 items-center gap-4">
          <span>CPU</span>
          <TRSeparator orientation="vertical" />
          <span>Memory</span>
          <TRSeparator orientation="vertical" />
          <span>Storage</span>
        </div>
      </div>
    </div>
  );
}` }]}
/>

<ComponentExampleTabs
  id="separator-semantics"
  title="Meaningful and decorative"
  description="Keep the default role for a content boundary. Remove separator semantics from a line that is only visual."
  preview={(
    <div className="grid gap-4">
      <section className="grid gap-2"><strong>Related settings</strong><TRSeparator /><span>The separator marks a section boundary.</span></section>
      <div className="grid gap-2"><strong>Decorative accent</strong><TRSeparator role="presentation" /><span>The line adds visual rhythm only.</span></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/separator.css';
import { TRSeparator } from '@tinyrack/ui/components/separator';

export function SeparatorSemantics() {
  return (
    <div className="grid gap-4">
      <section className="grid gap-2">
        <strong>Related settings</strong>
        <TRSeparator />
        <span>The separator marks a section boundary.</span>
      </section>
      <div className="grid gap-2">
        <strong>Decorative accent</strong>
        <TRSeparator role="presentation" />
        <span>The line adds visual rhythm only.</span>
      </div>
    </div>
  );
}` }]}
/>

## API

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `orientation` | `'horizontal' \| 'vertical'` | `'horizontal'` | Sets the visual direction and the separator's `aria-orientation`. |
| `role` | `React.AriaRole` | `'separator'` | Use `presentation` for a purely decorative line; separator-only orientation ARIA is then omitted. |
| `render` | `ReactElement` | `<div />` | Replaces the rendered element while preserving separator behavior and classes. |

All supported native `<div>` props, `className`, `style`, and `ref` are forwarded. Customize appearance with `--tr-separator-color`, `--tr-separator-thickness`, and `--tr-separator-min-length`.
