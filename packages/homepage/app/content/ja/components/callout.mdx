---
title: "Callout"
description: "共有の TRAlert サーフェスを基盤にした、意味のあるドキュメント通知です。"
section: components
order: 51
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { TRCallout } from '@tinyrack/ui/components/callout';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/callout.demo.js';

## コントラクト

Callout は補足ガイダンスを文書の主要な流れから分離します。`note` は背景情報、`tip` は任意の助言、`caution` は回避可能なリスク、`danger` はデータ損失など重大な損害を招く操作に使用します。既定では静的なコンテンツであり、ライブ通知ではありません。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/callout.css';", "import { TRCallout } from '@tinyrack/ui/components/callout';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs id="callout-basic" title="記述したガイダンス" preview={<div data-docs-example-item-count="1"><Stories.CalloutPreview variant="note" /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/callout.css';
import { TRCallout } from '@tinyrack/ui/components/callout';

export function CredentialsNote() {
  return <TRCallout title="注記" variant="note">認証情報をソース管理に保存しないでください。</TRCallout>;
}` }]} />

## 例

<ComponentExampleTabs
  id="callout-variants"
  title="意味論的バリアント"
  description="装飾ではなく、メッセージの目的や結果に基づいてバリアントを選択します。"
  previewLayout="stretch"
  preview={<div className="grid w-full gap-4"><TRCallout data-docs-example-item="" title="注記" variant="note">作業の理解に役立つ背景情報です。</TRCallout><TRCallout data-docs-example-item="" title="ヒント" variant="tip">作業を進めやすくする任意の方法です。</TRCallout><TRCallout data-docs-example-item="" title="注意" variant="caution">予期しない結果を招く可能性がある条件です。</TRCallout><TRCallout data-docs-example-item="" title="危険" variant="danger">データ損失や停止につながる操作です。</TRCallout></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/callout.css';
import { TRCallout, type TRCalloutVariant } from '@tinyrack/ui/components/callout';

const messages: Record<TRCalloutVariant, string> = {
  note: '作業の理解に役立つ背景情報です。',
  tip: '作業を進めやすくする任意の方法です。',
  caution: '予期しない結果を招く可能性がある条件です。',
  danger: 'データ損失や停止につながる操作です。',
};
const titles: Record<TRCalloutVariant, string> = { note: '注記', tip: 'ヒント', caution: '注意', danger: '危険' };

export function CalloutVariants() {
  return (<> {Object.entries(messages).map(([variant, message]) => (
    <TRCallout key={variant} title={titles[variant as TRCalloutVariant]} variant={variant as TRCalloutVariant}>{message}</TRCallout>
  ))} </>);
}` }]}
/>

<ComponentExampleTabs
  id="callout-rich-content"
  title="構造化されたガイダンス"
  description="children はブロックコンテンツを受け取るため、段落、リスト、インラインコードは正しい HTML 構造を保ちます。"
  preview={<div data-docs-example-item-count="1"><Stories.CalloutRichContentPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/callout.css';
import '@tinyrack/ui/components/code.css';
import { TRCallout } from '@tinyrack/ui/components/callout';
import { TRCode } from '@tinyrack/ui/components/code';

export function DeploymentCaution() {
  return <TRCallout title="デプロイ前の確認" variant="caution">
    <p>本番環境が選択されていることを確認してから、次を行います。</p>
    <ul>
      <li>保留中のマイグレーションを確認する</li>
      <li>データベースのバックアップを作成する</li>
    </ul>
    <p>両方の確認が完了してから <TRCode>pnpm deploy</TRCode> を実行してください。</p>
  </TRCallout>;
}` }]}
/>

## API

### 内容、バリアント、HTML

| プロパティ | 型 | 既定値 | 用途 |
| --- | --- | --- | --- |
| `children` | `ReactNode` | 必須 | インラインまたはブロックレベルのガイダンスです。 |
| `variant` | `'note' \| 'tip' \| 'caution' \| 'danger'` | `'note'` | 意味論的な見出しと視覚表現を選択します。 |
| `title` | `ReactNode` | バリアント別の見出し | ローカライズを含め、既定の英語見出しを置き換えます。 |

`TRCallout` はネイティブの `div` プロパティ、イベント、`className`、`style`、React 19 の `ref` も転送します。動的に挿入する緊急または丁寧な通知には `role="alert"` または `role="status"` を明示してください。静的な文書ガイダンスにはライブリージョンのロールを追加しないでください。
