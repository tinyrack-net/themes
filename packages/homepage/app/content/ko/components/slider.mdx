---
title: "Slider"
description: "가로 또는 세로 track에서 하나의 값이나 정렬된 범위를 선택해요."
section: components
order: 45
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/slider.demo.js';

## 핵심 속성

| 속성 | 동작 |
| --- | --- |
| value | 단일 slider는 숫자 또는 항목 하나짜리 배열을 받고, range는 정렬된 배열과 값마다 인덱스를 지정한 Thumb를 사용해요. |
| range | `min`, `max`, `step`, `minStepsBetweenValues`가 pointer와 keyboard 변경 폭을 제한해요. |
| form | `name`은 현재 thumb 값을 native hidden input으로 직렬화해요. |
| validation | TRSlider에는 native `required`가 없어요. 비즈니스 규칙은 `TRField.Root`에 두고 `TRField.Error`를 표시한 뒤 유효하지 않은 thumb로 focus를 옮기세요. |

밀도 높은 컨트롤 화면에 slider를 넣을 때는 `uiSize="sm"`을 사용하세요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/slider.css';"],
      codeImports: ["import { TRSlider } from '@tinyrack/ui/components/slider';","import { TRField } from '@tinyrack/ui/components/field';","import { TRForm } from '@tinyrack/ui/components/form';","import { TRButton } from '@tinyrack/ui/components/button';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs
  id="slider-basic"
  title="볼륨"
  preview={<Stories.SliderPreview data-docs-example-item="" defaultValue={48} disabled={false} label="볼륨이에요" orientation="horizontal" />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSlider } from '@tinyrack/ui/components/slider';

<TRSlider.Root defaultValue={[48]} name="volume">
  <TRSlider.Label>볼륨</TRSlider.Label>
  <TRSlider.Value />
  <TRSlider.Control>
    <TRSlider.Track><TRSlider.Indicator /></TRSlider.Track>
    <TRSlider.Thumb aria-label="볼륨이에요" />
  </TRSlider.Control>
</TRSlider.Root>` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/slider.css';" },
    },
  ]}
/>

## 예시

<ComponentExampleTabs
  id="slider-sizes"
  title="크기"
  preview={<div data-docs-example-item-count="3"><Stories.SliderSizeComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSlider } from '@tinyrack/ui/components/slider';

<div className="grid gap-6">
  {(['sm', 'md', 'lg'] as const).map((uiSize) => (
    <TRSlider.Root defaultValue={[48]} key={uiSize} uiSize={uiSize}>
      <TRSlider.Label>{uiSize.toUpperCase()} volume</TRSlider.Label>
      <TRSlider.Value />
      <TRSlider.Control>
        <TRSlider.Track><TRSlider.Indicator /></TRSlider.Track>
        <TRSlider.Thumb aria-label={uiSize.toUpperCase() + ' volume'} />
      </TRSlider.Control>
    </TRSlider.Root>
  ))}
</div>` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/slider.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="slider-states"
  title="방향"
  preview={<div data-docs-example-item-count="2"><Stories.SliderStateComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSlider } from '@tinyrack/ui/components/slider';

<div className="grid gap-6 sm:grid-cols-2">
  <TRSlider.Root defaultValue={[48]} name="horizontal-volume">
    <TRSlider.Label>가로 볼륨</TRSlider.Label><TRSlider.Value /><TRSlider.Control><TRSlider.Track><TRSlider.Indicator /></TRSlider.Track><TRSlider.Thumb aria-label="가로 볼륨" /></TRSlider.Control>
  </TRSlider.Root>
  <TRSlider.Root defaultValue={[36]} name="vertical-volume" orientation="vertical">
    <TRSlider.Label>세로 볼륨</TRSlider.Label><TRSlider.Value /><TRSlider.Control><TRSlider.Track><TRSlider.Indicator /></TRSlider.Track><TRSlider.Thumb aria-label="세로 볼륨" /></TRSlider.Control>
  </TRSlider.Root>
</div>` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/slider.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="slider-disabled"
  title="사용할 수 없는 슬라이더"
  description="값은 보여 주되 조작하거나 제출할 수 없어야 할 때 Root를 비활성화하세요."
  preview={<div data-docs-example-item-count="1"><Stories.SliderAvailabilityPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSlider } from '@tinyrack/ui/components/slider';

<TRSlider.Root defaultValue={[82]} disabled name="volume">
  <TRSlider.Label>사용할 수 없는 볼륨</TRSlider.Label><TRSlider.Value />
  <TRSlider.Control><TRSlider.Track><TRSlider.Indicator /></TRSlider.Track><TRSlider.Thumb aria-label="사용할 수 없는 볼륨이에요" /></TRSlider.Control>
</TRSlider.Root>` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/slider.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="slider-range"
  title="두 thumb 범위"
  description="Root는 Intl.NumberFormat으로 보이는 값을 형식화하고, 각 Thumb은 보조 기술에 역할을 설명하는 값 텍스트를 제공해요."
  preview={<div data-docs-example-item-count="1"><Stories.SliderRangePreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSlider } from '@tinyrack/ui/components/slider';

<TRSlider.Root
  defaultValue={[20, 80]}
  format={{ maximumFractionDigits: 0, style: 'unit', unit: 'percent' }}
  locale="ko"
  minStepsBetweenValues={10}
  name="window"
>
  <TRSlider.Label>유지보수 구간</TRSlider.Label>
  <TRSlider.Value />
  <TRSlider.Control>
    <TRSlider.Track><TRSlider.Indicator /></TRSlider.Track>
    <TRSlider.Thumb
      aria-label="시작이에요"
      getAriaValueText={(formattedValue) => formattedValue + '에 시작해요'}
      index={0}
    />
    <TRSlider.Thumb
      aria-label="끝이에요"
      getAriaValueText={(formattedValue) => formattedValue + '에 끝나요'}
      index={1}
    />
  </TRSlider.Control>
</TRSlider.Root>` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/slider.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="slider-form"
  title="폼 제출"
  preview={<div data-docs-example-item-count="1"><Stories.SliderFormPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { useState } from 'react';
import { TRButton } from '@tinyrack/ui/components/button';
import { TRForm } from '@tinyrack/ui/components/form';
import { TRSlider } from '@tinyrack/ui/components/slider';

function VolumeForm() {
  const [submitted, setSubmitted] = useState<number | null>(null);
  const [value, setValue] = useState([48]);

  return (
    <TRForm onSubmit={(event) => { event.preventDefault(); setSubmitted(value[0]); }}>
      <TRSlider.Root name="volume" onValueChange={setValue} value={value}>
        <TRSlider.Label>볼륨</TRSlider.Label><TRSlider.Value /><TRSlider.Control><TRSlider.Track><TRSlider.Indicator /></TRSlider.Track><TRSlider.Thumb aria-label="볼륨" /></TRSlider.Control>
      </TRSlider.Root>
      <TRButton type="submit">볼륨을 저장하세요</TRButton>
      <output aria-live="polite">{submitted === null ? '' : '볼륨 ' + submitted + '를 저장했어요.'}</output>
    </TRForm>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/button.css';\n@import '@tinyrack/ui/components/form.css';\n@import '@tinyrack/ui/components/slider.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="slider-validation"
  title="Field가 소유한 유효성 검사와 포커스 복구"
  description="60% 미만으로 submit해 thumb에 focus하고 오류를 표시하세요. 값을 올려 오류를 지운 다음 다시 submit하세요."
  preview={<div data-docs-example-item-count="1"><Stories.SliderValidationPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { useId, useRef, useState } from 'react';
import { TRButton } from '@tinyrack/ui/components/button';
import { TRField } from '@tinyrack/ui/components/field';
import { TRForm } from '@tinyrack/ui/components/form';
import { TRSlider } from '@tinyrack/ui/components/slider';

function CapacitySliderForm() {
  const errorId = useId();
  const thumbInputRef = useRef<HTMLInputElement>(null);
  const [attempted, setAttempted] = useState(false);
  const [submitted, setSubmitted] = useState<number | null>(null);
  const [value, setValue] = useState(30);
  const invalid = attempted && value < 60;

  return (
    <TRForm
      noValidate
      onSubmit={(event) => {
        event.preventDefault();
        setAttempted(true);
        if (value < 60) {
          setSubmitted(null);
          thumbInputRef.current?.focus();
          return;
        }
        setSubmitted(value);
      }}
    >
      <TRField.Root invalid={invalid}>
        <TRSlider.Root
          name="capacity"
          onValueChange={(nextValue) => {
            setValue(Array.isArray(nextValue) ? (nextValue[0] ?? 0) : Number(nextValue));
            setSubmitted(null);
          }}
          value={[value]}
        >
          <TRSlider.Label>예약 용량</TRSlider.Label>
          <TRSlider.Value />
          <TRSlider.Control>
            <TRSlider.Track><TRSlider.Indicator /></TRSlider.Track>
            <TRSlider.Thumb
              aria-label="예약 용량이에요"
              aria-describedby={invalid ? errorId : undefined}
              inputRef={thumbInputRef}
            />
          </TRSlider.Control>
        </TRSlider.Root>
        <TRField.Description>용량을 60% 이상 예약하세요.</TRField.Description>
        {invalid ? <TRField.Error id={errorId} match>예약 용량을 60% 이상으로 높이세요.</TRField.Error> : null}
      </TRField.Root>
      <TRButton type="submit">용량을 예약하세요</TRButton>
      <output aria-live="polite">
        {submitted === null ? '' : '용량 ' + submitted + '%를 예약했어요.'}
      </output>
    </TRForm>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/button.css';\n@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/form.css';\n@import '@tinyrack/ui/components/slider.css';" },
    },
  ]}
/>

## API

`Root`, `Label`, `Value`, `Control`, `Track`, `Indicator`, `Thumb`와 prop/state type을 `@tinyrack/ui/components/slider`에서 export해요. `Root`는 thumb 하나에는 scalar number 또는 readonly number array를 받아요. range에는 array를 쓰고, 각 range `Thumb`에 0부터 시작하는 `index`를 지정해 SSR과 hydration이 올바른 값에 매핑되도록 하세요.

| 목적 | 공개 prop과 기본값 |
| --- | --- |
| 척도 | `min={0}`, `max={100}`, `step={1}`, `largeStep={10}`이 기본값이에요. |
| layout | `orientation="horizontal" | "vertical"`을 받고 기본값은 `"horizontal"`이에요. `uiSize="sm" | "md" | "lg"`를 받고 기본값은 `"md"`예요. |
| range | `minStepsBetweenValues` 기본값은 `0`이에요. `thumbCollisionBehavior="push" | "swap" | "none"`을 받고 기본값은 `"push"`예요. |
| thumb 가장자리 | `thumbAlignment="center" | "edge" | "edge-client-only"`를 받고 기본값은 `"center"`예요. |
| 상태와 form | 제어형 `value` 또는 `defaultValue`, `disabled={false}`, `name`, 외부 `form`을 받아요. |

`Value`가 렌더링하는 `Intl.NumberFormat` 출력을 설정하려면 `Root`에서 `format`과 `locale`을 사용하세요. 각 Thumb에 간결한 접근 가능한 이름을 제공하고, 형식화된 숫자에 “20퍼센트에서 시작해요” 같은 role 맥락이 필요하면 `getAriaValueText(formattedValue, value, index)`를 사용하세요. 값이 바뀌어도 텍스트가 올바른 경우에만 고정 `aria-valuetext`를 사용하세요.

`onValueChange`는 pointer와 keyboard의 실시간 변경을 보고해요. 완료된 상호작용 후 한 번만 실행할 작업에는 `onValueCommitted`를 사용하세요. 화살표 키는 `step`만큼 값을 바꾸고 Page Up/Page Down과 Shift+Arrow는 `largeStep`을 사용하며 Home/End는 경계로 이동해요. `name`은 form 제출용 native range input을 만들고, Thumb의 `inputRef`는 focus를 되돌릴 때 그 input에 접근해요. `minStepsBetweenValues`와 `thumbCollisionBehavior`는 range thumb가 서로 가까워지거나 교차하는 방식을 정의해요.
