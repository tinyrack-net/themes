import { ComponentPlayground } from '../../playground/playground.js';
export const meta = () => [{ title: "PreviewCard · Tinyrack UI" }];

import { PreviewCard } from '@tinyrack/ui/components/preview-card';
import { Link } from '@tinyrack/ui/components/link';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import { baseUiExampleSources } from '../shared/base-ui-example-sources.js';
import * as Stories from './preview-card.demo.js';


# PreviewCard

A non-modal hover or focus preview for linked content.

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Playground controls cover `label`, `open`.

`Trigger` waits `600ms` before opening and `300ms` before closing by default.
Override those values with `delay` and `closeDelay` only when the trigger-to-card
pointer path remains forgiving.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/preview-card.css';", "import { PreviewCard } from '@tinyrack/ui/components/preview-card';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs
  id="preview-card-basic"
  title="Default"
  preview={<PreviewCard.Root><PreviewCard.Trigger href="#rack-alpha">Rack Alpha</PreviewCard.Trigger><PreviewCard.Portal><PreviewCard.Positioner><PreviewCard.Popup><strong>Rack Alpha</strong><p>Healthy · 12 services</p></PreviewCard.Popup></PreviewCard.Positioner></PreviewCard.Portal></PreviewCard.Root>}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['preview-card'] }]}
/>

## Examples

<ComponentExampleTabs
  id="preview-card-states"
  title="Interactive service preview"
  description="Hover or focus the link to reveal the non-modal preview. Move the pointer into the card without dismissing it, then leave or press Escape."
  preview={<PreviewCard.Root><PreviewCard.Trigger href="#rack-beta">Rack Beta</PreviewCard.Trigger><PreviewCard.Portal><PreviewCard.Positioner sideOffset={8}><PreviewCard.Popup><strong>Rack Beta</strong><p>Degraded · 8 of 10 services healthy</p><Link href="#rack-beta-incidents">View incidents</Link><PreviewCard.Arrow /></PreviewCard.Popup></PreviewCard.Positioner></PreviewCard.Portal></PreviewCard.Root>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<PreviewCard.Root>
  <PreviewCard.Trigger href="#rack-beta">Rack Beta</PreviewCard.Trigger>
  <PreviewCard.Portal>
    <PreviewCard.Positioner sideOffset={8}>
      <PreviewCard.Popup>
        <strong>Rack Beta</strong>
        <p>Degraded · 8 of 10 services healthy</p>
        <Link href="#rack-beta-incidents">View incidents</Link>
        <PreviewCard.Arrow />
      </PreviewCard.Popup>
    </PreviewCard.Positioner>
  </PreviewCard.Portal>
</PreviewCard.Root>` }]}
/>

## API

Every compound part and prop/state type is exported from `@tinyrack/ui/components/preview-card`; styles remain opt-in through the matching CSS subpath. `Trigger` owns the anchor destination plus the `delay` and `closeDelay` timing props, while `Root` owns controlled or uncontrolled open state. Keep interactive preview content keyboard reachable and dismissible with Escape.

PreviewCard is a progressive enhancement for mouse hover and keyboard focus, not
a touch-only disclosure. The trigger remains a real link: tapping it follows its
`href`, so do not put required information or the only path to an action inside
the preview. Provide a separate button/dialog pattern when touch users must open
the supplemental surface without navigating.
