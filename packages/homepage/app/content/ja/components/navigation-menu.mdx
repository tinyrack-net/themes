---
title: "NavigationMenu"
description: "サイトレベルのリンクと任意のリッチなフライアウトを提供します。"
section: components
order: 38
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/navigation-menu.demo.js';

## 主なプロパティ

| プロパティ | 選択肢 |
| --- | --- |
| 移動先 | 直接移動は `Link`、関連リンク群は `Trigger` と `Content` を使います。 |
| 方向 | `horizontal` または `vertical`。デフォルトは `horizontal` です。 |
| 位置 | `side` は top/right/bottom/left、`align` は start/center/end です。 |
| 狭い画面 | 同じ移動先を持つ drawer にデスクトップ構成を置き換えます。 |

タスク中心のフライアウトを作り、直接移動する項目はリンクにします。サイトナビゲーション用であり、アプリケーションコマンドのメニューバーではありません。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/navigation-menu.css';", "import { TRNavigationMenu } from '@tinyrack/ui/components/navigation-menu';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs id="navigation-menu-basic" title="サイトヘッダー" previewLayout="stretch" preview={<div data-docs-example-item-count="1"><Stories.NavigationMenuPreview active={false} align="center" disabled={false} locale="ja" navigationLabel="サイトナビゲーション" openSection="none" orientation="horizontal" side="bottom" /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/link.css';
import '@tinyrack/ui/components/navigation-menu.css';
import { TRNavigationMenu } from '@tinyrack/ui/components/navigation-menu';
export function SiteHeader() { return <TRNavigationMenu.Root aria-label="サイトナビゲーション"><TRNavigationMenu.List><TRNavigationMenu.Item><TRNavigationMenu.Link active href="/pricing">料金</TRNavigationMenu.Link></TRNavigationMenu.Item></TRNavigationMenu.List></TRNavigationMenu.Root>; }` }]} />

## 例

### 移動先の状態を示す

<ComponentExampleTabs id="navigation-menu-states" title="非アクティブ、現在地、無効" preview={<div data-docs-example-item-count="3"><Stories.NavigationMenuStateComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`export function ControlledNavigation() { const [value, setValue] = useState<string | null>(null); return <TRNavigationMenu.Root value={value} onValueChange={setValue}><TRNavigationMenu.Link active href="/pricing">料金</TRNavigationMenu.Link><TRNavigationMenu.Trigger disabled>製品は利用できません</TRNavigationMenu.Trigger></TRNavigationMenu.Root>; }` }]} />

### 操作方向を選ぶ

<ComponentExampleTabs id="navigation-menu-orientations" title="横方向と縦方向" preview={<div data-docs-example-item-count="2"><Stories.NavigationMenuOrientationComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<><TRNavigationMenu.Root orientation="horizontal">{items}</TRNavigationMenu.Root><TRNavigationMenu.Root orientation="vertical">{items}</TRNavigationMenu.Root></>` }]} />

### フライアウトを配置する

<ComponentExampleTabs id="navigation-menu-position-sides" title="上、右、下、左" preview={<div data-docs-example-item-count="4"><Stories.NavigationMenuSideComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`{(['top', 'right', 'bottom', 'left'] as const).map((side) => <TRNavigationMenu.Positioner key={side} side={side}>{popup}</TRNavigationMenu.Positioner>)}` }]} />

<ComponentExampleTabs id="navigation-menu-position-alignments" title="開始、中央、終了" preview={<div data-docs-example-item-count="3"><Stories.NavigationMenuAlignComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`{(['start', 'center', 'end'] as const).map((align) => <TRNavigationMenu.Positioner align={align} key={align}>{popup}</TRNavigationMenu.Positioner>)}` }]} />

### 狭い画面でも移動先を保つ

<ComponentExampleTabs id="navigation-menu-responsive-alternative" title="デスクトップフライアウトとモバイル drawer" previewLayout="stretch" preview={<div data-docs-example-item-count="1"><Stories.NavigationMenuResponsivePreview locale="ja" /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/drawer.css';
import '@tinyrack/ui/components/link.css';
import { TRDrawer } from '@tinyrack/ui/components/drawer';
import { TRLink } from '@tinyrack/ui/components/link';
export function ResponsiveNavigation() { return <><div className="hidden md:block">{desktopNavigation}</div><div className="md:hidden"><TRDrawer.Root swipeDirection="right"><TRDrawer.Trigger>ナビゲーションを開く</TRDrawer.Trigger><TRDrawer.Portal><TRDrawer.Popup><nav><TRLink href="/pricing">料金</TRLink></nav></TRDrawer.Popup></TRDrawer.Portal></TRDrawer.Root></div></>; }` }]} />

### キーボードモデルに従う

<ComponentExampleTabs id="navigation-menu-keyboard" title="ロービングフォーカスとフライアウト終了" description="方向は矢印キーの軸を決めます。Enter または Space で開き、Escape で閉じてトリガーへフォーカスを戻します。Tab はフライアウトリンクへ入ります。" preview={<div data-docs-example-item-count="1"><Stories.NavigationMenuKeyboardPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRNavigationMenu.Root aria-label="キーボードナビゲーション" delay={100} closeDelay={200}>{items}</TRNavigationMenu.Root>` }]} />

## API

全パーツは `Root`、`List`、`Item`、`Trigger`、`Icon`、`Content`、`Portal`、`Backdrop`、`Positioner`、`Popup`、`Arrow`、`Viewport`、`Link` です。すべてのパーツと prop・状態型を名前付きで公開します。

`Root` は `value` と `onValueChange` による制御状態、`defaultValue` による非制御状態、`orientation`、ポインターの `delay`、`closeDelay` を所有します。`Trigger` は無効にできます。`Icon` はデフォルトで `ChevronDown` を表示し、カスタムの子を受け取ります。`Link.active` は `aria-current="page"` を設定し、`closeOnClick` はフライアウトを閉じます。

`Positioner.side` は `top`、`right`、`bottom`、`left`、`align` は `start`、`center`、`end` を受け取ります。`sideOffset` のデフォルトは `8` です。衝突回避は配置を移動または反転します。`Portal` はカスタムコンテナを受け取り、`Popup` が面を所有し、`Viewport` が有効な `Content` を受け取ります。

公開 CSS パスを読み込んでください。トークンには `--tr-navigation-menu-popup-max-width` があります。構成は SSR とハイドレーションをサポートし、portal 内容はハイドレーション後にコンテナへマウントされます。狭い画面では同じ移動先をタッチサイズの drawer に表示します。
