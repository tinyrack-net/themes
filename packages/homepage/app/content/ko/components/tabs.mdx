---
title: "Tabs"
description: "세 가지 밀도, 두 가지 방향, 수동·자동 활성화를 지원하는 키보드 탐색 가능 뷰예요."
section: components
order: 23
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRTabs } from '@tinyrack/ui/components/tabs';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/tabs.demo.js';

## 계약

| 축 | 값 | 기본값 |
| --- | --- | --- |
| `uiSize` | `sm`, `md`, `lg` | `md` |
| `orientation` | `horizontal`, `vertical` | `horizontal` |
| disabled tab | `TRTabs.Tab disabled` | `false` |
| activation | manual or `activateOnFocus` | manual |
| focus | looping or bounded | looping |
| state | controlled `value`, including `null`, or uncontrolled `defaultValue` | first enabled tab |

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/tabs.css';", "import { TRTabs } from '@tinyrack/ui/components/tabs';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs
  id="tabs-basic"
  title="제어형 수동 활성화"
  description="화살표 키로 focus를 이동해요. Enter 또는 Space로 focused tab을 선택하고 결과를 업데이트해요."
  previewLayout="stretch"
  preview={<div data-docs-example-item-count="1"><Stories.TabsInteractiveExample /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.tabsControlledSource }]}
/>

## 예시

<ComponentExampleTabs
  id="tabs-sizes"
  data-docs-example-item-count="3"
  title="크기와 활성화 비교"
  previewLayout="stretch"
  preview={<div className="grid gap-5"><TRTabs.Root data-docs-example-item="" defaultValue="one" uiSize="sm"><TRTabs.List aria-label="Small manual tabs"><TRTabs.Tab value="one">Small</TRTabs.Tab><TRTabs.Tab value="two">Details</TRTabs.Tab></TRTabs.List><TRTabs.Panel value="one">Manual activation at small density.</TRTabs.Panel><TRTabs.Panel value="two">Details</TRTabs.Panel></TRTabs.Root><TRTabs.Root data-docs-example-item="" defaultValue="one" uiSize="md"><TRTabs.List activateOnFocus aria-label="Automatic tabs"><TRTabs.Tab value="one">Automatic</TRTabs.Tab><TRTabs.Tab value="two">Details</TRTabs.Tab></TRTabs.List><TRTabs.Panel value="one">Arrow focus also selects.</TRTabs.Panel><TRTabs.Panel value="two">Details</TRTabs.Panel></TRTabs.Root><TRTabs.Root data-docs-example-item="" defaultValue="one" uiSize="lg"><TRTabs.List aria-label="Bounded focus tabs" loopFocus={false}><TRTabs.Tab value="one">Bounded focus</TRTabs.Tab><TRTabs.Tab value="two">Details</TRTabs.Tab></TRTabs.List><TRTabs.Panel value="one">Focus stops at each end.</TRTabs.Panel><TRTabs.Panel value="two">Details</TRTabs.Panel></TRTabs.Root></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.tabsSizesSource }]}
/>

<ComponentExampleTabs
  id="tabs-vertical"
  data-docs-example-item-count="1"
  title="세로, disabled, persistent 콘텐츠"
  previewLayout="stretch"
  preview={<TRTabs.Root data-docs-example-item="" defaultValue="overview" orientation="vertical"><TRTabs.List aria-label="Vertical rack details"><TRTabs.Tab value="overview">Overview</TRTabs.Tab><TRTabs.Tab value="network">네트워크</TRTabs.Tab><TRTabs.Tab disabled value="logs">Logs</TRTabs.Tab></TRTabs.List><TRTabs.Panel value="overview">All systems operational.</TRTabs.Panel><TRTabs.Panel keepMounted value="network">10 Gbps uplink. This panel stays mounted.</TRTabs.Panel><TRTabs.Panel value="logs">Unavailable</TRTabs.Panel></TRTabs.Root>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.tabsVerticalSource }]}
/>

<ComponentExampleTabs
  id="tabs-overflow-indicator"
  data-docs-example-item-count="1"
  title="Overflow와 indicator"
  description="좁은 tab strip은 가로로 scroll되며 선택적 indicator가 active tab을 따라가요."
  preview={(
    <div className="grid max-w-64 gap-2">
      <span className="text-tinyrack-xs text-tinyrack-text-muted">Scroll the tab strip for more sections.</span>
      <TRTabs.Root data-docs-example-item="" defaultValue="Overview">
        <TRTabs.List aria-label="Deployment sections">
          {['Overview', '네트워크', '스토리지', '활동', '설정', '감사 로그'].map((label) => <TRTabs.Tab key={label} value={label}>{label}</TRTabs.Tab>)}
          <TRTabs.Indicator />
        </TRTabs.List>
        {['Overview', '네트워크', '스토리지', '활동', '설정', '감사 로그'].map((label) => <TRTabs.Panel key={label} value={label}>{label} panel</TRTabs.Panel>)}
      </TRTabs.Root>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: Stories.tabsOverflowIndicatorSource }]}
/>

## 구조

- `Root`는 제어형 또는 비제어형 선택 상태, 방향, `uiSize`를 관리해요.
- `List`는 접근 가능한 이름이 있는 tablist이며 활성화 방식과 focus 순환을 설정해요.
- `Tab`은 `value`로 view를 식별해요. disabled tab은 focus할 수 있지만 활성화할 수 없어요.
- `Indicator`는 선택 사항이며 overflow strip에서도 active tab을 따라가요.
- `Panel`은 같은 `value`를 가진 tab과 접근 가능한 콘텐츠를 연결해요.

## API

| 파트 | 주요 props | 설명 |
| --- | --- | --- |
| `Root` | `value`, `defaultValue`, `onValueChange`, `orientation`, `uiSize` | active tab이 없게 하려면 `value={null}`을 사용해요. 값을 지정하지 않으면 첫 enabled tab이 선택돼요. |
| `List` | `activateOnFocus`, `loopFocus` | 수동 활성화와 focus 순환이 기본값이에요. |
| `Tab` | `value`, `disabled`, `render` | 화살표 키는 focus를 이동하고 수동 모드에서는 Enter 또는 Space로 활성화해요. |
| `Indicator` | `renderBeforeHydration`, `render` | Base UI geometry 변수로 움직이는 선택적 시각 marker예요. |
| `Panel` | `value`, `keepMounted`, `hiddenUntilFound`, `render` | tab value와 일치하며 ARIA 관계를 유지해요. |

Base UI가 role, focus 이동, selection, hydration-safe 상태를 제공해요. Tinyrack foundation style 뒤에 `tabs.css`를 import하세요. `--tr-tabs-indicator-background`, `--tr-tabs-indicator-size`, `--tr-tabs-radius`, `--tr-tabs-tab-height`, `--tr-tabs-panel-padding`으로 스타일을 조정할 수 있어요.
