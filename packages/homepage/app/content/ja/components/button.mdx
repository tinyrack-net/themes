---
title: "Button"
description: "コマンドやフォーム操作に使うボタンです。6 種類の目的、3 種類の外観、3 種類のサイズを選べます。"
section: components
order: 5
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRButton } from '@tinyrack/ui/components/button';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/button.demo.js';

## 主なプロパティ

| プロパティ | 値 | 既定値 | 用途 |
| --- | --- | --- | --- |
| `intent` | `neutral`, `primary`, `info`, `success`, `warning`, `danger` | `neutral` | 操作の目的や結果を伝えます。 |
| `appearance` | `solid`, `outline`, `ghost` | `solid` | 目的を変えずに視覚的な強調度を調整します。 |
| `uiSize` | `sm`, `md`, `lg` | `md` | 周囲のコントロールのサイズに合わせます。 |
| `disabled` | `boolean` | `false` | 操作を実行できない状態にします。 |
| `loading` | `boolean` | `false` | 操作を無効にし、処理中であることを伝えます。 |

## インストール

<ComponentInstall
  surfaces={[
    {
      label: 'React',
      install: 'pnpm add @tinyrack/ui',
      imports: [
        "import '@tinyrack/ui/components/button.css';",
        "import { TRButton } from '@tinyrack/ui/components/button';",
      ],
    },
  ]}
/>

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="button-basic"
  title="基本アクション"
  description="主要な操作には primary を指定します。TRButton は既定で HTML ボタン要素をレンダリングします。"
  preview={<TRButton data-docs-example-item="" intent="primary">変更をデプロイ</TRButton>}
  sources={[
    {
      label: 'React',
      language: 'tsx',
      code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/button.css';
import { TRButton } from '@tinyrack/ui/components/button';

export function DeployAction() {
  return <TRButton intent="primary">変更をデプロイ</TRButton>;
}`,
    },
  ]}
/>

## 例

<ComponentExampleTabs
  id="button-solid-intents"
  title="ソリッドの目的別表示"
  description="最も強く強調したい操作にはソリッドボタンを使います。"
  preview={(
    <div className="flex flex-wrap gap-2">
      <TRButton data-docs-example-item="" appearance="solid" intent="neutral">通常</TRButton>
      <TRButton data-docs-example-item="" appearance="solid" intent="primary">主要</TRButton>
      <TRButton data-docs-example-item="" appearance="solid" intent="info">情報</TRButton>
      <TRButton data-docs-example-item="" appearance="solid" intent="success">成功</TRButton>
      <TRButton data-docs-example-item="" appearance="solid" intent="warning">警告</TRButton>
      <TRButton data-docs-example-item="" appearance="solid" intent="danger">危険</TRButton>
    </div>
  )}
  sources={[
    {
      label: 'React',
      language: 'tsx',
      code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/button.css';
import { TRButton } from '@tinyrack/ui/components/button';

export function SolidButtonIntents() {
  return <div className="flex flex-wrap gap-2">
  <TRButton appearance="solid" intent="neutral">通常</TRButton>
  <TRButton appearance="solid" intent="primary">主要</TRButton>
  <TRButton appearance="solid" intent="info">情報</TRButton>
  <TRButton appearance="solid" intent="success">成功</TRButton>
  <TRButton appearance="solid" intent="warning">警告</TRButton>
  <TRButton appearance="solid" intent="danger">危険</TRButton>
</div>;
}`,
    },
  ]}
/>

<ComponentExampleTabs
  id="button-outline-intents"
  title="アウトラインの目的別表示"
  description="目立たせながら強調を抑えたい操作にはアウトラインボタンを使います。"
  preview={(
    <div className="flex flex-wrap gap-2">
      <TRButton data-docs-example-item="" appearance="outline" intent="neutral">通常</TRButton>
      <TRButton data-docs-example-item="" appearance="outline" intent="primary">主要</TRButton>
      <TRButton data-docs-example-item="" appearance="outline" intent="info">情報</TRButton>
      <TRButton data-docs-example-item="" appearance="outline" intent="success">成功</TRButton>
      <TRButton data-docs-example-item="" appearance="outline" intent="warning">警告</TRButton>
      <TRButton data-docs-example-item="" appearance="outline" intent="danger">危険</TRButton>
    </div>
  )}
  sources={[
    {
      label: 'React',
      language: 'tsx',
      code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/button.css';
import { TRButton } from '@tinyrack/ui/components/button';

export function OutlineButtonIntents() {
  return <div className="flex flex-wrap gap-2">
  <TRButton appearance="outline" intent="neutral">通常</TRButton>
  <TRButton appearance="outline" intent="primary">主要</TRButton>
  <TRButton appearance="outline" intent="info">情報</TRButton>
  <TRButton appearance="outline" intent="success">成功</TRButton>
  <TRButton appearance="outline" intent="warning">警告</TRButton>
  <TRButton appearance="outline" intent="danger">危険</TRButton>
</div>;
}`,
    },
  ]}
/>

<ComponentExampleTabs
  id="button-ghost-intents"
  title="ゴーストの目的別表示"
  description="情報量の少ない画面で強調を抑えたい操作にはゴーストボタンを使います。"
  preview={(
    <div className="flex flex-wrap gap-2">
      <TRButton data-docs-example-item="" appearance="ghost" intent="neutral">通常</TRButton>
      <TRButton data-docs-example-item="" appearance="ghost" intent="primary">主要</TRButton>
      <TRButton data-docs-example-item="" appearance="ghost" intent="info">情報</TRButton>
      <TRButton data-docs-example-item="" appearance="ghost" intent="success">成功</TRButton>
      <TRButton data-docs-example-item="" appearance="ghost" intent="warning">警告</TRButton>
      <TRButton data-docs-example-item="" appearance="ghost" intent="danger">危険</TRButton>
    </div>
  )}
  sources={[
    {
      label: 'React',
      language: 'tsx',
      code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/button.css';
import { TRButton } from '@tinyrack/ui/components/button';

export function GhostButtonIntents() {
  return <div className="flex flex-wrap gap-2">
  <TRButton appearance="ghost" intent="neutral">通常</TRButton>
  <TRButton appearance="ghost" intent="primary">主要</TRButton>
  <TRButton appearance="ghost" intent="info">情報</TRButton>
  <TRButton appearance="ghost" intent="success">成功</TRButton>
  <TRButton appearance="ghost" intent="warning">警告</TRButton>
  <TRButton appearance="ghost" intent="danger">危険</TRButton>
</div>;
}`,
    },
  ]}
/>

<ComponentExampleTabs
  id="button-sizes"
  title="サイズ"
  description="操作の重要度ではなく、周囲のコントロールに合わせてサイズを選びます。"
  preview={(
    <div className="flex flex-wrap items-center gap-3">
      <TRButton data-docs-example-item="" intent="primary" uiSize="sm">小サイズ</TRButton>
      <TRButton data-docs-example-item="" intent="primary" uiSize="md">中サイズ</TRButton>
      <TRButton data-docs-example-item="" intent="primary" uiSize="lg">大サイズ</TRButton>
    </div>
  )}
  sources={[
    {
      label: 'React',
      language: 'tsx',
      code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/button.css';
import { TRButton } from '@tinyrack/ui/components/button';

export function ButtonUiSizes() {
  return <div className="flex flex-wrap items-center gap-3">
  <TRButton intent="primary" uiSize="sm">小サイズ</TRButton>
  <TRButton intent="primary" uiSize="md">中サイズ</TRButton>
  <TRButton intent="primary" uiSize="lg">大サイズ</TRButton>
</div>;
}`,
    },
  ]}
/>

<ComponentExampleTabs
  id="button-states"
  title="状態"
  description="操作できないときは disabled、処理中は loading を使います。"
  preview={(
    <div className="flex flex-wrap items-center gap-3">
      <TRButton data-docs-example-item="">変更をデプロイ</TRButton>
      <TRButton data-docs-example-item="" disabled>変更をデプロイ</TRButton>
      <TRButton data-docs-example-item="" intent="primary" loading loadingLabel="変更をデプロイ中">変更をデプロイ</TRButton>
    </div>
  )}
  sources={[
    {
      label: 'React',
      language: 'tsx',
      code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/button.css';
import { TRButton } from '@tinyrack/ui/components/button';

export function ButtonStates() {
  return <div className="flex flex-wrap items-center gap-3">
  <TRButton>変更をデプロイ</TRButton><TRButton disabled>変更をデプロイ</TRButton>
  <TRButton intent="primary" loading loadingLabel="変更をデプロイ中">変更をデプロイ</TRButton>
</div>;
}`,
    },
  ]}
/>

## API

### Tinyrack 固有のプロパティ

| プロパティ | 型 | デフォルト | 用途 |
| --- | --- | --- | --- |
| `intent` | `neutral \| primary \| info \| success \| warning \| danger` | `neutral` | 操作の意味と色の役割を選びます。 |
| `appearance` | `solid \| outline \| ghost` | `solid` | 意味を変えずに視覚的な強調方法を選びます。 |
| `uiSize` | `sm \| md \| lg` | `md` | ボタンのサイズを選びます。 |
| `loading` | `boolean` | `false` | 操作を無効にし、処理中であることを伝えます。 |
| `loadingLabel` | `string` | なし | 読み込み中に使うアクセシブル名を指定します。 |
| `variant` | `secondary \| primary \| danger` | `secondary` | 以前の API との互換性を保つためのプロパティです。新しいコードでは `intent` を使ってください。 |

### HTML 要素と継承するプロパティ

`TRButtonProps` は Base UI Button のプロパティを拡張します。

| 継承するプロパティ | デフォルト | 動作 |
| --- | --- | --- |
| `disabled` | `false` | 操作を実行できない状態にします。 |
| `focusableWhenDisabled` | `false` | `true` にすると、無効なボタンをフォーカス順序に残します。 |
| `render` | なし | 別の要素やコンポーネントにボタンの動作とスタイルを適用します。 |
| `nativeButton` | `true` | レンダリングした要素がネイティブの `<button>` かどうかを指定します。 |
| `type` | ネイティブボタンでは `button` | 通常のボタン、送信、リセットの動作を選びます。 |

ネイティブボタンの属性、React のイベントハンドラー、状態に応じて値を返せる
`className` と `style`、React 19 の `ref` も利用できます。

`TRButton` はデフォルトで `type="button"` の HTML `<button>` をレンダリング
します。フォームの送信やリセットに使う場合は、`type="submit"` または
`type="reset"` を明示してください。`render` で `<button>` 以外の要素を使う
場合は `nativeButton={false}` を設定します。Base UI はネイティブの `type`
属性を追加せず、ボタンとしての動作を提供します。ボタンの役割は変わらない
ため、ページの移動には [`TRLink`](/ja/components/link/) を使ってください。

### 読み込み中の状態とアクセシブル名

`loading` が `true` の間は操作を無効にし、`aria-hidden` のスピナーを先頭に
追加して `aria-busy="true"` を設定します。`loadingLabel` を省略すると、通常の
アクセシブル名を維持します。`loadingLabel` を指定すると、読み込み中の一時的な
アクセシブル名として使い、既存の `aria-labelledby` に代わります。`loading` が
`false` の場合は、明示した `aria-busy` の値を維持します。

### 以前のバリアントとの互換性

非推奨の `variant` は以前のコードとの互換性を保つために残しています。
`secondary` は `neutral` に、`primary` と `danger` は同名の `intent` に対応します。
`intent` を明示すると `variant` より優先されます。
