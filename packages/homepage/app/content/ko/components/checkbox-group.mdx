---
title: "체크박스 그룹"
description: "독립적으로 전환하는 값을 하나의 접근 가능한 그룹으로 조정해요."
section: components
group: selection-controls
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/checkbox-group.demo.js';

## 핵심 속성

| 판단 기준 | 선택 방법 |
| --- | --- |
| 값 소유권 | 비제어 상태는 `defaultValue`, 제어 상태는 `value`와 `onValueChange`를 써요. |
| 사용 가능 여부 | 그룹 전체는 `disabled`, 폼 값을 유지할 개별 항목은 `readOnly`를 써요. |
| 폼 | 자식 체크박스에 같은 `name`을 주면 선택한 각 항목이 `value`를 제공해요. |
| 부모 선택 | `allValues`를 전달하고 자식 체크박스에 `parent`를 써요. |
| 유효성 검사 | 네이티브 필수 조건보다 복잡한 선택 개수 규칙은 제어 배열을 검사해요. |

그룹에는 `aria-label`이나 `aria-labelledby`로 이름을 주고, 모든 자식 체크박스에도 이름을 제공해요. 그룹은 독립 선택을 조정해요. 하나의 값만 골라야 한다면 `TRRadioGroup`을 써요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/checkbox-group.css';"],
      codeImports: ["import { TRCheckboxGroup } from '@tinyrack/ui/components/checkbox-group';","import { TRCheckbox } from '@tinyrack/ui/components/checkbox';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="checkbox-group-basic" title="랙 기능을 골라요" preview={<div data-docs-example-item-count="1"><Stories.CheckboxGroupPreview defaultSelectedValues={['metrics']} disabled={false} label="랙 기능을 골라요" /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRCheckboxGroup aria-label="랙 기능을 골라요" defaultValue={['metrics']}>
  {features.map((feature) => <label key={feature.value}><TRCheckbox.Root name="features" value={feature.value}><TRCheckbox.Indicator>✓</TRCheckbox.Indicator></TRCheckbox.Root>{feature.label}</label>)}
</TRCheckboxGroup>` }]} />

## 예시

### 전체 집합의 사용 가능 여부를 정해요

<ComponentExampleTabs id="checkbox-group-states" title="편집 가능, 읽기 전용, 사용 불가 그룹" preview={<div data-docs-example-item-count="3"><Stories.CheckboxGroupStateComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<>
  <TRCheckboxGroup aria-label="편집할 수 있어요" defaultValue={['metrics']}>{items}</TRCheckboxGroup>
  <TRCheckboxGroup aria-label="읽기 전용이에요" defaultValue={['metrics']}>{readOnlyItems}</TRCheckboxGroup>
  <TRCheckboxGroup aria-label="사용할 수 없어요" defaultValue={['metrics']} disabled>{items}</TRCheckboxGroup>
</>` }]} />

### 선택 개수를 검사해요

<ComponentExampleTabs id="checkbox-group-form" title="값을 한 개나 두 개 골라요" description="제품 규칙에 최소와 최대 선택 개수가 있다면 값 배열을 제어해요." preview={<div data-docs-example-item-count="1"><Stories.CheckboxGroupFormPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRCheckboxGroup aria-invalid={invalid || undefined} onValueChange={setSelected} value={selected}>{items}</TRCheckboxGroup>` }]} />

### 모두 선택하는 부모를 추가해요

<ComponentExampleTabs id="checkbox-group-parent" title="부모와 일부 선택 상태" description="모든 값을 고르면 부모가 선택되고, 일부 값만 고르면 일부 선택 상태가 돼요." preview={<div data-docs-example-item-count="1"><Stories.CheckboxGroupParentPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRCheckboxGroup allValues={values} onValueChange={setValue} value={value}>
  <TRCheckbox.Root parent><TRCheckbox.Indicator render={(props, state) => <span {...props}>{state.indeterminate ? '−' : '✓'}</span>} /></TRCheckbox.Root>
  {items}
</TRCheckboxGroup>` }]} />

### 외부 폼에 연결해요

<ComponentExampleTabs id="checkbox-group-external-form" title="외부 소유 폼과 초기화" description="모든 자식 체크박스에 `form`을 설정해요. 초기화하면 그룹의 비제어 `defaultValue`로 돌아가요." preview={<div data-docs-example-item-count="1"><Stories.CheckboxGroupExternalFormPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRCheckboxGroup aria-label="외부 폼의 기능을 골라요" defaultValue={['metrics']}>
  <TRCheckbox.Root form="feature-form" name="features" value="metrics" />
  <TRCheckbox.Root form="feature-form" name="features" value="alerts" />
</TRCheckboxGroup>` }]} />

## API

### 값과 사용 가능 여부

| Prop | 타입 / 기본값 | 용도 |
| --- | --- | --- |
| `value`, `defaultValue` | `string[]`, `[]` | 선택한 자식 값을 제어하거나 초기화해요. |
| `onValueChange` | `(value, details) => void` | 다음 값 배열을 알려줘요. |
| `disabled` | `boolean`, `false` | 모든 자식을 비활성화하고 폼 데이터에서 빼요. |
| `allValues` | `string[]` | `parent` 체크박스가 쓸 전체 집합을 정의해요. |

### 합성과 폼

각기 다른 `value`와 같은 `name`을 가진 `TRCheckbox.Root` 자식을 렌더링해요. `readOnly`와 폼 소유권은 숨겨진 입력을 가진 자식 체크박스에 설정해요. 비제어 그룹은 초기화할 때 `defaultValue`로 돌아가고, 제어 그룹은 `value`를 따라요.

루트는 Base UI의 `render`, `ref`, 이벤트, 네이티브 속성, 상태 기반 `className`과 `style`을 유지해요. 모듈은 `TRCheckboxGroup`, `TRCheckboxGroupProps`, `TRCheckboxGroupState`를 내보내요. CSS는 직접 가져와야 해요.
