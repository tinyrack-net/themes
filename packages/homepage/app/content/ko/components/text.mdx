---
title: "Text"
description: "이름 붙은 텍스트 역할을 어떤 요소에나 입히는 타이포그래피 프리미티브예요."
section: components
order: 62
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRText } from '@tinyrack/ui/components/text';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/text.demo.js';

## 핵심 속성

| 속성 | 값 | 기본값 |
| --- | --- | --- |
| `variant` | `caption`, `label`, `body`, `bodySm`, `code`, `headingSm`, `headingMd`, `headingLg`, `display` | `body` |
| `color` | `default`, `muted`, `placeholder`, `inverse`, `primary`, `info`, `success`, `warning`, `danger` | 상속 |
| `as` | 모든 요소 타입 | `span` |

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/text.css';"],
      codeImports: ["import { TRText } from '@tinyrack/ui/components/text';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="text-basic" title="본문 텍스트" preview={<TRText data-docs-example-item="" variant="body">All nodes are available.</TRText>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRText } from '@tinyrack/ui/components/text';

<TRText variant="body">All nodes are available.</TRText>` }]} />

## 예시

<ComponentExampleTabs
  id="text-type-roles"
  title="타입 역할"
  description="의미에 맞는 역할을 먼저 고르고, as 프롭으로 요소를 지정해 제목의 시맨틱을 유지하세요."
  previewLayout="stretch"
  preview={(
    <div className="grid gap-2">
      <TRText data-docs-example-item="" as="p" variant="display">Display</TRText>
      <TRText data-docs-example-item="" as="h2" variant="headingLg">Heading large</TRText>
      <TRText data-docs-example-item="" as="h3" variant="headingMd">Heading medium</TRText>
      <TRText data-docs-example-item="" as="h4" variant="headingSm">Heading small</TRText>
      <TRText data-docs-example-item="" as="p" variant="body">Body copy for paragraphs and descriptions.</TRText>
      <TRText data-docs-example-item="" as="p" variant="bodySm">Body small for dense supporting text.</TRText>
      <TRText data-docs-example-item="" variant="code">const rack = createRack();</TRText>
      <TRText data-docs-example-item="" variant="caption">Caption for metadata.</TRText>
      <TRText data-docs-example-item="" variant="label">Label</TRText>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRText } from '@tinyrack/ui/components/text';

<TRText as="p" variant="display">Display</TRText>
<TRText as="h2" variant="headingLg">Heading large</TRText>
<TRText as="h3" variant="headingMd">Heading medium</TRText>
<TRText as="h4" variant="headingSm">Heading small</TRText>
<TRText as="p" variant="body">Body copy for paragraphs and descriptions.</TRText>
<TRText as="p" variant="bodySm">Body small for dense supporting text.</TRText>
<TRText variant="code">const rack = createRack();</TRText>
<TRText variant="caption">Caption for metadata.</TRText>
<TRText variant="label">Label</TRText>` }]}
/>

<ComponentExampleTabs
  id="text-semantic-color"
  title="시맨틱 색상"
  description="의미에 따라 색상 토큰을 고르고, color를 지정하지 않으면 주변 텍스트 색을 상속해요."
  preview={(
    <div className="flex flex-wrap gap-3">
      <TRText data-docs-example-item="" color="default">Default</TRText>
      <TRText data-docs-example-item="" color="muted">Muted</TRText>
      <TRText data-docs-example-item="" color="primary">Primary</TRText>
      <TRText data-docs-example-item="" color="info">Info</TRText>
      <TRText data-docs-example-item="" color="success">Success</TRText>
      <TRText data-docs-example-item="" color="warning">Warning</TRText>
      <TRText data-docs-example-item="" color="danger">Danger</TRText>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRText } from '@tinyrack/ui/components/text';

<TRText color="default">Default</TRText>
<TRText color="muted">Muted</TRText>
<TRText color="primary">Primary</TRText>
<TRText color="info">Info</TRText>
<TRText color="success">Success</TRText>
<TRText color="warning">Warning</TRText>
<TRText color="danger">Danger</TRText>` }]}
/>

<ComponentExampleTabs
  id="text-alignment-and-truncation"
  title="정렬과 말줄임"
  description="논리 정렬은 텍스트 방향을 따르고, truncate는 넘치는 한 줄을 말줄임으로 잘라요."
  previewLayout="stretch"
  preview={(
    <div className="grid gap-3">
      <div className="w-full border border-tinyrack-border rounded-tinyrack-md p-tinyrack-md">
        <TRText data-docs-example-item="" align="center" as="p" variant="body">Centered status</TRText>
      </div>
      <div className="w-full max-w-xs border border-tinyrack-border rounded-tinyrack-md p-tinyrack-md">
        <TRText data-docs-example-item="" truncate variant="bodySm">Waiting for maintenance approval from the operator on call</TRText>
      </div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRText } from '@tinyrack/ui/components/text';

<TRText align="center" as="p" variant="body">Centered status</TRText>

<div className="max-w-xs">
  <TRText truncate variant="bodySm">Waiting for maintenance approval from the operator on call</TRText>
</div>` }]}
/>

## API

### 옵션

| Prop | 타입 | 기본값 | 용도 |
| --- | --- | --- | --- |
| `variant` | `'caption' \| 'label' \| 'body' \| 'bodySm' \| 'code' \| 'headingSm' \| 'headingMd' \| 'headingLg' \| 'display'` | `'body'` | 디자인 토큰의 이름 붙은 타입 역할을 선택해요. |
| `as` | `ElementType` | `'span'` | 시각적 역할은 그대로 두고 렌더링할 요소만 바꿔요. |
| `color` | `'default' \| 'muted' \| 'placeholder' \| 'inverse' \| 'primary' \| 'info' \| 'success' \| 'warning' \| 'danger'` | 상속 | 시맨틱 색상 토큰을 적용하고, 지정하지 않으면 부모 색을 상속해요. |
| `align` | `'start' \| 'center' \| 'end'` | 상속 | 현재 방향에 맞는 논리 텍스트 정렬을 지정해요. |
| `truncate` | `boolean` | `false` | 넘치는 한 줄을 말줄임(…)으로 잘라요. |
| `weight` | `'regular' \| 'medium' \| 'heading' \| 'bold' \| 'strong'` | 역할 기본값 | variant의 기본 굵기를 덮어써요. |

### HTML과 접근성

`TRText`는 기본적으로 `span`을 렌더링하고 네이티브 속성, 이벤트, 스타일, React 19 `ref`를 전달해요. `as`로 시각적 `variant`와 무관하게 `p`, 제목, `label` 같은 시맨틱 요소를 렌더링해 제목이 문서 구조를 유지하도록 하세요. `TRTextProps`, `TRTextVariant`, `TRTextColor`, `TRTextAlign`, `TRTextWeight` 타입을 내보내요. 인스턴스별 재정의는 `--tr-text-*` 커스텀 속성으로 할 수 있어요.
