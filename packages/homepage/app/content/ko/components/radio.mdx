---
title: "라디오"
description: "라디오 그룹이 선택 상태를 소유하는 하나의 선택 항목이에요."
section: components
group: selection-controls
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/radio.demo.js';

## 핵심 속성

| 판단 기준 | 선택 방법 |
| --- | --- |
| 합성 | 모든 `TRRadio.Root`를 `TRRadioGroup` 안에 배치해요. |
| 선택 | 각 루트에 고유한 `value`를 주고 그룹이 선택한 값을 소유해요. |
| 크기 | `sm`, `md`, `lg` 중에서 고르며 기본값은 `md`예요. |
| 항목 사용 가능 여부 | 한 항목만 그룹과 달라야 할 때 루트의 `readOnly`나 `disabled`를 써요. |

라디오는 단독 불리언 컨트롤이 아니에요. 하나의 독립 값을 켜고 끌 수 있다면 `TRCheckbox`나 `TRSwitch`를 써요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/radio.css';"],
      codeImports: ["import { TRRadio } from '@tinyrack/ui/components/radio';","import { TRRadioGroup } from '@tinyrack/ui/components/radio-group';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="radio-basic" title="지원 요금제를 골라요" preview={<div data-docs-example-item-count="1"><Stories.RadioPlanExample /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRRadioGroup aria-label="지원 요금제를 골라요" defaultValue="standard" name="support-plan">
  {plans.map((plan) => <label key={plan.value}><TRRadio.Root value={plan.value}><TRRadio.Indicator aria-hidden="true" /></TRRadio.Root>{plan.label}</label>)}
</TRRadioGroup>` }]} />

## 예시

### 선택 상태를 보여줘요

<ComponentExampleTabs id="radio-states" title="선택 전과 선택 후" description="선택 상태는 그룹이 소유한 값과 비교할 때만 의미가 있어요." preview={<div data-docs-example-item-count="2"><Stories.RadioStateComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRRadioGroup aria-label="대상을 골라요" value="production">
  <TRRadio.Root aria-label="스테이징을 골라요" value="staging" />
  <TRRadio.Root aria-label="프로덕션을 골라요" value="production" />
</TRRadioGroup>` }]} />

### 컨트롤 밀도에 맞춰요

<ComponentExampleTabs id="radio-sizes" title="작게, 보통으로, 크게" preview={<div data-docs-example-item-count="3"><Stories.RadioSizeComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRRadioGroup aria-label="밀도를 골라요" defaultValue="md">{(['sm', 'md', 'lg'] as const).map((uiSize) => <TRRadio.Root aria-label={uiSize} key={uiSize} uiSize={uiSize} value={uiSize} />)}</TRRadioGroup>` }]} />

### 한 항목의 사용 가능 여부를 바꿔요

<ComponentExampleTabs id="radio-availability" title="편집 가능, 읽기 전용, 사용 불가" description="모든 항목이 같은 상태라면 그룹 prop을 우선해요." preview={<div data-docs-example-item-count="3"><Stories.RadioAvailabilityComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRRadioGroup aria-label="사용 가능 여부를 골라요" value="editable">
  <TRRadio.Root aria-label="편집할 수 있어요" value="editable" />
  <TRRadio.Root aria-label="읽기 전용이에요" readOnly value="readonly" />
  <TRRadio.Root aria-label="사용할 수 없어요" disabled value="disabled" />
</TRRadioGroup>` }]} />

## API

### `Root` 속성

| Prop | 타입 / 기본값 | 용도 |
| --- | --- | --- |
| `value` | `unknown` | 그룹 안에서 이 항목을 구분해요. |
| `uiSize` | `'sm' \| 'md' \| 'lg'`, `'md'` | 보이는 글리프 크기를 정해요. |
| `disabled`, `readOnly` | `boolean`, 상속하거나 `false` | 한 항목의 사용 가능 여부를 덮어써요. |
| `inputRef`, `ref` | 입력 ref, 루트 ref | 숨겨진 입력이나 보이는 루트에 접근해요. |

선택 변경은 루트가 아니라 `TRRadioGroup.onValueChange`로 알려줘요. Root는 네이티브 prop과 이벤트를 전달하고 Base UI의 `render`, 상태 기반 `className`과 `style`을 지원해요.

### Indicator와 접근성

`TRRadio.Indicator`는 선택했을 때 마운트하고, `keepMounted`를 설정하면 계속 유지해요. 그룹과 모든 항목에 접근 가능한 이름을 제공해요. 방향키 탐색, 폼 소유권, 필수 검사, 초기화는 `TRRadioGroup`이 담당해요.

모듈은 `TRRadio`, 두 이름 있는 파트, 각 prop 및 상태 타입, `TRRadioUiSize`를 내보내요. CSS는 직접 가져와야 해요.
