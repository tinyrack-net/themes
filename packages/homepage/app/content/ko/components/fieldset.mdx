---
title: "Fieldset"
description: "관련 컨트롤을 접근 가능한 네이티브 legend 하나로 묶어요."
section: components
order: 32
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/fieldset.demo.js';

## 계약

`Root`는 네이티브 `<fieldset>`을 렌더링해요. `Legend`는 기본적으로 네이티브 `<legend>`를 렌더링해 그룹에 접근 가능한 이름을 제공해요. 각 하위 컨트롤에는 고유한 표시 레이블이 필요해요. `Root`의 `disabled`는 네이티브 폼 컨트롤 비활성화 전파를 사용해요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/fieldset.css';", "import { TRFieldset } from '@tinyrack/ui/components/fieldset';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs
  id="fieldset-basic"
  title="알림 채널"
  preview={<Stories.FieldsetPreview data-docs-example-item="" defaultEmailAlerts legend="알림" disabled={false} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/checkbox.css';
import '@tinyrack/ui/components/fieldset.css';
import { TRCheckbox } from '@tinyrack/ui/components/checkbox';
import { TRFieldset } from '@tinyrack/ui/components/fieldset';

export function NotificationChannels() {
  return (
<TRFieldset.Root>
  <TRFieldset.Legend>알림</TRFieldset.Legend>
  <label htmlFor="email-alerts">
    <TRCheckbox.Root defaultChecked id="email-alerts"><TRCheckbox.Indicator>✓</TRCheckbox.Indicator></TRCheckbox.Root>
    이메일 알림을 받아요
  </label>
  <label htmlFor="incident-summaries">
    <TRCheckbox.Root defaultChecked id="incident-summaries"><TRCheckbox.Indicator>✓</TRCheckbox.Indicator></TRCheckbox.Root>
    인시던트 요약을 받아요
  </label>
</TRFieldset.Root>
  );
}` }]}
/>

## 예시

<ComponentExampleTabs
  id="fieldset-states"
  title="활성 및 비활성 그룹"
  preview={<div className="grid gap-5 sm:grid-cols-2"><Stories.FieldsetPreview data-docs-example-item="" defaultEmailAlerts disabled={false} legend="직접 변경하는 설정" /><Stories.FieldsetPreview data-docs-example-item="" defaultEmailAlerts disabled legend="관리되는 설정" /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/checkbox.css';
import '@tinyrack/ui/components/fieldset.css';
import { TRCheckbox } from '@tinyrack/ui/components/checkbox';
import { TRFieldset } from '@tinyrack/ui/components/fieldset';

export function FieldsetStates() {
  return (
<div className="grid gap-5 sm:grid-cols-2">
  <TRFieldset.Root>
    <TRFieldset.Legend>직접 변경하는 설정</TRFieldset.Legend>
    <label><TRCheckbox.Root defaultChecked aria-label="이메일 알림을 받아요"><TRCheckbox.Indicator>✓</TRCheckbox.Indicator></TRCheckbox.Root> 이메일 알림을 받아요</label>
  </TRFieldset.Root>
  <TRFieldset.Root disabled>
    <TRFieldset.Legend>관리되는 설정</TRFieldset.Legend>
    <label><TRCheckbox.Root defaultChecked aria-label="이메일 알림을 받아요"><TRCheckbox.Indicator>✓</TRCheckbox.Indicator></TRCheckbox.Root> 이메일 알림을 받아요</label>
  </TRFieldset.Root>
</div>
  );
}` }]}
/>

<ComponentExampleTabs
  id="fieldset-composition"
  title="중첩 그룹"
  preview={<div data-docs-example-item=""><Stories.FieldsetCompositionExample /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/checkbox.css';
import '@tinyrack/ui/components/fieldset.css';
import { TRCheckbox } from '@tinyrack/ui/components/checkbox';
import { TRFieldset } from '@tinyrack/ui/components/fieldset';

export function IncidentNotifications() {
  return (
    <TRFieldset.Root>
      <TRFieldset.Legend>인시던트 알림</TRFieldset.Legend>
      <label htmlFor="incident-enabled">
        <TRCheckbox.Root defaultChecked id="incident-enabled">
          <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
        </TRCheckbox.Root>
        인시던트 알림을 사용해요
      </label>
      <TRFieldset.Root>
        <TRFieldset.Legend>전송 채널</TRFieldset.Legend>
        <label htmlFor="delivery-email">
          <TRCheckbox.Root defaultChecked id="delivery-email">
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          이메일로 받아요
        </label>
        <label htmlFor="delivery-sms">
          <TRCheckbox.Root id="delivery-sms">
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          문자 메시지로 받아요
        </label>
      </TRFieldset.Root>
    </TRFieldset.Root>
  );
}` }]}
/>

## API

`TRFieldsetRoot`와 `TRFieldsetLegend`는 개별적으로 내보내며, `TRFieldset.Root`와 `TRFieldset.Legend`로도 사용할 수 있어요.

| 파트 | 공개 계약 |
| --- | --- |
| `Root` | 네이티브 fieldset 속성, `disabled`, `render`, `className`, `style`, 이벤트 및 ref |
| `Legend` | 기본적으로 네이티브 legend를 렌더링하며 `render`, `className`, `style`, 이벤트 및 ref 지원 |

네이티브 `<legend>` 배치가 적합하지 않을 때만 `render`를 사용하세요. 사용자 지정 legend도 Base UI가 `aria-labelledby`로 fieldset과 연결해요.
