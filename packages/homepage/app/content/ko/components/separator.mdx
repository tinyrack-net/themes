---
title: "Separator"
description: "수평 섹션과 수직 컨트롤 그룹을 위한 의미 있는 구분선입니다."
section: components
order: 12
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRSeparator } from '@tinyrack/ui/components/separator';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/separator.demo.js';

## 계약

`TRSeparator`는 의미 있는 경계를 `role="separator"`로 표시합니다. `orientation`은 `horizontal` 또는 `vertical`을 받으며 기본값은 `horizontal`입니다. 선이 콘텐츠를 구분하지 않고 순수하게 시각적인 경우에만 `role="presentation"`을 사용하세요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/separator.css';", "import { TRSeparator } from '@tinyrack/ui/components/separator';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="separator-basic" title="섹션 구분선" previewLayout="stretch" preview={<section className="grid w-full gap-3"><h3 className="m-0">개요</h3><TRSeparator /><p className="m-0">네트워크 활동이 정상입니다.</p></section>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/separator.css';
import { TRSeparator } from '@tinyrack/ui/components/separator';

export function NetworkOverview() {
  return (
    <section className="grid gap-3">
      <h3>Overview</h3>
      <TRSeparator />
      <p>Network activity is healthy.</p>
    </section>
  );
}` }]} />

## 예시

<ComponentExampleTabs
  id="separator-orientations"
  title="방향"
  previewLayout="stretch"
  preview={(
    <div className="grid gap-6">
      <div className="grid gap-2"><strong>Horizontal</strong><TRSeparator /></div>
      <div className="grid gap-2"><strong>Vertical</strong><div className="flex h-12 items-center gap-4"><span>CPU</span><TRSeparator orientation="vertical" /><span>Memory</span><TRSeparator orientation="vertical" /><span>스토리지</span></div></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/separator.css';
import { TRSeparator } from '@tinyrack/ui/components/separator';

export function SeparatorOrientations() {
  return (
    <div className="grid gap-6">
      <div className="grid gap-2">
        <strong>Horizontal</strong>
        <TRSeparator />
      </div>
      <div className="grid gap-2">
        <strong>Vertical</strong>
        <div className="flex h-12 items-center gap-4">
          <span>CPU</span>
          <TRSeparator orientation="vertical" />
          <span>Memory</span>
          <TRSeparator orientation="vertical" />
          <span>Storage</span>
        </div>
      </div>
    </div>
  );
}` }]}
/>

<ComponentExampleTabs
  id="separator-semantics"
  title="의미 있는 구분선과 장식용 구분선"
  description="콘텐츠 경계에는 기본 역할을 유지하세요. 시각적 용도로만 쓰는 선에서는 구분선 시맨틱을 제거하세요."
  preview={(
    <div className="grid gap-4">
      <section className="grid gap-2"><strong>관련 설정</strong><TRSeparator /><span>구분선이 섹션 경계를 표시합니다.</span></section>
      <div className="grid gap-2"><strong>장식용 강조선</strong><TRSeparator role="presentation" /><span>선이 시각적 리듬만 더합니다.</span></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/separator.css';
import { TRSeparator } from '@tinyrack/ui/components/separator';

export function SeparatorSemantics() {
  return (
    <div className="grid gap-4">
      <section className="grid gap-2">
        <strong>Related settings</strong>
        <TRSeparator />
        <span>The separator marks a section boundary.</span>
      </section>
      <div className="grid gap-2">
        <strong>Decorative accent</strong>
        <TRSeparator role="presentation" />
        <span>The line adds visual rhythm only.</span>
      </div>
    </div>
  );
}` }]}
/>

## API

| Prop | 타입 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `orientation` | `'horizontal' \| 'vertical'` | `'horizontal'` | 시각적 방향과 구분선의 `aria-orientation`을 설정합니다. |
| `role` | `React.AriaRole` | `'separator'` | 순수한 장식용 선에는 `presentation`을 사용합니다. 이 경우 구분선 전용 방향 ARIA가 생략됩니다. |
| `render` | `ReactElement` | `<div />` | 구분선 동작과 클래스를 유지하면서 렌더링 요소를 교체합니다. |

지원되는 모든 네이티브 `<div>` prop과 `className`, `style`, `ref`가 전달됩니다. `--tr-separator-color`, `--tr-separator-thickness`, `--tr-separator-min-length`로 모양을 사용자 지정할 수 있습니다.
