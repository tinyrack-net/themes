import { Meta } from '@storybook/addon-docs/blocks';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import { baseUiExampleSources } from '../shared/base-ui-example-sources.js';
import * as Stories from './meter.stories';

<Meta of={Stories} />

# Meter

Displays a scalar measurement within a known range.

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Storybook controls cover `label`, `value`.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/meter.css';", "import { Meter } from '@tinyrack/ui/components/meter';"] }]} />

## Usage

<ComponentExampleTabs
  id="meter-basic"
  title="Default"
  preview={<Stories.MeterPreview label={'Storage usage'} value={64} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['meter'] }]}
/>

## Examples

<ComponentExampleTabs
  id="meter-states"
  title="State comparison"
  description="Use Controls above to inspect every supported public state."
  preview={<Stories.MeterPreview label={'Storage usage'} value={92} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['meter'] }]}
/>

## API

Every compound part and prop/state type is exported from `@tinyrack/ui/components/meter`; styles remain opt-in through the matching CSS subpath.
