---
title: "AlertDialog"
description: "파괴적이거나 되돌릴 수 없는 작업을 확인하는 모달 표면이에요."
section: components
order: 26
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/alert-dialog.demo.js';

## 핵심 속성

이 Tinyrack 래퍼는 Base UI 동작, React 19 ref, 네이티브 속성, 이벤트, `render` 규약을 그대로 지켜요. 플레이그라운드에서는 `label`과 `disabled`를 조절할 수 있어요. `open`은 컴포넌트 이벤트와 동기화되는 미리 보기 전용 상호작용 상태로 관리하고, 초기화하면 처음 상태와 결과가 복원돼요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/alert-dialog.css';"],
      codeImports: ["import { TRAlertDialog } from '@tinyrack/ui/components/alert-dialog';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs
  id="alert-dialog-basic"
  data-docs-example-item-count="1"
  title="파괴적 동작 확인"
  description="취소는 가장 덜 파괴적인 첫 번째 동작이에요. 확인하면 대화상자가 닫히고 실시간 결과가 갱신돼요."
  preview={<Stories.AlertDialogPreview label="랙을 삭제하세요" open={false} disabled={false} />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.alertDialogBasicSourceKo }]}
/>

## 예시

<ComponentExampleTabs
  id="alert-dialog-states"
  data-docs-example-item-count="2"
  title="상태 비교"
  description="파괴적 동작 확인을 연 뒤 Escape나 취소를 누르고, 포커스가 랙 삭제 버튼으로 돌아오는지 확인하세요. 비활성 상태에서는 열리지 않아요."
  preview={<div className="grid gap-4"><Stories.AlertDialogPreview label={'모바일에서도 읽기 쉬운 긴 확인 레이블로 랙을 삭제하세요'} open={false} disabled={false} /><Stories.AlertDialogPreview label="삭제할 수 없어요" open={false} disabled /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.alertDialogStatesSourceKo }]}
/>

## API

구조는 `Root`, `Trigger`, `Portal`, `Backdrop`, `Viewport`, `Popup`, `Title`, `Description`, `Close`예요. 모든 파트와 속성·상태 타입은 `@tinyrack/ui/components/alert-dialog`에서 내보내며 스타일은 대응하는 CSS 하위 경로로 별도로 적용해요.

`Root`는 항상 모달이에요. 열려 있는 동안 포커스는 내부에 머물고, Escape와 `Close`는 닫기를 요청하지만 바깥 영역을 눌러도 닫히지 않아요. 닫히면 포커스가 `Trigger`로 돌아가요. 첫 번째 `Close`는 안전한 취소 용도로 두고, 파괴적 작업은 위험 확인용 `Close`에만 연결하세요. 비활성화된 trigger와 close는 동작하지 않아요.

`Root` 밖에 있는 trigger에는 `TRAlertDialog.createHandle<Payload>()`로 타입이 지정된 브리지를 만들어 `Trigger`와 `Root` 양쪽에 handle을 전달하고, `Trigger`에는 payload를 함께 넘기세요. `Portal`은 원하는 컨테이너를 받을 수 있어요. 확인 컨트롤을 `tr-alert-dialog-actions`로 묶으면 좁은 화면에서도 읽기 쉽게 줄바꿈돼요.
