---
title: "AppShell"
description: "A responsive application frame with a static desktop sidebar and modal mobile navigation."
section: components
order: 3
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/app-shell.demo.js';

## Contract

`Main` owns the page's single main landmark. `Sidebar` is sticky and independently scrollable above the selected breakpoint. `sidebarMode` switches it between the expanded width and an icon rail, while `SidebarLabel` keeps every navigation name accessible in both states. Below the breakpoint, `mobileSidebar="drawer"` preserves the Base UI TRDrawer behavior and `mobileSidebar="rail"` opts into an inline fixed rail.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/app-shell.css';", "import { TRAppShell } from '@tinyrack/ui/components/app-shell';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs
  description="Switch layout, breakpoint, drawer state, sidebar mode, mobile behavior, and control appearance. Reset restores the complete controlled preview."
  id="app-shell-basic"
  data-docs-example-item-count="1"
  preview={<Stories.AppShellPreview breakpoint="lg" contained layout="sidebar-first" />}
  previewClassName="!min-h-96 !min-w-0 !p-0"
  previewLayout="stretch"
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/app-shell.css';
import '@tinyrack/ui/components/link.css';
import { TRAppShell } from '@tinyrack/ui/components/app-shell';
import { TRLink } from '@tinyrack/ui/components/link';
import { GaugeIcon, MenuIcon, PanelLeftCloseIcon, RocketIcon, XIcon } from 'lucide-react';
import { useState } from 'react';

export function WorkspaceShell() {
  const [navigationOpen, setNavigationOpen] = useState(false);
  const [sidebarMode, setSidebarMode] = useState<'expanded' | 'rail'>('expanded');

  return (
    <TRAppShell.Root
      breakpoint="lg"
      layout="sidebar-first"
      mobileSidebar="drawer"
      open={navigationOpen}
      onOpenChange={setNavigationOpen}
      sidebarMode={sidebarMode}
      onSidebarModeChange={setSidebarMode}
    >
      <TRAppShell.Header>
        <TRAppShell.Trigger aria-label="Open workspace navigation">
          <MenuIcon aria-hidden="true" />
        </TRAppShell.Trigger>
        <strong>Acme Cloud</strong>
      </TRAppShell.Header>
      <TRAppShell.Sidebar aria-label="Workspace navigation">
        <TRAppShell.SidebarToggle aria-label="Toggle sidebar width">
          <PanelLeftCloseIcon aria-hidden="true" />
        </TRAppShell.SidebarToggle>
        <TRAppShell.Close aria-label="Close workspace navigation">
          <XIcon aria-hidden="true" />
        </TRAppShell.Close>
        <nav aria-label="Workspace pages">
          <TRLink href="/overview"><GaugeIcon aria-hidden="true" /><TRAppShell.SidebarLabel>Overview</TRAppShell.SidebarLabel></TRLink>
          <TRLink href="/deployments"><RocketIcon aria-hidden="true" /><TRAppShell.SidebarLabel>Deployments</TRAppShell.SidebarLabel></TRLink>
        </nav>
      </TRAppShell.Sidebar>
      <TRAppShell.Main><h1>Production overview</h1></TRAppShell.Main>
    </TRAppShell.Root>
  );
}` }]}
  title="Responsive shell"
/>

## Examples

<ComponentExampleTabs
  description="Three production-minded patterns compare the 48rem and 64rem drawer breakpoints with a persistent mobile rail. Resize the page to see each shell adapt in place."
  id="app-shell-layouts"
  data-docs-example-item-count="3"
  preview={<Stories.AppShellLayoutMatrix />}
  previewClassName="!min-w-0"
  previewLayout="stretch"
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/app-shell.css';
import { TRAppShell } from '@tinyrack/ui/components/app-shell';
import { MenuIcon, XIcon } from 'lucide-react';

const patterns = [
  { name: 'Workspace', breakpoint: 'sm', layout: 'sidebar-first', mobileSidebar: 'drawer', sidebarMode: 'expanded' },
  { name: 'Header first', breakpoint: 'lg', layout: 'header-first', mobileSidebar: 'drawer', sidebarMode: 'expanded' },
  { name: 'Mobile rail', breakpoint: 'lg', layout: 'sidebar-first', mobileSidebar: 'rail', sidebarMode: 'rail' },
] as const;

export function ShellMatrix() {
  return patterns.map((pattern) => (
    <TRAppShell.Root breakpoint={pattern.breakpoint} key={pattern.name} layout={pattern.layout} mobileSidebar={pattern.mobileSidebar} sidebarMode={pattern.sidebarMode}>
      <TRAppShell.Header>
        <TRAppShell.Trigger aria-label="Open workspace navigation">
          <MenuIcon aria-hidden="true" />
        </TRAppShell.Trigger>
      </TRAppShell.Header>
      <TRAppShell.Sidebar aria-label="Workspace">
        <TRAppShell.Close aria-label="Close workspace navigation">
          <XIcon aria-hidden="true" />
        </TRAppShell.Close>
        Workspace navigation
      </TRAppShell.Sidebar>
      <TRAppShell.Main>Workspace content</TRAppShell.Main>
    </TRAppShell.Root>
  ));
}` }]}
  title="Responsive navigation patterns"
/>

<ComponentExampleTabs
  description="Compare solid, outline, and ghost shell controls without mixing in layout decisions."
  id="app-shell-controls"
  data-docs-example-item-count="3"
  preview={<Stories.AppShellControlAppearances />}
  previewClassName="!min-w-0"
  previewLayout="stretch"
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/app-shell.css';
import { TRAppShell } from '@tinyrack/ui/components/app-shell';

const appearances = ['solid', 'outline', 'ghost'] as const;

export function ShellControls() {
  return appearances.map((appearance) => (
    <TRAppShell.Root breakpoint="lg" key={appearance} layout="header-first">
      <TRAppShell.Header>
        <TRAppShell.Trigger appearance={appearance} aria-label="Open navigation" />
        <strong>{appearance} controls</strong>
      </TRAppShell.Header>
      <TRAppShell.Sidebar aria-label={appearance + ' navigation'}>
        Navigation
      </TRAppShell.Sidebar>
      <TRAppShell.Main>Workspace content</TRAppShell.Main>
    </TRAppShell.Root>
  ));
}` }]}
  title="Control appearances"
/>

## API

`Root` accepts `breakpoint` (`sm` or `lg`), `layout` (`header-first` or `sidebar-first`), and `mobileSidebar` (`drawer` or `rail`). Drawer state supports `open`, `defaultOpen`, and `onOpenChange`; sidebar width supports `sidebarMode`, `defaultSidebarMode`, and `onSidebarModeChange`. Use `portalContainer` for a contained preview or embedded application and `drawerPopupClassName` for popup styling.

`Header` renders the header landmark. `Sidebar` renders an independently scrollable `aside` and supplies its accessible name to the mobile dialog. `Trigger` opens the mobile drawer, `Close` dismisses it, and `SidebarToggle` switches expanded and rail modes while retaining focus and exposing `aria-expanded`. `SidebarLabel` is visually hidden rather than removed in rail mode. `Main` renders the page's main landmark and supports Base UI's `render` contract.

The stable styling surface includes `--tr-app-shell-background`, `--tr-app-shell-sidebar-background`, `--tr-app-shell-sidebar-width`, `--tr-app-shell-sidebar-rail-width`, and `--tr-app-shell-drawer-block-size`. Root state is reflected by `data-breakpoint`, `data-layout`, `data-mobile-sidebar`, and `data-sidebar-mode`.

For SSR, the server renders the desktop/static structure and hydrates without reading browser globals during module evaluation. The client then applies the selected media query; avoid deriving different child markup from `window` in application render code. The modal drawer traps focus, closes on Escape or backdrop activation, restores focus to `Trigger`, and prevents page scrolling while open.
