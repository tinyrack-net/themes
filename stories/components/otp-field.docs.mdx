import { Meta } from '@storybook/addon-docs/blocks';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import * as Stories from './otp-field.stories';

<Meta of={Stories} />

# OTP Field

Captures a one-time code in a single accessible input while rendering a segmented visual surface.

## Contract

| Axis | Behavior |
| --- | --- |
| value | uncontrolled with `defaultValue`, controlled with `value` and `onValueChange` |
| input | supports typing, full-code paste, autofill, and one native form value |
| length | `length` controls the rendered inputs and native required validity |

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/otp-field.css';", "import '@tinyrack/ui/components/form.css';", "import '@tinyrack/ui/components/field.css';", "import '@tinyrack/ui/components/button.css';", "import { OTPField } from '@tinyrack/ui/components/otp-field';", "import { Form } from '@tinyrack/ui/components/form';", "import { Field } from '@tinyrack/ui/components/field';", "import { Button } from '@tinyrack/ui/components/button';"] }]} />

## Usage

<ComponentExampleTabs
  id="otp-field-basic"
  title="Six-digit verification code"
  preview={<Stories.OTPFieldPreview defaultValue="" disabled={false} length={6} readOnly={false} required={false} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div>
  <label id="verification-code-label">Verification code</label>
  <OTPField.Root aria-labelledby="verification-code-label" length={6} name="verificationCode">
    <OTPField.Input />
    <OTPField.Input />
    <OTPField.Input />
    <OTPField.Separator />
    <OTPField.Input />
    <OTPField.Input />
    <OTPField.Input />
  </OTPField.Root>
</div>` }]}
/>

## Examples

<ComponentExampleTabs
  id="otp-field-length-states"
  title="Length and availability"
  preview={<Stories.OTPFieldStateComparison />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid gap-6 sm:grid-cols-3">
  <OTPField.Root defaultValue="123" length={4} name="short-code">
    <OTPField.Input /><OTPField.Input /><OTPField.Input /><OTPField.Input />
  </OTPField.Root>
  <OTPField.Root defaultValue="482901" length={6} name="readonly-code" readOnly>
    <OTPField.Input /><OTPField.Input /><OTPField.Input /><OTPField.Input /><OTPField.Input /><OTPField.Input />
  </OTPField.Root>
  <OTPField.Root disabled length={6} name="disabled-code">
    <OTPField.Input /><OTPField.Input /><OTPField.Input /><OTPField.Input /><OTPField.Input /><OTPField.Input />
  </OTPField.Root>
</div>` }]}
/>

<ComponentExampleTabs
  id="otp-field-validation"
  title="Required code and recovery"
  preview={<Stories.OTPFieldValidationPreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`function RequiredCode() {
  const [attempted, setAttempted] = useState(false);
  const [value, setValue] = useState('');
  const invalid = attempted && value.length !== 4;

  return (
    <Form noValidate onSubmit={(event) => {
      event.preventDefault();
      event.currentTarget.checkValidity();
      setAttempted(true);
    }}>
      <Field.Root invalid={invalid}>
        <Field.Label id="required-code-label">Verification code</Field.Label>
        <OTPField.Root aria-labelledby="required-code-label" length={4} name="code" onValueChange={setValue} required value={value}>
          <OTPField.Input /><OTPField.Input /><OTPField.Input /><OTPField.Input />
        </OTPField.Root>
        <Field.Error match>Enter all four digits.</Field.Error>
      </Field.Root>
      <Button type="submit">Verify</Button>
    </Form>
  );
}` }]}
/>

## API

`Root`, `Input`, `Separator`, and their prop and state types are exported from `@tinyrack/ui/components/otp-field`.
