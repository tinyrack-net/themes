---
title: "Menubar"
description: "Coordinates application menus with desktop-style keyboard navigation."
section: components
group: navigation
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/menubar.demo.js';

## Contract

| Decision | Choose |
| --- | --- |
| composition | render `TRMenu.Root` children inside one `TRMenubar` |
| orientation | `horizontal` uses Left/Right; `vertical` uses Up/Down |
| availability | disable one `TRMenu.Root`, or disable the whole menubar |
| boundary | `loopFocus` wraps at the ends; default `true` |

Use a menubar for persistent application commands, not site navigation.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/menubar.css';"],
      codeImports: ["import { TRMenubar } from '@tinyrack/ui/components/menubar';","import { TRMenu } from '@tinyrack/ui/components/menu';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs id="menubar-basic" title="Application commands" preview={<div data-docs-example-item-count="1"><Stories.MenubarBasicPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: Stories.menubarBasicSource.en }]} />

## Examples

### Set availability

<ComponentExampleTabs id="menubar-states" title="Enabled, item disabled, and fully disabled" preview={<div data-docs-example-item-count="3"><Stories.MenubarStateComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: Stories.menubarApplicationSource.en }]} />

### Choose navigation direction

<ComponentExampleTabs id="menubar-configurations" title="Horizontal and vertical" preview={<div data-docs-example-item-count="2"><Stories.MenubarConfigurationMatrix /></div>} sources={[{ label: 'React', language: 'tsx', code: Stories.menubarConfigurationsSource.en }]} />

### Choose boundary behavior

<ComponentExampleTabs id="menubar-keyboard-loop" title="Looping and bounded focus" description="The orientation axis chooses the arrow keys. `loopFocus` only decides whether movement wraps at the first and last menu." preview={<div data-docs-example-item-count="2"><Stories.MenubarKeyboardComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: Stories.menubarApplicationSource.en }]} />

## API

`TRMenubar` accepts Base UI root props. `orientation` is `'horizontal' | 'vertical'` with default `'horizontal'`; `loopFocus` is boolean with default `true`; `disabled` is boolean with default `false`. Give the root an accessible name. It maintains one roving tab stop, skips disabled child menus, switches an open menu as focus moves, and restores trigger focus after Escape.

Compose `TRMenu.Root` children and their overlay anatomy from `@tinyrack/ui/components/menu`. Base UI `render`, root `ref`, native props, events, functional `className` / `style`, and state data attributes are preserved. `@tinyrack/ui/components/menubar.css` includes the styles for the composed menu parts; do not import menu CSS separately.

`TRMenubar`, `TRMenubarProps`, and `TRMenubarState` are exported from `@tinyrack/ui/components/menubar`.
