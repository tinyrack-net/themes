import { Meta } from '@storybook/addon-docs/blocks';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import { baseUiExampleSources } from '../shared/base-ui-example-sources.js';
import * as Stories from './form.stories';

<Meta of={Stories} />

# Form

A native form wrapper that integrates Base UI validation errors.

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Storybook controls cover `label`, `required`, `submitLabel`.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/form.css';", "import '@tinyrack/ui/components/input.css';", "import '@tinyrack/ui/components/button.css';", "import { Form } from '@tinyrack/ui/components/form';", "import { Input } from '@tinyrack/ui/components/input';", "import { Button } from '@tinyrack/ui/components/button';"] }]} />

## Usage

<ComponentExampleTabs
  id="form-basic"
  title="Default"
  preview={<Stories.FormPreview label={'Rack name'} required={true} submitLabel={'Save'} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['form'] }]}
/>

## Examples

<ComponentExampleTabs
  id="form-states"
  title="State comparison"
  description="Use Controls above to inspect every supported public state."
  preview={<Stories.FormPreview label={'Rack name'} required={false} submitLabel={'Save'} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['form'] }]}
/>

## API

Every compound part and prop/state type is exported from `@tinyrack/ui/components/form`; styles remain opt-in through the matching CSS subpath.
