---
title: "Switch"
description: "ネイティブの checked 状態とフォームの意味を保ちながら、即時の二値設定を切り替えます。"
section: components
order: 46
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import { switchStateComparisonSource } from '../../shared/base-ui-example-sources.js';
import * as Stories from '../../components/switch.demo.js';

## コントラクト

| 軸 | 動作 |
| --- | --- |
| state | uncontrolled with `defaultChecked`, controlled with `checked` and `onCheckedChange` |
| semantics | pair the switch with a visible label; use a checkbox when submitting a batch choice |
| form | supports `name`, `value`, and `required` through its hidden native input |

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/switch.css';", "import '@tinyrack/ui/components/form.css';", "import '@tinyrack/ui/components/field.css';", "import '@tinyrack/ui/components/button.css';", "import { TRSwitch } from '@tinyrack/ui/components/switch';", "import { TRForm } from '@tinyrack/ui/components/form';", "import { TRField } from '@tinyrack/ui/components/field';", "import { TRButton } from '@tinyrack/ui/components/button';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="switch-basic"
  title="自動バックアップ"
  preview={<Stories.SwitchPreview defaultChecked disabled={false} label="自動バックアップ" readOnly={false} required={false} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<label htmlFor="automatic-backups" className="flex items-center gap-3">
  <TRSwitch.Root aria-label="Automatic backups" defaultChecked id="automatic-backups" name="automaticBackups">
    <TRSwitch.Thumb />
  </TRSwitch.Root>
  Automatic backups
</label>` }]}
/>

## 例

<ComponentExampleTabs
  id="switch-states"
  title="状態の比較"
  preview={<Stories.SwitchStateComparison />}
  sources={[{ label: 'React', language: 'tsx', code: switchStateComparisonSource }]}
/>

<ComponentExampleTabs
  id="switch-validation"
  title="必須確認と復旧"
  preview={<Stories.SwitchValidationPreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { useState } from 'react';

function RequiredSwitch() {
  const [attempted, setAttempted] = useState(false);
  const [checked, setChecked] = useState(false);
  const invalid = attempted && !checked;

  return (
    <TRForm noValidate onSubmit={(event) => {
      event.preventDefault();
      event.currentTarget.checkValidity();
      setAttempted(true);
    }}>
      <TRField.Root invalid={invalid}>
        <TRField.Label>
          <TRSwitch.Root aria-label="Enable health monitoring" checked={checked} name="monitoring" onCheckedChange={setChecked} required>
            <TRSwitch.Thumb />
          </TRSwitch.Root>
          Enable health monitoring.
        </TRField.Label>
        {invalid ? <TRField.Error match>Enable health monitoring to continue.</TRField.Error> : null}
      </TRField.Root>
      <TRButton type="submit">Continue</TRButton>
      <output aria-live="polite">{attempted && checked ? 'Health monitoring enabled.' : ''}</output>
    </TRForm>
  );
}` }]}
/>

## API

`Root`、`Thumb`、prop type、Base UI state type は `@tinyrack/ui/components/switch` から export されます。CSS は opt-in のままです。表示する label text を `Root` に関連付け、Space で切り替えます。利用できない control には `disabled`、変更できない focusable value には `readOnly` を選びます。`name`、`value`、`required` はフォーム送信と検証のため hidden native input が持ちます。
