import { Meta } from '@storybook/addon-docs/blocks';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import { baseUiExampleSources } from '../shared/base-ui-example-sources.js';
import * as Stories from './switch.stories';

<Meta of={Stories} />

# Switch

Toggles an immediate setting between on and off.

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Storybook controls cover `label`, `checked`, `disabled`.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/switch.css';", "import { Switch } from '@tinyrack/ui/components/switch';"] }]} />

## Usage

<ComponentExampleTabs
  id="switch-basic"
  title="Default"
  preview={<Stories.SwitchPreview label={'Automatic updates'} checked={true} disabled={false} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['switch'] }]}
/>

## Examples

<ComponentExampleTabs
  id="switch-states"
  title="State comparison"
  description="Use Controls above to inspect every supported public state."
  preview={<Stories.SwitchPreview label={'Automatic updates'} checked={false} disabled={true} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['switch'] }]}
/>

## API

Every compound part and prop/state type is exported from `@tinyrack/ui/components/switch`; styles remain opt-in through the matching CSS subpath.
