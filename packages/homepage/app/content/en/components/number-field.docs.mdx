---
title: "Number Field"
description: "Edits a bounded numeric value through typing, stepping buttons, keyboard commands, or a scrub area."
section: components
order: 39
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/number-field.demo.js';

## Contract

| Axis | Behavior |
| --- | --- |
| value | uncontrolled with `defaultValue`, controlled with `value` and `onValueChange`; empty resolves to `null` |
| bounds | `min`, `max`, and `step` govern buttons, keys, and native validity |
| anatomy | Root → ScrubArea + Group → Decrement + Input + Increment |

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/number-field.css';", "import '@tinyrack/ui/components/form.css';", "import '@tinyrack/ui/components/field.css';", "import '@tinyrack/ui/components/button.css';", "import { NumberField } from '@tinyrack/ui/components/number-field';", "import { Form } from '@tinyrack/ui/components/form';", "import { Field } from '@tinyrack/ui/components/field';", "import { Button } from '@tinyrack/ui/components/button';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs
  id="number-field-basic"
  title="Replica count"
  preview={<Stories.NumberFieldPreview defaultValue={3} disabled={false} label="Replicas" max={20} min={0} readOnly={false} required={false} step={1} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<NumberField.Root defaultValue={3} max={20} min={0} name="replicas">
  <NumberField.ScrubArea>
    <label htmlFor="replica-count">Replicas</label>
    <NumberField.ScrubAreaCursor>↕</NumberField.ScrubAreaCursor>
  </NumberField.ScrubArea>
  <NumberField.Group>
    <NumberField.Decrement aria-label="Decrease">−</NumberField.Decrement>
    <NumberField.Input id="replica-count" />
    <NumberField.Increment aria-label="Increase">+</NumberField.Increment>
  </NumberField.Group>
</NumberField.Root>` }]}
/>

## Examples

<ComponentExampleTabs
  id="number-field-states"
  title="Availability states"
  preview={<Stories.NumberFieldStateComparison />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid gap-5 sm:grid-cols-2">
  <NumberField.Root defaultValue={3} max={20} min={0} name="editable-replicas">
    <NumberField.ScrubArea><label htmlFor="editable-replicas">Editable replicas</label><NumberField.ScrubAreaCursor>↕</NumberField.ScrubAreaCursor></NumberField.ScrubArea>
    <NumberField.Group><NumberField.Decrement aria-label="Decrease">−</NumberField.Decrement><NumberField.Input id="editable-replicas" /><NumberField.Increment aria-label="Increase">+</NumberField.Increment></NumberField.Group>
  </NumberField.Root>
  <NumberField.Root defaultValue={8} disabled max={20} min={0} name="disabled-replicas">
    <NumberField.ScrubArea><label htmlFor="disabled-replicas">Disabled replicas</label><NumberField.ScrubAreaCursor>↕</NumberField.ScrubAreaCursor></NumberField.ScrubArea>
    <NumberField.Group><NumberField.Decrement aria-label="Decrease">−</NumberField.Decrement><NumberField.Input id="disabled-replicas" /><NumberField.Increment aria-label="Increase">+</NumberField.Increment></NumberField.Group>
  </NumberField.Root>
  <NumberField.Root defaultValue={12} max={20} min={0} name="readonly-replicas" readOnly>
    <NumberField.ScrubArea><label htmlFor="readonly-replicas">Read-only replicas</label><NumberField.ScrubAreaCursor>↕</NumberField.ScrubAreaCursor></NumberField.ScrubArea>
    <NumberField.Group><NumberField.Decrement aria-label="Decrease">−</NumberField.Decrement><NumberField.Input id="readonly-replicas" /><NumberField.Increment aria-label="Increase">+</NumberField.Increment></NumberField.Group>
  </NumberField.Root>
</div>` }]}
/>

<ComponentExampleTabs
  id="number-field-validation"
  title="Required value and recovery"
  preview={<Stories.NumberFieldValidationPreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`function ReplicaForm() {
  const [attempted, setAttempted] = useState(false);
  const [value, setValue] = useState<number | null>(null);
  const invalid = attempted && value === null;

  return (
    <Form noValidate onSubmit={(event) => {
      event.preventDefault();
      event.currentTarget.checkValidity();
      setAttempted(true);
    }}>
      <Field.Root invalid={invalid}>
        <NumberField.Root max={20} min={0} name="replicas" onValueChange={setValue} required value={value}>
          <NumberField.ScrubArea><label htmlFor="required-replicas">Replica count</label><NumberField.ScrubAreaCursor>↕</NumberField.ScrubAreaCursor></NumberField.ScrubArea>
          <NumberField.Group><NumberField.Decrement aria-label="Decrease">−</NumberField.Decrement><NumberField.Input id="required-replicas" /><NumberField.Increment aria-label="Increase">+</NumberField.Increment></NumberField.Group>
        </NumberField.Root>
        {invalid ? <Field.Error match>Choose a replica count.</Field.Error> : null}
      </Field.Root>
      <Button type="submit">Create service</Button>
    </Form>
  );
}` }]}
/>

<ComponentExampleTabs
  id="number-field-format"
  title="Bounds, step, and unit formatting"
  description="Typing, buttons, arrow keys, and scrub gestures share the same 16–256 GB range and 16 GB step."
  preview={<Stories.NumberFieldFormatPreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<NumberField.Root
  defaultValue={64}
  format={{ style: 'unit', unit: 'gigabyte', unitDisplay: 'short' }}
  max={256}
  min={16}
  name="storage"
  step={16}
>
  <NumberField.ScrubArea>
    <label htmlFor="storage-capacity">Storage capacity</label>
    <NumberField.ScrubAreaCursor>↕</NumberField.ScrubAreaCursor>
  </NumberField.ScrubArea>
  <NumberField.Group>
    <NumberField.Decrement aria-label="Decrease by 16 gigabytes">−</NumberField.Decrement>
    <NumberField.Input id="storage-capacity" />
    <NumberField.Increment aria-label="Increase by 16 gigabytes">+</NumberField.Increment>
  </NumberField.Group>
</NumberField.Root>` }]}
/>

## API

`Root`, `Group`, `Input`, step buttons, scrub-area parts, and their prop and state types are exported from `@tinyrack/ui/components/number-field`. Keep `min ≤ max`, use a positive `step`, and use `format` for both editing and display units; the Playground normalizes invalid control combinations before rendering.
