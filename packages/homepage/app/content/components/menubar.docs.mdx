import { ComponentPlayground } from '../../playground/playground.js';

import { Menu } from '@tinyrack/ui/components/menu';
import { Menubar } from '@tinyrack/ui/components/menubar';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import { baseUiExampleSources } from '../shared/base-ui-example-sources.js';
import * as Stories from './menubar.demo.js';


# Menubar

Coordinates application menus with desktop-style arrow-key navigation.

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Playground controls cover `disabled`, `orientation`, `loopFocus`, and the accessible `label`.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/menubar.css';", "import '@tinyrack/ui/components/menu.css';", "import { Menubar } from '@tinyrack/ui/components/menubar';", "import { Menu } from '@tinyrack/ui/components/menu';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs
  id="menubar-basic"
  title="Default"
  preview={<Menubar aria-label="Application menu"><Menu.Root><Menu.Trigger>File</Menu.Trigger><Menu.Portal><Menu.Positioner><Menu.Popup><Menu.Item>New</Menu.Item><Menu.Item>Open</Menu.Item></Menu.Popup></Menu.Positioner></Menu.Portal></Menu.Root></Menubar>}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['menubar'] }]}
/>

## Examples

<ComponentExampleTabs
  id="menubar-states"
  title="Three-menu application bar"
  description="File, Edit, and View are separate top-level menus. Arrow keys move between them; Enter or Space opens the focused menu; Escape closes it and restores focus."
  preview={<Stories.MenubarPreview disabled={false} loopFocus={true} orientation={'horizontal'} label={'Application menu'} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`function ApplicationMenu() {
  const [result, setResult] = useState('No command selected');

  return <div>
    <Menubar aria-label="Application menu" loopFocus orientation="horizontal">
      <Menu.Root>
        <Menu.Trigger>File</Menu.Trigger>
        <Menu.Portal><Menu.Positioner><Menu.Popup>
          <Menu.Item onClick={() => setResult('New rack selected')}>New</Menu.Item>
          <Menu.Item onClick={() => setResult('Open selected')}>Open</Menu.Item>
        </Menu.Popup></Menu.Positioner></Menu.Portal>
      </Menu.Root>
      <Menu.Root>
        <Menu.Trigger>Edit</Menu.Trigger>
        <Menu.Portal><Menu.Positioner><Menu.Popup>
          <Menu.Item onClick={() => setResult('Rename selected')}>Rename</Menu.Item>
          <Menu.Item disabled>Duplicate unavailable</Menu.Item>
        </Menu.Popup></Menu.Positioner></Menu.Portal>
      </Menu.Root>
      <Menu.Root>
        <Menu.Trigger>View</Menu.Trigger>
        <Menu.Portal><Menu.Positioner><Menu.Popup>
          <Menu.CheckboxItem defaultChecked>
            <Menu.CheckboxItemIndicator aria-hidden="true">✓</Menu.CheckboxItemIndicator>
            Show status
          </Menu.CheckboxItem>
          <Menu.LinkItem href="#shortcuts">Keyboard shortcuts</Menu.LinkItem>
        </Menu.Popup></Menu.Positioner></Menu.Portal>
      </Menu.Root>
    </Menubar>
    <output aria-live="polite">{result}</output>
  </div>;
}` }]}
/>

## API

Every compound part and prop/state type is exported from `@tinyrack/ui/components/menubar`; styles remain opt-in through the matching CSS subpath.
