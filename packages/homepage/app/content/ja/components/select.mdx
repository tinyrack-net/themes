---
title: "Select"
description: "既知のコレクションから 1 つの値を選び、ボタンで開くキーボード操作可能なポップアップを提供します。"
section: components
group: selection-controls
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/select.demo.js';

## 主なプロパティ

| プロパティ | 動作 |
| --- | --- |
| value | `defaultValue` で非制御、または `value` と `onValueChange` で制御します。 |
| open | `defaultOpen` で非制御、または `open` と `onOpenChange` で制御します。 |
| disclosure | `Trigger` が focus を持ち、`Portal`、`Positioner`、`Popup` が floating layer を構成します。 |
| collection | group、label、separator、disabled item、text、indicator が menu anatomy を表します。 |
| form | `name`、`form`、`required`、`inputRef` が native submit、validation、reset と統合されます。 |

trigger が密度の高いコントロールサーフェスに属する場合は `TRSelect.Trigger uiSize="sm"` を使います。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/select.css';"],
      codeImports: ["import { TRSelect } from '@tinyrack/ui/components/select';","import { TRForm } from '@tinyrack/ui/components/form';","import { TRField } from '@tinyrack/ui/components/field';","import { TRButton } from '@tinyrack/ui/components/button';","import { ChevronDown } from 'lucide-react';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="select-basic"
  title="グループ化したデプロイ rack"
  preview={<Stories.SelectPreview data-docs-example-item="" defaultValue="alpha" disabled={false} disabledItem readOnly={false} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSelect } from '@tinyrack/ui/components/select';
import { ChevronDown } from 'lucide-react';

const racks = { alpha: 'ラックアルファ', beta: 'ラックベータ', gamma: 'ラックガンマ', staging: 'ステージングラック' };

<TRSelect.Root defaultValue="alpha" items={racks} name="rack">
  <TRSelect.Label>デプロイ先ラック</TRSelect.Label>
  <TRSelect.Trigger aria-label="デプロイ先ラック">
    <TRSelect.Value placeholder="ラックを選択" />
    <TRSelect.Icon aria-hidden="true"><ChevronDown /></TRSelect.Icon>
  </TRSelect.Trigger>
  <TRSelect.Portal>
    <TRSelect.Positioner sideOffset={8}>
      <TRSelect.Popup>
        <TRSelect.Arrow />
        <TRSelect.ScrollUpArrow aria-label="上にスクロール">↑</TRSelect.ScrollUpArrow>
        <TRSelect.List>
          <TRSelect.Group>
            <TRSelect.GroupLabel>本番</TRSelect.GroupLabel>
            <TRSelect.Item value="alpha"><TRSelect.ItemText>ラックアルファ</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
            <TRSelect.Item value="beta"><TRSelect.ItemText>ラックベータ</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
            <TRSelect.Item disabled value="gamma"><TRSelect.ItemText>ラックガンマ · オフライン</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
          </TRSelect.Group>
          <TRSelect.Separator />
          <TRSelect.Group>
            <TRSelect.GroupLabel>非本番</TRSelect.GroupLabel>
            <TRSelect.Item value="staging"><TRSelect.ItemText>ステージングラック</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
          </TRSelect.Group>
        </TRSelect.List>
        <TRSelect.ScrollDownArrow aria-label="下にスクロール">↓</TRSelect.ScrollDownArrow>
      </TRSelect.Popup>
    </TRSelect.Positioner>
  </TRSelect.Portal>
</TRSelect.Root>` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/select.css';" },
    },
  ]}
/>

## 例

<ComponentExampleTabs
  id="select-sizes"
  title="トリガーのサイズ"
  description="ポップアップの一覧とは別にトリガーの密度を選べます。"
  preview={<div data-docs-example-item-count="3"><Stories.SelectSizeComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSelect } from '@tinyrack/ui/components/select';

{(['sm', 'md', 'lg'] as const).map((uiSize) => (
  <TRSelect.Root defaultValue="alpha" items={{ alpha: 'ラックアルファ' }} key={uiSize}>
    <TRSelect.Label>{uiSize.toUpperCase() + ' デプロイ先ラック'}</TRSelect.Label>
    <TRSelect.Trigger uiSize={uiSize}><TRSelect.Value /></TRSelect.Trigger>
    <TRSelect.Portal><TRSelect.Positioner><TRSelect.Popup><TRSelect.List>
      <TRSelect.Item value="alpha"><TRSelect.ItemText>ラックアルファ</TRSelect.ItemText></TRSelect.Item>
    </TRSelect.List></TRSelect.Popup></TRSelect.Positioner></TRSelect.Portal>
  </TRSelect.Root>
))}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/select.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="select-states"
  title="利用可能状態"
  preview={<div data-docs-example-item-count="3"><Stories.SelectStateComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.selectStatesSourceJa }]}
/>

<ComponentExampleTabs
  id="select-validation"
  title="必須選択と復旧"
  preview={<div data-docs-example-item-count="1"><Stories.SelectValidationPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRButton } from '@tinyrack/ui/components/button';
import { TRField } from '@tinyrack/ui/components/field';
import { TRForm } from '@tinyrack/ui/components/form';
import { TRSelect } from '@tinyrack/ui/components/select';
import { ChevronDown } from 'lucide-react';
import { useState } from 'react';

const racks = { alpha: 'ラックアルファ', beta: 'ラックベータ', gamma: 'ラックガンマ', staging: 'ステージングラック' } as const;

function RackSelectForm() {
  const [attempted, setAttempted] = useState(false);
  const [value, setValue] = useState<string | null>(null);
  const invalid = attempted && value === null;

  return (
    <TRForm className="grid w-full max-w-80 min-w-0 gap-3" noValidate onSubmit={(event) => { event.preventDefault(); setAttempted(true); event.currentTarget.checkValidity(); }}>
      <TRField.Root invalid={invalid}>
        <TRSelect.Root items={racks} name="rack" onValueChange={setValue} required value={value}>
          <TRSelect.Label>デプロイ先ラック</TRSelect.Label>
          <TRSelect.Trigger aria-label="デプロイ先ラック"><TRSelect.Value placeholder="ラックを選択" /><TRSelect.Icon aria-hidden="true"><ChevronDown /></TRSelect.Icon></TRSelect.Trigger>
          <TRSelect.Portal><TRSelect.Positioner sideOffset={8}><TRSelect.Popup><TRSelect.Arrow /><TRSelect.ScrollUpArrow aria-label="上にスクロール">↑</TRSelect.ScrollUpArrow><TRSelect.List>
            <TRSelect.Group><TRSelect.GroupLabel>本番</TRSelect.GroupLabel>
              <TRSelect.Item value="alpha"><TRSelect.ItemText>ラックアルファ</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
              <TRSelect.Item value="beta"><TRSelect.ItemText>ラックベータ</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
              <TRSelect.Item value="gamma"><TRSelect.ItemText>ラックガンマ</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
            </TRSelect.Group><TRSelect.Separator /><TRSelect.Group><TRSelect.GroupLabel>非本番</TRSelect.GroupLabel>
              <TRSelect.Item value="staging"><TRSelect.ItemText>ステージングラック</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
            </TRSelect.Group>
          </TRSelect.List><TRSelect.ScrollDownArrow aria-label="下にスクロール">↓</TRSelect.ScrollDownArrow></TRSelect.Popup></TRSelect.Positioner></TRSelect.Portal>
        </TRSelect.Root>
        {invalid ? <TRField.Error match>デプロイ先ラックを選択してください。</TRField.Error> : null}
      </TRField.Root>
      <TRButton type="submit">デプロイ</TRButton>
      <output aria-live="polite">{attempted && value ? racks[value as keyof typeof racks] + ' にデプロイする準備ができました。' : ''}</output>
    </TRForm>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/button.css';\n@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/form.css';\n@import '@tinyrack/ui/components/select.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="select-long-collection"
  title="長くスクロールする collection"
  description="ポップアップを開き、高さ制限、スクロール矢印、長いラベル、強調されたフォーカス表示を確認します。"
  preview={<div data-docs-example-item-count="1"><Stories.SelectLongCollection /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSelect } from '@tinyrack/ui/components/select';
import { ChevronDown } from 'lucide-react';

const racks = Object.fromEntries(
  Array.from({ length: 18 }, (_, index) => [
    'rack-' + (index + 1),
    'ラック ' + String(index + 1).padStart(2, '0') + ' · ソウル可用性ゾーン',
  ]),
);

<TRSelect.Root defaultValue="rack-1" items={racks}>
  <TRSelect.Label>長いラック一覧</TRSelect.Label>
  <TRSelect.Trigger aria-label="長いラック一覧">
    <TRSelect.Value /><TRSelect.Icon aria-hidden="true"><ChevronDown /></TRSelect.Icon>
  </TRSelect.Trigger>
  <TRSelect.Portal>
    <TRSelect.Positioner sideOffset={8}>
      <TRSelect.Popup>
        <TRSelect.Arrow />
        <TRSelect.ScrollUpArrow aria-label="上にスクロール">↑</TRSelect.ScrollUpArrow>
        <TRSelect.List>
          {Object.entries(racks).map(([value, label]) => (
            <TRSelect.Item key={value} value={value}><TRSelect.ItemText>{label}</TRSelect.ItemText><TRSelect.ItemIndicator aria-hidden="true">✓</TRSelect.ItemIndicator></TRSelect.Item>
          ))}
        </TRSelect.List>
        <TRSelect.ScrollDownArrow aria-label="下にスクロール">↓</TRSelect.ScrollDownArrow>
      </TRSelect.Popup>
    </TRSelect.Positioner>
  </TRSelect.Portal>
</TRSelect.Root>` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/select.css';" },
    },
  ]}
/>

## API

完全な anatomy は `Root`、`Label`、`Trigger`、`Value`、`Icon`、`Portal`、`Backdrop`、`Positioner`、`Popup`、`List`、`Item`、`ItemIndicator`、`ItemText`、`Arrow`、`ScrollDownArrow`、`ScrollUpArrow`、`Group`、`GroupLabel`、`Separator` です。各 part は `TRSelect.Part` と named `TRSelectPart` export の両方で提供され、対応する `TRSelectPartProps` と `TRSelectPartState` type も export されます。

| 目的 | 公開 prop とデフォルト |
| --- | --- |
| 値 | `defaultValue` または制御された `value` を使います。`multiple` のデフォルトは `false` で、`items` は値と表示ラベルを対応付けます。 |
| 開閉 | `defaultOpen` のデフォルトは `false` です。制御された `open` も使え、`modal` と `highlightItemOnHover` のデフォルトは `true` です。 |
| 利用状態 | `disabled`、`readOnly`、`required` のデフォルトはすべて `false` です。 |
| トリガー | `uiSize="sm" | "md" | "lg"` を受け取り、デフォルトは `"md"` です。 |
| 配置 | `Positioner` は `alignItemWithTrigger={false}` と `sideOffset={4}` がデフォルトです。その他の衝突・配置 prop は Base UI から合成されます。 |
| フォーム | `name`、`form`、`autoComplete`、`inputRef` を受け取ります。非制御 select は native reset で `defaultValue` に戻ります。 |

`Trigger` にアクセシブルな名前を付けます。キーボードでは Arrow Down で開き、有効な option 間を移動し、typeahead を使い、Enter で確定し、Escape で閉じた後に trigger へ focus が戻ります。必要に応じて `value` と `open` を独立して制御します。利用できない control には `disabled`、変更できない focusable value には `readOnly` を使います。
