// Generated by scripts/generate-showcase-stories.mjs
import '../../../src/showcase/showcase.css';
import type { Meta, StoryObj } from '@storybook/react-vite';
import type { ShowcaseStoryKind } from '../../../src/showcase/index.js';
import {
  daisyUiShowcaseEntries,
  SingleShowcaseStory,
} from '../../../src/showcase/index.js';

type ComponentStoryProps = {
  storyKind?: ShowcaseStoryKind;
};

const entry = daisyUiShowcaseEntries.find(
  (showcaseEntry) => showcaseEntry.id === 'daisyui-loading',
);

if (!entry) {
  throw new Error('Missing showcase entry: daisyui-loading');
}

function LoadingStory({ storyKind = 'default' }: ComponentStoryProps) {
  return <SingleShowcaseStory entry={entry} library="daisyui" storyKind={storyKind} />;
}

const meta = {
  title: 'daisyUI/Loading',
  component: LoadingStory,
  tags: ['autodocs'],
  args: {
    storyKind: 'default',
  },
  argTypes: {
    storyKind: {
      control: 'select',
      options: ['default'],
      description: 'Switch between component stories.',
    },
  },
  parameters: {
    layout: 'fullscreen',
    docs: {
      description: {
        component: entry.description,
      },
    },
  },
} satisfies Meta<typeof LoadingStory>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {
  args: {
    storyKind: 'default',
  },
};
