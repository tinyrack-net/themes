---
title: "Code Block"
description: "Block-level source display with optional Shiki highlighting and explicit wrapping."
section: components
group: data-content
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRCodeBlock } from '@tinyrack/ui/components/code-block';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/code-block.demo.js';

## Contract

| Axis | Values | Default |
| --- | --- | --- |
| `language` | Shiki bundled language | plain text |
| Syntax theme | `github-light-high-contrast`, `github-dark-high-contrast` | inherited from the Tinyrack theme |
| `wrap` | `true`, `false` | `false` |

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/code-block.css';"],
      codeImports: ["import { TRCodeBlock } from '@tinyrack/ui/components/code-block';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs id="code-block-basic" title="Highlighted TypeScript" previewLayout="stretch" preview={<TRCodeBlock code={"const status = 'healthy';"} data-docs-example-item="" language="ts" />} sources={[{ label: 'React', language: 'tsx', code: Stories.codeBlockBasicSource }]} />

## Examples

<ComponentExampleTabs
  id="code-block-modes"
  title="Plain, highlighted, and wrapped"
  previewLayout="stretch"
  preview={(
    <div className="grid min-w-0 gap-4">
      <div data-docs-example-item=""><strong>Plain text</strong><TRCodeBlock code="rack-a: healthy" /></div>
      <div data-docs-example-item=""><strong>Highlighted JSON</strong><TRCodeBlock code={'{\n  "status": "healthy"\n}'} language="json" /></div>
      <div data-docs-example-item=""><strong>Theme-aware TypeScript</strong><TRCodeBlock code="const region = 'icn';" language="ts" /></div>
      <div data-docs-example-item=""><strong>Wrapped</strong><TRCodeBlock code="const message = 'A deliberately long line that wraps inside narrow documentation layouts';" language="ts" wrap /></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: Stories.codeBlockModesSource }]}
/>

<ComponentExampleTabs
  id="code-block-languages"
  title="Additional languages"
  description="Use the language identifier that matches the source so Shiki can load the correct grammar."
  previewLayout="stretch"
  preview={(
    <div className="grid min-w-0 gap-4">
      <div data-docs-example-item=""><strong>TSX</strong><TRCodeBlock code={"export const Status = () => <span>healthy</span>;"} language="tsx" /></div>
      <div data-docs-example-item=""><strong>JavaScript</strong><TRCodeBlock code="export const ready = true;" language="js" /></div>
      <div data-docs-example-item=""><strong>CSS</strong><TRCodeBlock code={".status {\n  color: green;\n}"} language="css" /></div>
      <div data-docs-example-item=""><strong>HTML</strong><TRCodeBlock code="<span>healthy</span>" language="html" /></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: Stories.codeBlockLanguagesSource }]}
/>

<ComponentExampleTabs
  id="code-block-copy"
  title="Copy action with visible result"
  description="Copy is an application action, so compose it with TRButton and a live result instead of hiding clipboard state inside TRCodeBlock."
  preview={<div data-docs-example-item=""><Stories.CodeBlockPreview code="pnpm verify" language="shellscript" wrap={false} /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.copyableCodeBlockSource }]}
/>

The composed copy example additionally needs these application-action imports:

```tsx
import { useState } from 'react';
import { TRButton } from '@tinyrack/ui/components/button';
import '@tinyrack/ui/components/button.css';
```

## API

Highlighting happens after hydration; the original source remains SSR-safe and visible before Shiki loads. Highlighted blocks automatically use `github-light-high-contrast` inside `tinyrack-light` and `github-dark-high-contrast` inside `tinyrack-dark`. Changing `code` or `language` cancels stale work so an earlier async result can never replace the current source.

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `code` | `string` | required | Source text rendered inside semantic `<pre><code>` markup. |
| `language` | `BundledLanguage` | `undefined` | Enables progressive Shiki highlighting; omit it for plain text. |
| `wrap` | `boolean` | `false` | Wraps long lines instead of exposing horizontal overflow. |
| `ref` | `Ref<HTMLPreElement>` | — | References the native `pre` element. |
| `className`, `style` | native `pre` props | — | Applies consumer classes, inline styles, and component-token overrides. |

All other native `pre` attributes and events are forwarded. Add an `aria-label` when surrounding prose does not already identify the source. Highlighting never changes the accessible source text, and a failed language load leaves the SSR fallback intact.

## Styles

| Token | Purpose |
| --- | --- |
| `--tr-code-block-background` | Surface, including highlighted blocks |
| `--tr-code-block-border` | Border color |
| `--tr-code-block-border-width` | Border width |
| `--tr-code-block-color` | Fallback source color |
| `--tr-code-block-font-family` | Monospace family |
| `--tr-code-block-font-size` | Source size |
| `--tr-code-block-line-height` | Source leading |
| `--tr-code-block-padding-x` | Inline padding |
| `--tr-code-block-padding-y` | Block padding |
| `--tr-code-block-radius` | Corner radius |

With `wrap={false}`, the block contains long lines with horizontal overflow. With `wrap`, long tokens break inside the available width. Both modes stay bounded by their parent.
