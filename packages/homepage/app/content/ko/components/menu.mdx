---
title: "메뉴"
description: "전용 트리거에서 키보드로 탐색할 수 있는 명령을 보여줘요."
section: components
order: 15
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/menu.demo.js';

## 핵심 속성

| 판단 기준 | 선택 방법 |
| --- | --- |
| 명령 의미 | 작업은 `Item`, 이동은 `LinkItem`, 유지할 선택은 선택 항목을 써요. |
| 방향 | 일반 메뉴는 `vertical`, 가로 명령 집합은 `horizontal`을 써요. |
| 배치 | `Positioner`의 `side` 네 값과 `align` 세 값으로 정해요. |
| 소유권 | Trigger와 Root를 함께 두거나 `createHandle`로 분리된 파트를 연결해요. |

전용 트리거가 명령을 열 때 메뉴를 써요. 임의의 오른쪽 클릭 대상에는 `TRContextMenu`, 사이트 목적지에는 `TRNavigationMenu`를 써요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/menu.css';"],
      codeImports: ["import { TRMenu } from '@tinyrack/ui/components/menu';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="menu-basic" title="작업 메뉴를 열어요" preview={<div data-docs-example-item-count="1"><Stories.MenuBasicPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRMenu } from '@tinyrack/ui/components/menu';
export function ActionMenu() { return <TRMenu.Root><TRMenu.Trigger>작업을 열어요</TRMenu.Trigger><TRMenu.Portal><TRMenu.Positioner><TRMenu.Popup><TRMenu.Item>다시 시작해요</TRMenu.Item><TRMenu.Item>삭제해요</TRMenu.Item></TRMenu.Popup></TRMenu.Positioner></TRMenu.Portal></TRMenu.Root>; }` ,
      styles: { code: "@import '@tinyrack/ui/components/menu.css';" },
    },
  ]}
/>

## 예시

### 명령 의미를 골라요

<ComponentExampleTabs id="menu-item-states" title="실행 가능, 실행 불가, 이동 명령" preview={<div data-docs-example-item-count="3"><Stories.MenuItemStateComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<><TRMenu.Item>다시 시작해요</TRMenu.Item><TRMenu.Item disabled>중지할 수 없어요</TRMenu.Item><TRMenu.LinkItem href="/racks">랙 목록으로 이동해요</TRMenu.LinkItem></>` }]} />

<ComponentExampleTabs id="menu-selection-items" title="체크박스와 라디오 선택" description="선택 항목은 메뉴를 닫지 않고 값을 바꿔요." preview={<div data-docs-example-item-count="2"><Stories.MenuSelectionItemComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<><TRMenu.CheckboxItem defaultChecked><TRMenu.CheckboxItemIndicator>✓</TRMenu.CheckboxItemIndicator>레이블을 보여요</TRMenu.CheckboxItem><TRMenu.RadioGroup defaultValue="compact"><TRMenu.RadioItem value="compact"><TRMenu.RadioItemIndicator>●</TRMenu.RadioItemIndicator>조밀하게 보여요</TRMenu.RadioItem></TRMenu.RadioGroup></>` }]} />

### 키보드 방향을 골라요

<ComponentExampleTabs id="menu-orientations" title="세로와 가로" preview={<div data-docs-example-item-count="2"><Stories.MenuOrientationComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<><TRMenu.Root orientation="vertical">{verticalMenu}</TRMenu.Root><TRMenu.Root orientation="horizontal">{horizontalMenu}</TRMenu.Root></>` }]} />

### 팝업을 배치해요

<ComponentExampleTabs id="menu-position-sides" title="위, 오른쪽, 아래, 왼쪽" preview={<div data-docs-example-item-count="4"><Stories.MenuSideComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`{(['top', 'right', 'bottom', 'left'] as const).map((side) => <TRMenu.Positioner key={side} side={side}>{popup}</TRMenu.Positioner>)}` }]} />

<ComponentExampleTabs id="menu-position-alignments" title="시작, 가운데, 끝 정렬" preview={<div data-docs-example-item-count="3"><Stories.MenuAlignComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`{(['start', 'center', 'end'] as const).map((align) => <TRMenu.Positioner align={align} key={align}>{popup}</TRMenu.Positioner>)}` }]} />

### 분리된 트리거를 연결해요

<ComponentExampleTabs id="menu-handle" title="타입이 있는 제공자 핸들" preview={<div data-docs-example-item-count="1"><Stories.MenuHandleExample /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`const menuHandle = TRMenu.createHandle<{ rack: string }>();
<TRMenu.Trigger handle={menuHandle} payload={{ rack: '랙 델타' }}>분리된 랙 작업</TRMenu.Trigger>
<TRMenu.Root handle={menuHandle}>{({ payload }) => (<TRMenu.Portal><TRMenu.Positioner><TRMenu.Popup><TRMenu.GroupLabel>{payload?.rack}</TRMenu.GroupLabel><TRMenu.Item>랙 살펴보기</TRMenu.Item></TRMenu.Popup></TRMenu.Positioner></TRMenu.Portal>)}</TRMenu.Root>` }]} />

### 키보드 모델을 따라요

<ComponentExampleTabs id="menu-keyboard" title="순환 초점과 닫기" description="방향키로 이동하고, 타이핑으로 명령을 찾고, Enter나 Space로 실행해요. Escape는 닫은 뒤 트리거에 초점을 돌려줘요." preview={<div data-docs-example-item-count="1"><Stories.MenuKeyboardPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRMenu.Root loopFocus><TRMenu.Trigger>키보드 메뉴를 열어요</TRMenu.Trigger>{menuSurface}</TRMenu.Root>` }]} />

## API

### 상태와 오버레이

`Root`는 `open`과 `onOpenChange`를 쓰는 제어 상태, `defaultOpen`을 쓰는 비제어 상태, `orientation`, `loopFocus`, `modal`, `disabled`를 지원해요. `Trigger`가 메뉴를 열어요. `Portal`, `Backdrop`, `Positioner`, `Popup`, `Arrow`, 선택 사항인 `Viewport`가 충돌을 피하는 레이어를 구성해요. `Positioner`는 `side`, `align`, 간격, 충돌 회피, 사용자 지정 기준 요소를 지원해요.

### 콘텐츠와 합성

`Group`, `GroupLabel`, `Separator`로 콘텐츠를 구분해요. `Item`, `LinkItem`, `CheckboxItem`, `CheckboxItemIndicator`, `RadioGroup`, `RadioItem`, `RadioItemIndicator`로 명령 의미를 정해요. `SubmenuRoot`와 `SubmenuTrigger`는 중첩 메뉴 상태와 키보드 진입을 관리해요. `LinkItem`은 `closeOnClick`을 설정할 때 닫혀요.

`createHandle`은 분리된 `Trigger` payload와 `Root`를 연결하는 타입 있는 Base UI 핸들을 만들어요. 모든 파트의 컴포넌트, prop 타입, 상태 타입을 `@tinyrack/ui/components/menu`에서 이름으로 내보내요. Base UI의 `render`, ref, 네이티브 prop, 이벤트, 상태 데이터 속성을 유지해요. CSS는 직접 가져와야 해요.
