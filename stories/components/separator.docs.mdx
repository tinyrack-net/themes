import { Meta } from '@storybook/addon-docs/blocks';
import { Separator } from '../../src/components/separator/index.js';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import * as Stories from './separator.stories';

<Meta of={Stories} />

# Separator

Semantic separators for horizontal sections and vertical control groups.

## Contract

`orientation` accepts `horizontal` or `vertical` and defaults to `horizontal`.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/separator.css';", "import { Separator } from '@tinyrack/ui/components/separator';"] }]} />

## Usage

<ComponentExampleTabs id="separator-basic" title="Section separator" previewLayout="stretch" preview={<div className="grid w-full gap-3"><span>Overview</span><Separator /><span>Network</span></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<span>Overview</span>
<Separator />
<span>Network</span>` }]} />

## Examples

<ComponentExampleTabs
  id="separator-orientations"
  title="Orientations"
  previewLayout="stretch"
  preview={(
    <div className="grid gap-6">
      <div className="grid gap-2"><strong>Horizontal</strong><Separator /></div>
      <div className="grid gap-2"><strong>Vertical</strong><div className="flex h-12 items-center gap-4"><span>CPU</span><Separator orientation="vertical" /><span>Memory</span><Separator orientation="vertical" /><span>Storage</span></div></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<Separator orientation="horizontal" />
<div className="flex h-12 items-center gap-4">
  <span>CPU</span>
  <Separator orientation="vertical" />
  <span>Memory</span>
</div>` }]}
/>

## API

Vertical separators set separator semantics and `aria-orientation="vertical"` automatically.
