---
title: "Document Pagination"
description: "Router-neutral previous and next document cards."
section: components
group: docs-site
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/document-pagination.demo.js';

## Contract

Previous and next destinations are data. `renderLink` injects a router element without coupling the component to routing state. The landmark renders only when at least one destination exists.

| Axis | Contract |
| --- | --- |
| direction | previous links expose `rel="prev"`; next links expose `rel="next"` |
| availability | omit a missing destination; set its `disabled` field when it must remain visible but unavailable |
| routing | `renderLink(destination, direction)` composes a router link without nested anchors |
| events | `onNavigate(destination, direction)` runs only for enabled links |

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/document-pagination.css';"],
      codeImports: ["import { TRDocumentPagination } from '@tinyrack/ui/components/document-pagination';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs id="document-pagination-basic" data-docs-example-item-count="1" title="Adjacent documents" preview={<Stories.DocumentPaginationPreview direction="both" />} sources={[{ label: 'React', language: 'tsx', code: Stories.documentPaginationBasicSource }]} />

## Examples

Pass only one destination at the beginning or end of a sequence. A disabled destination stays visible, has no `href`, leaves the tab order, and does not fire `onNavigate`. Passing neither destination renders nothing.

<ComponentExampleTabs id="document-pagination-states" data-docs-example-item-count="3" title="Available directions" preview={<Stories.DocumentPaginationStatesPreview />} sources={[{ label: 'React', language: 'tsx', code: Stories.documentPaginationStatesSource }]} />

<ComponentExampleTabs id="document-pagination-disabled" data-docs-example-item-count="1" title="Disabled destination" preview={<Stories.DocumentPaginationDisabledPreview />} sources={[{ label: 'React', language: 'tsx', code: Stories.documentPaginationDisabledSource }]} />

## API

`previous` and `next` accept `{ path, title, label?, description?, disabled? }`. Direction text and accessible labels are localizable with `previousLabel`, `nextLabel`, `previousAriaLabel`, `nextAriaLabel`, and landmark `label`. `renderLink` receives the destination and `previous | next` direction; use it for React Router or another client router. `onNavigate` receives the same values after an enabled link is activated.

The root preserves native `nav` attributes, `className`, `style`, events, and React 19 `ref`. Cards wrap long content and switch from one column to two at the responsive breakpoint; a lone next link occupies the second desktop column. Styles use Tinyrack semantic color, spacing, typography, focus, opacity, border, and motion tokens.
