---
title: "Document Pagination"
description: "라우터와 무관한 이전·다음 문서 카드입니다."
section: components
order: 56
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/document-pagination.demo.js';

## 계약

이전 및 다음 목적지는 데이터로 전달됩니다. `renderLink`는 컴포넌트를 라우팅 상태와 결합하지 않고 라우터 요소를 주입합니다.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/document-pagination.css';", "import { TRDocumentPagination } from '@tinyrack/ui/components/document-pagination';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="document-pagination-basic" title="인접 문서" preview={<Stories.DocumentPaginationPreview direction="both" />} sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/document-pagination.css';
import { TRDocumentPagination } from '@tinyrack/ui/components/document-pagination';

const previous = {
  label: 'Guide',
  path: '/install',
  title: 'Installation',
  description: 'Add Tinyrack UI and its component styles to your application.',
};
const next = {
  label: 'Guide',
  path: '/configure',
  title: 'Configuration',
  description: 'Choose the defaults and tokens for your documentation site.',
};

export function DocumentPaginationExample() {
  return <TRDocumentPagination previous={previous} next={next} />;
}` }]} />

## 예시

지역화된 문서 순서의 시작이나 끝에서는 목적지 하나만 전달하세요.

## API

방향 및 landmark label은 지역화할 수 있습니다. 목적지가 없으면 빈 placeholder 없이 해당 영역이 축소됩니다.
