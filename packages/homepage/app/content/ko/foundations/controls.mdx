---
title: "Controls"
description: "하나의 크기 레시피로 컴포넌트 시스템 전체의 액션, 필드, 탐색을 정렬합니다."
section: foundations
order: 7
---
import { Button } from '@tinyrack/ui/components/button';
import { Field } from '@tinyrack/ui/components/field';
import { Code } from '@tinyrack/ui/components/code';
import { Table } from '@tinyrack/ui/components/table';
import { tinyrackControlMetrics } from '@tinyrack/ui/core';

## 원칙

밀도 높은 보조 액션에는 `sm`, 기본 운영 인터페이스에는 `md`, 터치 편의나 강조가 필요할 때만 `lg`를 사용하세요.

## 시각적 스케일

<div className="grid gap-tinyrack-md" role="list">
  {['sm', 'md', 'lg'].map((size) => (
    <section className="grid items-center gap-tinyrack-md border border-tinyrack-border p-tinyrack-lg md:grid-cols-[7rem_minmax(0,1fr)]" key={size} role="listitem">
      <div><strong>{size}</strong><div className="text-tinyrack-sm text-tinyrack-text-muted"><Code>{tinyrackControlMetrics[size].height}</Code></div></div>
      <div className="flex flex-wrap items-center gap-tinyrack-sm"><Button uiSize={size}>Deploy</Button><Field.Root><Field.Control aria-label={`${size} rack name`} data-uiSize={size} placeholder="rack-a" /></Field.Root></div>
    </section>
  ))}
</div>

## 적용 패턴

<section className="grid gap-tinyrack-lg border border-tinyrack-border p-tinyrack-lg">
  <div className="grid gap-tinyrack-sm md:grid-cols-[minmax(0,1fr)_auto]"><Field.Root><Field.Control aria-label="Rack 이름" placeholder="rack-a" /></Field.Root><Button variant="primary">Save rack</Button></div>
  <div className="text-tinyrack-sm text-tinyrack-text-muted">두 컨트롤은 동일한 40px 중간 높이와 타이포그래피 레시피를 사용합니다.</div>
</section>

## 구현

제품 전체에 다른 밀도가 필요하면 전역 metric을 재정의하고, 하나의 컴포넌트에 의도적인 예외가 필요하면 `--tr-*` 레시피 토큰을 사용하세요.

```css
:root {
  --tinyrack-control-height-md: 2.5rem;
}

.rack-toolbar {
  --tr-btn-height: 2.25rem;
}
```

## 참조

<Table.Root containerProps={{ 'aria-label': 'Control metric reference', tabIndex: 0 }} data-foundation-reference="controls">
    <thead><tr><th scope="col">Size</th><th scope="col">Height</th><th scope="col">Padding</th><th scope="col">Gap</th><th scope="col">Type</th></tr></thead>
    <tbody>{['sm', 'md', 'lg'].map((size) => <tr key={size}><th scope="row">{size}</th><td><Code>{tinyrackControlMetrics[size].height}</Code></td><td><Code>{tinyrackControlMetrics[size].paddingInline}</Code></td><td><Code>{tinyrackControlMetrics[size].gap}</Code></td><td><Code>{tinyrackControlMetrics[size].fontSize}</Code></td></tr>)}</tbody>
  </Table.Root>