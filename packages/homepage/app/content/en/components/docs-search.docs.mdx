---
title: "Docs Search"
description: "Asynchronous documentation search dialog with shortcut and keyboard navigation."
section: components
order: 54
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/docs-search.demo.js';

## Contract

`onSearch(query, signal)` owns the data source. The TRDialog owns Ctrl/⌘+K, loading, empty, fallback, highlight, selection, focus, and request cancellation behavior.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/docs-search.css';", "import { TRDocsSearch } from '@tinyrack/ui/components/docs-search';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs id="docs-search-basic" title="Search trigger and dialog" preview={<Stories.DocsSearchPreview open={false} />} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRDocsSearch.Trigger onClick={() => setOpen(true)} />
<TRDocsSearch.Dialog open={open} onOpenChange={setOpen} onSearch={search} onSelect={openResult} />` }]} />

## Examples

Set `fallback` when a primary search index is unavailable to explain the reduced result set.

## API

All visible messages are localizable; `fallback` communicates when the primary Pagefind source is unavailable.
