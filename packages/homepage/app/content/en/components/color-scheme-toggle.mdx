---
title: "Color Scheme Toggle"
description: "An accessible controlled light and dark color-scheme switch for application adapters."
section: components
order: 52
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/color-scheme-toggle.demo.js';

## Contract

`TRColorSchemeToggle` is a controlled two-state button. Its icon and accessible name describe the next action: `light` offers dark, and `dark` offers light. The component never reads `localStorage`, queries the `system` preference, or mutates the document; resolve and persist those concerns in the application adapter.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/color-scheme-toggle.css';", "import { TRColorSchemeToggle } from '@tinyrack/ui/components/color-scheme-toggle';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs
  id="color-scheme-toggle-basic"
  title="Controlled scheme"
  description="Activating the button emits the next concrete scheme and leaves state ownership with the application."
  preview={<Stories.ColorSchemeTogglePreview value="light" />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.colorSchemeToggleBasicSource }]}
/>

## Examples

<ComponentExampleTabs
  id="color-scheme-toggle-adapter"
  title="System preference and persistence"
  description="Keep `system` as an application preference, resolve it to light or dark, and pass only that concrete value to the toggle. The deterministic initial value avoids hydration recovery; apply an equivalent pre-hydration document script when preventing a first-paint theme flash."
  preview={<Stories.ColorSchemeTogglePreview value="light" />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.colorSchemeToggleAdapterSource }]}
/>

<ComponentExampleTabs
  id="color-scheme-toggle-states"
  title="Sizes and disabled state"
  description="All Button focus, keyboard, sizing, disabled, token, ref, and native event behavior is preserved."
  preview={<Stories.ColorSchemeToggleStatesPreview />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.colorSchemeToggleStatesSource }]}
/>

## API

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `value` | `"light" \| "dark"` | required | Current resolved color scheme. |
| `onValueChange` | `(value: TRColorScheme) => void` | required | Receives the opposite scheme after uncancelled activation. |
| `darkLabel` | `string` | `"Use dark color scheme"` | Accessible action label while the next value is dark. |
| `lightLabel` | `string` | `"Use light color scheme"` | Accessible action label while the next value is light. |
| `aria-labelledby` | `string` | — | Replaces the generated label with an external accessible name. |
| `disabled` | `boolean` | `false` | Prevents focus and activation. |
| `uiSize` | `"sm" \| "md" \| "lg"` | `"md"` | Inherited IconButton size. |
| `ref` | `Ref<HTMLButtonElement>` | — | Ref forwarded to the button. |
| `onClick` | `MouseEventHandler<HTMLButtonElement>` | — | Runs before `onValueChange`; `preventDefault()` cancels the value change. |

Other safe `TRIconButton` button attributes, native events, `className`, `style`, and customization tokens are inherited. The visual appearance remains `ghost` and the control remains a native button.
