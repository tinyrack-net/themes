---
title: "Code"
description: "본문, 목록, 밀도 높은 metadata 안에서도 읽기 쉬운 인라인 코드 토큰입니다."
section: components
order: 7
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { Code } from '@tinyrack/ui/components/code';
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/code.demo.js';

## 계약

`Code`는 얇은 semantic `code` element입니다. 시각적 variant 축이 없고 주변 text size를 상속합니다.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/code.css';", "import { Code } from '@tinyrack/ui/components/code';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="code-basic" title="인라인 명령" preview={<p className="m-0">Run <Code>pnpm verify</Code> before publishing.</p>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<p>Run <Code>pnpm verify</Code> before publishing.</p>` }]} />

## 예시

<ComponentExampleTabs
  id="code-contexts"
  title="콘텐츠 context"
  description="inline code는 prose, configuration, 긴 token에서 semantic surface를 유지합니다."
  previewLayout="start"
  preview={(
    <div className="grid max-w-full gap-3">
      <p className="m-0">Import <Code>@tinyrack/ui/components/button</Code>.</p>
      <p className="m-0">Set <Code>data-theme=&quot;tinyrack-dark&quot;</Code> on the root.</p>
      <p className="m-0 max-w-64"><Code>very-long-rack-identifier-with-overflow-safe-wrapping-01</Code></p>
      <p className="m-0 text-xl">Inherited size: <Code>{'pnpm test\npnpm verify'}</Code></p>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid max-w-full gap-3">
  <p className="m-0">Import <Code>@tinyrack/ui/components/button</Code>.</p>
  <p className="m-0">Set <Code>data-theme="tinyrack-dark"</Code> on the root.</p>
  <p className="m-0 max-w-64"><Code>very-long-rack-identifier-with-overflow-safe-wrapping-01</Code></p>
  <p className="m-0 text-xl">Inherited size: <Code>{'pnpm test\npnpm verify'}</Code></p>
</div>` }]}
/>

## API

기본 font size는 주변 prose에서 상속됩니다. 긴 fragment와 multiline fragment는 복제된 line box와 안전한 줄바꿈을 사용합니다.

| CSS token | 기본값 | 용도 |
| --- | --- | --- |
| `--tr-code-background` | `--tinyrack-surface-muted` | Inline code surface |
| `--tr-code-border` | `--tinyrack-border` | Decorative hairline |
| `--tr-code-color` | `--tinyrack-text` | Code text |
| `--tr-code-font-size` | `inherit` | Surrounding text size |
| `--tr-code-line-height` | `--tinyrack-leading-sm` | Multiline rhythm |
| `--tr-code-radius` | `--tinyrack-radius-sm` | Corner radius |
| `--tr-code-padding-x` | `--tinyrack-space-xs` | Inline padding |
| `--tr-code-padding-y` | `--tinyrack-space-3xs` | Block padding |