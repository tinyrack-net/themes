---
title: "Slider"
description: "水平または垂直トラック上で 1 つの値または順序付き範囲を選択します。"
section: components
order: 45
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/slider.demo.js';

## 主なプロパティ

| プロパティ | 動作 |
| --- | --- |
| value | 単一 slider は数値または 1 要素の配列を受け取り、range は順序付き配列と値ごとに index を指定した Thumb を 1 つ使います。 |
| range | `min`、`max`、`step`、`minStepsBetweenValues` が pointer と keyboard による変更を制限します。 |
| form | `name` は現在の thumb 値を native hidden input を通してシリアライズします。 |
| 検証 | TRSlider にネイティブの `required` はありません。ビジネスルールは `TRField.Root` に置き、`TRField.Error` を表示して無効なつまみにフォーカスします。 |

slider が密度の高いコントロールサーフェスの一部である場合は `uiSize="sm"` を使います。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/slider.css';"],
      codeImports: ["import { TRSlider } from '@tinyrack/ui/components/slider';","import { TRField } from '@tinyrack/ui/components/field';","import { TRForm } from '@tinyrack/ui/components/form';","import { TRButton } from '@tinyrack/ui/components/button';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="slider-basic"
  title="音量"
  preview={<Stories.SliderPreview data-docs-example-item="" defaultValue={48} disabled={false} label="音量" orientation="horizontal" />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSlider } from '@tinyrack/ui/components/slider';

<TRSlider.Root defaultValue={[48]} name="volume">
  <TRSlider.Label>音量</TRSlider.Label>
  <TRSlider.Value />
  <TRSlider.Control>
    <TRSlider.Track><TRSlider.Indicator /></TRSlider.Track>
    <TRSlider.Thumb aria-label="音量" />
  </TRSlider.Control>
</TRSlider.Root>` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/slider.css';" },
    },
  ]}
/>

## 例

<ComponentExampleTabs
  id="slider-sizes"
  title="サイズ"
  preview={<div data-docs-example-item-count="3"><Stories.SliderSizeComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSlider } from '@tinyrack/ui/components/slider';

<div className="grid gap-6">
  {(['sm', 'md', 'lg'] as const).map((uiSize) => (
    <TRSlider.Root defaultValue={[48]} key={uiSize} uiSize={uiSize}>
      <TRSlider.Label>{uiSize.toUpperCase()} volume</TRSlider.Label>
      <TRSlider.Value />
      <TRSlider.Control>
        <TRSlider.Track><TRSlider.Indicator /></TRSlider.Track>
        <TRSlider.Thumb aria-label={uiSize.toUpperCase() + ' volume'} />
      </TRSlider.Control>
    </TRSlider.Root>
  ))}
</div>` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/slider.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="slider-states"
  title="向き"
  preview={<div data-docs-example-item-count="2"><Stories.SliderStateComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSlider } from '@tinyrack/ui/components/slider';

<div className="grid gap-6 sm:grid-cols-2">
  <TRSlider.Root defaultValue={[48]} name="horizontal-volume">
    <TRSlider.Label>横向きの音量</TRSlider.Label><TRSlider.Value /><TRSlider.Control><TRSlider.Track><TRSlider.Indicator /></TRSlider.Track><TRSlider.Thumb aria-label="横向きの音量" /></TRSlider.Control>
  </TRSlider.Root>
  <TRSlider.Root defaultValue={[36]} name="vertical-volume" orientation="vertical">
    <TRSlider.Label>縦向きの音量</TRSlider.Label><TRSlider.Value /><TRSlider.Control><TRSlider.Track><TRSlider.Indicator /></TRSlider.Track><TRSlider.Thumb aria-label="縦向きの音量" /></TRSlider.Control>
  </TRSlider.Root>
</div>` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/slider.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="slider-disabled"
  title="無効なスライダー"
  description="値を表示しつつ操作や送信を許可しない場合は Root を無効にします。"
  preview={<div data-docs-example-item-count="1"><Stories.SliderAvailabilityPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSlider } from '@tinyrack/ui/components/slider';

<TRSlider.Root defaultValue={[82]} disabled name="volume">
  <TRSlider.Label>無効な音量</TRSlider.Label><TRSlider.Value />
  <TRSlider.Control><TRSlider.Track><TRSlider.Indicator /></TRSlider.Track><TRSlider.Thumb aria-label="無効な音量" /></TRSlider.Control>
</TRSlider.Root>` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/slider.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="slider-range"
  title="2 つの thumb の範囲"
  description="Root は Intl.NumberFormat で表示値をフォーマットし、各 Thumb は支援技術に役割を伝える値テキストを提供します。"
  preview={<div data-docs-example-item-count="1"><Stories.SliderRangePreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSlider } from '@tinyrack/ui/components/slider';

<TRSlider.Root
  defaultValue={[20, 80]}
  format={{ maximumFractionDigits: 0, style: 'unit', unit: 'percent' }}
  locale="en"
  minStepsBetweenValues={10}
  name="window"
>
  <TRSlider.Label>メンテナンス範囲</TRSlider.Label>
  <TRSlider.Value />
  <TRSlider.Control>
    <TRSlider.Track><TRSlider.Indicator /></TRSlider.Track>
    <TRSlider.Thumb
      aria-label="開始"
      getAriaValueText={(formattedValue) => formattedValue + ' で開始します'}
      index={0}
    />
    <TRSlider.Thumb
      aria-label="End"
      getAriaValueText={(formattedValue) => formattedValue + ' で終了します'}
      index={1}
    />
  </TRSlider.Control>
</TRSlider.Root>` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/slider.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="slider-form"
  title="Form 送信"
  preview={<div data-docs-example-item-count="1"><Stories.SliderFormPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { useState } from 'react';
import { TRButton } from '@tinyrack/ui/components/button';
import { TRForm } from '@tinyrack/ui/components/form';
import { TRSlider } from '@tinyrack/ui/components/slider';

function VolumeForm() {
  const [submitted, setSubmitted] = useState<number | null>(null);
  const [value, setValue] = useState([48]);

  return (
    <TRForm onSubmit={(event) => { event.preventDefault(); setSubmitted(value[0]); }}>
      <TRSlider.Root name="volume" onValueChange={setValue} value={value}>
        <TRSlider.Label>音量</TRSlider.Label><TRSlider.Value /><TRSlider.Control><TRSlider.Track><TRSlider.Indicator /></TRSlider.Track><TRSlider.Thumb aria-label="音量" /></TRSlider.Control>
      </TRSlider.Root>
      <TRButton type="submit">音量を保存</TRButton>
      <output aria-live="polite">{submitted === null ? '' : '音量 ' + submitted + ' を保存しました。'}</output>
    </TRForm>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/button.css';\n@import '@tinyrack/ui/components/form.css';\n@import '@tinyrack/ui/components/slider.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="slider-validation"
  title="Field 所有の validation と focus 復元"
  description="60% 未満の値で送信すると、サムへフォーカスが移り、エラーが表示されます。値を上げるとエラーが消えるため、もう一度送信してください。"
  preview={<div data-docs-example-item-count="1"><Stories.SliderValidationPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { useId, useRef, useState } from 'react';
import { TRButton } from '@tinyrack/ui/components/button';
import { TRField } from '@tinyrack/ui/components/field';
import { TRForm } from '@tinyrack/ui/components/form';
import { TRSlider } from '@tinyrack/ui/components/slider';

function CapacitySliderForm() {
  const errorId = useId();
  const thumbInputRef = useRef<HTMLInputElement>(null);
  const [attempted, setAttempted] = useState(false);
  const [submitted, setSubmitted] = useState<number | null>(null);
  const [value, setValue] = useState(30);
  const invalid = attempted && value < 60;

  return (
    <TRForm
      noValidate
      onSubmit={(event) => {
        event.preventDefault();
        setAttempted(true);
        if (value < 60) {
          setSubmitted(null);
          thumbInputRef.current?.focus();
          return;
        }
        setSubmitted(value);
      }}
    >
      <TRField.Root invalid={invalid}>
        <TRSlider.Root
          name="capacity"
          onValueChange={(nextValue) => {
            setValue(Array.isArray(nextValue) ? (nextValue[0] ?? 0) : Number(nextValue));
            setSubmitted(null);
          }}
          value={[value]}
        >
          <TRSlider.Label>予約容量</TRSlider.Label>
          <TRSlider.Value />
          <TRSlider.Control>
            <TRSlider.Track><TRSlider.Indicator /></TRSlider.Track>
            <TRSlider.Thumb
              aria-label="予約容量"
              aria-describedby={invalid ? errorId : undefined}
              inputRef={thumbInputRef}
            />
          </TRSlider.Control>
        </TRSlider.Root>
        <TRField.Description>容量を 60% 以上予約してください。</TRField.Description>
        {invalid ? <TRField.Error id={errorId} match>予約容量を 60% 以上に増やしてください。</TRField.Error> : null}
      </TRField.Root>
      <TRButton type="submit">容量を予約</TRButton>
      <output aria-live="polite">
        {submitted === null ? '' : '容量を ' + submitted + '% 予約しました。'}
      </output>
    </TRForm>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/button.css';\n@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/form.css';\n@import '@tinyrack/ui/components/slider.css';" },
    },
  ]}
/>

## API

`Root`、`Label`、`Value`、`Control`、`Track`、`Indicator`、`Thumb` と prop/state type は `@tinyrack/ui/components/slider` から export されます。`Root` は 1 つの thumb に scalar number または readonly number array を受け取ります。range には array を使い、各 range `Thumb` に 0 始まりの `index` を指定して SSR と hydration が正しい値へ対応できるようにします。

| 目的 | 公開 prop とデフォルト |
| --- | --- |
| スケール | `min={0}`、`max={100}`、`step={1}`、`largeStep={10}` がデフォルトです。 |
| レイアウト | `orientation="horizontal" | "vertical"` を受け取り、デフォルトは `"horizontal"` です。`uiSize="sm" | "md" | "lg"` のデフォルトは `"md"` です。 |
| range | `minStepsBetweenValues` のデフォルトは `0` です。`thumbCollisionBehavior="push" | "swap" | "none"` のデフォルトは `"push"` です。 |
| thumb の端 | `thumbAlignment="center" | "edge" | "edge-client-only"` を受け取り、デフォルトは `"center"` です。 |
| 状態と form | 制御された `value` または `defaultValue`、`disabled={false}`、`name`、外部 `form` を受け取ります。 |

`Value` がレンダリングする `Intl.NumberFormat` の出力を設定するには `Root` の `format` と `locale` を使います。各 Thumb に簡潔なアクセシブルな名前を付け、書式化した数値に「20 パーセントから開始」のような role の文脈が必要な場合は `getAriaValueText(formattedValue, value, index)` を使います。値が変わってもテキストが正しい場合だけ固定の `aria-valuetext` を使います。

`onValueChange` は pointer と keyboard のリアルタイムな変更を報告します。完了した操作の後に 1 回だけ実行する処理には `onValueCommitted` を使います。矢印キーは `step` で変化し、Page Up/Page Down と Shift+Arrow は `largeStep` を使い、Home/End は境界へ移動します。`name` はフォーム送信用のネイティブ range input を作り、Thumb の `inputRef` は focus 復元のため対応する input に到達します。`minStepsBetweenValues` と `thumbCollisionBehavior` は range thumb が近づいたり交差したりする方法を定義します。
