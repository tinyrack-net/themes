---
title: "Skeleton"
description: "텍스트, 사각형, 원 형태로 레이아웃을 보존하는 로딩 플레이스홀더예요."
section: components
order: 20
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRSkeleton } from '@tinyrack/ui/components/skeleton';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/skeleton.demo.js';

## 핵심 속성

`shape`은 `text`, `rectangle`, `circle`을 받고 기본값은 `text`예요. TRSkeleton은 기본적으로 쉬머 효과를 보여 줘요. 움직임 없는 플레이스홀더에는 `animate={false}`를 설정하세요. 크기는 레이아웃이나 `style`로 정해요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/skeleton.css';", "import { TRSkeleton } from '@tinyrack/ui/components/skeleton';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="skeleton-basic" title="장식용 로딩 플레이스홀더" previewLayout="stretch" preview={<TRSkeleton data-docs-example-item="" style={{ height: 48, width: '100%' }} />} sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/skeleton.css';
import { TRSkeleton } from '@tinyrack/ui/components/skeleton';

<TRSkeleton style={{ height: 48, width: '100%' }} />` }]} />

## 예시

<ComponentExampleTabs
  id="skeleton-shapes"
  title="형태"
  previewLayout="stretch"
  preview={(
    <div className="grid gap-5">
      <div className="grid gap-2" data-docs-example-item=""><strong>텍스트</strong><TRSkeleton shape="text" style={{ width: '70%' }} /><TRSkeleton shape="text" style={{ width: '45%' }} /></div>
      <div className="grid gap-2" data-docs-example-item=""><strong>사각형</strong><TRSkeleton shape="rectangle" style={{ height: 72, width: '100%' }} /></div>
      <div className="grid gap-2" data-docs-example-item=""><strong>원</strong><TRSkeleton shape="circle" style={{ height: 48, width: 48 }} /></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/skeleton.css';
import { TRSkeleton } from '@tinyrack/ui/components/skeleton';

<div className="grid gap-5">
  <div className="grid gap-2"><strong>텍스트</strong><TRSkeleton shape="text" style={{ width: '70%' }} /><TRSkeleton shape="text" style={{ width: '45%' }} /></div>
  <div className="grid gap-2"><strong>사각형</strong><TRSkeleton shape="rectangle" style={{ height: 72, width: '100%' }} /></div>
  <div className="grid gap-2"><strong>원</strong><TRSkeleton shape="circle" style={{ height: 48, width: 48 }} /></div>
</div>` }]}
/>

<ComponentExampleTabs
  id="skeleton-semantics"
  title="하나의 로딩 상태와 장식용 자식"
  description="레이블이 없는 스켈레톤은 기본적으로 aria-hidden이에요. 로딩 상태를 알려야 하면 한 영역에 접근 가능한 레이블을 배치하세요."
  preview={(
    <div aria-busy="true" aria-label="운영자 프로필 로딩 중" className="grid gap-3" data-docs-example-item="" role="status">
      <TRSkeleton shape="circle" style={{ height: 48, width: 48 }} />
      <TRSkeleton shape="text" style={{ width: '60%' }} />
      <TRSkeleton shape="text" style={{ width: '35%' }} />
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/skeleton.css';
import { TRSkeleton } from '@tinyrack/ui/components/skeleton';

<div aria-busy="true" aria-label="운영자 프로필 로딩 중" className="grid gap-3" role="status">
  <TRSkeleton shape="circle" style={{ height: 48, width: 48 }} />
  <TRSkeleton shape="text" style={{ width: '60%' }} />
  <TRSkeleton shape="text" style={{ width: '35%' }} />
</div>` }]}
/>

<ComponentExampleTabs
  id="skeleton-motion"
  title="동작"
  previewLayout="stretch"
  preview={(
    <div className="grid gap-5 md:grid-cols-2">
      <div className="grid gap-2" data-docs-example-item=""><strong>애니메이션</strong><TRSkeleton shape="rectangle" style={{ height: 72, width: '100%' }} /></div>
      <div className="grid gap-2" data-docs-example-item=""><strong>정적</strong><TRSkeleton animate={false} shape="rectangle" style={{ height: 72, width: '100%' }} /></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/skeleton.css';
import { TRSkeleton } from '@tinyrack/ui/components/skeleton';

<div className="grid gap-5 md:grid-cols-2">
  <div className="grid gap-2"><strong>애니메이션</strong><TRSkeleton shape="rectangle" style={{ height: 72, width: '100%' }} /></div>
  <div className="grid gap-2"><strong>정적</strong><TRSkeleton animate={false} shape="rectangle" style={{ height: 72, width: '100%' }} /></div>
</div>` }]}
/>

## API

`TRSkeleton`은 네이티브 `div`의 속성, 이벤트, 스타일, React 19 `ref`를 전달해요.

| 속성 | 타입 | 기본값 | 용도 |
| --- | --- | --- | --- |
| `animate` | `boolean` | `true` | 쉬머 애니메이션 활성화 |
| `shape` | `'text' \| 'rectangle' \| 'circle'` | `'text'` | 기본 크기와 반경 선택 |

레이블이 없는 스켈레톤에는 `aria-hidden="true"`가 적용돼요. `aria-label`, `aria-labelledby`, `aria-live`, `aria-busy` 또는 `role="status"`를 추가하면 라이브 영역 하나로 바뀌어요. 모션 감소 설정을 켜면 쉬머가 멈춰요.

| CSS 토큰 | 기본값 | 용도 |
| --- | --- | --- |
| `--tr-skeleton-fill` | `--tinyrack-skeleton-fill` | 플레이스홀더 채움색 |
| `--tr-skeleton-highlight` | `--tinyrack-skeleton-highlight` | 쉬머 강조색 |
| `--tr-skeleton-height` | 형태별 레시피 | 플레이스홀더 높이 |
| `--tr-skeleton-circle-size` | `--tinyrack-control-height-lg` | 원의 너비와 높이 |
| `--tr-skeleton-radius` | 형태별 레시피 | 모서리 반경 |
| `--tr-skeleton-duration` | `--tinyrack-duration-loading` | 쉬머 지속 시간 |
| `--tr-skeleton-easing` | `--tinyrack-ease-linear` | 쉬머 이징 |
