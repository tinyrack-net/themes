---
title: "Steps"
description: "Numbered semantic procedures for authored documentation."
section: components
order: 59
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/steps.demo.js';

## Contract

`Root` renders an ordered list and `Item` renders a list item. Authored MDX children remain untouched.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/steps.css';", "import { Steps } from '@tinyrack/ui/components/steps';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs id="steps-basic" title="Installation procedure" preview={<Stories.StepsPreview count={3} />} sources={[{ label: 'React', language: 'tsx', code: String.raw`<Steps.Root><Steps.Item>Install the package</Steps.Item><Steps.Item>Create the config</Steps.Item></Steps.Root>` }]} />

## Examples

Place headings, paragraphs, and code blocks directly inside each step.

## API

Both parts forward native ordered-list props, list-item props, and React 19 refs.
