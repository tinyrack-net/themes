---
title: "Card"
description: "意味のあるセクション、elevation、padding 密度を備えた構造化コンテンツのサーフェスです。"
section: components
order: 6
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRCard } from '@tinyrack/ui/components/card';
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/card.demo.js';

## コントラクト

| 軸 | 値 | 既定値 |
| --- | --- | --- |
| `variant` | `default`, `outlined`, `elevated` | `default` |
| `padding` | `none`, `sm`, `md`, `lg` | `md` |
| parts | `Root`, `Header`, `Title`, `Description`, `Content`, `Footer` | compound API |

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/card.css';", "import { TRCard } from '@tinyrack/ui/components/card';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="card-basic"
  title="構造化された surface"
  preview={<TRCard.Root className="w-full max-w-md" variant="elevated"><TRCard.Header><TRCard.Title render={<h4>Rack A</h4>} /><TRCard.Description>12 nodes</TRCard.Description></TRCard.Header><TRCard.Content>All systems operational.</TRCard.Content><TRCard.Footer>Updated now</TRCard.Footer></TRCard.Root>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRCard.Root variant="elevated">
  <TRCard.Header>
    <TRCard.Title render={<h4>Rack A</h4>} />
    <TRCard.Description>12 nodes</TRCard.Description>
  </TRCard.Header>
  <TRCard.Content>All systems operational.</TRCard.Content>
  <TRCard.Footer>Updated now</TRCard.Footer>
</TRCard.Root>` }]}
/>

## 例

<ComponentExampleTabs
  id="card-variants"
  title="surface variant"
  previewLayout="stretch"
  preview={(
    <div className="grid gap-4 md:grid-cols-3">
      <TRCard.Root variant="default"><TRCard.Title render={<h4>Default</h4>} /><TRCard.Content>標準 contained surface.</TRCard.Content></TRCard.Root>
      <TRCard.Root variant="outlined"><TRCard.Title render={<h4>Outlined</h4>} /><TRCard.Content>Transparent outlined surface.</TRCard.Content></TRCard.Root>
      <TRCard.Root variant="elevated"><TRCard.Title render={<h4>Elevated</h4>} /><TRCard.Content>Raised above its context.</TRCard.Content></TRCard.Root>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid gap-4 md:grid-cols-3">
  <TRCard.Root variant="default"><TRCard.Title render={<h4>Default</h4>} /><TRCard.Content>Standard contained surface.</TRCard.Content></TRCard.Root>
  <TRCard.Root variant="outlined"><TRCard.Title render={<h4>Outlined</h4>} /><TRCard.Content>Transparent outlined surface.</TRCard.Content></TRCard.Root>
  <TRCard.Root variant="elevated"><TRCard.Title render={<h4>Elevated</h4>} /><TRCard.Content>Raised above its context.</TRCard.Content></TRCard.Root>
</div>` }]}
/>

<ComponentExampleTabs
  id="card-padding"
  title="variant × padding"
  description="すべての surface variant を対応する各 interior spacing recipe とともに示します。"
  previewLayout="stretch"
  preview={(
    <div className="grid gap-4 md:grid-cols-3">
      {['default', 'outlined', 'elevated'].map((variant) => (
        <div className="grid gap-3" key={variant}>
          <strong>{variant}</strong>
          {['none', 'sm', 'md', 'lg'].map((padding) => <TRCard.Root key={padding} padding={padding} variant={variant}><TRCard.Content>{padding}</TRCard.Content></TRCard.Root>)}
        </div>
      ))}
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid gap-4 md:grid-cols-3">
  {['default', 'outlined', 'elevated'].map((variant) => (
    <div className="grid gap-3" key={variant}>
      <strong>{variant}</strong>
      {['none', 'sm', 'md', 'lg'].map((padding) => (
        <TRCard.Root key={padding} padding={padding} variant={variant}>
          <TRCard.Content>{padding}</TRCard.Content>
        </TRCard.Root>
      ))}
    </div>
  ))}
</div>` }]}
/>

<ComponentExampleTabs
  id="card-edge-to-edge"
  title="端まで広がる media"
  description="media を card の境界まで広げる場合は外側の card に padding none を使い、semantic section に spacing を置きます。"
  preview={(
    <TRCard.Root className="w-full max-w-md overflow-hidden" padding="none" variant="outlined">
      <div className="grid min-h-32 place-items-center bg-tinyrack-surface-muted text-tinyrack-text-muted">Rack topology preview</div>
      <TRCard.Header className="px-4 pt-4"><TRCard.Title render={<h4>Rack Alpha</h4>} /><TRCard.Description>Edge-to-edge topology</TRCard.Description></TRCard.Header>
      <TRCard.Content className="px-4">All systems operational.</TRCard.Content>
      <TRCard.Footer className="px-4 pb-4">Updated now</TRCard.Footer>
    </TRCard.Root>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRCard.Root className="overflow-hidden" padding="none" variant="outlined">
  <div className="min-h-32">Rack topology preview</div>
  <TRCard.Header className="px-4 pt-4">
    <TRCard.Title render={<h4>Rack Alpha</h4>} />
    <TRCard.Description>Edge-to-edge topology</TRCard.Description>
  </TRCard.Header>
  <TRCard.Content className="px-4">All systems operational.</TRCard.Content>
  <TRCard.Footer className="px-4 pb-4">Updated now</TRCard.Footer>
</TRCard.Root>` }]}
/>

<ComponentExampleTabs
  id="card-semantic-render"
  title="semantic article と heading"
  description="render prop は wrapper node を挿入せず既定の element を置き換えます。周囲の page と heading level を揃えます。"
  preview={<TRCard.Root className="max-w-md" render={<article aria-labelledby="rack-card-title" />} variant="outlined"><TRCard.Header><TRCard.Title render={<h4 id="rack-card-title">Rack A health</h4>} /><TRCard.Description>Updated two minutes ago</TRCard.Description></TRCard.Header><TRCard.Content>All twelve nodes are accepting traffic.</TRCard.Content></TRCard.Root>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRCard.Root
  render={<article aria-labelledby="rack-card-title" />}
  variant="outlined"
>
  <TRCard.Header>
    <TRCard.Title render={<h4 id="rack-card-title">Rack A health</h4>} />
    <TRCard.Description>Updated two minutes ago</TRCard.Description>
  </TRCard.Header>
  <TRCard.Content>All twelve nodes are accepting traffic.</TRCard.Content>
</TRCard.Root>` }]}
/>

## API

`TRCard.Root` and `TRCard.Title` support Base UI's additive `render` contract. Every part forwards native props and `ref`; keep headings in document order when composing cards.
