---
title: "Popover"
description: "ダイアログの意味、タイトル、説明、閉じるアクション、配置、解除を備えた非モーダルのアンカーコンテンツです。"
section: components
order: 18
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { Popover } from '@tinyrack/ui/components/popover';
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/popover.demo.js';

## コントラクト

制御付き `open` または非制御の `defaultOpen` を使います。`Positioner` は Base UI の side、alignment、offset、collision prop を受け取ります。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/popover.css';", "import { Popover } from '@tinyrack/ui/components/popover';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs id="popover-basic" title="Rack の詳細" preview={<Popover.Root><Popover.Trigger>Rack の詳細</Popover.Trigger><Popover.Portal><Popover.Positioner sideOffset={8}><Popover.Popup><Popover.Arrow /><Popover.Title>Rack A</Popover.Title><Popover.Description>すべての node が online です。</Popover.Description><Popover.Close>閉じる</Popover.Close></Popover.Popup></Popover.Positioner></Popover.Portal></Popover.Root>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<Popover.Root>
  <Popover.Trigger>Rack details</Popover.Trigger>
  <Popover.Portal><Popover.Positioner sideOffset={8}><Popover.Popup>
    <Popover.Arrow />
    <Popover.Title>Rack A</Popover.Title>
    <Popover.Description>All nodes online.</Popover.Description>
    <Popover.Close>Close</Popover.Close>
  </Popover.Popup></Popover.Positioner></Popover.Portal>
</Popover.Root>` }]} />

## 例

<ComponentExampleTabs
  id="popover-sides"
  title="配置する側"
  description="各 trigger が要求した側に実際の anchored popup を開き、collision を処理します。"
  preview={<div className="flex flex-wrap gap-3">{['top', 'right', 'bottom', 'left'].map((side) => <Popover.Root key={side}><Popover.Trigger>{side}</Popover.Trigger><Popover.Portal><Popover.Positioner side={side} sideOffset={8}><Popover.Popup><Popover.Arrow /><Popover.Title>{side}</Popover.Title><Popover.Description>Anchored content.</Popover.Description><Popover.Close>閉じる</Popover.Close></Popover.Popup></Popover.Positioner></Popover.Portal></Popover.Root>)}</div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`{['top', 'right', 'bottom', 'left'].map((side) => (
  <Popover.Root key={side}>
    <Popover.Trigger>{side}</Popover.Trigger>
    <Popover.Portal>
      <Popover.Positioner side={side} sideOffset={8}>
        <Popover.Popup>
          <Popover.Arrow />
          <Popover.Title>{side}</Popover.Title>
          <Popover.Description>Anchored content.</Popover.Description>
          <Popover.Close>Close</Popover.Close>
        </Popover.Popup>
      </Popover.Positioner>
    </Popover.Portal>
  </Popover.Root>
))}` }]}
/>

<ComponentExampleTabs
  id="popover-collision-offset"
  title="制御付き lifecycle、collision、offset"
  description="live state は制御された open の変更を反映し、side と alignment の offset は inline edge 付近で collision-aware のままです。"
  previewLayout="stretch"
  preview={<Stories.PopoverControlledLifecycle />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`function ControlledPopover() {
  const [open, setOpen] = useState(false);
  return <>
    <Popover.Root onOpenChange={setOpen} open={open}>
      <Popover.Trigger>Rack details</Popover.Trigger>
      <Popover.Portal>
        <Popover.Positioner
          align="end"
          alignOffset={12}
          collisionAvoidance={{ align: 'flip', side: 'flip' }}
          side="right"
          sideOffset={16}
        >
          <Popover.Popup>
            <Popover.Arrow />
            <Popover.Title>Controlled edge popover</Popover.Title>
            <Popover.Description>Collision-aware content.</Popover.Description>
            <Popover.Close>Close</Popover.Close>
          </Popover.Popup>
        </Popover.Positioner>
      </Popover.Portal>
    </Popover.Root>
    <output aria-live="polite">Popover is {open ? 'open' : 'closed'}.</output>
  </>;
}` }]}
/>

## API

Base UI が anchor の配置、collision 回避、focus 動作、portal、dialog 関係、outside/Escape による解除を担当します。補助的な action では surface を non-modal に保ち、背後の page をブロックする必要がある task には Dialog を使います。