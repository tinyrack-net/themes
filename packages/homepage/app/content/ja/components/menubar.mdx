---
title: "Menubar"
description: "デスクトップ形式のキーボード操作でアプリケーションメニューを連携します。"
section: components
order: 36
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/menubar.demo.js';

## 主なプロパティ

| プロパティ | 選択肢 |
| --- | --- |
| 合成 | 1つの `TRMenubar` 内に `TRMenu.Root` を配置します。 |
| 方向 | `horizontal` は左右キー、`vertical` は上下キーを使います。 |
| 利用可否 | 1つの `TRMenu.Root` またはメニューバー全体を無効にします。 |
| 境界 | `loopFocus` は端で循環し、デフォルトは `true` です。 |

常時表示するアプリケーションコマンドに使います。サイトナビゲーションには使いません。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/menubar.css';"],
      codeImports: ["import { TRMenubar } from '@tinyrack/ui/components/menubar';","import { TRMenu } from '@tinyrack/ui/components/menu';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs id="menubar-basic" title="アプリケーションコマンド" preview={<div data-docs-example-item-count="1"><Stories.MenubarBasicPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: Stories.menubarBasicSource.ja }]} />

## 例

### 利用可否を決める

<ComponentExampleTabs id="menubar-states" title="有効、1 項目を無効化、全体を無効化" preview={<div data-docs-example-item-count="3"><Stories.MenubarStateComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: Stories.menubarApplicationSource.ja }]} />

### 操作方向を選ぶ

<ComponentExampleTabs id="menubar-configurations" title="横方向と縦方向" preview={<div data-docs-example-item-count="2"><Stories.MenubarConfigurationMatrix /></div>} sources={[{ label: 'React', language: 'tsx', code: Stories.menubarConfigurationsSource.ja }]} />

### 境界動作を選ぶ

<ComponentExampleTabs id="menubar-keyboard-loop" title="循環フォーカスと境界フォーカス" description="`orientation` は方向キーの軸を選び、`loopFocus` は最初と最後の間を循環するか決めます。" preview={<div data-docs-example-item-count="2"><Stories.MenubarKeyboardComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: Stories.menubarApplicationSource.ja }]} />

## API

`TRMenubar` は Base UI ルート prop を受け取ります。`orientation` は `'horizontal' | 'vertical'`、デフォルトは `'horizontal'` です。`loopFocus` は boolean でデフォルト `true`、`disabled` は boolean でデフォルト `false` です。アクセシブルな名前を付けてください。1つのロービング tab stop を保ち、無効な子を飛ばし、フォーカス移動時に開くメニューを切り替え、Escape 後にトリガーへフォーカスを戻します。

`@tinyrack/ui/components/menu` の `TRMenu.Root` とオーバーレイパーツを合成します。Base UI の `render`、ルート `ref`、ネイティブ prop、イベント、関数形式の `className` / `style`、状態 data 属性を保ちます。`@tinyrack/ui/components/menubar.css` は合成するメニューパーツのスタイルを含むため、menu CSS は別途読み込みません。

`TRMenubar`、`TRMenubarProps`、`TRMenubarState` を公開します。
