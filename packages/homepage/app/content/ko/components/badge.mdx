---
title: "Badge"
description: "의미 있는 색상과 크기 옵션을 제공하는 간결한 상태 레이블이에요."
section: components
order: 4
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { CircleCheck, Clock } from 'lucide-react';
import { TRBadge } from '@tinyrack/ui/components/badge';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/badge.demo.js';

## 핵심 속성

| 속성 | 값 | 기본값 |
| --- | --- | --- |
| `variant` | `neutral`, `info`, `success`, `warning`, `danger` | `neutral` |
| `uiSize` | `sm`, `md`, `lg` | `md` |

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/badge.css';"],
      codeImports: ["import { TRBadge } from '@tinyrack/ui/components/badge';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="badge-basic" title="상태 레이블" preview={<TRBadge data-docs-example-item="" variant="success">정상</TRBadge>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRBadge } from '@tinyrack/ui/components/badge';

<TRBadge variant="success">정상</TRBadge>` }]} />

## 예시

<ComponentExampleTabs
  id="badge-icon-and-long-label"
  title="아이콘과 너비가 제한된 긴 레이블"
  description="옆의 텍스트가 같은 상태를 전달하면 아이콘은 장식용이에요. 긴 레이블은 상위 요소 안에서 줄바꿈돼요."
  preview={(
    <div className="grid max-w-64 gap-3">
      <TRBadge data-docs-example-item="" variant="success"><CircleCheck aria-hidden size="1em" />정상</TRBadge>
      <TRBadge data-docs-example-item="" variant="warning"><Clock aria-hidden size="1em" />운영자의 유지 보수 승인 대기 중</TRBadge>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRBadge } from '@tinyrack/ui/components/badge';
import { CircleCheck, Clock } from 'lucide-react';

<div className="grid max-w-64 gap-3">
  <TRBadge variant="success">
    <CircleCheck aria-hidden size="1em" />
    정상
  </TRBadge>
  <TRBadge variant="warning">
    <Clock aria-hidden size="1em" />
    운영자의 유지 보수 승인 대기 중
  </TRBadge>
</div>` }]}
/>

<ComponentExampleTabs
  id="badge-size-variant-matrix"
  title="상태 variant"
  description="색상만 보지 말고 상태의 의미에 따라 variant를 선택하세요."
  previewLayout="stretch"
  preview={(
    <div className="flex flex-wrap gap-2">
      <TRBadge data-docs-example-item="" variant="neutral">일반</TRBadge><TRBadge data-docs-example-item="" variant="info">정보</TRBadge><TRBadge data-docs-example-item="" variant="success">성공</TRBadge><TRBadge data-docs-example-item="" variant="warning">경고</TRBadge><TRBadge data-docs-example-item="" variant="danger">위험</TRBadge>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRBadge } from '@tinyrack/ui/components/badge';

<div className="flex flex-wrap gap-2">
  <TRBadge variant="neutral">일반</TRBadge><TRBadge variant="info">정보</TRBadge><TRBadge variant="success">성공</TRBadge><TRBadge variant="warning">경고</TRBadge><TRBadge variant="danger">위험</TRBadge>
</div>` }]}
/>

<ComponentExampleTabs id="badge-sizes" title="크기" description="주변 텍스트와 컨트롤의 밀도에 맞추세요." preview={<div className="flex items-center gap-3"><TRBadge data-docs-example-item="" uiSize="sm">작게</TRBadge><TRBadge data-docs-example-item="" uiSize="md">중간</TRBadge><TRBadge data-docs-example-item="" uiSize="lg">크게</TRBadge></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRBadge } from '@tinyrack/ui/components/badge';

<div className="flex items-center gap-3"><TRBadge uiSize="sm">작게</TRBadge><TRBadge uiSize="md">중간</TRBadge><TRBadge uiSize="lg">크게</TRBadge></div>` }]} />

## API

### 옵션

| Prop | 타입 | 기본값 | 용도 |
| --- | --- | --- | --- |
| `variant` | `'neutral' \| 'info' \| 'success' \| 'warning' \| 'danger'` | `'neutral'` | 상태 의미와 색상 표현을 선택해요. |
| `uiSize` | `'sm' \| 'md' \| 'lg'` | `'md'` | Badge 밀도를 정해요. |

### HTML과 접근성

`TRBadge`는 `span`을 렌더링하고 네이티브 속성, 이벤트, 스타일, React 19 `ref`를 전달해요. `TRBadgeProps`, `TRBadgeUiSize`, `TRBadgeVariant` 타입도 내보내요. role이나 live-region 동작은 자동으로 지정하지 않아요. 상태 텍스트를 표시하고, 아이콘만 사용할 때는 애플리케이션에서 접근 가능한 레이블을 제공하세요.
