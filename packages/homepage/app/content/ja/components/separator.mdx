---
title: "Separator"
description: "水平セクションと垂直コントロールグループのための意味のある区切りです。"
section: components
order: 12
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRSeparator } from '@tinyrack/ui/components/separator';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/separator.demo.js';

## コントラクト

`orientation` は `horizontal` または `vertical` を受け取り、既定値は `horizontal` です。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui tailwindcss react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/separator.css';", "import '@tinyrack/ui/components/button.css';", "import { TRSeparator } from '@tinyrack/ui/components/separator';", "import { TRButton } from '@tinyrack/ui/components/button';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs id="separator-basic" title="section separator" previewLayout="stretch" preview={<div className="grid w-full gap-3"><span>Overview</span><TRSeparator /><span>ネットワーク</span></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid w-full gap-3">
  <span>Overview</span>
  <TRSeparator />
  <span>Network</span>
</div>` }]} />

## 例

<ComponentExampleTabs
  id="separator-orientations"
  title="向き"
  previewLayout="stretch"
  preview={(
    <div className="grid gap-6">
      <div className="grid gap-2"><strong>Horizontal</strong><TRSeparator /></div>
      <div className="grid gap-2"><strong>Vertical</strong><div className="flex h-12 items-center gap-4"><span>CPU</span><TRSeparator orientation="vertical" /><span>Memory</span><TRSeparator orientation="vertical" /><span>ストレージ</span></div></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid gap-6">
  <div className="grid gap-2"><strong>Horizontal</strong><TRSeparator /></div>
  <div className="grid gap-2">
    <strong>Vertical</strong>
    <div className="flex h-12 items-center gap-4">
      <span>CPU</span><TRSeparator orientation="vertical" />
      <span>Memory</span><TRSeparator orientation="vertical" />
      <span>Storage</span>
    </div>
  </div>
</div>` }]}
/>

<ComponentExampleTabs
  id="separator-semantics"
  title="semantic または装飾用"
  description="意味のある境界には role=separator を使います。周囲の layout が group をすでに示す場合は presentation を使います。aria-orientation は separator role に対してのみ出力されます。"
  preview={(
    <div className="grid gap-4">
      <div><span>Semantic boundary</span><TRSeparator role="separator" /></div>
      <div><span>Decorative rule</span><TRSeparator role="presentation" /></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid gap-4">
  <div><span>Semantic boundary</span><TRSeparator role="separator" /></div>
  <div><span>Decorative rule</span><TRSeparator role="presentation" /></div>
</div>` }]}
/>

## API

Vertical separators default to `role="separator"`. If `role="presentation"` is supplied, `aria-orientation` is omitted to avoid invalid decorative ARIA.