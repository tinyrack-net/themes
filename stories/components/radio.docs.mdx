import { Meta } from '@storybook/addon-docs/blocks';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import { baseUiExampleSources } from '../shared/base-ui-example-sources.js';
import * as Stories from './radio.stories';

<Meta of={Stories} />

# Radio

A single option within a mutually exclusive radio group.

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Storybook controls cover `label`, `selected`, `disabled`.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/radio.css';", "import '@tinyrack/ui/components/radio-group.css';", "import { Radio } from '@tinyrack/ui/components/radio';", "import { RadioGroup } from '@tinyrack/ui/components/radio-group';"] }]} />

## Usage

<ComponentExampleTabs
  id="radio-basic"
  title="Default"
  preview={<Stories.RadioPreview label={'Primary rack'} selected={true} disabled={false} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['radio'] }]}
/>

## Examples

<ComponentExampleTabs
  id="radio-states"
  title="State comparison"
  description="Use Controls above to inspect every supported public state."
  preview={<Stories.RadioPreview label={'Primary rack'} selected={false} disabled={true} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['radio'] }]}
/>

## API

Every compound part and prop/state type is exported from `@tinyrack/ui/components/radio`; styles remain opt-in through the matching CSS subpath.
