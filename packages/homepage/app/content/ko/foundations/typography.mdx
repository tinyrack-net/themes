---
title: "타이포그래피"
description: "이름이 있는 글자 역할로 위계, 읽기 좋은 줄 길이, 현지화에 유연한 레이아웃을 만들어요."
section: foundations
order: 2
---
import { TRCode } from '@tinyrack/ui/components/code';
import { TRTable } from '@tinyrack/ui/components/table';
import { tinyrackTypography } from '@tinyrack/ui/core';

## 위계 선택

읽는 순서를 먼저 정한 뒤 그 순서를 표현하는 가장 작은 역할을 선택하세요. `display`는 드문 페이지 수준 문장에, 제목 역할은 중첩 섹션에, `body`와 `bodySm`은 본문에, `caption`은 보조 정보에, `label`은 짧은 식별자에 사용해요.

## 글자 역할

<div className="grid gap-3" role="list">{Object.entries(tinyrackTypography.textStyle).map(([name, recipe]) => (<div className="grid gap-1 border-b border-tinyrack-border pb-3" key={name} role="listitem"><span style={{ fontSize: tinyrackTypography.fontSize[recipe.fontSize], fontWeight: tinyrackTypography.fontWeight[recipe.fontWeight], letterSpacing: tinyrackTypography.letterSpacing[recipe.letterSpacing], lineHeight: tinyrackTypography.lineHeight[recipe.lineHeight] }}>{name === 'label' ? '랙 상태' : name === 'code' ? 'rack.deploy()' : '클러스터 상태와 용량'}</span><TRCode>{name}</TRCode></div>))}</div>

## 줄 길이와 현지화

문단은 훑어보기 좋은 너비로 제한하고 레이블은 늘어날 수 있게 만드세요. 한국어와 일본어는 영어와 다른 위치에서 줄바꿈될 수 있어요. 영어 문자열을 기준으로 너비를 고정하거나 설명 문장을 한 줄로 강제하지 마세요. 대문자 자간은 짧은 라틴 레이블에만 사용하고 한국어 본문에는 적용하지 마세요.

## 적용 예시

<section className="grid max-w-tinyrack-measure-lg gap-tinyrack-sm border border-tinyrack-border p-tinyrack-lg"><div className="text-tinyrack-xs font-tinyrack-bold text-tinyrack-text-muted">랙 A</div><h3 className="m-0 text-tinyrack-2xl font-tinyrack-heading leading-tinyrack-sm">클러스터 상태</h3><p className="m-0 text-tinyrack-md leading-tinyrack-md">모든 노드가 응답하고 있어요. 다음 백업은 {'03:18'}에 시작해요.</p><span className="text-tinyrack-xs text-tinyrack-text-muted">2분 전에 업데이트</span></section>

## 폰트 로딩 책임

Tinyrack 코어는 `body`, `heading`, `mono` 토큰에 `"IBM Plex Sans"`라는 글꼴 모음 이름만 선언해요. 폰트 파일을 제공하거나 불러오지 않아요. 사용하는 앱에서 필요한 굵기와 언어별 하위 집합을 불러와야 해요. 이 문서 앱은 IBM Plex Sans와 한국어·일본어 글꼴 모음을 불러와요.

## 구현과 참고

```css
@import "@tinyrack/ui/core.css";
.rack-title { font: var(--tinyrack-weight-heading) var(--tinyrack-text-2xl) / var(--tinyrack-leading-sm) var(--tinyrack-font-heading); }
```

<TRTable.Root containerProps={{ 'aria-label': '타이포그래피 토큰 참고', tabIndex: 0 }} data-foundation-reference="typography"><thead><tr><th scope="col">역할</th><th scope="col">크기</th><th scope="col">줄 높이</th><th scope="col">굵기</th><th scope="col">자간</th></tr></thead><tbody>{Object.entries(tinyrackTypography.textStyle).map(([name, recipe]) => (<tr key={name}><th scope="row">{name}</th><td><TRCode>{tinyrackTypography.fontSize[recipe.fontSize]}</TRCode></td><td><TRCode>{tinyrackTypography.lineHeight[recipe.lineHeight]}</TRCode></td><td><TRCode>{tinyrackTypography.fontWeight[recipe.fontWeight]}</TRCode></td><td><TRCode>{tinyrackTypography.letterSpacing[recipe.letterSpacing]}</TRCode></td></tr>))}</tbody></TRTable.Root>

[Tailwind 타이포그래피 매핑](/ko/foundations/tailwind/#typography)도 확인하세요.
