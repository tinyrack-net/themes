---
title: "Alert"
description: "意味のあるタイトルと説明、任意のアクションを持つ永続的なステータスメッセージです。"
section: components
order: 1
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { Alert } from '@tinyrack/ui/components/alert';
import { Button } from '@tinyrack/ui/components/button';
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/alert.demo.js';

## コントラクト

| 軸 | 値 | 既定値 |
| --- | --- | --- |
| `variant` | `neutral`, `info`, `success`, `warning`, `danger` | `neutral` |
| parts | `Root`, `Title`, `Description`, `Actions` | compound API |

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/alert.css';", "import { Alert } from '@tinyrack/ui/components/alert';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="alert-basic"
  title="状態メッセージ"
  previewLayout="stretch"
  preview={<Alert.Root role="status" variant="info"><Alert.Title>Deployment queued</Alert.Title><Alert.Description>The rollout will start shortly.</Alert.Description></Alert.Root>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { Alert } from '@tinyrack/ui/components/alert';

<Alert.Root role="status" variant="info">
  <Alert.Title>Deployment queued</Alert.Title>
  <Alert.Description>The rollout will start shortly.</Alert.Description>
</Alert.Root>` }]}
/>

## 例

<ComponentExampleTabs
  id="alert-variants"
  title="状態 variant"
  description="すべての semantic status color を 1 回の縦方向の確認で見られます。"
  previewLayout="stretch"
  preview={(
    <div className="grid gap-3">
      <Alert.Root variant="neutral"><Alert.Title>Neutral</Alert.Title><Alert.Description>No status has been assigned.</Alert.Description></Alert.Root>
      <Alert.Root variant="info"><Alert.Title>Info</Alert.Title><Alert.Description>Maintenance starts at 02:00.</Alert.Description></Alert.Root>
      <Alert.Root variant="success"><Alert.Title>Success</Alert.Title><Alert.Description>All nodes are healthy.</Alert.Description></Alert.Root>
      <Alert.Root variant="warning"><Alert.Title>Warning</Alert.Title><Alert.Description>ストレージ is nearing capacity.</Alert.Description></Alert.Root>
      <Alert.Root variant="danger"><Alert.Title>Danger</Alert.Title><Alert.Description>Deployment failed.</Alert.Description><Alert.Actions><Button appearance="outline" uiSize="sm" variant="danger">Retry</Button></Alert.Actions></Alert.Root>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid gap-3">
  <Alert.Root variant="neutral"><Alert.Title>Neutral</Alert.Title><Alert.Description>No status has been assigned.</Alert.Description></Alert.Root>
  <Alert.Root variant="info"><Alert.Title>Info</Alert.Title><Alert.Description>Maintenance starts at 02:00.</Alert.Description></Alert.Root>
  <Alert.Root variant="success"><Alert.Title>Success</Alert.Title><Alert.Description>All nodes are healthy.</Alert.Description></Alert.Root>
  <Alert.Root variant="warning"><Alert.Title>Warning</Alert.Title><Alert.Description>Storage is nearing capacity.</Alert.Description></Alert.Root>
  <Alert.Root variant="danger">
    <Alert.Title>Danger</Alert.Title><Alert.Description>Deployment failed.</Alert.Description>
    <Alert.Actions><Button appearance="outline" uiSize="sm" variant="danger">Retry</Button></Alert.Actions>
  </Alert.Root>
</div>` }]}
/>

<ComponentExampleTabs
  id="alert-role-recipe"
  title="通知 role のレシピ"
  description="穏やかな更新には status、新しく緊急な失敗だけに alert を使い、page load 時から存在する content には live-region role を付けません。"
  previewLayout="stretch"
  preview={(
    <div className="grid gap-3">
      <Alert.Root role="status" variant="success"><Alert.Title>Backup complete</Alert.Title><Alert.Description>The new recovery point is available.</Alert.Description></Alert.Root>
      <Alert.Root role="alert" variant="danger"><Alert.Title>Connection lost</Alert.Title><Alert.Description>Live metrics are no longer updating.</Alert.Description></Alert.Root>
      <Alert.Root variant="neutral"><Alert.Title>Planned maintenance</Alert.Title><Alert.Description>This notice was present when the page loaded.</Alert.Description></Alert.Root>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<Alert.Root role="status" variant="success">
  <Alert.Title>Backup complete</Alert.Title>
  <Alert.Description>The new recovery point is available.</Alert.Description>
</Alert.Root>
<Alert.Root role="alert" variant="danger">
  <Alert.Title>Connection lost</Alert.Title>
  <Alert.Description>Live metrics are no longer updating.</Alert.Description>
</Alert.Root>
<Alert.Root variant="neutral">
  <Alert.Title>Planned maintenance</Alert.Title>
  <Alert.Description>This notice was present when the page loaded.</Alert.Description>
</Alert.Root>` }]}
/>

<ComponentExampleTabs
  id="alert-actions"
  title="context に応じた heading と折り返し action"
  description="page の context から heading level を選びます。action は page overflow を起こさず alert 内で折り返されます。"
  previewClassName="!min-h-0 !p-4"
  previewLayout="stretch"
  preview={(
    <Alert.Root variant="warning">
      <Alert.Title render={<h3 />}>Maintenance approval required</Alert.Title>
      <Alert.Description>要求された window は active deployment と重なるため、明示的な判断が必要です。</Alert.Description>
      <Alert.Actions>
        <Button uiSize="sm" variant="primary">Approve window</Button>
        <Button appearance="outline" uiSize="sm">Review deployment</Button>
        <Button appearance="ghost" uiSize="sm">Dismiss</Button>
      </Alert.Actions>
    </Alert.Root>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<Alert.Root variant="warning">
  <Alert.Title render={<h3 />}>Maintenance approval required</Alert.Title>
  <Alert.Description>
    The requested window overlaps an active deployment and needs an explicit decision.
  </Alert.Description>
  <Alert.Actions>
    <Button uiSize="sm" variant="primary">Approve window</Button>
    <Button appearance="outline" uiSize="sm">Review deployment</Button>
    <Button appearance="ghost" uiSize="sm">Dismiss</Button>
  </Alert.Actions>
</Alert.Root>` }]}
/>

## API

`Alert.Root` はネイティブ `div` props と React 19 `ref` を転送します。通知の緊急度はアプリケーションの文脈で決まるため、live-region role は自動付与しません。既定の `div` や `p` が周囲の意味に合わない場合、`Alert.Title` と `Alert.Description` は Base UI の `render` contract に対応します。