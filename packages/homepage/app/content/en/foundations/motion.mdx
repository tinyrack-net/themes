---
title: "Motion"
description: "Motion should make a state change easier to follow, then get out of the way."
section: foundations
order: 8
---
import { TRButton } from '@tinyrack/ui/components/button';
import { TRDialog } from '@tinyrack/ui/components/dialog';
import { TRPopover } from '@tinyrack/ui/components/popover';
import { TRProgress } from '@tinyrack/ui/components/progress';
import { TRSkeleton } from '@tinyrack/ui/components/skeleton';
import { TRCode } from '@tinyrack/ui/components/code';
import { TRTable } from '@tinyrack/ui/components/table';
import { tinyrackMotion } from '@tinyrack/ui/core';
import {
  FastFeedbackExample,
  MotionDurationComparison,
  MotionEasingComparison,
  ReducedMotionPreview,
} from '../../foundations/motion-demo.js';

## Principle

Choose motion by the kind of change, not by preference or decoration.

<div className="grid gap-tinyrack-md md:grid-cols-3" role="list">
  <section className="grid content-start gap-tinyrack-sm border border-tinyrack-border p-tinyrack-lg" role="listitem">
    <div className="flex items-baseline justify-between gap-2"><strong>Direct feedback</strong><TRCode>fast · 120ms</TRCode></div>
    <p className="m-0 text-tinyrack-sm text-tinyrack-text-muted">Keep feedback attached to a press, toggle, or hover nearly instant.</p>
  </section>
  <section className="grid content-start gap-tinyrack-sm border border-tinyrack-border p-tinyrack-lg" role="listitem">
    <div className="flex items-baseline justify-between gap-2"><strong>Nearby context</strong><TRCode>normal · 160ms</TRCode></div>
    <p className="m-0 text-tinyrack-sm text-tinyrack-text-muted">Use a little more time when a menu, tooltip, or anchored layer enters nearby.</p>
  </section>
  <section className="grid content-start gap-tinyrack-sm border border-tinyrack-border p-tinyrack-lg" role="listitem">
    <div className="flex items-baseline justify-between gap-2"><strong>Task context</strong><TRCode>slow · 180ms</TRCode></div>
    <p className="m-0 text-tinyrack-sm text-tinyrack-text-muted">Reserve the slow step for dialogs and changes that temporarily reshape the task.</p>
  </section>
</div>

Loading is different: it repeats while work is active, so it uses a cycle rather than an entrance duration.

## Visual scale

<MotionDurationComparison />

### Loading is a cycle

<section className="grid gap-tinyrack-lg border border-tinyrack-border p-tinyrack-lg md:grid-cols-[minmax(0,1fr)_minmax(16rem,0.8fr)] md:items-center">
  <div className="grid gap-1">
    <div className="flex items-baseline gap-2"><strong>loading</strong><TRCode>{tinyrackMotion.duration.loading}</TRCode></div>
    <p className="m-0 text-tinyrack-sm text-tinyrack-text-muted">Use a linear loop only while progress is unknown. Keep the operation labelled and stop the loop when work completes.</p>
  </div>
  <div aria-busy="true" aria-label="Synchronizing rack data" className="grid gap-tinyrack-sm">
    <TRProgress.Root aria-label="Synchronizing rack data" value={null}>
      <TRProgress.Track><TRProgress.Indicator /></TRProgress.Track>
    </TRProgress.Root>
    <TRSkeleton className="w-full" shape="rectangle" />
  </div>
</section>

### Easing changes the rhythm

<MotionEasingComparison />

## Applied pattern

These examples use the real component contracts. Open them and compare how much context each action changes.

<div className="grid gap-tinyrack-md lg:grid-cols-3">
  <section className="grid content-start gap-tinyrack-md border border-tinyrack-border p-tinyrack-lg">
    <div className="grid gap-1">
      <div className="flex items-baseline justify-between gap-2"><strong>Direct feedback</strong><TRCode>fast</TRCode></div>
      <span className="text-tinyrack-sm text-tinyrack-text-muted">A local state updates without moving the task.</span>
    </div>
    <FastFeedbackExample />
  </section>

  <section className="grid content-start gap-tinyrack-md border border-tinyrack-border p-tinyrack-lg">
    <div className="grid gap-1">
      <div className="flex items-baseline justify-between gap-2"><strong>Anchored layer</strong><TRCode>normal</TRCode></div>
      <span className="text-tinyrack-sm text-tinyrack-text-muted">Nearby actions appear from their trigger.</span>
    </div>
    <TRPopover.Root>
      <TRPopover.Trigger render={<TRButton />}>Open rack actions</TRPopover.Trigger>
      <TRPopover.Portal>
        <TRPopover.Positioner align="start" side="bottom">
          <TRPopover.Popup>
            <p className="m-0">Rack actions</p>
            <TRPopover.Close>Close actions</TRPopover.Close>
          </TRPopover.Popup>
        </TRPopover.Positioner>
      </TRPopover.Portal>
    </TRPopover.Root>
  </section>

  <section className="grid content-start gap-tinyrack-md border border-tinyrack-border p-tinyrack-lg">
    <div className="grid gap-1">
      <div className="flex items-baseline justify-between gap-2"><strong>Task context</strong><TRCode>slow</TRCode></div>
      <span className="text-tinyrack-sm text-tinyrack-text-muted">A dialog briefly becomes the primary task.</span>
    </div>
    <TRDialog.Root>
      <TRDialog.Trigger render={<TRButton />}>Open deployment review</TRDialog.Trigger>
      <TRDialog.Portal>
        <TRDialog.Backdrop />
        <TRDialog.Popup>
          <TRDialog.Title>Review deployment</TRDialog.Title>
          <div className="tr-dialog-body">Confirm the rack target before continuing.</div>
          <TRDialog.Close>Close review</TRDialog.Close>
        </TRDialog.Popup>
      </TRDialog.Portal>
    </TRDialog.Root>
  </section>
</div>

### Reduced motion

Reduced motion removes decorative travel, not the state change or its meaning.

<ReducedMotionPreview />

## Implementation

Components consume semantic duration and easing variables. Keep one transition at the component boundary, and let reduced-motion preferences remove decorative travel.

```css
.rack-layer {
  transition:
    opacity var(--tinyrack-duration-normal) var(--tinyrack-ease-out),
    transform var(--tinyrack-duration-normal) var(--tinyrack-ease-out);
}

@media (prefers-reduced-motion: reduce) {
  .rack-layer {
    transition-duration: 1ms;
    transform: none;
  }
}
```

Use `--tinyrack-duration-*` for system-wide timing and component-level `--tr-*` variables only for a deliberate local exception.

## Reference

### Durations

<TRTable.Root containerProps={{ 'aria-label': 'Motion duration reference', tabIndex: 0 }} data-foundation-reference="motion-duration">
    <thead><tr><th scope="col">Token</th><th scope="col">Value</th><th scope="col">Use</th></tr></thead>
    <tbody>
      <tr><th scope="row"><TRCode>fast</TRCode></th><td><TRCode>{tinyrackMotion.duration.fast}</TRCode></td><td>Press, toggle, and hover feedback</td></tr>
      <tr><th scope="row"><TRCode>normal</TRCode></th><td><TRCode>{tinyrackMotion.duration.normal}</TRCode></td><td>Menus, tooltips, and anchored layers</td></tr>
      <tr><th scope="row"><TRCode>slow</TRCode></th><td><TRCode>{tinyrackMotion.duration.slow}</TRCode></td><td>Dialogs and task-context changes</td></tr>
      <tr><th scope="row"><TRCode>loading</TRCode></th><td><TRCode>{tinyrackMotion.duration.loading}</TRCode></td><td>Repeating progress and skeleton feedback</td></tr>
    </tbody>
  </TRTable.Root>

### Easing

<TRTable.Root containerProps={{ 'aria-label': 'Motion easing reference', tabIndex: 0 }} data-foundation-reference="motion-easing">
    <thead><tr><th scope="col">Token</th><th scope="col">Value</th><th scope="col">Use</th></tr></thead>
    <tbody>
      <tr><th scope="row"><TRCode>standard</TRCode></th><td><TRCode>{tinyrackMotion.easing.standard}</TRCode></td><td>Two-way state changes</td></tr>
      <tr><th scope="row"><TRCode>easeOut</TRCode></th><td><TRCode>{tinyrackMotion.easing.easeOut}</TRCode></td><td>Content entering the interface</td></tr>
      <tr><th scope="row"><TRCode>linear</TRCode></th><td><TRCode>{tinyrackMotion.easing.linear}</TRCode></td><td>Repeating progress only</td></tr>
    </tbody>
  </TRTable.Root>
