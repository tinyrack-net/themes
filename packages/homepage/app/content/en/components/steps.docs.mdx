---
title: "Steps"
description: "Numbered semantic procedures for authored documentation."
section: components
order: 59
---
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/steps.demo.js';

## Contract

| Part | Renders | Best for |
| --- | --- | --- |
| `Root` | semantic ordered list | the complete procedure |
| `Item` | semantic list item with a numbered marker | one task, including rich content |

`TRSteps` is intentionally presentational. It does not manage progress or selection, so authored headings, paragraphs, links, code blocks, and other components remain untouched inside each item.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui tailwindcss react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/steps.css';", "import { TRSteps } from '@tinyrack/ui/components/steps';"] }]} />

## Usage

<ComponentExampleTabs
  id="steps-basic"
  title="Installation procedure"
  description="Use one item per task. The marker and connecting line are supplied by TRSteps while the content stays ordinary React markup."
  previewLayout="stretch"
  preview={<Stories.StepsPreview count={3} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSteps } from '@tinyrack/ui/components/steps';

<TRSteps.Root>
  <TRSteps.Item>Install the package</TRSteps.Item>
  <TRSteps.Item>Create the config</TRSteps.Item>
  <TRSteps.Item>Build the site</TRSteps.Item>
</TRSteps.Root>` }]}
/>

## Examples

<ComponentExampleTabs
  id="steps-rich-content"
  title="A complete setup guide"
  description="Complex guides can combine headings, prose, package-manager tabs, and syntax-highlighted code inside the same numbered list."
  previewLayout="stretch"
  preview={<Stories.StepsGuidePreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRCodeBlock } from '@tinyrack/ui/components/code-block';
import { TRSteps } from '@tinyrack/ui/components/steps';
import { TRTabs } from '@tinyrack/ui/components/tabs';

<TRSteps.Root>
  <TRSteps.Item>
    <h3>Create a project</h3>
    <p>Start with a React app and add the Tinyrack packages.</p>
    <TRTabs.Root defaultValue="pnpm">
      <TRTabs.List aria-label="Package manager">
        <TRTabs.Tab value="pnpm">pnpm</TRTabs.Tab>
        <TRTabs.Tab value="npm">npm</TRTabs.Tab>
      </TRTabs.List>
      <TRTabs.Panel value="pnpm">
        <TRCodeBlock code="pnpm create vite my-app --template react-ts" language="shellscript" />
      </TRTabs.Panel>
      <TRTabs.Panel value="npm">
        <TRCodeBlock code="npm create vite@latest my-app -- --template react-ts" language="shellscript" />
      </TRTabs.Panel>
    </TRTabs.Root>
  </TRSteps.Item>
  <TRSteps.Item>
    <h3>Install and configure</h3>
    <p>Import the shared and component styles after installing the package.</p>
    <TRCodeBlock code="pnpm add @tinyrack/ui tailwindcss react react-dom" language="shellscript" />
  </TRSteps.Item>
  <TRSteps.Item>
    <h3>Write the guide</h3>
    <p>Continue with links, callouts, and other documentation components.</p>
  </TRSteps.Item>
</TRSteps.Root>` }]}
/>

Use `TRSteps` for authored procedures, not interactive progress indicators. If the user must move between states or complete a form, compose a dedicated progress or navigation component instead.

## API

Both parts forward native ordered-list props, list-item props, and React 19 refs.
