---
title: "Input"
description: "A tokenized native input that preserves labels, refs, validity, and form submission."
section: components
order: 34
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/input.demo.js';

## Contract

`TRInput` renders a native `<input>` and preserves its props, ref, events, validity, and form behavior. Labels are separate native elements: connect a visible `<label>` with `htmlFor` and `id`, then use `aria-describedby` for instructions or errors.

Use `uiSize="sm"` for dense controls, the default `md` for forms, and `lg` where a larger target is useful. `disabled`, `readOnly`, `required`, and `aria-invalid` retain their native semantics and Tinyrack state styles.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/input.css';", "import { TRInput } from '@tinyrack/ui/components/input';", "import { TRButton } from '@tinyrack/ui/components/button';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs
  id="input-basic"
  title="Labeled input"
  preview={<Stories.InputPreview defaultValue="" disabled={false} label="Rack name" placeholder="rack-alpha" readOnly={false} required={false} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/input.css';
import { TRInput } from '@tinyrack/ui/components/input';

export function RackNameInput() {
  return (
    <label htmlFor="rack-name">
      Rack name
      <TRInput id="rack-name" name="rack" placeholder="rack-alpha" />
    </label>
  );
}` }]}
/>

## Examples

<ComponentExampleTabs
  id="input-sizes"
  title="Sizes"
  preview={<Stories.InputSizeComparison />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/input.css';
import { TRInput } from '@tinyrack/ui/components/input';

export function InputSizes() {
  return (
    <div className="grid gap-4">
      <label>SM input <TRInput placeholder="rack-alpha" uiSize="sm" /></label>
      <label>MD input <TRInput placeholder="rack-alpha" /></label>
      <label>LG input <TRInput placeholder="rack-alpha" uiSize="lg" /></label>
    </div>
  );
}` }]}
/>

<ComponentExampleTabs
  id="input-states"
  title="Input states"
  preview={<Stories.InputStateComparison />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/input.css';
import { TRInput } from '@tinyrack/ui/components/input';

export function InputStates() {
  return (
    <div className="grid gap-4 sm:grid-cols-2">
      <label>Default <TRInput defaultValue="rack-alpha" /></label>
      <label>Disabled <TRInput defaultValue="rack-disabled" disabled /></label>
      <label>Read only <TRInput defaultValue="rack-locked" readOnly /></label>
      <label>
        Invalid
        <TRInput aria-invalid="true" defaultValue="rack duplicate" required />
      </label>
    </div>
  );
}` }]}
/>

<ComponentExampleTabs
  id="input-validation"
  title="Required submission and recovery"
  preview={<Stories.InputValidationPreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/button.css';
import '@tinyrack/ui/components/form.css';
import '@tinyrack/ui/components/input.css';
import { TRButton } from '@tinyrack/ui/components/button';
import { TRForm } from '@tinyrack/ui/components/form';
import { TRInput } from '@tinyrack/ui/components/input';
import { useId, useRef, useState } from 'react';

export function RequiredInput() {
  const inputId = useId();
  const errorId = useId();
  const inputRef = useRef<HTMLInputElement>(null);
  const [attempted, setAttempted] = useState(false);
  const [submitted, setSubmitted] = useState(false);
  const [value, setValue] = useState('');
  const invalid = attempted && value.trim().length === 0;

  return (
    <TRForm
      noValidate
      onSubmit={(event) => {
        event.preventDefault();
        const valid = event.currentTarget.checkValidity();
        setAttempted(true);
        setSubmitted(valid);
        if (!valid) inputRef.current?.focus();
      }}
    >
      <label htmlFor={inputId}>
        Rack name
        <TRInput
          aria-describedby={invalid ? errorId : undefined}
          aria-invalid={invalid || undefined}
          id={inputId}
          name="rack"
          onValueChange={(nextValue) => {
            setValue(nextValue);
            setSubmitted(false);
          }}
          onInvalid={(event) => event.preventDefault()}
          ref={inputRef}
          required
          value={value}
        />
      </label>
      {invalid ? <p id={errorId} role="alert">Rack name is required.</p> : null}
      <TRButton type="submit">Continue</TRButton>
      <output aria-live="polite">
        {submitted ? 'Ready to create ' + value + '.' : ''}
      </output>
    </TRForm>
  );
}` }]}
/>

## API

`TRInputProps` extends Base UI Input props and adds `uiSize?: 'sm' | 'md' | 'lg'` with a default of `md`. It forwards native input attributes such as `name`, `type`, `autoComplete`, `disabled`, `readOnly`, and `required`, plus `ref`, `className`, `style`, and event handlers.

Use `value` with `onValueChange(value, details)` for controlled text, or `defaultValue` for native uncontrolled and form-reset behavior. Use native `onChange` when the event object matters. `aria-invalid="true"`, Base UI `data-invalid`, and native `:invalid` all activate the invalid border. The `render` prop replaces the rendered element while Base UI merges behavior, state, ref, and `.tr-input` styling onto it.
