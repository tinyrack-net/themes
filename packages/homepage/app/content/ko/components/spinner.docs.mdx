---
title: "Spinner"
description: "크기와 의미 있는 색상 축을 갖고 알림되는 비결정적 진행률입니다."
section: components
order: 21
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRButton } from '@tinyrack/ui/components/button';
import { TRSpinner } from '@tinyrack/ui/components/spinner';
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/spinner.demo.js';

## 계약

| 축 | 값 | 기본값 |
| --- | --- | --- |
| `size` | `sm`, `md`, `lg` | `md` |
| `variant` | `current`, `muted`, `primary`, `danger` | `current` |
| `label` | accessible status text | `Loading` |
| `decorative` | `true`, `false` | `false` |

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/spinner.css';", "import { TRSpinner } from '@tinyrack/ui/components/spinner';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="spinner-basic" title="loading 상태" preview={<TRSpinner label="Loading servers" variant="primary" />} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRSpinner label="Loading servers" variant="primary" />` }]} />

## 예시

<ComponentExampleTabs
  id="spinner-size-variant-matrix"
  title="크기 × variant"
  description="지원되는 12가지 조합을 크기별로 묶었습니다."
  preview={(
    <div className="grid gap-5">
      {(['sm', 'md', 'lg']).map((size) => (
        <div className="grid gap-3" key={size}>
          <strong>{{ sm: 'Small', md: 'Medium', lg: 'Large' }[size]}</strong>
          <div className="flex flex-wrap items-end gap-4">
            {(['current', 'muted', 'primary', 'danger']).map((variant) => (
              <span className="grid justify-items-center gap-1 text-tinyrack-xs" key={variant}>
                <TRSpinner decorative uiSize={size} variant={variant} />
                <span>{variant}</span>
              </span>
            ))}
          </div>
        </div>
      ))}
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`const sizes = ['sm', 'md', 'lg'];
const variants = ['current', 'muted', 'primary', 'danger'];

<div className="grid gap-5">
  {sizes.map((size) => (
    <div className="grid gap-3" key={size}>
      <strong>{{ sm: 'Small', md: 'Medium', lg: 'Large' }[size]}</strong>
      <div className="flex flex-wrap items-end gap-4">
        {variants.map((variant) => (
          <span className="grid justify-items-center gap-1" key={variant}>
            <TRSpinner decorative uiSize={size} variant={variant} />
            <span>{variant}</span>
          </span>
        ))}
      </div>
    </div>
  ))}
</div>` }]}
/>

<ComponentExampleTabs
  id="spinner-status-ownership"
  title="독립 status 또는 control 장식"
  description="독립 TRSpinner가 status를 담당합니다. 이름이 있는 TRButton은 loading 상태를 담당하고 중첩된 spinner를 숨깁니다. reduced motion에서는 회전이 멈춥니다."
  preview={(
    <div className="flex flex-wrap items-center gap-4">
      <TRSpinner label="Loading server inventory" variant="primary" />
      <TRButton aria-busy="true" disabled><TRSpinner decorative uiSize="sm" />Refreshing inventory</TRButton>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="flex flex-wrap items-center gap-4">
  <TRSpinner label="Loading server inventory" variant="primary" />
  <TRButton aria-busy="true" disabled><TRSpinner decorative uiSize="sm" />Refreshing inventory</TRButton>
</div>` }]}
/>

<ComponentExampleTabs
  id="spinner-task-lifecycle"
  title="작업 lifecycle"
  description="task를 start, finish, reset하며 작업 중일 때만 공지된 status가 존재하는지 확인하세요."
  preview={<Stories.SpinnerLifecyclePreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`function DeploymentStatus() {
  const [state, setState] = useState('idle');
  return (
    <div>
      <TRButton onClick={() => setState('running')}>Start task</TRButton>
      <TRButton disabled={state !== 'running'} onClick={() => setState('complete')}>
        Finish task
      </TRButton>
      <output aria-live="polite">
        {state === 'running' ? <TRSpinner label="Deploying rack" variant="primary" /> : null}
        {state === 'complete' ? 'Deployment complete.' : null}
      </output>
    </div>
  );
}` }]}
/>

## API

TRSpinner는 기본적으로 `role="status"`를 노출하므로 task별 `label`을 사용하세요. 이미 이름이 있는 loading control이나 status region 안에서는 `decorative`를 설정해 하나의 element만 알리게 하세요.

| CSS token | 기본값 | 용도 |
| --- | --- | --- |
| `--tr-spinner-size` | size recipe | Indicator diameter |
| `--tr-spinner-stroke-width` | `--tinyrack-spinner-stroke-width` | Ring thickness |
| `--tr-spinner-color` | semantic variant color | Active and track color |
| `--tinyrack-spinner-size-sm` | foundation metric | Small diameter |
| `--tinyrack-spinner-size-md` | foundation metric | Medium diameter |
| `--tinyrack-spinner-size-lg` | foundation metric | Large diameter |
| `--tinyrack-spinner-track-opacity` | foundation metric | Inactive ring opacity |