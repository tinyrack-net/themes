---
title: "Dialog"
description: "다섯 가지 배치를 지원하는 focus-trap 대화상자 표면이에요."
section: components
order: 16
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRDialog } from '@tinyrack/ui/components/dialog';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/dialog.demo.js';

## 계약

| 축 | 값 | 기본값 |
| --- | --- | --- |
| `placement` on `Popup` | `middle`, `top`, `bottom`, `start`, `end` | `middle` |
| `modal` on `Root` | focus trap and scroll lock when true | `true` |
| state | controlled `open` or uncontrolled `defaultOpen` | closed |

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/dialog.css';", "import { TRDialog } from '@tinyrack/ui/components/dialog';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="dialog-basic" title="확인 dialog" preview={<div data-docs-example-item=""><TRDialog.Root><TRDialog.Trigger>dialog를 여세요</TRDialog.Trigger><TRDialog.Portal><TRDialog.Backdrop /><TRDialog.Popup placement="middle"><TRDialog.Title>변경 사항을 배포하세요</TRDialog.Title><TRDialog.Description>서비스가 다시 시작돼요.</TRDialog.Description><TRDialog.Close>취소하세요</TRDialog.Close></TRDialog.Popup></TRDialog.Portal></TRDialog.Root></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/dialog.css';
import { TRDialog } from '@tinyrack/ui/components/dialog';

<TRDialog.Root>
  <TRDialog.Trigger>dialog를 여세요</TRDialog.Trigger>
  <TRDialog.Portal>
    <TRDialog.Backdrop />
    <TRDialog.Popup placement="middle">
      <TRDialog.Title>변경 사항을 배포하세요</TRDialog.Title>
      <TRDialog.Description>서비스가 다시 시작돼요.</TRDialog.Description>
      <TRDialog.Close>취소하세요</TRDialog.Close>
    </TRDialog.Popup>
  </TRDialog.Portal>
</TRDialog.Root>` }]} />

## 예시

<ComponentExampleTabs
  id="dialog-placements"
  title="배치"
  description="같은 compound 구조를 사용하고 popup 배치만 변경하세요."
  preview={<div className="flex flex-wrap gap-2" data-docs-example-item-count={5}>{['middle', 'top', 'bottom', 'start', 'end'].map((placement) => <div data-docs-example-item="" key={placement}><TRDialog.Root><TRDialog.Trigger>{placement} 위치예요</TRDialog.Trigger><TRDialog.Portal><TRDialog.Backdrop /><TRDialog.Popup placement={placement}><TRDialog.Title>{placement} 위치 dialog예요</TRDialog.Title><TRDialog.Description>배치 미리 보기예요.</TRDialog.Description><TRDialog.Close>닫으세요</TRDialog.Close></TRDialog.Popup></TRDialog.Portal></TRDialog.Root></div>)}</div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/dialog.css';
import { TRDialog, type TRDialogPlacement } from '@tinyrack/ui/components/dialog';

const placements: TRDialogPlacement[] = ['middle', 'top', 'bottom', 'start', 'end'];

{placements.map((placement) => (
  <TRDialog.Root key={placement}>
    <TRDialog.Trigger>{placement}</TRDialog.Trigger>
    <TRDialog.Portal>
      <TRDialog.Backdrop />
      <TRDialog.Popup placement={placement}>
        <TRDialog.Title>{placement} dialog</TRDialog.Title>
        <TRDialog.Description>배치 미리 보기예요.</TRDialog.Description>
        <TRDialog.Close>닫으세요</TRDialog.Close>
      </TRDialog.Popup>
    </TRDialog.Portal>
  </TRDialog.Root>
))}` }]}
/>

<ComponentExampleTabs
  id="dialog-handle"
  title="분리된 trigger handle"
  description="안정적인 handle이 공통 ancestor를 공유하지 않는 trigger와 root를 연결하고 Viewport가 mobile containment를 담당해요."
  preview={<Stories.DialogHandleExample />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/button.css';
import '@tinyrack/ui/components/dialog.css';
import { TRButton } from '@tinyrack/ui/components/button';
import { TRDialog } from '@tinyrack/ui/components/dialog';

const handle = TRDialog.createHandle<void>();

export function DetachedDialog() {
  return <>
    <TRDialog.Trigger handle={handle}>분리된 dialog를 여세요</TRDialog.Trigger>
    <TRDialog.Root handle={handle}>
      <TRDialog.Portal>
        <TRDialog.Backdrop />
        <TRDialog.Viewport>
          <TRDialog.Popup placement="middle">
            <TRDialog.Title>분리된 trigger예요</TRDialog.Title>
            <TRDialog.Description>공유 handle이 Root 바깥의 trigger를 연결해요.</TRDialog.Description>
            <TRDialog.Close render={<TRButton variant="secondary" />}>닫으세요</TRDialog.Close>
          </TRDialog.Popup>
        </TRDialog.Viewport>
      </TRDialog.Portal>
    </TRDialog.Root>
  </>;
}` }]}
/>

<ComponentExampleTabs
  id="dialog-task-form"
  title="form이 있는 긴 modal 작업"
  description="modal은 열린 동안 focus를 가두고 긴 content를 viewport 안에 유지하며 실제 form을 submit해요. 닫힌 뒤 dialog 열기로 focus를 복원해요."
  preview={<Stories.DialogExample description={'저장하기 전에 배포 이름, 환경, 롤백 메모를 검토하세요. 결과는 dialog를 닫지 않고 form 아래에 안내돼요.'} longContent modal={true} open={false} placement={'middle'} title={'배포를 편집하세요'} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/button.css';
import '@tinyrack/ui/components/dialog.css';
import '@tinyrack/ui/components/field.css';
import '@tinyrack/ui/components/form.css';
import '@tinyrack/ui/components/input.css';
import '@tinyrack/ui/components/textarea.css';
import { useId, useState } from 'react';
import { TRButton } from '@tinyrack/ui/components/button';
import { TRDialog } from '@tinyrack/ui/components/dialog';
import { TRField } from '@tinyrack/ui/components/field';
import { TRForm } from '@tinyrack/ui/components/form';
import { TRInput } from '@tinyrack/ui/components/input';
import { TRTextarea } from '@tinyrack/ui/components/textarea';

function DeploymentDialog() {
  const inputId = useId();
  const notesId = useId();
  const [result, setResult] = useState('변경 사항을 저장하지 않았어요');

  return <TRDialog.Root modal>
    <TRDialog.Trigger>dialog를 여세요</TRDialog.Trigger>
    <TRDialog.Portal>
      <TRDialog.Backdrop />
      <TRDialog.Viewport>
        <TRDialog.Popup placement="middle">
          <TRDialog.Title>배포 편집</TRDialog.Title>
          <TRDialog.Description>
            저장하기 전에 배포 이름, 환경, 롤백 메모를 검토하세요. 결과는 dialog를 닫지 않고 form 아래에 안내돼요.
          </TRDialog.Description>
          <div className="tr-dialog-body grid gap-4" data-dialog-scroll-body="">
            <div className="grid gap-3">
              <p>배포를 저장하기 전에 대상 환경, 유지보수 시간, 롤백 담당자를 확인하세요.</p>
              <p>현재 랙 목록을 기준으로 변경 사항을 검토해요. 제목과 작업을 고정한 채 본문을 계속 읽을 수 있어요.</p>
              <TRField.Root>
                <TRField.Label htmlFor={notesId}>롤백 메모예요</TRField.Label>
                <TRTextarea defaultValue="이전 배포를 복원하고 랙 상태를 확인하세요." id={notesId} name="notes" rows={8} />
              </TRField.Root>
            </div>
            <TRForm className="grid gap-2" onSubmit={(event) => {
              event.preventDefault();
              setResult(new FormData(event.currentTarget).get('rack') + '을 저장했어요');
            }}>
              <TRField.Root>
                <TRField.Label htmlFor={inputId}>랙 이름이에요</TRField.Label>
                <TRInput defaultValue="rack-alpha" id={inputId} name="rack" required />
              </TRField.Root>
              <TRButton type="submit">변경 사항을 저장하세요</TRButton>
            </TRForm>
            <output aria-live="polite">{result}</output>
          </div>
          <TRDialog.Close>취소하세요</TRDialog.Close>
        </TRDialog.Popup>
      </TRDialog.Viewport>
    </TRDialog.Portal>
  </TRDialog.Root>;
}` }]}
/>

## API

Base UI는 포커스 트랩, 스크롤 잠금, 포털, 배경 상호작용, 포커스 복원, Escape 닫기를 담당해요. `Popup`은 모든 배치에서 하나의 반응형 너비 규칙을 사용해요. 긴 작업 콘텐츠는 Description과 작업 행 사이의 `tr-dialog-body` 컨테이너에 배치하세요. 그러면 팝업의 `minmax(0, 1fr)` 본문만 스크롤되고 제목과 작업은 계속 표시돼요.

| 공개 API | 용도 |
| --- | --- |
| `TRDialog.Root` / `TRDialogRoot` | 제어형 `open`, 비제어형 `defaultOpen`, 모달 동작, 닫기, 선택적 handle payload 상태를 관리해요. |
| `TRDialog.Trigger` / `TRDialogTrigger` | root를 열거나 handle과 선택적 payload를 사용해 분리된 root를 엽니다. |
| `TRDialog.Portal` / `TRDialogPortal` | 레이어를 `document.body` 또는 사용자 지정 `container`로 이동해요. |
| `TRDialog.Backdrop` / `TRDialogBackdrop` | 모달 배경과 외부 누르기 대상을 제공해요. |
| `TRDialog.Viewport` / `TRDialogViewport` | 긴 콘텐츠와 모바일 dialog 레이아웃을 포함해요. |
| `TRDialog.Popup` / `TRDialogPopup` | 이름이 지정된 `dialog`를 렌더링하고 `initialFocus`, `finalFocus`, Tinyrack `placement`를 받아요. |
| `TRDialog.Title` / `TRDialogTitle` | 접근 가능한 이름을 제공해요. |
| `TRDialog.Description` / `TRDialogDescription` | 접근 가능한 설명을 제공해요. |
| `TRDialog.Close` / `TRDialogClose` | 활성 dialog를 닫고 포커스를 복원해요. |
| `TRDialog.createHandle`, `createDialogHandle`, `TRDialogHandle<Payload>` | 분리된 trigger, 명령형 열기/닫기 호출, 타입이 지정된 payload를 연결해요. |

외부 상호작용을 유지하려면 `modal={false}`를 사용하고 스크롤 잠금 없이 포커스만 가두려면 `modal="trap-focus"`를 사용하세요. `disablePointerDismissal`은 외부 누르기로 dialog가 닫히지 않게 해요. 모든 part는 ref, `render`, 이벤트 핸들러, class, style, 네이티브 속성을 보존해요. 닫힌 portal은 SSR에 안전하며 기본으로 열린 portal도 공개 구조 변경 없이 hydrate돼요.
