---
title: "Meter"
description: "Displays a scalar measurement within a known range."
section: components
order: 37
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRMeter } from '@tinyrack/ui/components/meter';
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/meter.demo.js';

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Playground controls normalize invalid ranges and cover `label`, `min`, `max`, `value`, number formatting, and every explicit semantic variant.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/meter.css';", "import { TRMeter } from '@tinyrack/ui/components/meter';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs
  id="meter-basic"
  title="Formatted storage range"
  preview={<TRMeter.Root format={{ style: 'unit', unit: 'gigabyte', unitDisplay: 'short' }} max={128} min={0} value={64}><TRMeter.Label>Storage usage</TRMeter.Label><TRMeter.Track><TRMeter.Indicator /></TRMeter.Track><TRMeter.Value /></TRMeter.Root>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRMeter.Root
  format={{ style: 'unit', unit: 'gigabyte', unitDisplay: 'short' }}
  max={128}
  min={0}
  value={64}
>
  <TRMeter.Label>Storage usage</TRMeter.Label>
  <TRMeter.Track><TRMeter.Indicator /></TRMeter.Track>
  <TRMeter.Value />
</TRMeter.Root>` }]}
/>

## Examples

<ComponentExampleTabs
  id="meter-states"
  title="Semantic variants"
  description="Variant communicates application meaning; it does not infer thresholds from the numeric value."
  preview={<Stories.MeterVariantMatrix />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`const variants = ['neutral', 'info', 'success', 'warning', 'danger'] as const;

export function MeterVariants() {
  return <div className="grid gap-4">
    {variants.map((variant, index) => <TRMeter.Root key={variant} value={20 + index * 16} variant={variant}>
      <TRMeter.Label>{variant}</TRMeter.Label>
      <TRMeter.Value />
      <TRMeter.Track><TRMeter.Indicator /></TRMeter.Track>
    </TRMeter.Root>)}
  </div>;
}` }]}
/>

<ComponentExampleTabs
  id="meter-custom-range"
  title="Custom range and formatted value"
  description="A non-zero minimum, unit formatter, and human value text remain bound to the same meter contract."
  preview={<Stories.MeterCustomRangePreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRMeter.Root
  format={{ style: 'unit', unit: 'celsius', unitDisplay: 'short' }}
  getAriaValueText={(formatted) => formatted + ' within the safe temperature range'}
  max={80}
  min={20}
  value={50}
  variant="success"
>
  <TRMeter.Label>Rack inlet temperature range</TRMeter.Label>
  <TRMeter.Value />
  <TRMeter.Track><TRMeter.Indicator /></TRMeter.Track>
</TRMeter.Root>` }]}
/>

## API

Keep `value` inside the current `min`/`max` range and use `format` for visible units plus `getAriaValueText` for contextual announcements. `neutral`, `info`, `success`, `warning`, and `danger` are explicit; Tinyrack intentionally adds no implicit low/high threshold API.
