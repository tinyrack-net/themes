---
title: "NavigationMenu"
description: "Provides site-level links with optional rich flyout content."
section: components
group: navigation
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/navigation-menu.demo.js';

## Contract

| Decision | Choose |
| --- | --- |
| destination | direct `Link`, or `Trigger` plus `Content` for a small related group |
| orientation | `horizontal` or `vertical`; default `horizontal` |
| position | `side`: top/right/bottom/left; `align`: start/center/end |
| narrow screens | replace the desktop composition with a drawer containing the same destinations |

Use task-oriented flyouts and keep direct destinations as links. This is site navigation, not an application command menubar.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/navigation-menu.css';"],
      codeImports: ["import { TRNavigationMenu } from '@tinyrack/ui/components/navigation-menu';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs id="navigation-menu-basic" title="Site header" previewLayout="stretch" preview={<div data-docs-example-item-count="1"><Stories.NavigationMenuPreview active={false} align="center" disabled={false} locale="en" navigationLabel="Site navigation" openSection="none" orientation="horizontal" side="bottom" /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRNavigationMenu } from '@tinyrack/ui/components/navigation-menu';
export function SiteHeader() { return <TRNavigationMenu.Root aria-label="Site navigation"><TRNavigationMenu.List><TRNavigationMenu.Item><TRNavigationMenu.Link active href="/pricing">Pricing</TRNavigationMenu.Link></TRNavigationMenu.Item></TRNavigationMenu.List></TRNavigationMenu.Root>; }` ,
      styles: { code: "@import '@tinyrack/ui/components/link.css';\n@import '@tinyrack/ui/components/navigation-menu.css';" },
    },
  ]}
/>

## Examples

### Mark destination state

<ComponentExampleTabs id="navigation-menu-states" title="Inactive, active, and disabled" preview={<div data-docs-example-item-count="3"><Stories.NavigationMenuStateComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`export function ControlledNavigation() { const [value, setValue] = useState<string | null>(null); return <TRNavigationMenu.Root value={value} onValueChange={setValue}><TRNavigationMenu.Link active href="/pricing">Pricing</TRNavigationMenu.Link><TRNavigationMenu.Trigger disabled>Product</TRNavigationMenu.Trigger></TRNavigationMenu.Root>; }` }]} />

### Choose navigation direction

<ComponentExampleTabs id="navigation-menu-orientations" title="Horizontal and vertical" preview={<div data-docs-example-item-count="2"><Stories.NavigationMenuOrientationComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<><TRNavigationMenu.Root orientation="horizontal">{items}</TRNavigationMenu.Root><TRNavigationMenu.Root orientation="vertical">{items}</TRNavigationMenu.Root></>` }]} />

### Place flyout content

<ComponentExampleTabs id="navigation-menu-position-sides" title="Top, right, bottom, and left" preview={<div data-docs-example-item-count="4"><Stories.NavigationMenuSideComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`{(['top', 'right', 'bottom', 'left'] as const).map((side) => <TRNavigationMenu.Positioner key={side} side={side}>{popup}</TRNavigationMenu.Positioner>)}` }]} />

<ComponentExampleTabs id="navigation-menu-position-alignments" title="Start, center, and end alignment" preview={<div data-docs-example-item-count="3"><Stories.NavigationMenuAlignComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`{(['start', 'center', 'end'] as const).map((align) => <TRNavigationMenu.Positioner align={align} key={align}>{popup}</TRNavigationMenu.Positioner>)}` }]} />

### Preserve destinations on narrow screens

<ComponentExampleTabs id="navigation-menu-responsive-alternative" title="Desktop flyout and mobile drawer" previewLayout="stretch" preview={<div data-docs-example-item-count="1"><Stories.NavigationMenuResponsivePreview locale="en" /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRDrawer } from '@tinyrack/ui/components/drawer';
import { TRLink } from '@tinyrack/ui/components/link';
export function ResponsiveNavigation() { return <><div className="hidden md:block">{desktopNavigation}</div><div className="md:hidden"><TRDrawer.Root swipeDirection="right"><TRDrawer.Trigger>Open navigation</TRDrawer.Trigger><TRDrawer.Portal><TRDrawer.Popup><nav><TRLink href="/pricing">Pricing</TRLink></nav></TRDrawer.Popup></TRDrawer.Portal></TRDrawer.Root></div></>; }` ,
      styles: { code: "@import '@tinyrack/ui/components/drawer.css';\n@import '@tinyrack/ui/components/link.css';" },
    },
  ]}
/>

### Follow the keyboard model

<ComponentExampleTabs id="navigation-menu-keyboard" title="Roving focus and flyout dismissal" description="Orientation selects the arrow-key axis. Enter or Space opens content, Escape closes it and restores trigger focus, and Tab enters flyout links." preview={<div data-docs-example-item-count="1"><Stories.NavigationMenuKeyboardPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRNavigationMenu.Root aria-label="Keyboard navigation" delay={100} closeDelay={200}>{items}</TRNavigationMenu.Root>` }]} />

## API

The anatomy is `Root`, `List`, `Item`, `Trigger`, `Icon`, `Content`, `Portal`, `Backdrop`, `Positioner`, `Popup`, `Arrow`, `Viewport`, and `Link`. Every part and its prop/state types are named exports.

`Root` owns controlled `value` with `onValueChange`, uncontrolled `defaultValue`, `orientation` (`'horizontal' | 'vertical'`, default horizontal), pointer `delay`, and `closeDelay`. `Trigger` can be disabled. `Icon` renders `ChevronDown` by default and accepts custom children. `Link.active` sets `aria-current="page"`; `closeOnClick` dismisses flyout content.

`Positioner.side` accepts `top`, `right`, `bottom`, and `left`; `align` accepts `start`, `center`, and `end`. `sideOffset` defaults to `8`; collision avoidance shifts alignment and flips sides. `Portal` accepts a custom container. `Popup` owns the surface and `Viewport` receives active `Content`.

Import CSS from the public subpath. Tokens include `--tr-navigation-menu-popup-max-width`. The composition supports SSR and hydration; portal content mounts in its container after hydration. On narrow screens, render the same destinations in a touch-sized drawer instead of compressing the desktop keyboard model.
