---
title: "PreviewCard"
description: "リンク先コンテンツを hover または focus したときに表示する非モーダルプレビューです。"
section: components
order: 40
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRPreviewCard } from '@tinyrack/ui/components/preview-card';
import { TRLink } from '@tinyrack/ui/components/link';
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import { baseUiExampleSources } from '../../shared/base-ui-example-sources.js';
import * as Stories from '../../components/preview-card.demo.js';

## コントラクト

この Tinyrack wrapper は Base UI の動作、React 19 refs、ネイティブ props、events、`render` contract を保ちます。Playground control は `label`、制御付き `open`、`delay`、`closeDelay`、`side`、`align` に対応します。

`Trigger` は既定で開く前に `600ms`、閉じる前に `300ms` 待ちます。
trigger から card へ移動する pointer path に十分な余裕がある場合だけ、`delay` と `closeDelay` で値を override します。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/preview-card.css';", "import { TRPreviewCard } from '@tinyrack/ui/components/preview-card';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="preview-card-basic"
  title="既定"
  preview={<TRPreviewCard.Root><TRPreviewCard.Trigger href="#rack-alpha">Rack Alpha</TRPreviewCard.Trigger><TRPreviewCard.Portal><TRPreviewCard.Positioner><TRPreviewCard.Popup><strong>Rack Alpha</strong><p>正常 · 12 services</p></TRPreviewCard.Popup></TRPreviewCard.Positioner></TRPreviewCard.Portal></TRPreviewCard.Root>}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['preview-card'] }]}
/>

## 例

<ComponentExampleTabs
  id="preview-card-states"
  title="インタラクティブな service プレビュー"
  description="link を hover または focus して non-modal preview を表示します。閉じないよう pointer を card 内へ移動し、その後外へ出るか Escape を押します。"
  preview={<TRPreviewCard.Root defaultOpen><TRPreviewCard.Trigger href="#rack-beta">Rack Beta</TRPreviewCard.Trigger><TRPreviewCard.Portal><TRPreviewCard.Positioner sideOffset={8}><TRPreviewCard.Popup><strong>Rack Beta</strong><p>Degraded · 8 of 10 services healthy</p><TRLink href="#rack-beta-incidents">View incidents</TRLink><TRPreviewCard.Arrow /></TRPreviewCard.Popup></TRPreviewCard.Positioner></TRPreviewCard.Portal></TRPreviewCard.Root>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRPreviewCard.Root defaultOpen>
  <TRPreviewCard.Trigger href="#rack-beta">Rack Beta</TRPreviewCard.Trigger>
  <TRPreviewCard.Portal>
    <TRPreviewCard.Positioner sideOffset={8}>
      <TRPreviewCard.Popup>
        <strong>Rack Beta</strong>
        <p>Degraded · 8 of 10 services healthy</p>
        <TRLink href="#rack-beta-incidents">View incidents</TRLink>
        <TRPreviewCard.Arrow />
      </TRPreviewCard.Popup>
    </TRPreviewCard.Positioner>
  </TRPreviewCard.Portal>
</TRPreviewCard.Root>` }]}
/>

<ComponentExampleTabs
  id="preview-card-positioning"
  title="timing と collision-safe な配置"
  description="開いた card が zero-delay の audit 状態、top-side 配置、end alignment、arrow、viewport-aware containment を示します。"
  preview={<Stories.PreviewCardPreview align="end" closeDelay={150} delay={0} label="Edge rack" open side="top" />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRPreviewCard.Root defaultOpen>
  <TRPreviewCard.Trigger closeDelay={150} delay={0} href="#edge-rack">Edge rack</TRPreviewCard.Trigger>
  <TRPreviewCard.Portal>
    <TRPreviewCard.Positioner align="end" side="top">
      <TRPreviewCard.Popup>
        <TRPreviewCard.Arrow />
        <strong>Edge rack</strong>
        <p>Collision-aware preview content remains inside the viewport.</p>
      </TRPreviewCard.Popup>
    </TRPreviewCard.Positioner>
  </TRPreviewCard.Portal>
</TRPreviewCard.Root>` }]}
/>

## API

すべての compound part と prop/state type は `@tinyrack/ui/components/preview-card` から export されます。style は対応する CSS subpath による opt-in のままです。`Trigger` は anchor destination と `delay`、`closeDelay` timing prop を持ち、`Root` は制御付き・非制御の open 状態を持ちます。interactive な preview content はキーボードで到達でき、Escape で解除できるようにします。

TRPreviewCard is a progressive enhancement for mouse hover and keyboard focus, not
a touch-only disclosure. The trigger remains a real link: tapping it follows its
`href`, so do not put required information or the only path to an action inside
the preview. Provide a separate button/dialog pattern when touch users must open
the supplemental surface without navigating.
