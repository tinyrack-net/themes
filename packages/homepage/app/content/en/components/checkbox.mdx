---
title: "Checkbox"
description: "A binary or mixed selection control with a native form value."
section: components
order: 28
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/checkbox.demo.js';

## Contract

| Decision | Choose |
| --- | --- |
| selection | unchecked, checked, or `indeterminate` for a partially selected set |
| availability | enabled, `readOnly` when focus and form value must remain, or `disabled` when unavailable |
| size | `sm`, `md`, or `lg`; default `md` |
| form | `name`, `value`, `uncheckedValue`, `form`, `required`, and native reset |

Give every checkbox a visible label or accessible name. Use a checkbox for an independent or batched choice; use `TRCheckboxGroup` to coordinate several checkbox values.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/checkbox.css';", "import { TRCheckbox } from '@tinyrack/ui/components/checkbox';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs
  id="checkbox-basic"
  title="Labeled choice"
  preview={<div data-docs-example-item-count="1"><Stories.CheckboxPreview defaultChecked disabled={false} indeterminate={false} label="Enable backups" readOnly={false} required={false} /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/checkbox.css';
import { TRCheckbox } from '@tinyrack/ui/components/checkbox';

export function BackupChoice() {
  return <label><TRCheckbox.Root defaultChecked name="backups" value="enabled"><TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator></TRCheckbox.Root> Enable backups</label>;
}` }]}
/>

## Examples

### Choose a selection state

<ComponentExampleTabs id="checkbox-states" title="Unchecked, checked, and mixed" description="Use mixed only when the checkbox summarizes a partially selected set." preview={<div data-docs-example-item-count="3"><Stories.CheckboxStateComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<>
  <TRCheckbox.Root aria-label="Unchecked" />
  <TRCheckbox.Root aria-label="Checked" defaultChecked />
  <TRCheckbox.Root aria-label="Partially selected" indeterminate />
</>` }]} />

### Match the control density

<ComponentExampleTabs id="checkbox-sizes" title="Small, medium, and large" preview={<div data-docs-example-item-count="3"><Stories.CheckboxSizeComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`{(['sm', 'md', 'lg'] as const).map((uiSize) => <TRCheckbox.Root aria-label={uiSize} key={uiSize} uiSize={uiSize} />)}` }]} />

### Keep or remove interaction

<ComponentExampleTabs id="checkbox-availability" title="Editable, read only, and disabled" description="Read-only controls stay focusable and keep their form value; disabled controls leave the tab order and submission." preview={<div data-docs-example-item-count="3"><Stories.CheckboxAvailabilityComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<>
  <TRCheckbox.Root aria-label="Editable" defaultChecked />
  <TRCheckbox.Root aria-label="Read only" defaultChecked readOnly />
  <TRCheckbox.Root aria-label="Disabled" defaultChecked disabled />
</>` }]} />

### Require an agreement

<ComponentExampleTabs id="checkbox-validation" title="Required choice and recovery" preview={<div data-docs-example-item-count="1"><Stories.CheckboxValidationPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRCheckbox.Root checked={checked} name="terms" onCheckedChange={setChecked} required><TRCheckbox.Indicator>✓</TRCheckbox.Indicator></TRCheckbox.Root>` }]} />

### Submit explicit on and off values

<ComponentExampleTabs id="checkbox-form-values" title="External form ownership" description="Use `form` to associate the hidden input with another form and `uncheckedValue` when the off state must also serialize." preview={<div data-docs-example-item-count="1"><Stories.CheckboxFormValuesPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRCheckbox.Root form="monitoring-form" name="monitoring" uncheckedValue="disabled" value="enabled"><TRCheckbox.Indicator>✓</TRCheckbox.Indicator></TRCheckbox.Root>` }]} />

## API

### Selection and interaction

| Prop | Type / default | Purpose |
| --- | --- | --- |
| `checked`, `defaultChecked` | `boolean`, `false` | Controls or initializes selection. |
| `indeterminate` | `boolean`, `false` | Exposes the mixed state; clear it when a user resolves the choice. |
| `onCheckedChange` | `(checked, details) => void` | Reports an attempted change. |
| `disabled`, `readOnly` | `boolean`, `false` | Removes interaction, or keeps a focusable immutable value. |
| `uiSize` | `'sm' \| 'md' \| 'lg'`, `'md'` | Sets the visual control size. |

### Forms and composition

`name`, `value`, `uncheckedValue`, `form`, `required`, and `inputRef` are forwarded to the hidden native checkbox. Uncontrolled roots restore `defaultChecked` on owner-form reset; controlled roots remain governed by `checked`. `render`, root `ref`, event props, and state-driven `className` and `style` follow Base UI composition.

`TRCheckbox.Indicator` renders for checked or mixed state. Its render callback receives `state.indeterminate`; `keepMounted` keeps it in the DOM for transitions. Root and Indicator expose relevant `data-checked`, `data-unchecked`, `data-indeterminate`, `data-disabled`, `data-readonly`, and `data-invalid` attributes.

The module exports `TRCheckbox`, both named parts, their prop and state types, and `TRCheckboxUiSize`. CSS is opt-in.
