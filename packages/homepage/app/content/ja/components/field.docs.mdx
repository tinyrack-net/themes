---
title: "Field"
description: "ラベル、コントロール、説明、ネイティブ妥当性、エラーを組み合わせるアクセシブルな構成です。"
section: components
order: 13
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRField } from '@tinyrack/ui/components/field';
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/field.demo.js';

## コントラクト

| Part | Responsibility |
| --- | --- |
| `Root` | disabled, invalid, dirty, touched, and validation state |
| `Label` | accessible name and enlarged target |
| `Control` | native input rendered through Base UI |
| `Description` | persistent instructions |
| `Error` | validity-specific recovery message |
| `Validity` | render-prop access to native validity |
| `Item` | repeatable option within a collection field |

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/field.css';", "import '@tinyrack/ui/components/form.css';", "import '@tinyrack/ui/components/button.css';", "import '@tinyrack/ui/components/checkbox.css';", "import { TRField } from '@tinyrack/ui/components/field';", "import { TRForm } from '@tinyrack/ui/components/form';", "import { TRButton } from '@tinyrack/ui/components/button';", "import { TRCheckbox } from '@tinyrack/ui/components/checkbox';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="field-basic"
  title="label 付き Field"
  previewLayout="stretch"
  preview={<Stories.FieldPreview defaultValue="ops@example.com" disabled={false} invalid={false} label="メール" placeholder="ops@example.com" readOnly={false} required={true} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRField.Root>
  <TRField.Label>Email</TRField.Label>
  <TRField.Control defaultValue="ops@example.com" required type="email" />
  <TRField.Description>Operational alerts are sent here.</TRField.Description>
  <TRField.Error match="valueMissing">Email is required.</TRField.Error>
  <TRField.Error match="typeMismatch">Enter a valid email.</TRField.Error>
</TRField.Root>` }]}
/>

## 例

<ComponentExampleTabs
  id="field-field-states"
  title="Field の状態"
  description="既定、invalid、disabled、read-only の TRField をひと目で区別できます。"
  previewLayout="stretch"
  preview={(
    <div className="grid gap-5 sm:grid-cols-2">
      <TRField.Root><TRField.Label>Default</TRField.Label><TRField.Control defaultValue="rack-a" /><TRField.Description>Editable value.</TRField.Description></TRField.Root>
      <TRField.Root invalid><TRField.Label>Invalid</TRField.Label><TRField.Control aria-invalid="true" defaultValue="bad value" /><TRField.Error match>Use a valid rack name.</TRField.Error></TRField.Root>
      <TRField.Root disabled><TRField.Label>Disabled</TRField.Label><TRField.Control defaultValue="rack-b" /><TRField.Description>Not available.</TRField.Description></TRField.Root>
      <TRField.Root><TRField.Label>Read only</TRField.Label><TRField.Control readOnly value="rack-c" /><TRField.Description>Managed by policy.</TRField.Description></TRField.Root>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid gap-5 sm:grid-cols-2">
  <TRField.Root>
    <TRField.Label>Default</TRField.Label>
    <TRField.Control defaultValue="rack-a" />
    <TRField.Description>Editable value.</TRField.Description>
  </TRField.Root>
  <TRField.Root invalid>
    <TRField.Label>Invalid</TRField.Label>
    <TRField.Control aria-invalid="true" defaultValue="bad value" />
    <TRField.Error match>Use a valid rack name.</TRField.Error>
  </TRField.Root>
  <TRField.Root disabled>
    <TRField.Label>Disabled</TRField.Label>
    <TRField.Control defaultValue="rack-b" />
    <TRField.Description>Not available.</TRField.Description>
  </TRField.Root>
  <TRField.Root>
    <TRField.Label>Read only</TRField.Label>
    <TRField.Control readOnly value="rack-c" />
    <TRField.Description>Managed by policy.</TRField.Description>
  </TRField.Root>
</div>` }]}
/>

<ComponentExampleTabs
  id="field-validation"
  title="submit、エラー、復旧"
  preview={<Stories.FieldValidationPreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`function EmailField() {
  const [submitted, setSubmitted] = useState(false);
  const [value, setValue] = useState('');
  const invalid = submitted && !value.includes('@');

  return (
    <TRForm noValidate onSubmit={(event) => { event.preventDefault(); event.currentTarget.checkValidity(); setSubmitted(true); }}>
      <TRField.Root invalid={invalid}>
        <TRField.Label>TRAlert email</TRField.Label>
        <TRField.Control
          aria-invalid={invalid || undefined}
          name="email"
          onChange={(event) => setValue(event.currentTarget.value)}
          required
          type="email"
          value={value}
        />
        <TRField.Description>Used only for operational alerts.</TRField.Description>
        {invalid ? <TRField.Error match>{value ? 'Enter a valid email.' : 'Email is required.'}</TRField.Error> : null}
      </TRField.Root>
      <TRButton type="submit">Save email</TRButton>
    </TRForm>
  );
}` }]}
/>

<ComponentExampleTabs
  id="field-item-validity"
  title="Collection item と native validity"
  description="Item は option label と description をまとめ、Validity は validation rule を重複させず現在の native validity 状態を公開します。"
  preview={<Stories.FieldItemValidityPreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRField.Root>
  <TRField.Label>Notification channels</TRField.Label>
  <TRField.Item>
    <TRCheckbox.Root defaultChecked name="channel" value="email">
      <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
    </TRCheckbox.Root>
    <div>
      <TRField.Label>Email</TRField.Label>
      <TRField.Description>Send deployment results by email.</TRField.Description>
    </div>
  </TRField.Item>
  <TRField.Validity>
    {(state) => <output>Native field state: {state.validity.valid ? 'valid' : 'invalid'}</output>}
  </TRField.Validity>
</TRField.Root>` }]}
/>

## API

すべての part は個別に export され、Base UI state callback、ref、event、`render`、`className`、`style` を保持します。