---
title: "コントロールと状態"
description: "完全な sm、md、lg のコントロールレシピを選び、タスク内のコンポーネントを揃え、すべての操作状態を保ちます。"
section: foundations
order: 6
---
import { TRButton } from '@tinyrack/ui/components/button';
import { TRField } from '@tinyrack/ui/components/field';
import { TRCode } from '@tinyrack/ui/components/code';
import { TRTable } from '@tinyrack/ui/components/table';
import { tinyrackControlMetrics } from '@tinyrack/ui/core';

## sm、md、lg を選択

ポインター中心の高密度な補助コントロールには `sm`、標準の運用画面には `md`、タッチ操作やタスクの強調に大きなターゲットが必要なら `lg` を使います。視覚的な階層を表すためだけに一つの操作行でサイズを混在させず、`intent` と `appearance` を使ってください。

## 完全なサイズレシピ

サイズは高さ、左右の内側余白、間隔、文字サイズ、行間をまとめて変更します。

<TRTable.Root containerProps={{ 'aria-label': 'コントロールサイズレシピ', tabIndex: 0 }} density="compact"><thead><tr><th scope="col">サイズ</th><th scope="col">高さ</th><th scope="col">内側余白</th><th scope="col">間隔</th><th scope="col">文字</th><th scope="col">行間</th></tr></thead><tbody>{Object.entries(tinyrackControlMetrics).map(([size, recipe]) => <tr key={size}><th scope="row"><TRCode>{size}</TRCode></th><td>{recipe.height}</td><td>{recipe.paddingInline}</td><td>{recipe.gap}</td><td>{recipe.fontSize}</td><td>{recipe.lineHeight}</td></tr>)}</tbody></TRTable.Root>

## タスク内でコンポーネントを整列

隣接するコンポーネントには同じ `uiSize` を設定します。フィールド系コントロールは `TRField.Root` にサイズを設定し、ラベル、説明、エラー、コントロールのレシピを揃えてください。

<div className="grid gap-3" role="list">{(['sm', 'md', 'lg']).map((uiSize) => <div className="flex flex-wrap items-end gap-2" key={uiSize} role="listitem"><TRField.Root uiSize={uiSize}><TRField.Label>{uiSize} ラック</TRField.Label><TRField.Control placeholder="rack-a" /></TRField.Root><TRButton uiSize={uiSize}>デプロイ</TRButton></div>)}</div>

## 操作状態を保つ

- ホバーは利用可能な操作を予告しますが、唯一の手がかりにはしません。
- 押下は即時の入力フィードバック、選択は入力後も残る決定です。
- 無効化は利用できない操作を止めます。理由が明らかでない場合は見える説明を加えてください。
- フォーカスはキーボード操作中も見え、切り取られないようにします。

無関係な色を組み合わせず、コンポーネントの props と状態属性を使います。

## タッチターゲットを保つ

大きなレシピを配置できるタッチ中心のタスクには `lg` を選びます。小さな見た目が必要でも、操作領域を縮めたりターゲット同士を近づけたりせず、周囲のレイアウト間隔を広げてください。

## 実装

```tsx
<TRField.Root uiSize="md"><TRField.Label>ラック名</TRField.Label><TRField.Control /></TRField.Root>
<TRButton appearance="solid" intent="primary" uiSize="md">ラックを保存</TRButton>
```

製品全体の密度を変える場合だけ、グローバルな `--tinyrack-control-*` 寸法を上書きします。意図的な局所例外には、文書化されたコンポーネントの `--tr-*` 変数を使い、高さだけを変更しないでください。
