import { Meta } from '@storybook/addon-docs/blocks';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import { baseUiExampleSources } from '../shared/base-ui-example-sources.js';
import * as Stories from './select.stories';

<Meta of={Stories} />

# Select

Selects one value from a positioned, keyboard-accessible popup list.

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Storybook controls cover `open`, `disabled`, `value`.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/select.css';", "import { Select } from '@tinyrack/ui/components/select';"] }]} />

## Usage

<ComponentExampleTabs
  id="select-basic"
  title="Default"
  preview={<Stories.SelectPreview open={false} disabled={false} interactive={true} modal={false} value={'alpha'} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['select'] }]}
/>

## Examples

<ComponentExampleTabs
  id="select-states"
  title="Disabled"
  description="Disabled selects remain visible but cannot be opened or changed."
  preview={<Stories.SelectPreview open={false} disabled={true} interactive={true} modal={false} value={'beta'} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['select'] }]}
/>

## API

Every compound part and prop/state type is exported from `@tinyrack/ui/components/select`; styles remain opt-in through the matching CSS subpath.
