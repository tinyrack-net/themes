---
title: "Steps"
description: "Numbered semantic procedures for authored documentation."
section: components
order: 59
---
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/steps.demo.js';

## Contract

| Part | Renders | Best for |
| --- | --- | --- |
| `Root` | semantic ordered list | the complete procedure |
| `Item` | semantic list item with a numbered marker | one task, including rich content |

`Steps` is intentionally presentational. It does not manage progress or selection, so authored headings, paragraphs, links, code blocks, and other components remain untouched inside each item.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/steps.css';", "import { Steps } from '@tinyrack/ui/components/steps';"] }]} />

## Usage

<ComponentExampleTabs
  id="steps-basic"
  title="Installation procedure"
  description="Use one item per task. The marker and connecting line are supplied by Steps while the content stays ordinary React markup."
  previewLayout="stretch"
  preview={<Stories.StepsPreview count={3} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { Steps } from '@tinyrack/ui/components/steps';

<Steps.Root>
  <Steps.Item>Install the package</Steps.Item>
  <Steps.Item>Create the config</Steps.Item>
  <Steps.Item>Build the site</Steps.Item>
</Steps.Root>` }]}
/>

## Examples

<ComponentExampleTabs
  id="steps-rich-content"
  title="A complete setup guide"
  description="Complex guides can combine headings, prose, package-manager tabs, and syntax-highlighted code inside the same numbered list."
  previewLayout="stretch"
  preview={<Stories.StepsGuidePreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { CodeBlock } from '@tinyrack/ui/components/code-block';
import { Steps } from '@tinyrack/ui/components/steps';
import { Tabs } from '@tinyrack/ui/components/tabs';

<Steps.Root>
  <Steps.Item>
    <h3>Create a project</h3>
    <p>Start with a React app and add the Tinyrack packages.</p>
    <Tabs.Root defaultValue="pnpm">
      <Tabs.List aria-label="Package manager">
        <Tabs.Tab value="pnpm">pnpm</Tabs.Tab>
        <Tabs.Tab value="npm">npm</Tabs.Tab>
      </Tabs.List>
      <Tabs.Panel value="pnpm">
        <CodeBlock code="pnpm create vite my-app --template react-ts" language="shellscript" />
      </Tabs.Panel>
      <Tabs.Panel value="npm">
        <CodeBlock code="npm create vite@latest my-app -- --template react-ts" language="shellscript" />
      </Tabs.Panel>
    </Tabs.Root>
  </Steps.Item>
  <Steps.Item>
    <h3>Install and configure</h3>
    <p>Import the shared and component styles after installing the package.</p>
    <CodeBlock code="pnpm add @tinyrack/ui react react-dom" language="shellscript" />
  </Steps.Item>
  <Steps.Item>
    <h3>Write the guide</h3>
    <p>Continue with links, callouts, and other documentation components.</p>
  </Steps.Item>
</Steps.Root>` }]}
/>

Use `Steps` for authored procedures, not interactive progress indicators. If the user must move between states or complete a form, compose a dedicated progress or navigation component instead.

## API

Both parts forward native ordered-list props, list-item props, and React 19 refs.
