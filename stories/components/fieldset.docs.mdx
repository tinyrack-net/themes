import { Meta } from '@storybook/addon-docs/blocks';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import { baseUiExampleSources } from '../shared/base-ui-example-sources.js';
import * as Stories from './fieldset.stories';

<Meta of={Stories} />

# Fieldset

Groups related controls under an accessible legend.

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Storybook controls cover `legend`, `disabled`.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/fieldset.css';", "import { Fieldset } from '@tinyrack/ui/components/fieldset';"] }]} />

## Usage

<ComponentExampleTabs
  id="fieldset-basic"
  title="Default"
  preview={<Stories.FieldsetPreview legend={'Notifications'} disabled={false} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['fieldset'] }]}
/>

## Examples

<ComponentExampleTabs
  id="fieldset-states"
  title="State comparison"
  description="Use Controls above to inspect every supported public state."
  preview={<Stories.FieldsetPreview legend={'Notifications'} disabled={true} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['fieldset'] }]}
/>

## API

Every compound part and prop/state type is exported from `@tinyrack/ui/components/fieldset`; styles remain opt-in through the matching CSS subpath.
