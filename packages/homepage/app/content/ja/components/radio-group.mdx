---
title: "Radio Group"
description: "複数候補から1つを選び、方向キー操作と1つのネイティブフォーム値を提供します。"
section: components
order: 42
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/radio-group.demo.js';

## 主なプロパティ

| プロパティ | 選択肢 |
| --- | --- |
| ラベル | `aria-label`、`aria-labelledby`、フィールドセットの凡例 |
| 値 | 非制御は `defaultValue`、制御は `value` と `onValueChange` を使います。 |
| 利用可否 | `readOnly` は選択項目のフォーカスを保ち、`disabled` は操作とフォームデータを除外します。 |
| フォーム | グループの `name`、`form`、`required`。リセットは `defaultValue` を復元します。 |
| キーボード | 方向キーは無効項目を飛ばし、フォーカスと選択を循環します。 |

グループでは 1 つの値だけを選択できます。2 軸の方向キーを使用し、公開 `orientation` prop はありません。見た目の方向はレイアウトクラスで指定します。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/radio-group.css';"],
      codeImports: ["import { TRRadioGroup } from '@tinyrack/ui/components/radio-group';","import { TRRadio } from '@tinyrack/ui/components/radio';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs id="radio-group-basic" title="デプロイ先ラックを選ぶ" preview={<div data-docs-example-item-count="1"><Stories.RadioGroupPreview defaultValue="alpha" disabled={false} label="デプロイ先ラック" readOnly={false} required={false} /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRRadioGroup aria-label="デプロイ先ラック" defaultValue="alpha" name="rack">{racks.map((rack) => <label key={rack.value}><TRRadio.Root value={rack.value}><TRRadio.Indicator /></TRRadio.Root>{rack.label}</label>)}</TRRadioGroup>` }]} />

## 例

### 選択全体の利用可否を決める

<ComponentExampleTabs id="radio-group-states" title="編集可能、読み取り専用、無効なグループ" preview={<div data-docs-example-item-count="3"><Stories.RadioGroupStateComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<><TRRadioGroup aria-label="編集可能" defaultValue="alpha">{items}</TRRadioGroup><TRRadioGroup aria-label="読み取り専用" defaultValue="beta" readOnly>{items}</TRRadioGroup><TRRadioGroup aria-label="無効" defaultValue="gamma" disabled>{items}</TRRadioGroup></>` }]} />

### 1つの値を必須にする

<ComponentExampleTabs id="radio-group-validation" title="必須選択とエラー回復" preview={<div data-docs-example-item-count="1"><Stories.RadioGroupValidationPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRRadioGroup aria-invalid={invalid || undefined} name="rack" onValueChange={setValue} required value={value}>{items}</TRRadioGroup>` }]} />

### 外部フォームに関連付ける

<ComponentExampleTabs id="radio-group-external-form" title="外部所有とリセット" description="`form` は非表示のラジオ入力を別のフォームに関連付けます。ネイティブリセットは非制御のデフォルト値を復元します。" preview={<div data-docs-example-item-count="1"><Stories.RadioGroupExternalFormPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRRadioGroup aria-label="フォーム外のラック" defaultValue="alpha" form="rack-form" name="rack" required>{items}</TRRadioGroup>` }]} />

## API

### 値、利用可否、フォーム

| プロパティ | 型 / 既定値 | 用途 |
| --- | --- | --- |
| `value`, `defaultValue` | `unknown`, 未選択 | 選択した子の値を制御または初期化します。 |
| `onValueChange` | `(value, details) => void` | 選択を通知し、`details.cancel()` で拒否できます。 |
| `disabled`, `readOnly` | `boolean`, `false` | グループを無効にするか、選択値をフォーカス可能な読み取り専用にします。 |
| `name`, `form`, `required` | ネイティブフォーム prop | 直列化、外部所有、制約検証を設定します。 |
| `inputRef` | `Ref<HTMLInputElement>` | グループの非表示ネイティブ入力へアクセスします。 |

非制御グループは所有フォームのリセット時に `defaultValue` へ戻り、制御グループは `value` に従います。無効なグループは `FormData` から除外されます。

### 合成とアクセシビリティ

固有値を持つ `TRRadio.Root` をレンダリングし、グループにアクセシブルな名前を付けます。ルートは Base UI の `render`、`ref`、ネイティブイベントと属性、状態に応じた `className` と `style` をサポートします。SSR とハイドレーションでも選択を維持します。

モジュールは `TRRadioGroup`、`TRRadioGroupProps`、`TRRadioGroupState` を公開します。CSS は明示的に読み込みます。
