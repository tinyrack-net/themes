import { Meta } from '@storybook/addon-docs/blocks';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import { baseUiExampleSources } from '../shared/base-ui-example-sources.js';
import * as Stories from './toggle.stories';

<Meta of={Stories} />

# Toggle

A two-state button for formatting or view options.

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Storybook controls cover `label`, `pressed`, `disabled`.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/toggle.css';", "import { Toggle } from '@tinyrack/ui/components/toggle';"] }]} />

## Usage

<ComponentExampleTabs
  id="toggle-basic"
  title="Default"
  preview={<Stories.TogglePreview label={'Bold'} pressed={false} disabled={false} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['toggle'] }]}
/>

## Examples

<ComponentExampleTabs
  id="toggle-states"
  title="State comparison"
  description="Use Controls above to inspect every supported public state."
  preview={<Stories.TogglePreview label={'Bold'} pressed={true} disabled={false} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['toggle'] }]}
/>

## API

Every compound part and prop/state type is exported from `@tinyrack/ui/components/toggle`; styles remain opt-in through the matching CSS subpath.
