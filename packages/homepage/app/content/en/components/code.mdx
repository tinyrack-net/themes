---
title: "Code"
description: "Inline code tokens that stay readable inside prose, lists, and dense metadata."
section: components
group: data-content
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRCode } from '@tinyrack/ui/components/code';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/code.demo.js';

## Contract

`TRCode` renders a semantic `<code>` element for short, inline machine-readable text. It accepts native code-element props, `className`, `style`, and `ref`, and inherits the surrounding font size. Use Code Block for standalone code samples.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/code.css';"],
      codeImports: ["import { TRCode } from '@tinyrack/ui/components/code';"] }]} />

## Playground

Edit the content to check short commands, long identifiers, or multiline fragments. Reset restores the default inline command.

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs id="code-basic" title="Inline command" preview={<p className="m-0" data-docs-example-item="">Run <TRCode>pnpm verify</TRCode> before publishing.</p>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRCode } from '@tinyrack/ui/components/code';

<p className="m-0">Run <TRCode>pnpm verify</TRCode> before publishing.</p>` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/code.css';" },
    },
  ]}
/>

## Examples

<ComponentExampleTabs
  id="code-contexts"
  title="Content contexts"
  description="Inline code keeps its semantic surface in prose, configuration, and long tokens."
  previewLayout="start"
  preview={(
    <div className="grid max-w-full gap-3">
      <p className="m-0" data-docs-example-item="">Import <TRCode>@tinyrack/ui/components/button</TRCode>.</p>
      <p className="m-0" data-docs-example-item="">Set <TRCode>data-theme=&quot;tinyrack-dark&quot;</TRCode> on the root.</p>
      <p className="m-0 max-w-64" data-docs-example-item=""><TRCode>very-long-rack-identifier-with-overflow-safe-wrapping-01</TRCode></p>
      <p className="m-0 text-xl" data-docs-example-item="">Inherited size: <TRCode>{'pnpm test\npnpm verify'}</TRCode></p>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRCode } from '@tinyrack/ui/components/code';

<div className="grid max-w-full gap-3">
  <p className="m-0">Import <TRCode>@tinyrack/ui/components/button</TRCode>.</p>
  <p className="m-0">Set <TRCode>data-theme="tinyrack-dark"</TRCode> on the root.</p>
  <p className="m-0 max-w-64"><TRCode>very-long-rack-identifier-with-overflow-safe-wrapping-01</TRCode></p>
  <p className="m-0 text-xl">Inherited size: <TRCode>{'pnpm test\npnpm verify'}</TRCode></p>
</div>` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/code.css';" },
    },
  ]}
/>

## API

`TRCodeProps` extends React's native `ComponentProps<'code'>`. The default font size is inherited from surrounding prose. `break-spaces`, `overflow-wrap: anywhere`, and cloned line decorations preserve whitespace while keeping long content inside its context.

| CSS token | Default | Purpose |
| --- | --- | --- |
| `--tr-code-background` | `--tinyrack-surface-muted` | Inline code surface |
| `--tr-code-border` | `--tinyrack-border` | Decorative hairline |
| `--tr-code-border-width` | `--tinyrack-border-width-default` | Border thickness |
| `--tr-code-color` | `--tinyrack-text` | TRCode text |
| `--tr-code-font-family` | `--tinyrack-font-mono` | Code typeface |
| `--tr-code-font-size` | `inherit` | Surrounding text size |
| `--tr-code-line-height` | `--tinyrack-leading-sm` | Multiline rhythm |
| `--tr-code-radius` | `--tinyrack-radius-sm` | Corner radius |
| `--tr-code-padding-x` | `--tinyrack-space-xs` | Inline padding |
| `--tr-code-padding-y` | `--tinyrack-space-3xs` | Block padding |
