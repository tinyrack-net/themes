---
title: "Checkbox Group"
description: "独立して切り替えられる値を 1 つのアクセシブルなグループとして連携させます。"
section: components
order: 29
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/checkbox-group.demo.js';

## 主なプロパティ

| 判断軸 | 選択肢 |
| --- | --- |
| 値の所有 | 非制御は `defaultValue`、制御は `value` と `onValueChange` を使います。 |
| 利用可否 | グループ全体は `disabled`、フォーム値を保つ個別項目は `readOnly` を使います。 |
| フォーム | 子チェックボックスに同じ `name` を付けると、選択した各項目が `value` を提供します。 |
| 親選択 | `allValues` と `parent` を持つ子チェックボックスを使います。 |
| バリデーション | 選択数のルールは制御された配列を検証します。 |

グループと各子チェックボックスにアクセシブルな名前を付けます。独立した複数選択をまとめるためのコンポーネントです。1つだけ選ぶ場合は `TRRadioGroup` を使います。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/checkbox-group.css';", "import { TRCheckboxGroup } from '@tinyrack/ui/components/checkbox-group';", "import { TRCheckbox } from '@tinyrack/ui/components/checkbox';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs id="checkbox-group-basic" title="ラック機能を選ぶ" preview={<div data-docs-example-item-count="1"><Stories.CheckboxGroupPreview defaultSelectedValues={['metrics']} disabled={false} label="ラック機能" /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRCheckboxGroup aria-label="ラック機能" defaultValue={['metrics']}>{features.map((feature) => <label key={feature.value}><TRCheckbox.Root name="features" value={feature.value} />{feature.label}</label>)}</TRCheckboxGroup>` }]} />

## 例

### 集合全体の利用可否を決める

<ComponentExampleTabs id="checkbox-group-states" title="編集可能、読み取り専用、無効なグループ" preview={<div data-docs-example-item-count="3"><Stories.CheckboxGroupStateComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<><TRCheckboxGroup aria-label="編集可能" defaultValue={['metrics']}>{items}</TRCheckboxGroup><TRCheckboxGroup aria-label="読み取り専用" defaultValue={['metrics']}>{readOnlyItems}</TRCheckboxGroup><TRCheckboxGroup aria-label="無効" defaultValue={['metrics']} disabled>{items}</TRCheckboxGroup></>` }]} />

### 選択数を検証する

<ComponentExampleTabs id="checkbox-group-form" title="1つまたは2つを選ぶ" description="最小数と最大数のルールがある場合は値の配列を制御します。" preview={<div data-docs-example-item-count="1"><Stories.CheckboxGroupFormPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRCheckboxGroup aria-invalid={invalid || undefined} onValueChange={setSelected} value={selected}>{items}</TRCheckboxGroup>` }]} />

### すべて選択する親を追加する

<ComponentExampleTabs id="checkbox-group-parent" title="親と一部選択" description="全値を選ぶと親が選択済みになり、一部だけを選ぶと一部選択になります。" preview={<div data-docs-example-item-count="1"><Stories.CheckboxGroupParentPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRCheckboxGroup allValues={values} onValueChange={setValue} value={value}><TRCheckbox.Root parent><TRCheckbox.Indicator /></TRCheckbox.Root>{items}</TRCheckboxGroup>` }]} />

### 外部フォームに関連付ける

<ComponentExampleTabs id="checkbox-group-external-form" title="外部所有とリセット" description="各子チェックボックスに `form` を設定します。リセットすると非制御の `defaultValue` に戻ります。" preview={<div data-docs-example-item-count="1"><Stories.CheckboxGroupExternalFormPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRCheckboxGroup aria-label="外部フォームの機能" defaultValue={['metrics']}><TRCheckbox.Root form="feature-form" name="features" value="metrics" /><TRCheckbox.Root form="feature-form" name="features" value="alerts" /></TRCheckboxGroup>` }]} />

## API

### 値と利用可否

| プロパティ | 型 / 既定値 | 用途 |
| --- | --- | --- |
| `value`, `defaultValue` | `string[]`, `[]` | 選択した子の値を制御または初期化します。 |
| `onValueChange` | `(value, details) => void` | 次の値配列を通知します。 |
| `disabled` | `boolean`, `false` | すべての子を無効にしてフォームデータから除外します。 |
| `allValues` | `string[]` | `parent` が使う全集合を定義します。 |

### 合成とフォーム

異なる `value` と共通の `name` を持つ `TRCheckbox.Root` をレンダリングします。`readOnly` とフォーム所有は子チェックボックスに設定します。非制御グループはリセット時に `defaultValue` へ戻り、制御グループは `value` に従います。

ルートは Base UI の `render`、`ref`、イベント、ネイティブ属性、状態に応じた `className` と `style` を維持します。モジュールは `TRCheckboxGroup` とプロパティ・状態型を公開します。CSS は明示的に読み込みます。
