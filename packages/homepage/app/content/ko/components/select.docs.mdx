---
title: "Select"
description: "알려진 컬렉션에서 하나의 값을 선택하고 버튼으로 열며 키보드 탐색 가능한 popup을 제공합니다."
section: components
order: 44
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/select.demo.js';

## 계약

| 축 | 동작 |
| --- | --- |
| value | uncontrolled with `defaultValue`, controlled with `value` and `onValueChange` |
| disclosure | `Trigger` owns focus; `Portal`, `Positioner`, and `Popup` own the floating layer |
| collection | groups, labels, separators, disabled items, text, and indicators describe the menu anatomy |

trigger가 밀도 높은 컨트롤 표면에 속하면 `Select.Trigger uiSize="sm"`을 사용하세요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui lucide-react react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/select.css';", "import '@tinyrack/ui/components/form.css';", "import '@tinyrack/ui/components/field.css';", "import '@tinyrack/ui/components/button.css';", "import { Select } from '@tinyrack/ui/components/select';", "import { Form } from '@tinyrack/ui/components/form';", "import { Field } from '@tinyrack/ui/components/field';", "import { Button } from '@tinyrack/ui/components/button';", "import { ChevronDown } from 'lucide-react';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs
  id="select-basic"
  title="group으로 묶은 배포 rack"
  preview={<Stories.SelectPreview defaultValue="alpha" disabled={false} modal={false} readOnly={false} required={false} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { ChevronDown } from 'lucide-react';

const racks = { alpha: 'Rack Alpha', beta: 'Rack Beta', gamma: 'Rack Gamma', staging: 'Staging rack' };

<Select.Root defaultValue="alpha" items={racks} name="rack">
  <Select.Label>Deployment rack</Select.Label>
  <Select.Trigger aria-label="Deployment rack">
    <Select.Value placeholder="Choose a rack" />
    <Select.Icon aria-hidden="true"><ChevronDown /></Select.Icon>
  </Select.Trigger>
  <Select.Portal>
    <Select.Positioner sideOffset={8}>
      <Select.Popup>
        <Select.Arrow />
        <Select.ScrollUpArrow aria-label="Scroll up">↑</Select.ScrollUpArrow>
        <Select.List>
          <Select.Group>
            <Select.GroupLabel>Production</Select.GroupLabel>
            <Select.Item value="alpha"><Select.ItemText>Rack Alpha</Select.ItemText><Select.ItemIndicator aria-hidden="true">✓</Select.ItemIndicator></Select.Item>
            <Select.Item value="beta"><Select.ItemText>Rack Beta</Select.ItemText><Select.ItemIndicator aria-hidden="true">✓</Select.ItemIndicator></Select.Item>
            <Select.Item disabled value="gamma"><Select.ItemText>Rack Gamma · Offline</Select.ItemText><Select.ItemIndicator aria-hidden="true">✓</Select.ItemIndicator></Select.Item>
          </Select.Group>
          <Select.Separator />
          <Select.Group>
            <Select.GroupLabel>Non-production</Select.GroupLabel>
            <Select.Item value="staging"><Select.ItemText>Staging rack</Select.ItemText><Select.ItemIndicator aria-hidden="true">✓</Select.ItemIndicator></Select.Item>
          </Select.Group>
        </Select.List>
        <Select.ScrollDownArrow aria-label="Scroll down">↓</Select.ScrollDownArrow>
      </Select.Popup>
    </Select.Positioner>
  </Select.Portal>
</Select.Root>` }]}
/>

## 예시

<ComponentExampleTabs
  id="select-states"
  title="사용 가능 상태"
  preview={<Stories.SelectStateComparison />}
  sources={[{ label: 'React', language: 'tsx', code: Stories.selectStatesSource }]}
/>

<ComponentExampleTabs
  id="select-validation"
  title="필수 선택과 복구"
  preview={<Stories.SelectValidationPreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { ChevronDown } from 'lucide-react';

function RackSelectForm() {
  const [attempted, setAttempted] = useState(false);
  const [value, setValue] = useState<string | null>(null);
  const invalid = attempted && value === null;

  return (
    <Form noValidate onSubmit={(event) => { event.preventDefault(); event.currentTarget.checkValidity(); setAttempted(true); }}>
      <Field.Root invalid={invalid}>
        <Select.Root items={{ alpha: 'Rack Alpha', beta: 'Rack Beta' }} name="rack" onValueChange={setValue} required value={value}>
          <Select.Label>Deployment rack</Select.Label>
          <Select.Trigger aria-label="Deployment rack"><Select.Value placeholder="Choose a rack" /><Select.Icon aria-hidden="true"><ChevronDown /></Select.Icon></Select.Trigger>
          <Select.Portal><Select.Positioner><Select.Popup><Select.List>
            <Select.Item value="alpha"><Select.ItemText>Rack Alpha</Select.ItemText><Select.ItemIndicator aria-hidden="true">✓</Select.ItemIndicator></Select.Item>
            <Select.Item value="beta"><Select.ItemText>Rack Beta</Select.ItemText><Select.ItemIndicator aria-hidden="true">✓</Select.ItemIndicator></Select.Item>
          </Select.List></Select.Popup></Select.Positioner></Select.Portal>
        </Select.Root>
        {invalid ? <Field.Error match>Choose a deployment rack.</Field.Error> : null}
      </Field.Root>
      <Button type="submit">Deploy</Button>
    </Form>
  );
}` }]}
/>

<ComponentExampleTabs
  id="select-long-collection"
  title="길게 스크롤되는 collection"
  description="popup을 열어 제한된 height, scroll arrow, 긴 label과 강조된 focus 처리를 확인하세요."
  preview={<Stories.SelectLongCollection />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<Select.Root defaultValue="rack-1" items={racks}>
  <Select.Label>Long rack collection</Select.Label>
  <Select.Trigger aria-label="Long rack collection">
    <Select.Value /><Select.Icon aria-hidden="true"><ChevronDown /></Select.Icon>
  </Select.Trigger>
  <Select.Portal>
    <Select.Positioner>
      <Select.Popup>
        <Select.Arrow />
        <Select.ScrollUpArrow aria-label="Scroll up">↑</Select.ScrollUpArrow>
        <Select.List>
          {Object.entries(racks).map(([value, label]) => (
            <Select.Item key={value} value={value}><Select.ItemText>{label}</Select.ItemText></Select.Item>
          ))}
        </Select.List>
        <Select.ScrollDownArrow aria-label="Scroll down">↓</Select.ScrollDownArrow>
      </Select.Popup>
    </Select.Positioner>
  </Select.Portal>
</Select.Root>` }]}
/>

## API

trigger, popup, collection, item, arrow, backdrop, scroll-arrow의 전체 anatomy와 대응하는 prop/state type은 `@tinyrack/ui/components/select`에서 export됩니다. `Trigger`에 접근 가능한 이름을 제공하고 필요하면 `value`와 `open`을 독립적으로 제어하세요. 사용할 수 없는 control에는 `disabled`, 변경할 수 없는 focusable value에는 `readOnly`를 사용합니다.
