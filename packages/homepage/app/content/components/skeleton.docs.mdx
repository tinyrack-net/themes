import { ComponentPlayground } from '../../playground/playground.js';
export const meta = () => [{ title: "Skeleton · Tinyrack UI" }];

import { Skeleton } from '@tinyrack/ui/components/skeleton';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import * as Stories from './skeleton.demo.js';


# Skeleton

Layout-preserving loading placeholders with text, rectangle, and circle shapes.

## Contract

`shape` accepts `text`, `rectangle`, or `circle` and defaults to `text`. Skeletons shimmer by default; set `animate={false}` for a static placeholder. Dimensions come from layout or `style`.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/skeleton.css';", "import { Skeleton } from '@tinyrack/ui/components/skeleton';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs id="skeleton-basic" title="Decorative loading placeholder" previewLayout="stretch" preview={<Skeleton style={{ height: 48, width: '100%' }} />} sources={[{ label: 'React', language: 'tsx', code: String.raw`<Skeleton style={{ height: 48, width: '100%' }} />` }]} />

## Examples

<ComponentExampleTabs
  id="skeleton-shapes"
  title="Shapes"
  previewLayout="stretch"
  preview={(
    <div className="grid gap-5">
      <div className="grid gap-2"><strong>Text</strong><Skeleton shape="text" style={{ width: '70%' }} /><Skeleton shape="text" style={{ width: '45%' }} /></div>
      <div className="grid gap-2"><strong>Rectangle</strong><Skeleton shape="rectangle" style={{ height: 72, width: '100%' }} /></div>
      <div className="grid gap-2"><strong>Circle</strong><Skeleton shape="circle" style={{ height: 48, width: 48 }} /></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid gap-5">
  <div className="grid gap-2"><strong>Text</strong><Skeleton shape="text" style={{ width: '70%' }} /><Skeleton shape="text" style={{ width: '45%' }} /></div>
  <div className="grid gap-2"><strong>Rectangle</strong><Skeleton shape="rectangle" style={{ height: 72, width: '100%' }} /></div>
  <div className="grid gap-2"><strong>Circle</strong><Skeleton shape="circle" style={{ height: 48, width: 48 }} /></div>
</div>` }]}
/>

<ComponentExampleTabs
  id="skeleton-semantics"
  title="One loading status, decorative children"
  description="Unlabeled skeletons are aria-hidden by default. Put the accessible label on one region when the loading state needs an announcement."
  preview={(
    <div aria-busy="true" aria-label="Loading operator profile" className="grid gap-3" role="status">
      <Skeleton shape="circle" style={{ height: 48, width: 48 }} />
      <Skeleton shape="text" style={{ width: '60%' }} />
      <Skeleton shape="text" style={{ width: '35%' }} />
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div aria-busy="true" aria-label="Loading operator profile" className="grid gap-3" role="status">
  <Skeleton shape="circle" style={{ height: 48, width: 48 }} />
  <Skeleton shape="text" style={{ width: '60%' }} />
  <Skeleton shape="text" style={{ width: '35%' }} />
</div>` }]}
/>

<ComponentExampleTabs
  id="skeleton-motion"
  title="Motion"
  previewLayout="stretch"
  preview={(
    <div className="grid gap-5 md:grid-cols-2">
      <div className="grid gap-2"><strong>Animated</strong><Skeleton shape="rectangle" style={{ height: 72, width: '100%' }} /></div>
      <div className="grid gap-2"><strong>Static</strong><Skeleton animate={false} shape="rectangle" style={{ height: 72, width: '100%' }} /></div>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid gap-5 md:grid-cols-2">
  <div className="grid gap-2"><strong>Animated</strong><Skeleton shape="rectangle" style={{ height: 72, width: '100%' }} /></div>
  <div className="grid gap-2"><strong>Static</strong><Skeleton animate={false} shape="rectangle" style={{ height: 72, width: '100%' }} /></div>
</div>` }]}
/>

## API

An unlabeled Skeleton has `aria-hidden="true"`; adding `aria-label`, `aria-labelledby`, or an explicit status role opts into one live region. The shimmer is disabled automatically when the user prefers reduced motion.
