---
title: "File Tree"
description: "記述したドキュメント向けの意味のあるファイルと折りたたみ可能なディレクトリです。"
section: components
order: 57
---
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/file-tree.demo.js';

## コントラクト

`TRFileTree` は Markdown の unordered list を children として受け取ります。ネストした list はディレクトリになり、`/` で終わる list item は空のディレクトリになります。ディレクトリはプラットフォームの `details` disclosure を使います。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui tailwindcss react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/file-tree.css';", "import { TRFileTree } from '@tinyrack/ui/components/file-tree';"] }]} />

## 使用方法

<ComponentExampleTabs id="file-tree-basic" title="プロジェクトファイル" preview={<Stories.FileTreePreview />} sources={[{ label: 'MDX', language: 'mdx', code: String.raw`<TRFileTree>

- astro.config.mjs
- package.json
- src
  - components
    - Header.astro
    - Title.astro
  - pages/

</TRFileTree>` }]} />

## 例

別の Markdown list をネストしてディレクトリを作成します。`...` または `…` は placeholder として使用でき、`**bold**` や `` `code` `` などの inline Markdown も保持されます。

## API

`TRFileTree` は native `ul` props と React 19 refs を転送します。ディレクトリはデフォルトで開いており、個別に切り替えられます。
