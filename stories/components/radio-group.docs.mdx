import { Meta } from '@storybook/addon-docs/blocks';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import { baseUiExampleSources } from '../shared/base-ui-example-sources.js';
import * as Stories from './radio-group.stories';

<Meta of={Stories} />

# RadioGroup

Coordinates a mutually exclusive set of radio controls.

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Storybook controls cover `value` and `disabled`.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/radio-group.css';", "import '@tinyrack/ui/components/radio.css';", "import { RadioGroup } from '@tinyrack/ui/components/radio-group';", "import { Radio } from '@tinyrack/ui/components/radio';"] }]} />

## Usage

<ComponentExampleTabs
  id="radio-group-basic"
  title="Default"
  preview={<Stories.RadioGroupPreview value={'alpha'} disabled={false} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['radio-group'] }]}
/>

## Examples

<ComponentExampleTabs
  id="radio-group-states"
  title="State comparison"
  description="Use Controls above to inspect every supported public state."
  preview={<Stories.RadioGroupPreview value={'beta'} disabled={true} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['radio-group'] }]}
/>

## API

Every compound part and prop/state type is exported from `@tinyrack/ui/components/radio-group`; styles remain opt-in through the matching CSS subpath.
