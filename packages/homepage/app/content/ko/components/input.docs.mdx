---
title: "Input"
description: "label, ref, 유효성, form 제출을 보존하는 tokenized native input입니다."
section: components
order: 34
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/input.demo.js';

## 계약

Controls expose `label`, `value`, `placeholder`, `disabled`, `readOnly`, and `required`. Use a visible `<label>` and stable `id`; use `aria-describedby` for errors or instructions.

playground처럼 밀도 높은 컨트롤 표면에 input이 속하면 `uiSize="sm"`을 사용하세요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui tailwindcss react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/input.css';", "import '@tinyrack/ui/components/button.css';", "import { TRInput } from '@tinyrack/ui/components/input';", "import { TRButton } from '@tinyrack/ui/components/button';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs
  id="input-basic"
  title="label이 있는 Input"
  preview={<Stories.InputPreview defaultValue="" disabled={false} label="Rack 이름" placeholder="rack-alpha" readOnly={false} required={false} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<label htmlFor="rack-name">
  Rack name
  <TRInput id="rack-name" name="rack" placeholder="rack-alpha" />
</label>` }]}
/>

## 예시

<ComponentExampleTabs
  id="input-states"
  title="Input 상태"
  preview={(
    <div className="grid gap-5 sm:grid-cols-2">
      <Stories.InputPreview defaultValue="rack-a" disabled={false} label="Default" placeholder="rack-alpha" readOnly={false} required={false} />
      <Stories.InputPreview defaultValue="rack-b" disabled label="Disabled" placeholder="rack-alpha" readOnly={false} required={false} />
      <Stories.InputPreview defaultValue="rack-c" disabled={false} label="Read only" placeholder="rack-alpha" readOnly required={false} />
      <Stories.InputPreview defaultValue="" disabled={false} label="Required" placeholder="rack-alpha" readOnly={false} required />
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid gap-5 sm:grid-cols-2">
  <label>Default <TRInput defaultValue="rack-a" /></label>
  <label>Disabled <TRInput defaultValue="rack-b" disabled /></label>
  <label>Read only <TRInput defaultValue="rack-c" readOnly /></label>
  <label>Required <TRInput name="rack" required /></label>
</div>` }]}
/>

<ComponentExampleTabs
  id="input-validation"
  title="필수 제출과 복구"
  preview={<Stories.InputValidationPreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`function RequiredInput() {
  const [attempted, setAttempted] = useState(false);
  const [value, setValue] = useState('');
  const invalid = attempted && value.trim().length === 0;

  return (
    <TRForm noValidate onSubmit={(event) => { event.preventDefault(); event.currentTarget.checkValidity(); setAttempted(true); }}>
      <label htmlFor="required-name">Rack name</label>
      <TRInput
        aria-describedby={invalid ? 'required-name-error' : undefined}
        aria-invalid={invalid || undefined}
        id="required-name"
        name="rack"
        onValueChange={setValue}
        required
        value={value}
      />
      {invalid ? <p id="required-name-error" role="alert">Rack name is required.</p> : null}
      <TRButton type="submit">Continue</TRButton>
    </TRForm>
  );
}` }]}
/>

## API

`TRInput` forwards native input props, refs, events, `className`, `style`, and Base UI's `render` contract. Prefer `onValueChange(value, details)` when the next string value is what the state model needs; use native `onChange` when the event object itself matters. The `render` prop can replace the rendered element while Tinyrack merges behavior, state, ref, and `.tr-input` styling onto it.