---
title: "Input"
description: "ラベル、ref、妥当性、フォーム送信を保持するトークン化されたネイティブ input です。"
section: components
order: 34
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/input.demo.js';

## コントラクト

Controls expose `label`, `value`, `placeholder`, `disabled`, `readOnly`, and `required`. Use a visible `<label>` and stable `id`; use `aria-describedby` for errors or instructions.

playground のような密度の高いコントロールサーフェスに input を置く場合は `uiSize="sm"` を使います。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/input.css';", "import '@tinyrack/ui/components/button.css';", "import { Input } from '@tinyrack/ui/components/input';", "import { Button } from '@tinyrack/ui/components/button';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="input-basic"
  title="label 付き Input"
  preview={<Stories.InputPreview defaultValue="" disabled={false} label="Rack 名" placeholder="rack-alpha" readOnly={false} required={false} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<label htmlFor="rack-name">
  Rack name
  <Input id="rack-name" name="rack" placeholder="rack-alpha" />
</label>` }]}
/>

## 例

<ComponentExampleTabs
  id="input-states"
  title="Input の状態"
  preview={(
    <div className="grid gap-5 sm:grid-cols-2">
      <Stories.InputPreview defaultValue="rack-a" disabled={false} label="Default" placeholder="rack-alpha" readOnly={false} required={false} />
      <Stories.InputPreview defaultValue="rack-b" disabled label="Disabled" placeholder="rack-alpha" readOnly={false} required={false} />
      <Stories.InputPreview defaultValue="rack-c" disabled={false} label="Read only" placeholder="rack-alpha" readOnly required={false} />
      <Stories.InputPreview defaultValue="" disabled={false} label="Required" placeholder="rack-alpha" readOnly={false} required />
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<div className="grid gap-5 sm:grid-cols-2">
  <label>Default <Input defaultValue="rack-a" /></label>
  <label>Disabled <Input defaultValue="rack-b" disabled /></label>
  <label>Read only <Input defaultValue="rack-c" readOnly /></label>
  <label>Required <Input name="rack" required /></label>
</div>` }]}
/>

<ComponentExampleTabs
  id="input-validation"
  title="必須送信と復旧"
  preview={<Stories.InputValidationPreview />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`function RequiredInput() {
  const [attempted, setAttempted] = useState(false);
  const [value, setValue] = useState('');
  const invalid = attempted && value.trim().length === 0;

  return (
    <Form noValidate onSubmit={(event) => { event.preventDefault(); event.currentTarget.checkValidity(); setAttempted(true); }}>
      <label htmlFor="required-name">Rack name</label>
      <Input
        aria-describedby={invalid ? 'required-name-error' : undefined}
        aria-invalid={invalid || undefined}
        id="required-name"
        name="rack"
        onValueChange={setValue}
        required
        value={value}
      />
      {invalid ? <p id="required-name-error" role="alert">Rack name is required.</p> : null}
      <Button type="submit">Continue</Button>
    </Form>
  );
}` }]}
/>

## API

`Input` forwards native input props, refs, events, `className`, `style`, and Base UI's `render` contract. Prefer `onValueChange(value, details)` when the next string value is what the state model needs; use native `onChange` when the event object itself matters. The `render` prop can replace the rendered element while Tinyrack merges behavior, state, ref, and `.tr-input` styling onto it.