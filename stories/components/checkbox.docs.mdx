import { Meta } from '@storybook/addon-docs/blocks';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import { baseUiExampleSources } from '../shared/base-ui-example-sources.js';
import * as Stories from './checkbox.stories';

<Meta of={Stories} />

# Checkbox

A binary or indeterminate selection control with form semantics.

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Storybook controls cover `label`, `checked`, `disabled`.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/checkbox.css';", "import { Checkbox } from '@tinyrack/ui/components/checkbox';"] }]} />

## Usage

<ComponentExampleTabs
  id="checkbox-basic"
  title="Default"
  preview={<Stories.CheckboxPreview label={'Enable backups'} checked={true} disabled={false} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['checkbox'] }]}
/>

## Examples

<ComponentExampleTabs
  id="checkbox-states"
  title="State comparison"
  description="Use Controls above to inspect every supported public state."
  preview={<Stories.CheckboxPreview label={'Enable backups'} checked={false} disabled={true} />}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['checkbox'] }]}
/>

## API

Every compound part and prop/state type is exported from `@tinyrack/ui/components/checkbox`; styles remain opt-in through the matching CSS subpath.
