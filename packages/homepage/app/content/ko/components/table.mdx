---
title: "Table"
description: "반응형 overflow, 밀도, 선택적 행 줄무늬를 지원하는 의미 있는 데이터 테이블이에요."
section: components
order: 22
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRTable } from '@tinyrack/ui/components/table';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/table.demo.js';

## 핵심 속성

| 속성 | 값 | 기본값 |
| --- | --- | --- |
| `density` | `compact`, `comfortable`, `spacious` | `comfortable` |
| `striped` | `true`, `false` | `false` |
| parts | `Root`, `Caption`, `Header`, `Body`, `Footer`, `Row`, `Head`, `Cell` | compound API |

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/table.css';", "import { TRTable } from '@tinyrack/ui/components/table';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs
  id="table-basic"
  title="랙 상태"
  previewLayout="stretch"
  preview={(
    <TRTable.Root containerProps={{ 'aria-label': '랙 상태 스크롤 영역', tabIndex: 0 }} data-docs-example-item="" striped style={{ minWidth: '48rem' }}>
      <TRTable.Caption>랙 상태</TRTable.Caption>
      <TRTable.Header>
        <TRTable.Row>
          <TRTable.Head scope="col">랙</TRTable.Head>
          <TRTable.Head scope="col">위치</TRTable.Head>
          <TRTable.Head align="right" scope="col">노드</TRTable.Head>
          <TRTable.Head align="right" scope="col">부하</TRTable.Head>
          <TRTable.Head scope="col">상태</TRTable.Head>
        </TRTable.Row>
      </TRTable.Header>
      <TRTable.Body>
        {[
          ['랙 A', '서울 · 영역 1', 12, '42%', '정상'],
          ['랙 B', '서울 · 영역 2', 10, '67%', '정상'],
          ['랙 C', '부산 · 영역 1', 8, '81%', '성능 저하'],
          ['랙 D', '대전 · 영역 1', 6, '28%', '정상'],
          ['랙 E', '서울 · 영역 3', 12, '—', '점검 중'],
        ].map(([rack, location, nodes, load, status]) => (
          <TRTable.Row key={rack}>
            <TRTable.Head scope="row">{rack}</TRTable.Head>
            <TRTable.Cell>{location}</TRTable.Cell>
            <TRTable.Cell align="right">{nodes}</TRTable.Cell>
            <TRTable.Cell align="right">{load}</TRTable.Cell>
            <TRTable.Cell>{status}</TRTable.Cell>
          </TRTable.Row>
        ))}
      </TRTable.Body>
    </TRTable.Root>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/table.css';
import { TRTable } from '@tinyrack/ui/components/table';

const racks = [
  ['랙 A', '서울 · 영역 1', 12, '42%', '정상'],
  ['랙 B', '서울 · 영역 2', 10, '67%', '정상'],
  ['랙 C', '부산 · 영역 1', 8, '81%', '성능 저하'],
  ['랙 D', '대전 · 영역 1', 6, '28%', '정상'],
  ['랙 E', '서울 · 영역 3', 12, '—', '점검 중'],
] as const;

<TRTable.Root
  style={{ minWidth: '48rem' }}
  containerProps={{ 'aria-label': '랙 상태 스크롤 영역', tabIndex: 0 }}
  striped
>
  <TRTable.Caption>랙 상태</TRTable.Caption>
  <TRTable.Header>
    <TRTable.Row>
      <TRTable.Head scope="col">랙</TRTable.Head>
      <TRTable.Head scope="col">위치</TRTable.Head>
      <TRTable.Head align="right" scope="col">노드</TRTable.Head>
      <TRTable.Head align="right" scope="col">부하</TRTable.Head>
      <TRTable.Head scope="col">상태</TRTable.Head>
    </TRTable.Row>
  </TRTable.Header>
  <TRTable.Body>
    {racks.map(([rack, location, nodes, load, status]) => (
      <TRTable.Row key={rack}>
        <TRTable.Head scope="row">{rack}</TRTable.Head>
        <TRTable.Cell>{location}</TRTable.Cell>
        <TRTable.Cell align="right">{nodes}</TRTable.Cell>
        <TRTable.Cell align="right">{load}</TRTable.Cell>
        <TRTable.Cell>{status}</TRTable.Cell>
      </TRTable.Row>
    ))}
  </TRTable.Body>
</TRTable.Root>` }]}
/>

## 예시

<ComponentExampleTabs
  id="table-densities"
  title="밀도"
  description="지원하는 모든 밀도에서 같은 콘텐츠를 보여 줘요."
  previewLayout="stretch"
  preview={(
    <div className="grid gap-5" data-docs-example-item-count="3">
      {['compact', 'comfortable', 'spacious'].map((density) => (
        <div className="grid gap-2" key={density}>
          <strong>{density[0].toUpperCase() + density.slice(1)}</strong>
          <TRTable.Root containerProps={{ 'aria-label': `${density} 랙 밀도 표`, tabIndex: 0 }} data-docs-example-item="" density={density} style={{ minWidth: '42rem' }}>
            <TRTable.Caption>{density + ' 랙 상태'}</TRTable.Caption>
            <TRTable.Header>
              <TRTable.Row>
                <TRTable.Head scope="col">랙</TRTable.Head>
                <TRTable.Head scope="col">위치</TRTable.Head>
                <TRTable.Head align="right" scope="col">노드</TRTable.Head>
                <TRTable.Head scope="col">상태</TRTable.Head>
              </TRTable.Row>
            </TRTable.Header>
            <TRTable.Body>
              {[
                ['랙 A', '서울 · 영역 1', 12, '정상'],
                ['랙 C', '부산 · 영역 1', 8, '성능 저하'],
                ['랙 E', '서울 · 영역 3', 12, '점검 중'],
              ].map(([rack, location, nodes, status]) => (
                <TRTable.Row key={rack}>
                  <TRTable.Head scope="row">{rack}</TRTable.Head>
                  <TRTable.Cell>{location}</TRTable.Cell>
                  <TRTable.Cell align="right">{nodes}</TRTable.Cell>
                  <TRTable.Cell>{status}</TRTable.Cell>
                </TRTable.Row>
              ))}
            </TRTable.Body>
          </TRTable.Root>
        </div>
      ))}
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/table.css';
import { TRTable } from '@tinyrack/ui/components/table';

const densities = ['compact', 'comfortable', 'spacious'] as const;
const racks = [
  ['랙 A', '서울 · 영역 1', 12, '정상'],
  ['랙 C', '부산 · 영역 1', 8, '성능 저하'],
  ['랙 E', '서울 · 영역 3', 12, '점검 중'],
] as const;

<div className="grid gap-5">
  {densities.map((density) => (
    <div className="grid gap-2" key={density}>
      <strong>{density[0].toUpperCase() + density.slice(1)}</strong>
      <TRTable.Root
        style={{ minWidth: '42rem' }}
        containerProps={{ 'aria-label': density + ' rack density table', tabIndex: 0 }}
        density={density}
      >
        <TRTable.Caption>{density + ' rack status'}</TRTable.Caption>
        <TRTable.Header>
          <TRTable.Row>
            <TRTable.Head scope="col">랙</TRTable.Head>
            <TRTable.Head scope="col">위치</TRTable.Head>
            <TRTable.Head align="right" scope="col">노드</TRTable.Head>
            <TRTable.Head scope="col">상태</TRTable.Head>
          </TRTable.Row>
        </TRTable.Header>
        <TRTable.Body>
          {racks.map(([rack, location, nodes, status]) => (
            <TRTable.Row key={rack}>
              <TRTable.Head scope="row">{rack}</TRTable.Head>
              <TRTable.Cell>{location}</TRTable.Cell>
              <TRTable.Cell align="right">{nodes}</TRTable.Cell>
              <TRTable.Cell>{status}</TRTable.Cell>
            </TRTable.Row>
          ))}
        </TRTable.Body>
      </TRTable.Root>
    </div>
  ))}
</div>` }]}
/>

<ComponentExampleTabs
  id="table-striped-footer"
  title="줄무늬 행과 바닥글"
  previewLayout="stretch"
  preview={(
    <TRTable.Root containerProps={{ 'aria-label': '서비스 합계 표', tabIndex: 0 }} data-docs-example-item="" density="compact" striped style={{ minWidth: '48rem' }}>
      <TRTable.Caption>서비스 합계</TRTable.Caption>
      <TRTable.Header>
        <TRTable.Row>
          <TRTable.Head scope="col">서비스</TRTable.Head>
          <TRTable.Head scope="col">담당</TRTable.Head>
          <TRTable.Head scope="col">리전</TRTable.Head>
          <TRTable.Head align="right" scope="col">복제본</TRTable.Head>
          <TRTable.Head align="right" scope="col">분당 요청</TRTable.Head>
        </TRTable.Row>
      </TRTable.Header>
      <TRTable.Body>
        {[
          ['API 게이트웨이', '플랫폼', '전역', 3, '18,420'],
          ['인증', '아이덴티티', '서울', 4, '9,870'],
          ['이벤트 워커', '데이터', '부산', 6, '7,310'],
          ['이미지 프록시', '미디어', '전역', 3, '5,640'],
          ['검색 인덱서', '데이터', '서울', 2, '2,180'],
          ['메트릭 수집기', 'SRE', '전체 리전', 5, '12,760'],
        ].map(([service, owner, region, replicas, requests]) => (
          <TRTable.Row key={service}>
            <TRTable.Head scope="row">{service}</TRTable.Head>
            <TRTable.Cell>{owner}</TRTable.Cell>
            <TRTable.Cell>{region}</TRTable.Cell>
            <TRTable.Cell align="right">{replicas}</TRTable.Cell>
            <TRTable.Cell align="right">{requests}</TRTable.Cell>
          </TRTable.Row>
        ))}
      </TRTable.Body>
      <TRTable.Footer>
        <TRTable.Row>
          <TRTable.Head colSpan={3} scope="row">합계</TRTable.Head>
          <TRTable.Cell align="right">23</TRTable.Cell>
          <TRTable.Cell align="right">56,180</TRTable.Cell>
        </TRTable.Row>
      </TRTable.Footer>
    </TRTable.Root>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/table.css';
import { TRTable } from '@tinyrack/ui/components/table';

const services = [
  ['API 게이트웨이', '플랫폼', '전역', 3, '18,420'],
  ['인증', '아이덴티티', '서울', 4, '9,870'],
  ['이벤트 워커', '데이터', '부산', 6, '7,310'],
  ['이미지 프록시', '미디어', '전역', 3, '5,640'],
  ['검색 인덱서', '데이터', '서울', 2, '2,180'],
  ['메트릭 수집기', 'SRE', '전체 리전', 5, '12,760'],
] as const;

<TRTable.Root
  style={{ minWidth: '48rem' }}
  containerProps={{ 'aria-label': '서비스 합계 표', tabIndex: 0 }}
  density="compact"
  striped
>
  <TRTable.Caption>서비스 합계</TRTable.Caption>
  <TRTable.Header>
    <TRTable.Row>
      <TRTable.Head scope="col">서비스</TRTable.Head>
      <TRTable.Head scope="col">담당</TRTable.Head>
      <TRTable.Head scope="col">리전</TRTable.Head>
      <TRTable.Head align="right" scope="col">복제본</TRTable.Head>
      <TRTable.Head align="right" scope="col">분당 요청</TRTable.Head>
    </TRTable.Row>
  </TRTable.Header>
  <TRTable.Body>
    {services.map(([service, owner, region, replicas, requests]) => (
      <TRTable.Row key={service}>
        <TRTable.Head scope="row">{service}</TRTable.Head>
        <TRTable.Cell>{owner}</TRTable.Cell>
        <TRTable.Cell>{region}</TRTable.Cell>
        <TRTable.Cell align="right">{replicas}</TRTable.Cell>
        <TRTable.Cell align="right">{requests}</TRTable.Cell>
      </TRTable.Row>
    ))}
  </TRTable.Body>
  <TRTable.Footer>
    <TRTable.Row>
      <TRTable.Head colSpan={3} scope="row">합계</TRTable.Head>
      <TRTable.Cell align="right">23</TRTable.Cell>
      <TRTable.Cell align="right">56,180</TRTable.Cell>
    </TRTable.Row>
  </TRTable.Footer>
</TRTable.Root>` }]}
/>

<ComponentExampleTabs
  id="table-overflow-empty"
  title="넓은 표의 넘침"
  description="넓은 표를 이동해야 할 때 가로 스크롤 힌트, 접근 가능한 이름, 키보드 포커스 지점을 제공해요."
  previewLayout="stretch"
  preview={<div data-docs-example-item-count="1"><Stories.TableOverflowState /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/table.css';
import { TRTable } from '@tinyrack/ui/components/table';

export function DeploymentTable() {
  return <>
  <p>이름이 있는 영역 안에서 가로로 스크롤해 모든 열을 확인하세요.</p>
  <TRTable.Root
    style={{ minWidth: '48rem' }}
    containerProps={{ 'aria-label': '긴 배포 기록', tabIndex: 0 }}
  >
    <TRTable.Caption>긴 배포 기록</TRTable.Caption>
    <TRTable.Header>
      <TRTable.Row>
        <TRTable.Head scope="col">서비스</TRTable.Head>
        <TRTable.Head scope="col">아티팩트</TRTable.Head>
        <TRTable.Head scope="col">담당</TRTable.Head>
      </TRTable.Row>
    </TRTable.Header>
    <TRTable.Body>
      <TRTable.Row>
        <TRTable.Head scope="row">게이트웨이</TRTable.Head>
        <TRTable.Cell>registry.example.internal/platform/gateway:2026.07.14-release-candidate</TRTable.Cell>
        <TRTable.Cell>플랫폼 안정성</TRTable.Cell>
      </TRTable.Row>
    </TRTable.Body>
  </TRTable.Root>
  </>;
}` }]}
/>

<ComponentExampleTabs
  id="table-empty-state"
  title="빈 상태"
  description="빈 상태 안내는 Body 안의 Row와 Cell에 두고 colSpan으로 표시 열을 합쳐요."
  previewLayout="stretch"
  preview={<div data-docs-example-item-count="1"><Stories.TableEmptyState /></div>}
  sources={[{ label: 'React', language: 'tsx', code: Stories.tableEmptySource.replace('Queued deployments', '대기 중인 배포').replace('No deployments are queued.', '대기 중인 배포가 없어요.') }]}
/>

## API

`TRTable.Root`는 네이티브 표를 넘침 컨테이너로 감싸요. 키보드로 넓은 표를 이동해야 하면 `containerProps`로 해당 영역에 접근 가능한 이름과 `tabIndex={0}`을 제공하세요. `containerRef`는 같은 래퍼를 가리키고 일반 `ref`는 계속 `<table>`을 가리켜요.

| 파트 | 네이티브 요소와 속성 |
| --- | --- |
| `TRTable.Root` | `<table>` 속성과 함께 `density`, `striped`, `containerClassName`, `containerProps`, `containerRef`를 받아요. `ref`는 표를 가리켜요. |
| `TRTable.Caption` | 네이티브 `<caption>` 속성이에요. `ref`는 캡션을 가리켜요. 표의 접근 가능한 제목이나 설명에 사용해요. |
| `TRTable.Header` | 네이티브 `<thead>` 속성이에요. `ref`는 머리글 그룹을 가리켜요. |
| `TRTable.Body` | 네이티브 `<tbody>` 속성이에요. `ref`는 본문 그룹을 가리켜요. |
| `TRTable.Footer` | 네이티브 `<tfoot>` 속성이에요. `ref`는 바닥글 그룹을 가리켜요. |
| `TRTable.Row` | 네이티브 `<tr>` 속성이에요. `ref`는 행을 가리켜요. |
| `TRTable.Head` | 네이티브 `<th>` 속성이에요. `ref`는 머리글 셀을 가리켜요. 열 머리글에는 `scope="col"`, 행 머리글에는 `scope="row"`를 설정해요. |
| `TRTable.Cell` | 네이티브 `<td>` 속성이에요. `ref`는 데이터 셀을 가리켜요. `colSpan`, `rowSpan`, `headers` 같은 네이티브 속성을 지원해요. |
