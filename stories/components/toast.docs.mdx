import { Meta } from '@storybook/addon-docs/blocks';
import { Toast } from '../../src/components/toast/index.js';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import { ToastDemo, ToastPositionGallery, ToastVariantGallery } from './toast.stories';
import * as Stories from './toast.stories';

<Meta of={Stories} />

# Toast

Transient status notifications with semantic variants, actions, dismissal, and six viewport positions.

## Contract

| Axis | Values | Default |
| --- | --- | --- |
| `variant` | `neutral`, `info`, `success`, `warning`, `danger` | toast `type` or `neutral` |
| `position` | block start/end × inline start/center/end | `block-end-inline-end` |
| parts | `Provider`, `Portal`, `Viewport`, `Root`, `Title`, `Description`, `Action`, `Close` | compound API |

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/toast.css';", "import { Toast, useToastManager } from '@tinyrack/ui/components/toast';"] }]} />

## Usage

<ComponentExampleTabs
  id="toast-basic"
  title="Managed notification"
  preview={<Toast.Provider><ToastDemo /></Toast.Provider>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`function ToastButton() {
  const manager = useToastManager();
  return <Button onClick={() => manager.add({
    title: 'Deployment complete',
    description: 'Rack A is healthy.',
    type: 'success',
  })}>Show toast</Button>;
}

<Toast.Provider>
  <ToastButton />
  <Toast.Portal>
    <Toast.Viewport>
      {manager.toasts.map((toast) => (
        <Toast.Root key={toast.id} toast={toast}>...</Toast.Root>
      ))}
    </Toast.Viewport>
  </Toast.Portal>
</Toast.Provider>` }]}
/>

## Examples

<ComponentExampleTabs
  id="toast-variants"
  title="Status variants"
  description="Choose a semantic variant to publish it through the real toast manager."
  preview={<Toast.Provider><ToastVariantGallery /></Toast.Provider>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`function SuccessToastDemo() {
  const manager = useToastManager();
  return <>
    <Button onClick={() => manager.add({
      title: 'Deployment complete',
      description: 'Rack A status notification.',
      type: 'success',
    })}>Deployment complete</Button>
    <Toast.Portal>
      <Toast.Viewport>
        {manager.toasts.map((toast) => (
          <Toast.Root key={toast.id} toast={toast}>...</Toast.Root>
        ))}
      </Toast.Viewport>
    </Toast.Portal>
  </>;
}

<Toast.Provider><SuccessToastDemo /></Toast.Provider>` }]}
/>

<ComponentExampleTabs
  id="toast-positions"
  title="Viewport positions"
  description="Choose a logical position to publish a real toast there. The same positions work in left-to-right and right-to-left layouts."
  previewLayout="stretch"
  preview={<ToastPositionGallery />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`function PositionedToastDemo() {
  const manager = useToastManager();
  return <>
    <Button onClick={() => manager.add({ title: 'Start / Start' })}>
      Start / Start
    </Button>
    <Toast.Portal>
      <Toast.Viewport position="block-start-inline-start">
        {manager.toasts.map((toast) => (
          <Toast.Root key={toast.id} toast={toast}>...</Toast.Root>
        ))}
      </Toast.Viewport>
    </Toast.Portal>
  </>;
}

<Toast.Provider><PositionedToastDemo /></Toast.Provider>` }]}
/>

## API

Create or consume a toast manager inside `Provider`; Base UI owns queueing, timers, swipe gestures, focus, and live-region semantics.
