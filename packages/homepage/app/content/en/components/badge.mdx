---
title: "Badge"
description: "Compact status labels with semantic color and density axes."
section: components
group: feedback-status
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { CircleCheck, Clock } from 'lucide-react';
import { TRBadge } from '@tinyrack/ui/components/badge';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/badge.demo.js';

## Contract

| Axis | Values | Default |
| --- | --- | --- |
| `variant` | `neutral`, `info`, `success`, `warning`, `danger` | `neutral` |
| `uiSize` | `sm`, `md`, `lg` | `md` |

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/badge.css';"],
      codeImports: ["import { TRBadge } from '@tinyrack/ui/components/badge';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs id="badge-basic" title="Status label" preview={<TRBadge data-docs-example-item="" variant="success">Healthy</TRBadge>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRBadge } from '@tinyrack/ui/components/badge';

<TRBadge variant="success">Healthy</TRBadge>` }]} />

## Examples

<ComponentExampleTabs
  id="badge-icon-and-long-label"
  title="Icon and constrained long label"
  description="Icons are decorative when the adjacent text communicates the same status; long labels wrap inside their parent."
  preview={(
    <div className="grid max-w-64 gap-3">
      <TRBadge data-docs-example-item="" variant="success"><CircleCheck aria-hidden size="1em" />Healthy</TRBadge>
      <TRBadge data-docs-example-item="" variant="warning"><Clock aria-hidden size="1em" />Waiting for maintenance approval from operator</TRBadge>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRBadge } from '@tinyrack/ui/components/badge';
import { CircleCheck, Clock } from 'lucide-react';

<div className="grid max-w-64 gap-3">
  <TRBadge variant="success">
    <CircleCheck aria-hidden size="1em" />
    Healthy
  </TRBadge>
  <TRBadge variant="warning">
    <Clock aria-hidden size="1em" />
    Waiting for maintenance approval from operator
  </TRBadge>
</div>` }]}
/>

<ComponentExampleTabs
  id="badge-size-variant-matrix"
  title="Status variants"
  description="Choose the variant from the status meaning, not only its color."
  previewLayout="stretch"
  preview={(
    <div className="flex flex-wrap gap-2">
      <TRBadge data-docs-example-item="" variant="neutral">Neutral</TRBadge><TRBadge data-docs-example-item="" variant="info">Info</TRBadge><TRBadge data-docs-example-item="" variant="success">Success</TRBadge><TRBadge data-docs-example-item="" variant="warning">Warning</TRBadge><TRBadge data-docs-example-item="" variant="danger">Danger</TRBadge>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRBadge } from '@tinyrack/ui/components/badge';

<div className="flex flex-wrap gap-2">
  <TRBadge variant="neutral">Neutral</TRBadge><TRBadge variant="info">Info</TRBadge><TRBadge variant="success">Success</TRBadge><TRBadge variant="warning">Warning</TRBadge><TRBadge variant="danger">Danger</TRBadge>
</div>` }]}
/>

<ComponentExampleTabs
  id="badge-sizes"
  title="Sizes"
  description="Match badge density to nearby text and controls."
  preview={<div className="flex items-center gap-3"><TRBadge data-docs-example-item="" uiSize="sm">Small</TRBadge><TRBadge data-docs-example-item="" uiSize="md">Medium</TRBadge><TRBadge data-docs-example-item="" uiSize="lg">Large</TRBadge></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRBadge } from '@tinyrack/ui/components/badge';

<div className="flex items-center gap-3">
  <TRBadge uiSize="sm">Small</TRBadge><TRBadge uiSize="md">Medium</TRBadge><TRBadge uiSize="lg">Large</TRBadge>
</div>` }]}
/>

## API

### Options

| Prop | Type | Default | Purpose |
| --- | --- | --- | --- |
| `variant` | `'neutral' \| 'info' \| 'success' \| 'warning' \| 'danger'` | `'neutral'` | Selects the status meaning and color treatment. |
| `uiSize` | `'sm' \| 'md' \| 'lg'` | `'md'` | Sets badge density. |

### HTML and accessibility

`TRBadge` renders a `span` and forwards its native props, events, styles, and React 19 `ref`. It exports `TRBadgeProps`, `TRBadgeUiSize`, and `TRBadgeVariant`. It does not assign a role or live-region behavior. Keep status text visible; an icon alone needs an application-provided accessible label.
