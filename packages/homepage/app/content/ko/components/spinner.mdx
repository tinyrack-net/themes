---
title: "Spinner"
description: "크기와 의미별 색상을 제공하는 비확정 진행 상태예요."
section: components
group: feedback-status
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRButton } from '@tinyrack/ui/components/button';
import { TRSpinner } from '@tinyrack/ui/components/spinner';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/spinner.demo.js';

## 핵심 속성

| 속성 | 값 | 기본값 |
| --- | --- | --- |
| `uiSize` | `sm`, `md`, `lg` | `md` |
| `variant` | `current`, `muted`, `primary`, `danger` | `current` |
| `label` | 접근 가능한 상태 텍스트 | `Loading` |
| `decorative` | `true`, `false` | `false` |

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/spinner.css';"],
      codeImports: ["import { TRSpinner } from '@tinyrack/ui/components/spinner';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="spinner-basic" title="로딩 상태" preview={<TRSpinner data-docs-example-item="" label="서버 불러오는 중" variant="primary" />} sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSpinner } from '@tinyrack/ui/components/spinner';

<TRSpinner label="서버 불러오는 중" variant="primary" />` ,
      styles: { code: "@import '@tinyrack/ui/components/spinner.css';" },
    },
  ]}
/>

## 예시

<ComponentExampleTabs
  id="spinner-size-variant-matrix"
  title="크기"
  description="주변 텍스트나 컨트롤에 스피너 크기를 맞추세요."
  preview={<div className="flex flex-wrap items-end gap-5"><span className="grid justify-items-center gap-1"><TRSpinner data-docs-example-item="" decorative uiSize="sm" /><span>작게</span></span><span className="grid justify-items-center gap-1"><TRSpinner data-docs-example-item="" decorative uiSize="md" /><span>중간</span></span><span className="grid justify-items-center gap-1"><TRSpinner data-docs-example-item="" decorative uiSize="lg" /><span>크게</span></span></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSpinner } from '@tinyrack/ui/components/spinner';

export function SpinnerSizes() {
  return <div className="flex items-end gap-5">{(['sm', 'md', 'lg']).map((uiSize) => <TRSpinner decorative key={uiSize} uiSize={uiSize} />)}</div>;
}` ,
      styles: { code: "@import '@tinyrack/ui/components/spinner.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="spinner-variants"
  title="색상 변형"
  description="주변 텍스트 색상을 따를 때는 `current`, 강조를 낮출 때는 `muted`, 진행 중인 작업에는 `primary`, 실패한 작업에는 `danger`를 사용하세요."
  preview={<div className="flex flex-wrap items-center gap-5" data-docs-example-item-count="4">{(['current', 'muted', 'primary', 'danger']).map((variant) => <span className="grid justify-items-center gap-1" key={variant}><TRSpinner data-docs-example-item="" decorative variant={variant} /><span>{variant}</span></span>)}</div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRSpinner } from '@tinyrack/ui/components/spinner';

export function SpinnerVariants() {
  return <div className="flex items-center gap-5">{(['current', 'muted', 'primary', 'danger']).map((variant) => <span className="grid justify-items-center gap-1" key={variant}><TRSpinner decorative variant={variant} /><span>{variant}</span></span>)}</div>;
}` ,
      styles: { code: "@import '@tinyrack/ui/components/spinner.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="spinner-status-ownership"
  title="독립 상태 또는 컨트롤 장식"
  description="독립 `TRSpinner`가 상태 알림을 담당해요. 이름이 있는 `TRButton`은 로딩 상태를 담당하고 중첩된 스피너를 숨겨요. 모션 감소 설정에서는 회전이 멈춰요."
  preview={(
    <div className="flex flex-wrap items-center gap-4">
      <TRSpinner data-docs-example-item="" label="서버 목록 불러오는 중" variant="primary" />
      <TRButton data-docs-example-item="" aria-busy="true" disabled><TRSpinner decorative uiSize="sm" />목록 새로 고치는 중</TRButton>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRButton } from '@tinyrack/ui/components/button';
import { TRSpinner } from '@tinyrack/ui/components/spinner';

<div className="flex flex-wrap items-center gap-4">
  <TRSpinner label="서버 목록 불러오는 중" variant="primary" />
  <TRButton aria-busy="true" disabled><TRSpinner decorative uiSize="sm" />목록 새로 고치는 중</TRButton>
</div>` }]}
/>

<ComponentExampleTabs
  id="spinner-task-lifecycle"
  title="작업 수명 주기"
  description="작업을 시작하고 완료한 뒤 초기화하며, 작업 중일 때만 공지 상태가 존재하는지 확인하세요."
  preview={<div data-docs-example-item-count="1"><Stories.SpinnerLifecyclePreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRButton } from '@tinyrack/ui/components/button';
import { TRSpinner } from '@tinyrack/ui/components/spinner';
import { useState } from 'react';

function DeploymentStatus() {
  const [state, setState] = useState('idle');
  return (
    <div className="grid justify-items-start gap-3">
      <div className="flex flex-wrap gap-2">
        <TRButton disabled={state === 'running'} onClick={() => setState('running')}>
          작업 시작
        </TRButton>
        <TRButton appearance="outline" disabled={state !== 'running'} onClick={() => setState('complete')}>
          작업 완료
        </TRButton>
        <TRButton appearance="ghost" disabled={state === 'idle'} onClick={() => setState('idle')}>
          초기화
        </TRButton>
      </div>
      {state === 'running' ? <TRSpinner label="랙 배포 중" variant="primary" /> : null}
      {state === 'idle' ? <output aria-live="polite">작업이 시작되지 않았어요.</output> : null}
      {state === 'complete' ? <output aria-live="polite">작업이 완료됐어요.</output> : null}
    </div>
  );
}` }]}
/>

## API

TRSpinner는 기본적으로 `role="status"`를 노출하므로 작업에 맞는 비어 있지 않은 `label`을 지정하세요. `label`이 비어 있으면 `Loading`을 대신 사용해요. 이미 접근 가능한 이름이 있는 로딩 컨트롤이나 상태 영역 안에서는 `decorative`를 설정해 하나의 요소만 알리세요. 작업이 진행되는 동안에만 Spinner를 렌더링하세요. 필요하면 업데이트되는 영역에 `aria-busy="true"`를 설정하고, 완료 소식은 그 영역에서 알려 주세요. 사용자가 동작 줄이기를 요청하면 회전이 멈춰요.

| CSS 토큰 | 기본값 | 용도 |
| --- | --- | --- |
| `--tr-spinner-size` | 크기 레시피 | 표시기 지름 |
| `--tr-spinner-stroke-width` | `--tinyrack-spinner-stroke-width` | 링 두께 |
| `--tr-spinner-color` | 의미형 변형 색상 | 활성 링과 트랙 색상 |
| `--tinyrack-spinner-size-sm` | 기반 치수 | 작은 지름 |
| `--tinyrack-spinner-size-md` | 기반 치수 | 중간 지름 |
| `--tinyrack-spinner-size-lg` | 기반 치수 | 큰 지름 |
| `--tinyrack-spinner-track-opacity` | 기반 치수 | 비활성 링 불투명도 |
