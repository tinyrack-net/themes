---
title: "NavigationMenu"
description: "사이트 수준 링크와 선택 사항인 풍부한 플라이아웃 콘텐츠를 제공해요."
section: components
order: 38
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/navigation-menu.demo.js';

## 핵심 속성

| 판단 기준 | 선택 방법 |
| --- | --- |
| 목적지 | 직접 이동은 `Link`, 관련 링크 묶음은 `Trigger`와 `Content`를 써요. |
| 방향 | `horizontal`이나 `vertical`을 쓰며 기본값은 `horizontal`이에요. |
| 위치 | `side`는 top/right/bottom/left, `align`은 start/center/end 중에서 골라요. |
| 좁은 화면 | 같은 목적지를 담은 드로어로 데스크톱 합성을 교체해요. |

작업 중심 플라이아웃을 만들고 바로 이동할 목적지는 링크로 유지해요. 사이트 탐색용이며 애플리케이션 명령 메뉴 막대가 아니에요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/navigation-menu.css';"],
      codeImports: ["import { TRNavigationMenu } from '@tinyrack/ui/components/navigation-menu';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="navigation-menu-basic" title="사이트 헤더 탐색" previewLayout="stretch" preview={<div data-docs-example-item-count="1"><Stories.NavigationMenuPreview active={false} align="center" disabled={false} locale="ko" navigationLabel="사이트 탐색" openSection="none" orientation="horizontal" side="bottom" /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRNavigationMenu } from '@tinyrack/ui/components/navigation-menu';
export function SiteHeader() { return <TRNavigationMenu.Root aria-label="사이트를 탐색해요"><TRNavigationMenu.List><TRNavigationMenu.Item><TRNavigationMenu.Link active href="/pricing">요금을 봐요</TRNavigationMenu.Link></TRNavigationMenu.Item></TRNavigationMenu.List></TRNavigationMenu.Root>; }` ,
      styles: { code: "@import '@tinyrack/ui/components/link.css';\n@import '@tinyrack/ui/components/navigation-menu.css';" },
    },
  ]}
/>

## 예시

### 목적지 상태를 표시해요

<ComponentExampleTabs id="navigation-menu-states" title="비활성, 현재 위치, 사용 불가" preview={<div data-docs-example-item-count="3"><Stories.NavigationMenuStateComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`export function ControlledNavigation() { const [value, setValue] = useState<string | null>(null); return <TRNavigationMenu.Root value={value} onValueChange={setValue}><TRNavigationMenu.Link active href="/pricing">요금 보기</TRNavigationMenu.Link><TRNavigationMenu.Trigger disabled>제품 사용 불가</TRNavigationMenu.Trigger></TRNavigationMenu.Root>; }` }]} />

### 탐색 방향을 골라요

<ComponentExampleTabs id="navigation-menu-orientations" title="가로와 세로" preview={<div data-docs-example-item-count="2"><Stories.NavigationMenuOrientationComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<><TRNavigationMenu.Root orientation="horizontal">{items}</TRNavigationMenu.Root><TRNavigationMenu.Root orientation="vertical">{items}</TRNavigationMenu.Root></>` }]} />

### 플라이아웃을 배치해요

<ComponentExampleTabs id="navigation-menu-position-sides" title="위, 오른쪽, 아래, 왼쪽" preview={<div data-docs-example-item-count="4"><Stories.NavigationMenuSideComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`{(['top', 'right', 'bottom', 'left'] as const).map((side) => <TRNavigationMenu.Positioner key={side} side={side}>{popup}</TRNavigationMenu.Positioner>)}` }]} />

<ComponentExampleTabs id="navigation-menu-position-alignments" title="시작, 가운데, 끝 정렬" preview={<div data-docs-example-item-count="3"><Stories.NavigationMenuAlignComparison /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`{(['start', 'center', 'end'] as const).map((align) => <TRNavigationMenu.Positioner align={align} key={align}>{popup}</TRNavigationMenu.Positioner>)}` }]} />

### 좁은 화면에서도 목적지를 유지해요

<ComponentExampleTabs id="navigation-menu-responsive-alternative" title="데스크톱 플라이아웃과 모바일 드로어" previewLayout="stretch" preview={<div data-docs-example-item-count="1"><Stories.NavigationMenuResponsivePreview locale="ko" /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRDrawer } from '@tinyrack/ui/components/drawer';
import { TRLink } from '@tinyrack/ui/components/link';
export function ResponsiveNavigation() { return <><div className="hidden md:block">{desktopNavigation}</div><div className="md:hidden"><TRDrawer.Root swipeDirection="right"><TRDrawer.Trigger>탐색을 열어요</TRDrawer.Trigger><TRDrawer.Portal><TRDrawer.Popup><nav><TRLink href="/pricing">요금을 봐요</TRLink></nav></TRDrawer.Popup></TRDrawer.Portal></TRDrawer.Root></div></>; }` ,
      styles: { code: "@import '@tinyrack/ui/components/drawer.css';\n@import '@tinyrack/ui/components/link.css';" },
    },
  ]}
/>

### 키보드 모델을 따라요

<ComponentExampleTabs id="navigation-menu-keyboard" title="순환 초점과 플라이아웃 닫기" description="방향은 방향키 축을 정해요. Enter나 Space는 콘텐츠를 열고, Escape는 닫은 뒤 트리거에 초점을 돌려줘요. Tab은 플라이아웃 링크로 들어가요." preview={<div data-docs-example-item-count="1"><Stories.NavigationMenuKeyboardPreview /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TRNavigationMenu.Root aria-label="키보드로 탐색해요" delay={100} closeDelay={200}>{items}</TRNavigationMenu.Root>` }]} />

## API

전체 파트는 `Root`, `List`, `Item`, `Trigger`, `Icon`, `Content`, `Portal`, `Backdrop`, `Positioner`, `Popup`, `Arrow`, `Viewport`, `Link`예요. 모든 파트와 prop·상태 타입을 이름으로 내보내요.

`Root`는 `value`와 `onValueChange`를 쓰는 제어 상태, `defaultValue`를 쓰는 비제어 상태, `orientation`, 포인터 `delay`, `closeDelay`를 소유해요. `Trigger`는 비활성화할 수 있어요. `Icon`은 기본으로 `ChevronDown`을 렌더링하고 사용자 지정 자식을 받아요. `Link.active`는 `aria-current="page"`를 설정하고 `closeOnClick`은 플라이아웃을 닫아요.

`Positioner.side`는 `top`, `right`, `bottom`, `left`를 받고 `align`은 `start`, `center`, `end`를 받아요. `sideOffset` 기본값은 `8`이에요. 충돌 회피는 정렬을 옮기고 방향을 뒤집어요. `Portal`은 사용자 지정 컨테이너를 받고, `Popup`은 표면을 소유하며 `Viewport`는 활성 `Content`를 받아요.

공개 CSS 경로를 가져오세요. 토큰에는 `--tr-navigation-menu-popup-max-width`가 있어요. 합성은 SSR과 하이드레이션을 지원하고 포털 콘텐츠는 하이드레이션 뒤 컨테이너에 마운트해요. 좁은 화면에서는 데스크톱 키보드 모델을 압축하지 말고 같은 목적지를 터치 크기 드로어에 렌더링해요.
