---
title: "Steps"
description: "작성한 문서를 위한 번호가 매겨진 의미 있는 절차입니다."
section: components
order: 59
---
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/steps.demo.js';

## 계약

`Root`는 순서 있는 list를 렌더링하고 `Item`은 list item을 렌더링합니다. 작성한 MDX children은 그대로 유지됩니다.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/steps.css';", "import { Steps } from '@tinyrack/ui/components/steps';"] }]} />

## 사용법

<ComponentExampleTabs id="steps-basic" title="설치 절차" preview={<Stories.StepsPreview count={3} />} sources={[{ label: 'React', language: 'tsx', code: String.raw`<Steps.Root><Steps.Item>Install the package</Steps.Item><Steps.Item>Create the config</Steps.Item></Steps.Root>` }]} />

## 예시

각 단계 안에 heading, paragraph, code block을 직접 배치하세요.

## API

두 part는 native ordered-list props, list-item props, React 19 refs를 전달합니다.
