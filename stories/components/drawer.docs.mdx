import { Meta } from '@storybook/addon-docs/blocks';
import { Drawer } from '../../src/components/drawer/index.js';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import { baseUiExampleSources } from '../shared/base-ui-example-sources.js';
import * as Stories from './drawer.stories';

<Meta of={Stories} />

# Drawer

A swipe-aware task surface anchored to a viewport edge.

## Contract

This Tinyrack wrapper preserves Base UI behavior, React 19 refs, native props, events, and the `render` contract. Storybook controls cover `label`, `open`, `modal`, `swipeDirection`, and the controlled `activeSnapPoint` mapping.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/drawer.css';", "import { Drawer } from '@tinyrack/ui/components/drawer';"] }]} />

## Usage

<ComponentExampleTabs
  id="drawer-basic"
  title="Default"
  preview={<Drawer.Root><Drawer.Trigger>Open settings</Drawer.Trigger><Drawer.Portal><Drawer.Backdrop /><Drawer.Viewport><Drawer.Popup><Drawer.Content><Drawer.Title>Rack settings</Drawer.Title><Drawer.Description>Update deployment preferences.</Drawer.Description><Drawer.Close>Close</Drawer.Close></Drawer.Content></Drawer.Popup></Drawer.Viewport></Drawer.Portal></Drawer.Root>}
  sources={[{ label: 'React', language: 'tsx', code: baseUiExampleSources['drawer'] }]}
/>

## Examples

<ComponentExampleTabs
  id="drawer-states"
  title="State comparison"
  description="Open the modal drawer and close it with its button, Escape, backdrop press, or a supported swipe gesture; focus returns to the trigger."
  preview={<Stories.DrawerPreview activeSnapPoint={'compact'} label={'Open snap drawer'} open={false} modal={true} swipeDirection={'down'} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<Drawer.Root
  defaultSnapPoint={0.35}
  snapPoints={[0.35, 0.7, 1]}
  swipeDirection="down"
>
  <Drawer.Trigger>Open snap drawer</Drawer.Trigger>
  <Drawer.Portal>
    <Drawer.Backdrop />
    <Drawer.Viewport>
      <Drawer.Popup>
        <Drawer.Content>
          <Drawer.Title>Rack settings</Drawer.Title>
          <Drawer.Description>Drag between three snap points.</Drawer.Description>
          <Drawer.Root>
            <Drawer.Trigger>Open nested confirmation</Drawer.Trigger>
            <Drawer.Portal>
              <Drawer.Backdrop />
              <Drawer.Viewport>
                <Drawer.Popup>
                  <Drawer.Content>
                    <Drawer.Title>Confirm environment</Drawer.Title>
                    <Drawer.Description>Nested drawers indent their parent.</Drawer.Description>
                    <Drawer.Close>Back</Drawer.Close>
                  </Drawer.Content>
                </Drawer.Popup>
              </Drawer.Viewport>
            </Drawer.Portal>
          </Drawer.Root>
          <Drawer.Close>Close</Drawer.Close>
        </Drawer.Content>
      </Drawer.Popup>
    </Drawer.Viewport>
  </Drawer.Portal>
</Drawer.Root>` }]}
/>

## API

Every compound part and prop/state type is exported from `@tinyrack/ui/components/drawer`; styles remain opt-in through the matching CSS subpath.
