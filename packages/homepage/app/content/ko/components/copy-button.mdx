---
title: "CopyButton"
description: "Clipboard API 대체 동작과 접근 가능한 상태 알림을 제공하는 재사용 가능한 복사 동작이에요."
section: components
order: 10
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/copy-button.demo.js';

## 계약

컨트롤은 `data-copy-status`로 `idle`, `copied`, `unavailable`을 알려요. 가장 최근의 복사 시도만 반영하며 결과는 기본적으로 2초 후 `idle`로 초기화돼요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/copy-button.css';", "import { TRCopyButton } from '@tinyrack/ui/components/copy-button';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="copy-button-basic" title="명령 복사" preview={<div data-docs-example-item-count="1"><Stories.CopyButtonPreview copiedLabel="복사됨" idleLabel="명령 복사" statusLabel="상태" value="pnpm add @tinyrack/ui" /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import { useState } from 'react';
import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/copy-button.css';
import { TRCopyButton, type TRCopyButtonStatus } from '@tinyrack/ui/components/copy-button';

export function CopyCommand() {
  const [status, setStatus] = useState<TRCopyButtonStatus>('idle');
  return <div className="grid justify-items-start gap-2">
    <TRCopyButton copiedLabel="복사됨" idleLabel="명령 복사" onStatusChange={setStatus} value="pnpm add @tinyrack/ui" />
    <p>상태: {status}</p>
  </div>;
}` }]} />

## 예시

<ComponentExampleTabs id="copy-button-labels" title="상황에 맞는 레이블" preview={<div data-docs-example-item-count="1"><Stories.CopyButtonPreview copiedLabel="가져오기 구문 복사됨" idleLabel="가져오기 구문 복사" statusLabel="상태" unavailableLabel="복사할 수 없음" value="import { TRButton } from '@tinyrack/ui/components/button';" /></div>} sources={[{ label: 'React', language: 'tsx', code: String.raw`import { useState } from 'react';
import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/copy-button.css';
import { TRCopyButton, type TRCopyButtonStatus } from '@tinyrack/ui/components/copy-button';

const source = "import { TRButton } from '@tinyrack/ui/components/button';";

export function CopyImport() {
  const [status, setStatus] = useState<TRCopyButtonStatus>('idle');
  return <div className="grid justify-items-start gap-2">
    <TRCopyButton copiedLabel="가져오기 구문 복사됨" idleLabel="가져오기 구문 복사" onStatusChange={setStatus} unavailableLabel="복사할 수 없음" value={source} />
    <p>상태: {status}</p>
  </div>;
}` }]} />

<ComponentExampleTabs
  id="copy-button-combinations"
  title="상속된 Button 조합"
  description="`appearance`, `variant`, `uiSize`와 레이블을 Button 스타일 전반에서 조합할 수 있어요. 대기 및 성공 레이블은 같은 너비를 유지하고 더 긴 실패 레이블은 실패할 때만 보여요."
  preview={<div data-docs-example-item-count="3"><Stories.CopyButtonCombinationPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/core.css';
import '@tinyrack/ui/components/copy-button.css';
import { TRCopyButton } from '@tinyrack/ui/components/copy-button';

export function CopyButtonCombinations() {
  return <div className="flex flex-wrap items-center gap-3">
  <TRCopyButton appearance="solid" resetDelay={750} uiSize="sm" value="solid-primary" variant="primary" />
  <TRCopyButton
    appearance="outline"
    copiedLabel="Import copied"
    idleLabel="Copy import"
    unavailableLabel="Import unavailable"
    value="import { TRButton } from '@tinyrack/ui/components/button';"
    variant="secondary"
  />
  <TRCopyButton appearance="ghost" idleLabel="Copy cleanup command" uiSize="lg" value="pnpm clean" variant="danger" />
</div>;
}` }]}
/>

## API

| Prop | 타입 | 기본값 | 설명 |
| --- | --- | --- | --- |
| `value` | `string` | 필수 | Clipboard API 또는 화면 밖 선택 대체 동작으로 복사할 텍스트예요. |
| `idleLabel` | `string` | `"Copy"` | 복사 전 표시 및 접근성 레이블이에요. |
| `copiedLabel` | `string` | `"Copied"` | 성공 레이블과 polite 알림이에요. |
| `unavailableLabel` | `string` | `"Copy unavailable"` | 두 복사 방식이 모두 실패했을 때의 레이블과 polite 알림이에요. |
| `onStatusChange` | `(status) => void` | — | `copied` 또는 `unavailable`을 전달한 뒤 초기화할 때 `idle`을 전달해요. 오래됐거나 마운트 해제된 시도는 전달하지 않아요. |
| `resetDelay` | `number` | `2000` | 완료 결과가 `idle`로 초기화되기까지의 밀리초예요. |

`children`을 제외한 모든 `TRButton` prop을 상속하므로 ref, 네이티브 이벤트, `disabled`, `loading`, `loadingLabel`, `appearance`, `variant`, `uiSize`를 사용할 수 있어요. `onClick`에서 `preventDefault()`를 호출하면 복사 시도를 취소해요.
