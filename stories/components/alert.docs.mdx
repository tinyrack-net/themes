import { Meta } from '@storybook/addon-docs/blocks';
import { Alert } from '../../src/components/alert/index.js';
import { Button } from '../../src/components/button/index.js';
import { ComponentExampleTabs } from '../shared/component-example-tabs.js';
import { ComponentInstall } from '../shared/component-install.js';
import * as Stories from './alert.stories';

<Meta of={Stories} />

# Alert

Persistent status messaging with semantic titles, descriptions, and optional actions.

## Contract

| Axis | Values | Default |
| --- | --- | --- |
| `variant` | `neutral`, `info`, `success`, `warning`, `danger` | `neutral` |
| parts | `Root`, `Title`, `Description`, `Actions` | compound API |

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/alert.css';", "import { Alert } from '@tinyrack/ui/components/alert';"] }]} />

## Usage

<ComponentExampleTabs
  id="alert-basic"
  title="Status message"
  previewLayout="stretch"
  preview={<Alert.Root variant="info"><Alert.Title>Deployment queued</Alert.Title><Alert.Description>The rollout will start shortly.</Alert.Description></Alert.Root>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { Alert } from '@tinyrack/ui/components/alert';

<Alert.Root variant="info">
  <Alert.Title>Deployment queued</Alert.Title>
  <Alert.Description>The rollout will start shortly.</Alert.Description>
</Alert.Root>` }]}
/>

## Examples

<ComponentExampleTabs
  id="alert-variants"
  title="Status variants"
  description="All semantic status colors are visible in one vertical scan."
  previewLayout="stretch"
  preview={(
    <div className="grid gap-3">
      <Alert.Root variant="neutral"><Alert.Title>Neutral</Alert.Title><Alert.Description>No status has been assigned.</Alert.Description></Alert.Root>
      <Alert.Root variant="info"><Alert.Title>Info</Alert.Title><Alert.Description>Maintenance starts at 02:00.</Alert.Description></Alert.Root>
      <Alert.Root variant="success"><Alert.Title>Success</Alert.Title><Alert.Description>All nodes are healthy.</Alert.Description></Alert.Root>
      <Alert.Root variant="warning"><Alert.Title>Warning</Alert.Title><Alert.Description>Storage is nearing capacity.</Alert.Description></Alert.Root>
      <Alert.Root variant="danger"><Alert.Title>Danger</Alert.Title><Alert.Description>Deployment failed.</Alert.Description><Alert.Actions><Button appearance="outline" size="sm" variant="danger">Retry</Button></Alert.Actions></Alert.Root>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`<Alert.Root variant="neutral">...</Alert.Root>
<Alert.Root variant="info">...</Alert.Root>
<Alert.Root variant="success">...</Alert.Root>
<Alert.Root variant="warning">...</Alert.Root>
<Alert.Root variant="danger">
  <Alert.Title>Deployment failed</Alert.Title>
  <Alert.Description>Review the logs and retry.</Alert.Description>
  <Alert.Actions><Button variant="danger">Retry</Button></Alert.Actions>
</Alert.Root>` }]}
/>

## API

`Alert.Root` forwards native `div` props and React 19 `ref`; every part is also exported individually.
