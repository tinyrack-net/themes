---
title: "Table of Contents"
description: "데스크톱 rail과 모바일 disclosure를 위한 현재 heading 탐색입니다."
section: components
order: 60
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../shared/component-example-tabs.js';
import { ComponentInstall } from '../../shared/component-install.js';
import * as Stories from '../../components/table-of-contents.demo.js';

## 계약

h2/h3 heading 데이터와 현재 heading을 전달하세요. `renderLink`은 router link를 주입할 수 있으며 native hash navigation이 fallback으로 남습니다.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui react react-dom', imports: ["import '@tinyrack/ui/core.css';", "import '@tinyrack/ui/components/table-of-contents.css';", "import { TableOfContents } from '@tinyrack/ui/components/table-of-contents';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="table-of-contents-basic" title="Heading 개요" preview={<Stories.TableOfContentsPreview currentHeading="install" />} sources={[{ label: 'React', language: 'tsx', code: String.raw`<TableOfContents items={headings} currentHeading={activeHeading} onNavigate={scrollToHeading} />` }]} />

## 예시

adapter가 hash navigation을 제어하도록 유지하면서 intersection observer로 `currentHeading`을 업데이트하세요.

## API

Depth는 `2 | 3`으로 제한됩니다. 컴포넌트는 같은 데이터로 데스크톱 rail과 모바일 native `details` disclosure를 렌더링합니다.